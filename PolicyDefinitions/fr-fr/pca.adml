<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>tapez le nom complet ici</displayName>
  <description>tapez la description ici</description>
  <resources>
    <stringTable>


      <!-- Overall category text -->
      <string id="PcaScenarioCategory">Diagnostics de compatibilité des applications</string>
      

      <!-- Generic WDI text -->
      <string id="WdiScenarioExecutionPolicyLevelResolution">Détection, dépannage et résolution</string>
      <string id="WdiScenarioExecutionPolicyLevelTsOnly">Détection et dépannage seulement</string>


      <!-- Individual scenario text -->
      <string id="DetectInstallFailuresText">Détecter les défaillances d’installation d’une application</string>
      <string id="DetectUpdateFailuresText">Détecter les applications incapables de lancer les programmes d’installation régis par le contrôle de compte d’utilisateur</string>
      <string id="DetectDeprecatedComponentFailuresText">Détecter les défaillances d’application causées par des DLL Windows obsolètes</string>
      <string id="DetectDeprecatedCOMComponentFailuresText">Détecter les défaillances d’application causées par des objets COM obsolètes</string>
      <string id="DetectUndetectedInstallersText">Détecter les programmes d’installation d’applications devant être exécutés en mode administrateur</string>
      <string id="DetectBlockedDriversText">Notifier les pilotes bloqués</string>
      <!-- End individual scenario text -->


      <!-- Begin install failure scenario description -->
      <string id="DetectInstallFailuresExplain">Ce paramètre de stratégie configure l’Assistant Compatibilité des programmes pour qu’il diagnostique les défaillances d’installation d’applications. 

Si vous activez ce paramètre de stratégie, l’Assistant Compatibilité des programmes est configuré pour détecter les défaillances d’exécution des programmes d’installation d’applications par des méthodes heuristiques. Si des défaillances potentielles sont détectées, l’Assistant Compatibilité des programmes propose à l’utilisateur de redémarrer le programme d’installation en mode de compatibilité avec Microsoft Windows XP.

Si vous désactivez ce paramètre de stratégie, l’Assistant Compatibilité des programmes n’est pas configuré pour détecter les défaillances d’exécution des programmes d’installation.

Si vous ne configurez pas ce paramètre de stratégie, l’Assistant Compatibilité des programmes active ce scénario de diagnostic par défaut.

Remarque : si le paramètre de stratégie « Désactiver l’Assistant Compatibilité des programmes » est désactivé, ce paramètre de stratégie est sans effet. Le service de stratégie de diagnostic et le service Assistant Compatibilité des programmes doit être en cours d’exécution pour que l’Assistant Compatibilité des programmes puisse s’exécuter. Ces services sont configurables à l’aide du composant logiciel enfichable Services de la console MMC.</string>
      <!-- End install failure scenario description -->
      

      <!-- Begin update failure scenario description -->
      <string id="DetectUpdateFailuresExplain">Ce paramètre de stratégie configure l’Assistant Compatibilité des programmes pour qu’il diagnostique les défaillances des programmes régis par le contrôle de compte d’utilisateur. 

Si vous activez ce paramètre de stratégie, l’Assistant Compatibilité des programmes détecte les programmes qui n’ont pas réussi à lancer les processus enfants qui sont des programmes d’installation (en général les programmes de mise à jour). Lorsque cette défaillance est détectée, l’Assistant Compatibilité des programmes applique le mode de compatibilité ELEVATECREATEPROCESS, qui permet aux programmes de lancer correctement le programme d’installation la fois suivante, comme avec des privilèges d’administrateur.

Si vous désactivez ce paramètre de stratégie, l’Assistant Compatibilité des programmes ne détecte pas les applications qui ne parviennent pas à lancer les programmes d’installation régis par le contrôle de compte d’utilisateur.

Si vous désactivez ce paramètre de stratégie, l’Assistant Compatibilité des programmes détecte les programmes qui n’ont pas réussi à lancer les processus enfants qui sont des programmes d’installation (en général les programmes de mise à jour).

Remarque : si le paramètre de stratégie « Désactiver l’Assistant Compatibilité des programmes » est désactivé, ce paramètre de stratégie est sans effet. Le service de stratégie de diagnostic et le service Assistant Compatibilité des programmes doit être en cours d’exécution pour que l’Assistant Compatibilité des programmes puisse s’exécuter. Ces services sont configurables à l’aide du composant logiciel enfichable Services de la console MMC.</string>
      <!-- End update failure scenario description -->
      

      <!-- Begin deprecated component failure scenario description -->
      <string id="DetectDeprecatedComponentFailuresExplain">Ce paramètre de stratégie détermine si l’Assistant Compatibilité des programmes diagnostique les défaillances de chargement de DLL dans les programmes. 

Si vous activez ce paramètre de stratégie, l’Assistant Compatibilité des programmes détecte les programmes qui tentent de charger des DLL Microsoft Windows d’ancienne génération qui n’existent plus dans cette version de Windows. Lorsque cette défaillance est détectée, une fois le programme interrompu, l’Assistant Compatibilité des programmes avertit l’utilisateur de ce problème et offre la possibilité de rechercher des solutions sur le site Web de Microsoft.

Si vous désactivez ce paramètre de stratégie, l’Assistant Compatibilité des programmes ne détecte pas les programmes qui tentent de charger des DLL Windows d’ancienne génération.

Si vous ne configurez pas ce paramètre de stratégie, l’Assistant Compatibilité des programmes détecte les programmes qui tentent de charger des DLL Windows d’ancienne génération.

Remarque : si le paramètre de stratégie Désactiver l’Assistant Compatibilité des programmes est désactivé, ce paramètre de stratégie est sans effet. Le service de stratégie de diagnostic et le service Assistant Compatibilité des programmes doit être en cours d’exécution pour que l’Assistant Compatibilité des programmes puisse s’exécuter. Ces services sont configurables à l’aide du composant logiciel enfichable Services de la console MMC (Microsoft Management Console).</string>
      <!-- End deprecated component failure scenario description -->


      <!-- Begin deprecated COM component failure scenario description -->
      <string id="DetectDeprecatedCOMComponentFailuresExplain">Ce paramètre de stratégie détermine si l’Assistant Compatibilité des programmes diagnostique les défaillances de chargement de DLL ou de création d’objets COM dans les programmes. 

Si vous activez ce paramètre de stratégie, l’Assistant Compatibilité des programmes détecte les programmes qui tentent de créer des objets COM hérités qui n'existent plus dans cette version de Windows. Lorsque cette défaillance est détectée, une fois le programme interrompu, l’Assistant Compatibilité des programmes avertit l’utilisateur de ce problème et offre la possibilité de rechercher des solutions sur le site Web de Microsoft.

Si vous désactivez ce paramètre de stratégie, l’Assistant Compatibilité des programmes ne détecte pas les programmes qui tentent de créer des objets COM hérités.

Si vous ne configurez pas ce paramètre de stratégie, l’Assistant Compatibilité des programmes détecte les programmes qui tentent de créer des objets COM hérités.

Remarque : si le paramètre de stratégie Désactiver l’Assistant Compatibilité des programmes est désactivé, ce paramètre de stratégie est sans effet. Le service de stratégie de diagnostic et le service Assistant Compatibilité des programmes doit être en cours d’exécution pour que l’Assistant Compatibilité des programmes puisse s’exécuter. Ces services peuvent être configurés avec le composant logiciel enfichable Services dans la console MMC (Microsoft Management Console).</string>
      <!-- End deprecated COM component failure scenario description -->


      <!-- Begin undetected installer failure scenario description -->
      <string id="DetectUndetectedInstallersExplain">Ce paramètre de stratégie détermine si l’Assistant Compatibilité des programmes diagnostique les défaillances avec les programmes d’installation d’applications qui ne sont pas détectés pour être exécutés en mode administrateur. 

Si vous activez ce paramètre de stratégie, l’Assistant Compatibilité des programmes est configuré pour détecter les programmes d’installation d’applications qui ne disposent pas des privilèges pour être exécutés en mode administrateur par le contrôle d’accès utilisateur. Si des défaillances potentielles sont détectées, l’Assistant Compatibilité des programmes propose à l’utilisateur de redémarrer le programme d’installation en mode administrateur.

Si vous désactivez ce paramètre de stratégie, l’Assistant Compatibilité des programmes ne détecte pas les programmes d’installation d’applications qui ne disposent pas des privilèges pour être exécutés en mode administrateur par le contrôle d’accès utilisateur.

Si vous ne configurez pas ce paramètre de stratégie, l’Assistant Compatibilité des programmes est configuré pour détecter les programmes d’installation d’applications qui ne disposent pas des privilèges pour être exécutés en mode administrateur par le contrôle d’accès utilisateur.


Remarque : si le paramètre de stratégie « Désactiver l’Assistant Compatibilité des programmes » est désactivé, ce paramètre de stratégie est sans effet. Le service de stratégie de diagnostic et le service Assistant Compatibilité des programmes doit être en cours d’exécution pour que l’Assistant Compatibilité des programmes puisse s’exécuter. Ces services sont configurables à l’aide du composant logiciel enfichable Services de la console MMC.</string>
      <!-- End undetected installer failure scenario description -->


      <!-- Begin blocked drivers scenario description -->
      <string id="DetectBlockedDriversExplain">Ce paramètre de stratégie détermine si l’Assistant Compatibilité des programmes diagnostique les pilotes bloqués en raison de problèmes de compatibilité. 

Si vous activez ce paramètre de stratégie, l’Assistant Compatibilité des programmes avertit l’utilisateur des problèmes de pilotes bloqués, avec la possibilité de rechercher des solutions sur le site Web de Microsoft.

Si vous désactivez ce paramètre de stratégie, l’Assistant Compatibilité des programmes n’avertit pas l’utilisateur des problèmes de pilotes bloqués. Remarque : lorsque ce paramètre de stratégie est désactivé, l’utilisateur ne se voit pas proposer de solutions aux problèmes de pilotes bloqués.

Si vous ne configurez pas ce paramètre de stratégie, l’Assistant Compatibilité des programmes avertit l’utilisateur des problèmes de pilotes bloqués, avec la possibilité de rechercher des solutions sur le site Web de Microsoft.

Remarque : si le paramètre de stratégie « Désactiver l’Assistant Compatibilité des programmes » est désactivé, ce paramètre de stratégie est sans effet. Le service de stratégie de diagnostic et le service Assistant Compatibilité des programmes doit être en cours d’exécution pour que l’Assistant Compatibilité des programmes puisse s’exécuter. Ces services sont configurables à l’aide du composant logiciel enfichable Services de la console MMC.</string>
      <!-- End blocked drivers scenario description -->
    </stringTable>

    <presentationTable>
      <presentation id="DetectUpdateFailuresTable">
        <dropdownList refId="DetectUpdateFailuresLevel" noSort="true" defaultItem="1">Niveau d’exécution de scénario</dropdownList>
      </presentation>
      <presentation id="DetectDeprecatedComponentFailuresTable">
        <dropdownList refId="DetectDeprecatedComponentFailuresLevel" noSort="true" defaultItem="1">Niveau d’exécution de scénario</dropdownList>
      </presentation>
      <presentation id="DetectDeprecatedCOMComponentFailuresTable">
        <dropdownList refId="DetectDeprecatedCOMComponentFailuresLevel" noSort="true" defaultItem="1">Niveau d’exécution des scénarios</dropdownList>
      </presentation>
      <presentation id="DetectUndetectedInstallersTable">
        <dropdownList refId="DetectUndetectedInstallersLevel" noSort="true" defaultItem="1">Niveau d’exécution de scénario</dropdownList>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
