<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>tapez le nom complet ici</displayName>
  <description>tapez la description ici</description>
  <resources>
    <stringTable>
      <string id="AppCompat">Compatibilité des applications</string>
      <string id="AppCompat_Prevent16Bit_Help">Empêche ou non l’exécution du sous-système MS-DOS (ntvdm.exe) pour tous les utilisateurs. Ce paramètre affecte le lancement des applications 16 bits dans le système d’exploitation. Par défaut, le sous-système MS-DOS s’exécute pour tous les utilisateurs.

Vous pouvez utiliser ce paramètre pour désactiver le sous-système MS-DOS, ce qui permettra de réduire l’utilisation des ressources et d’empêcher les utilisateurs d’exécuter des applications 16 bits. Pour exécuter toute application 16 bits ou comportant des composants 16 bits, l’exécution de ntvdm.exe doit être autorisée. Le sous-système MS-DOS démarre lorsque la première application 16 bits est lancée. Lors de l’exécution du processus, toutes les applications 16 bits suivantes démarrent plus rapidement, mais l’utilisation globale des ressources du système augmente.

Si vous activez ce paramètre, ntvdm.exe ne peut pas être exécuté, ce qui empêche l’exécution de toutes les applications 16 bits. En outre, toutes les applications 32 bits comportant des programmes d’installation ou autres composants 16 bits ne peuvent pas être exécutées.

Si vous désactivez ce paramètre, le paramètre par défaut s’applique et le sous-système MS-DOS s’exécute pour tous les utilisateurs.

Si vous ne configurez pas ce paramètre, la valeur par défaut s’applique et ntvdm.exe s’exécute pour tous les utilisateurs. Toutefois, si un administrateur définit la valeur de Registre DWORD HKLM\System\CurrentControlSet\Control\WOW\DisallowedPolicyDefault sur 1, la valeur par défaut change et empêche l’exécution de toutes les applications 16 bits.

Remarque :  ce paramètre s’affiche à la fois dans Configuration ordinateur et Configuration utilisateur. Si les deux paramètres sont configurés, le paramètre Configuration ordinateur est prioritaire.</string>
      <string id="AppCompat_Prevent16BitMach_Help">Empêche ou non l’exécution du sous-système MS-DOS (ntvdm.exe) sur cet ordinateur. Ce paramètre affecte le lancement des applications 16 bits dans le système d’exploitation. Par défaut, le sous-système MS-DOS s’exécute pour tous les utilisateurs sur cet ordinateur.

Vous pouvez utiliser ce paramètre pour désactiver le sous-système MS-DOS, ce qui permet de réduire l’utilisation des ressources et d’empêcher les utilisateurs d’exécuter des applications 16 bits. Pour exécuter toute application 16 bits ou comportant des composants 16 bits, l’exécution de ntvdm.exe doit être autorisée. Le sous-système MS-DOS démarre lorsque la première application 16 bits est lancée. Lors de l’exécution du sous-système MS-DOS, toutes les applications 16 bits suivantes démarrent plus rapidement, mais l’utilisation globale des ressources du système augmente.

Si vous activez ce paramètre, ntvdm.exe ne peut pas être exécuté, ce qui empêche l’exécution de toutes les applications 16 bits. En outre, toutes les applications 32 bits comportant des programmes d’installation ou d’autres composants 16 bits ne peuvent pas être exécutées.

Si vous désactivez ce paramètre, le paramètre par défaut s’applique et le sous-système MS-DOS s’exécute pour tous les utilisateurs sur cet ordinateur.

Si vous ne configurez pas ce paramètre, la valeur par défaut s’applique et ntvdm.exe s’exécute pour tous les utilisateurs. Toutefois, si un administrateur définit la valeur de Registre DWORD HKLM\System\CurrentControlSet\Control\WOW\DisallowedPolicyDefault sur 1, la valeur par défaut change et empêche l’exécution de toutes les applications 16 bits.

Remarque :  ce paramètre s’affiche à la fois dans Configuration ordinateur et Configuration utilisateur. Si les deux paramètres sont configurés, le paramètre Configuration ordinateur est prioritaire.</string>
      <string id="AppCompat_RemoveProgramCompatPropPage_Help">Cette stratégie contrôle l’affichage de l’extension de l’environnement de la page de propriétés Compatibilité des programmes.  Cette extension de l’environnement est visible dans le menu contextuel des propriétés de tout raccourci de programme ou fichier exécutable.

La page de propriétés de la compatibilité affiche une liste d’options qui peuvent être sélectionnées et appliquées à l’application pour résoudre les problèmes les plus courants associés aux applications héritées.  L’activation de ce paramètre de stratégie supprime la page de propriétés des menus contextuels, mais n’affecte pas les précédents paramètres de compatibilité appliqués à l’application à l’aide de cette interface.</string>
        <string id="AppCompat_TurnOffApplicationImpactTelemetry_Help">Cette stratégie contrôle l'état du moteur de télémétrie applicative dans le système.

La télémétrie applicative est un mécanisme qui suit l'utilisation anonyme de composants système Windows spécifiques par les applications.

Le fait de désactiver la télémétrie applicative en sélectionnant Activer a pour effet d'arrêter la collecte des données liées à l'utilisation.

Si le programme d'amélioration des services est désactivé, la télémétrie applicative le sera également, quelle que soit la façon dont cette stratégie est définie.

La télémétrie sera désactivée pour toutes les prochaines applications lancées. Afin d'être certain que la collecte télémétrique est arrêtée pour toutes les applications, redémarrez l'ordinateur.</string>
      <string id="AppCompat_TurnOffEngine_Help"> Cette stratégie contrôle l’état du moteur de compatibilité des applications dans le système.

Le moteur fait partie du chargeur et analyse une base de données de compatibilité chaque fois qu’une application démarre dans le système. Si une correspondance est trouvée pour l’application, il fournit des solutions à l’exécution ou des correctifs de compatibilité, ou affiche un message de l’aide de l’application si celle-ci comporte un problème connu.

Si vous désactivez le moteur de compatibilité des applications, les performances du système seront améliorées. Toutefois, la compatibilité de nombreuses applications héritées répandues sera compromise et les applications incompatibles connues pourront malgré tout être installées. Il peut en résulter par exemple un écran bleu si une ancienne application antivirus est installée.

Les fonctionnalités de protection des ressources Windows et de contrôle de compte d’utilisateur de Windows utilisent le moteur de compatibilité des applications pour atténuer les problèmes au niveau des applications. Si le moteur est désactivé, ces atténuations ne sont pas appliquées aux applications et aux programmes d’installation correspondants et il se peut  que ces applications ne soient pas installées ou ne s’exécutent pas correctement.

Cette option est utile pour les administrateurs de serveur qui ont besoin de meilleures performances et qui connaissent la compatibilité des applications qu’ils utilisent. Elle est également particulièrement utile dans le contexte de serveurs Web sur lesquels des applications peuvent être lancées plusieurs centaines de fois par seconde et dans lequel les performances du chargeur sont essentielles.

REMARQUE : nombre de processus système mettent en cache la valeur de ce paramètre pour des raisons de performances. Si vous modifiez ce paramètre, redémarrez le système pour vous assurer que ces modifications ont été prises en compte.</string>
      <string id="AppCompat_TurnOffProgramCompatibilityAssistant_Help">Cette stratégie contrôle l’état de l’Assistant Compatibilité des programmes dans le système.
      
L’Assistant Compatibilité des programmes analyse les programmes initialisés par l’utilisateur pour détecter d’éventuels problèmes de compatibilité connus au moment de l’exécution. Si un problème potentiel est détecté avec une application, il fournit à l’utilisateur des liens vers les solutions recommandées.  Pour plus d’informations sur les différents scénarios de détection des problèmes couverts par l’Assistant Compatibilité des programmes et les stratégies permettant de les configurer, consultez les stratégies sous Système-&gt;Dépannage et diagnostic-&gt;Diagnostic de compatibilité des applications. 

L’Assistant Compatibilité des programmes est activé par défaut.

Si ce paramètre de stratégie est activé, l’Assistant Compatibilité des programmes est désactivé. Cette option est utile pour les administrateurs système qui nécessitent des performances plus rapides et connaissent la compatibilité des applications qu’ils utilisent. Remarque : si l’Assistant Compatibilité des programmes est désactivé, les solutions aux problèmes de compatibilité connus lors de l’exécution des applications ne sont pas présentées à l’utilisateur.

Si vous désactivez ce paramètre de stratégie ou ne le configurez pas, l’Assistant Compatibilité des programmes est activé. 

Remarque : Le service de stratégie de diagnostic et le service de l’Assistant Compatibilité des programmes doivent être en cours d’exécution pour exécuter l’Assistant Compatibilité des programmes. Ces services peuvent être configurés avec le composant logiciel enfichable Services dans la console MMC.</string>
      <string id="AppCompat_TurnOffUserActionRecord_Help">Cette stratégie contrôle l’état de la fonctionnalité Enregistreur d'actions utilisateur dans le système.

L'Enregistreur d'actions utilisateur permet de capturer les étapes effectuées par l'utilisateur avant qu'il ne rencontre un problème. Les données de sortie générées par cette fonctionnalité constituent un autre type de collecte de données activée par le biais de scénarios de commentaires tels que le Rapport d'erreurs Windows afin d'aider les développeurs à résoudre les problèmes. Les données capturées contiennent les descriptions des actions utilisateur et comportent des informations telles que les entrées effectuées via le clavier et la souris, les métadonnées sur les objets d'interface utilisateur ainsi que des captures d'écran. L'Enregistreur d'actions utilisateur comporte des options spécifiques permettant d'activer et de désactiver la collecte des données en cas de nécessité.

Par défaut, les utilisateurs ont la possibilité d'activer cette fonctionnalité dans les scénarios de rapports de problèmes.

Si vous activez ce paramètre de stratégie, l'Enregistreur d'actions utilisateur sera désactivé sur cet ordinateur.

Si vous le désactivez ou ne le configurez pas, la fonctionnalité sera disponible pour les utilisateurs.</string>

      <string id="AppCompat_TurnOffSwitchBack_Help">Cette stratégie contrôle l’état du moteur de compatibilité Switchback dans le système. 

Switchback est un mécanisme qui fournit des atténuations de compatibilité génériques à des applications antérieures en proposant les anciens comportements aux anciennes applications et les nouveaux comportements aux nouvelles applications. 

Ce mécanisme est activé par défaut.

Si vous activez ce paramètre de stratégie, Switchback sera désactivé. La désactivation de Switchback peut affecter la compatibilité des anciennes applications. Cette option est utile pour les administrateurs de serveur ayant besoin de performances et connaissant la compatibilité des applications qu’ils utilisent. 

Si vous désactivez ou ne configurez pas ce paramètre de stratégie, le mécanisme Switchback sera activé.

Redémarrez le système après avoir modifié le paramètre afin de vous assurer que ces modifications ont été prises en compte.</string>
      <string id="AppCompat_TurnOffProgramInventory_Help">Cette stratégie contrôle l’état de l'Inventory Collector des programmes dans le système.
      
Le PDU effectue un inventaire des programmes et des fichiers du système et transmet ces informations à Microsoft. Celles-ci sont utilisées pour associer les fichiers aux programmes et pour diagnostiquer les problèmes de compatibilité d'applications.

Le PDU est activé par défaut.

Si vous activez ce paramètre de stratégie, l'inventaire sera désactivé et les données ne seront pas envoyées à Microsoft. L'activation de ce paramètre désactive également la collecte des données d'installation via l'Assistant Compatibilité des programmes.

Si vous désactivez ou ne configurez pas ce paramètre, l'inventaire sera activé.</string>

      <string id="AppCompatPrevent16Bit">Empêcher l’accès aux applications 16 bits</string>
      <string id="AppCompatPrevent16BitMach">Empêcher l’accès aux applications 16 bits</string>
      <string id="AppCompatRemoveProgramCompatPropPage">Supprimer la page de propriétés Compatibilité des programmes</string>
      <string id="AppCompatTurnOffApplicationImpactTelemetry">Désactiver la télémétrie applicative</string>
      <string id="AppCompatTurnOffEngine">Désactiver le moteur de compatibilité des applications</string>
      <string id="AppCompatTurnOffProgramCompatibilityAssistant">Désactiver l’Assistant Compatibilité des programmes</string>
      <string id="AppCompatTurnOffSwitchBack">Désactiver le moteur de compatibilité Switchback</string>
      <string id="AppCompatTurnOffUserActionRecord">Désactiver l'Enregistreur d'actions utilisateur</string>
      <string id="AppCompatTurnOffProgramInventory">Désactiver l'inventaire des programmes</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
