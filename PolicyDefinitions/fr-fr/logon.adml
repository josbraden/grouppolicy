<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>entrer le nom complet ici</displayName>
  <description>entrer la description ici</description>
  <resources>
    <stringTable>
      <string id="UseOEMBackground">Toujours utiliser un arrière-plan d'ouverture de session personnalisé</string> 
      <string id="UseOEMBackground_Help">Ignore l'arrière-plan d'ouverture de session Windows.

Ce paramètre de stratégie peut être utilisé pour que Windows privilégie un arrière-plan d'ouverture de session personnalisé. 

Si vous activez ce paramètre, l'écran d'ouverture de session essaie toujours de charger un arrière-plan personnalisé plutôt que l'arrière-plan d'ouverture de session Windows. 

Si vous désactivez ou ne configurez pas ce paramètre de stratégie, Windows utilise l'arrière-plan d'ouverture de session Windows par défaut ou un arrière-plan personnalisé.</string> 
      <string id="DisableExplorerRunLegacy">Ne pas traiter la liste d’exécution héritée</string>
      <string id="DisableExplorerRunLegacy_Help">Ignore la liste d’exécution héritée.

Vous pouvez créer une liste personnalisée de programmes et de documents supplémentaires que le système démarre automatiquement lorsqu’il fonctionne sous Windows Vista, Windows XP Professionnel et Windows 2000 Professionnel. Ces programmes sont ajoutés à la liste d’exécution standard des programmes et des services démarrés par le système.

Si ce paramètre de stratégie est activé, le système ignore la liste d’exécution pour Windows Vista, Windows 2000 Professionnel et Windows XP Professionnel.

Si ce paramètre de stratégie est désactivé ou n’est pas configuré, Windows Vista ajoute à sa liste d’exécution toutes les listes d’exécution personnalisées.

Ce paramètre de stratégie apparaît dans les dossiers Configuration ordinateur et Configuration utilisateur. Si les deux paramètres de stratégie sont appliqués, le paramètre de stratégie dans Configuration ordinateur prévaut sur le paramètre dans Configuration utilisateur.

Remarque : pour créer une liste d’exécution personnalisée à l’aide d’une stratégie, utilisez le paramètre de stratégie « Exécuter ces applications au démarrage ».

Consultez également le paramètre de stratégie « Ne pas traiter la liste d’exécution unique ».</string>
      <string id="DisableExplorerRunOnceLegacy">Ne pas traiter la liste d’exécution unique</string>
      <string id="DisableExplorerRunOnceLegacy_Help">Ignore les listes d’exécution unique personnalisées.

Vous pouvez créer une liste personnalisée de programmes et de documents supplémentaires qui seront démarrés automatiquement au prochain démarrage du système (mais qui ne le seront plus ensuite). Ces programmes sont ajoutés à la liste standard des programmes et des services démarrés par le système.

Si vous activez ce paramètre, le système ignore la liste d’exécution unique.

Si vous désactivez ce paramètre ou si vous ne le configurez pas, le système exécute les programmes qui se trouvent dans la liste d’exécution unique.

Ce paramètre apparaît dans les dossiers Configuration ordinateur et Configuration utilisateur. Si les deux paramètres sont configurés, le paramètre dans Configuration ordinateur prévaut sur le paramètre dans Configuration utilisateur.

Remarque : les listes d’exécution unique personnalisées sont stockées dans le Registre dans HKEY_LOCAL_MACHINE\Software\Microsoft\Windows\CurrentVersion\RunOnce.

Consultez également le paramètre « Ne pas traiter la liste d’exécution héritée ».</string>
      <string id="DisableStatusMessages">Supprimer les messages d’état de Démarrage / Arrêt / Ouverture de session / Fermeture de session</string>
      <string id="DisableStatusMessages_Help">Supprime les messages sur l’état du système.

Si vous activez ce paramètre, le système n’affiche pas de message pour rappeler aux utilisateurs qu’ils doivent attendre pendant le démarrage ou l’arrêt du système ou pendant l’ouverture ou la fermeture d’une session.</string>
      <string id="Logon">Ouverture de session</string>
      <string id="LogonType">Utiliser toujours l’ouverture de session classique</string>
      <string id="LogonType_Help">Ce paramètre force l’utilisateur à ouvrir une session sur l’ordinateur en utilisant l’écran d’accueil de session classique. Par défaut, un groupe de travail est paramétré afin d’utiliser l’écran d’accueil de session simple. Ce paramètre ne fonctionne que lorsque l’ordinateur n’est pas sur un domaine.</string>
      <string id="NoWelcomeTips">Ne pas afficher l’écran de bienvenue Mise en route à l’ouverture de session</string>
      <string id="NoWelcomeTips_Help">Supprime l’écran d’accueil.

Ce paramètre masque l’écran d’accueil affiché dans Windows 2000 Professionnel à chaque fois que l’utilisateur ouvre une session.

Les utilisateurs peuvent toujours afficher l’écran d’accueil en le sélectionnant dans le menu Démarrer ou en entrant « Welcome » dans la boîte de dialogue Exécuter.

Ce paramètre s’applique uniquement à Windows 2000 Professionnel. Il n’affecte pas l’écran « Configurer votre serveur » dans Windows 2000 Server.

Remarque : ce paramètre apparaît dans les dossiers Configuration ordinateur et Configuration utilisateur. Si les deux paramètres sont configurés, le paramètre dans Configuration ordinateur prévaut sur le paramètre dans Configuration utilisateur.

Conseil : pour afficher l’écran d’accueil, cliquez sur Démarrer, pointez successivement sur Programmes, Accessoires et Outils système, puis cliquez sur « Mise en route ». Pour supprimer l’écran d’accueil sans définir un paramètre, désactivez la case à cocher « Afficher cet écran au démarrage » sur l’écran d’accueil.</string>
      <string id="Run">Exécuter ces programmes à l’ouverture de session utilisateur</string>
      <string id="Run_Help">Spécifie les programmes ou les documents supplémentaires démarrés automatiquement par Windows lorsqu’un utilisateur ouvre une session sur le système.

Pour spécifier des valeurs pour ce paramètre, cliquez sur Afficher. Dans la boîte de dialogue Afficher le contenu, dans la colonne Valeur, tapez le nom du programme exécutable (.exe) ou du document. Pour spécifier un autre nom, appuyez sur ENTRÉE, puis tapez le nom. Si le fichier ne se trouve pas dans le répertoire %Systemroot%, vous devez spécifier le chemin d’accès complet au fichier.

Remarque : ce paramètre apparaît dans les dossiers Configuration ordinateur et Configuration utilisateur. Si les deux paramètres sont configurés, le système démarre les programmes spécifiés dans le paramètre Configuration ordinateur avant de démarrer ceux spécifiés dans le paramètre Configuration utilisateur.

Consultez également les paramètres Ne pas traiter la liste d’exécution héritée et Ne pas traiter la liste d’exécution unique.</string>
      <string id="SyncForegroundPolicy">Toujours attendre le réseau lors du démarrage de l’ordinateur et de l’ouverture de session</string>
      <string id="SyncForegroundPolicy_Help">
Ce paramètre de stratégie détermine si le traitement de la stratégie de groupe est synchrone (c'est-à-dire si les ordinateurs attendent l'initialisation complète du réseau lors du démarrage de l'ordinateur et de l'ouverture de la session utilisateur). Par défaut, sur les ordinateurs clients, le traitement de la stratégie de groupe n'est pas synchrone ; les ordinateurs clients n’attend généralement pas que le réseau soit entièrement initialisé au démarrage et à l’ouverture de session. Les utilisateurs existants ouvrent une session en utilisant des informations d’identification mises en cache, ce qui accélère le processus d’ouverture de session. La stratégie de groupe s'applique en tâche de fond une fois que le réseau est disponible.

Étant donné que l’actualisation s’effectue en tâche de fond, les extensions du type Installation de logiciels et Redirection de dossiers nécessitent deux ouvertures de session pour que les modifications soient appliquées. Pour agir en toute sécurité, ces extensions nécessitent qu’aucun utilisateur n’ait de session ouverte. Elles doivent donc être traitées au premier plan avant que les utilisateurs ne soient actifs sur l’ordinateur. De plus, les modifications effectuées sur l’objet utilisateur, telles que l’ajout d’un chemin d’accès de profil itinérant, d’un répertoire de base ou d’un script de connexion d’objet utilisateur, peuvent nécessiter jusqu’à deux connexions avant d’être détectées.

Lorsqu’un utilisateur avec un profil itinérant, un répertoire d’accueil ou un script de connexion d’objet utilisateur ouvre une session sur un ordinateur, celui-ci attend toujours que le réseau soit initialisé avant d’ouvrir la session. Lorsqu’un utilisateur ouvre une session pour la première fois sur l’ordinateur, celui-ci attend toujours que le réseau soit initialisé.

Si vous activez ce paramètre de stratégie, les ordinateurs attendent que le réseau soit entièrement initialisé avant de procéder à l’ouverture de session utilisateur. La stratégie de groupe est appliquée en premier plan, de manière synchrone.

Sur les serveurs exécutant Windows Server 2008 ou une version ultérieure, ce paramètre de stratégie est ignoré lors du traitement de la stratégie de groupe au démarrage de l'ordinateur et le traitement de la stratégie de groupe est synchrone (ces serveurs attendent l'initialisation du réseau lors du démarrage de l'ordinateur).

Si le serveur est configuré comme suit, ce paramètre de stratégie est pris en compte au moment du traitement  de la stratégie de groupe lors de l'ouverture de session de l'utilisateur :
•  Le serveur est configuré en tant que serveur Terminal Server (autrement dit, le service de rôle Terminal Server est installé et configuré sur le serveur) ; et
•  Le paramètre de stratégie Autoriser un traitement asynchrone de la stratégie de groupe utilisateur lors d’une ouverture de session par le biais des services Terminal Server est activé. Ce paramètre de stratégie se trouve sous Configuration ordinateur\Polices\Modèles d'administration\Système\Stratégie de groupe\.
Si cette configuration n'est pas implémentée sur le serveur, le paramètre est ignoré. Dans ce cas, le traitement de la stratégie de groupe lors de l'ouverture de session utilisateur est synchrone (ces serveurs attendent que le réseau soit initialisé lors de l'ouverture de session de l'utilisateur).

Si vous désactivez ce paramètre ou si vous ne le configurez pas et si les utilisateurs ouvrent une session sur un ordinateur client  ou un serveur exécutant Windows Server 2008 ou une version ultérieure configurée tel que décrit précédemment, l'ordinateur n'attend généralement pas que le réseau soit entièrement initialisé. Dans ce cas, les utilisateurs ouvrent une session avec les informations d'identification mises en cache. La stratégie de groupe est appliquée en tâche de fond, de manière asynchrone.

Remarque : 
Si vous voulez garantir l’application de la Redirection de dossiers, de l’Installation de logiciels ou des paramètres du profil de l’utilisateur itinérant en une seule ouverture de session, activez ce paramètre pour vous assurer que Windows attend la disponibilité du réseau avant d’appliquer la stratégie. 
Si la stratégie de redirection de dossiers est appliquée à l'ouverture de session suivante, les stratégies de sécurité seront appliquées de façon asynchrone lors du prochain cycle de mise à jour si la connectivité réseau est disponible.</string>
      <string id="VerboseStatus">Messages d’état détaillés ou normaux</string>
      <string id="VerboseStatus_Help">Demande au système d’afficher des messages d’état très détaillés.

Si vous activez ce paramètre, le système affiche des messages d’état qui reflètent chaque étape du processus de démarrage, d’arrêt, d'ouverture ou de fermeture d’une session sur le système.

Ce paramètre est destiné aux utilisateurs expérimentés qui ont besoin de ces informations.

Remarque : ce paramètre est ignoré si le paramètre « Supprimer les messages d’état de Démarrage / Arrêt / Ouverture de session / Fermeture de session » est activé.</string>
      <string id="HideFastUserSwitching">Masquer les points d’entrée pour le changement rapide d’utilisateur</string>
      <string id="HideFastUserSwitching_Help">En activant la stratégie, les administrateurs masquent le bouton Changer d’utilisateur dans l’interface utilisateur d’ouverture de session, le menu Démarrer et le Gestionnaire des tâches.</string>
      <string id="DisableStartupSound">Désactiver le son de démarrage de Windows</string>
      <string id="DisableStartupSound_Help">Désactiver le son de démarrage de Windows et empêcher sa personnalisation dans le Panneau de configuration Sons.

Le son de démarrage de Microsoft Windows est lu pendant le démarrage du système et lors des démarrages à froid, et il peut être activé ou désactivé à l'aide du Panneau de configuration Sons.

L'activation ou la désactivation de ce paramètre empêche automatiquement les utilisateurs de personnaliser le comportement du son de démarrage de Windows.

Si ce paramètre de stratégie est activé, le son de démarrage de Windows sera désactivé pour tous les utilisateurs.

Si ce paramètre de stratégie est désactivé, le son de démarrage de Windows sera activé pour tous les utilisateurs.

Si ce paramètre de stratégie n'est pas configuré, le son de démarrage de Windows sera activé pour tous les utilisateurs par défaut et pourra être personnalisé à l'aide du Panneau de configuration Sons.

Ce paramètre de stratégie n'empêche pas les utilisateurs de définir leurs préférences pour les autres sons système.</string>

    </stringTable>
    <presentationTable>
      <presentation id="Run_1">
        <listBox refId="RunListBox1">Éléments à exécuter à l’ouverture de session</listBox>
      </presentation>
      <presentation id="Run_2">
        <listBox refId="RunListBox2">Éléments à exécuter à l’ouverture de session</listBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
