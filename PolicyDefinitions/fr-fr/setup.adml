<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>Entrer le nom complet ici</displayName>
  <description>Entrer la description ici</description>
  <resources>
    <stringTable>
      <string id="ServicePackSourcePath">Spécifier l'emplacement du fichier d'installation du Service Pack Windows</string>
      <string id="ServicePackSourcePath_Help">Spécifie un autre emplacement pour les fichiers d’installation du Service Pack Windows.

Pour activer ce paramètre, entrez le chemin d’accès complet du nouvel emplacement dans la zone « Chemin d’accès du fichier d’installation du Service Pack Windows ».

Si vous désactivez ce paramètre ou si vous ne le configurez pas, le chemin d’accès à la source d’installation du Service Pack Windows correspond à l’emplacement utilisé la dernière fois que l’installation du Service Pack Windows a été exécutée sur le système.

</string>
      <string id="SetupSourcePath">Spécifier l'emplacement du fichier d'installation de Windows</string>
      <string id="SetupSourcePath_Help">Spécifie un autre emplacement pour les fichiers d’installation Windows.

Pour activer ce paramètre, entrez le chemin d’accès complet du nouvel emplacement dans la zone « Chemin d’accès du fichier d’installation Windows ».

Si vous désactivez ce paramètre ou si vous ne le configurez pas, le chemin d’accès à la source d’installation de Windows correspond à l’emplacement utilisé la dernière fois que l’installation de Windows a été exécutée sur le système.

</string>
    </stringTable>
    <presentationTable>
      <presentation id="ServicePackSourcePath">
        <textBox refId="ServicePackSourcePathBox">
          <label>Chemin d'accès du fichier d'installation du Service Pack Windows :</label>
        </textBox>
      </presentation>
      <presentation id="SetupSourcePath">
        <textBox refId="SetupSourcePathBox">
          <label>Chemin d'accès du fichier d'installation Windows :</label>
        </textBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
