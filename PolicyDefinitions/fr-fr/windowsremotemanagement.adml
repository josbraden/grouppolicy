<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>entrer le nom complet ici</displayName>
  <description>entrer la description ici</description>
  <resources>
    <stringTable>
      <string id="AllowAutoConfig">Autoriser la configuration automatique des écouteurs</string>
      <string id="AllowBasic">Autoriser l’authentification de base</string>
      <string id="AllowBasicClientHelp">Ce paramètre de stratégie vous permet de définir si le client Gestion à distance de Windows (WinRM) devra utiliser l’authentification de base.

Si vous activez ce paramètre de stratégie, le client WinRM utilisera l’authentification de base. Si le service Gestion à distance de Windows est configuré pour utiliser le transport HTTP, le nom d’utilisateur et le mot de passe sont envoyés sur le réseau en texte clair.

Si vous désactivez ce paramètre de stratégie ou si vous ne le configurez pas, le client WinRM n’utilisera pas l’authentification de base.</string>
      <string id="AllowBasicServiceHelp">Ce paramètre de stratégie vous permet de définir si le service Gestion à distance de Windows (WinRM) acceptera l’authentification de base de la part d’un client distant.

Si vous activez ce paramètre de stratégie, le service WinRM acceptera l’authentification de base à partir d’un client distant.

Si vous désactivez ce paramètre de stratégie ou si vous ne le configurez pas, le service WinRM n’acceptera pas l’authentification de base à partir d’un client distant.</string>
      <string id="AllowUnencrypted">Autoriser le trafic non chiffré</string>
      <string id="AllowUnencryptedMessagesClientHelp">Ce paramètre de stratégie vous permet de définir si le client Gestion à distance de Windows (WinRM) échangera des messages non chiffrés sur le réseau.

Si vous activez ce paramètre de stratégie, le client WinRM échangera des messages non chiffrés sur le réseau.

Si vous désactivez ce paramètre de stratégie ou si vous ne le configurez pas, le client WinRM n’échangera que des messages chiffrés sur le réseau.</string>
      <string id="AllowUnencryptedMessagesServiceHelp">Ce paramètre de stratégie vous permet de définir si le service Gestion à distance de Windows (WinRM) échangera des messages non chiffrés sur le réseau.

Si vous activez ce paramètre de stratégie, le client WinRM échangera des messages non chiffrés sur le réseau.

Si vous désactivez ce paramètre de stratégie ou si vous ne le configurez pas, le client WinRM n’échangera que des messages chiffrés sur le réseau.</string>
      <string id="AutoConfigHelp">Ce paramètre de stratégie vous permet de définir si le service Gestion à distance de Windows (WinRM) écoutera automatiquement le réseau pour détecter les demandes utilisant le transport HTTP sur le port HTTP par défaut.

Si vous activez ce paramètre de stratégie, le service WinRM écoutera automatiquement le réseau pour détecter les demandes utilisant le transport HTTP sur le port HTTP par défaut.

Si vous désactivez ce paramètre de stratégie ou si vous ne le configurez pas, le service Gestion à distance de Windows n’écoutera pas automatiquement le réseau et vous devrez créer manuellement des écouteurs sur chaque ordinateur.

Pour autoriser le service WinRM à recevoir des demandes sur le réseau, configurez le paramètre de stratégie Pare-feu Windows en définissant des exceptions pour le port 5985 (port par défaut du protocole HTTP).

Le service écoutera les adresses spécifiées par les filtres IPv4 et IPv6. Un filtre IPv4 spécifie une ou plusieurs plages d’adresses IPv4 et un filtre IPv6 spécifie une ou plusieurs plages d’adresses IPv6. Si l’un de ces filtres est spécifié, le service énumérera les adresses IP disponibles sur l’ordinateur et n’utilisera que les adresses figurant dans l’une des plages du filtre.

Pour indiquer que le service écoute toutes les adresses IP disponibles sur l’ordinateur, utilisez un astérisque (*). Lorsque l’astérisque (*) est utilisé, les autres plages du filtre sont ignorées. Si le filtre reste vide, le service n’écoute aucune adresse.

Par exemple, si vous voulez que le service écoute uniquement les adresses IPv4, laissez le filtre IPv6 vide.

Pour spécifier des plages d’adresses, utilisez la syntaxe IP1-IP2. Pour séparer plusieurs plages d’adresses, utilisez une virgule « , ».

Exemple de filtre IPv4 :\n2.0.0.1-2.0.0.20, 24.0.0.1-24.0.0.22 Exemple de filtre IPv6 :\n3FFE:FFFF:7654:FEDA:1245:BA98:0000:0000-3FFE:FFFF:7654:FEDA:1245:BA98:3210:4562</string>
      <string id="DisallowDigest">Ne pas autoriser l’authentification Digest</string>
      <string id="DisallowDigestClientHelp">Ce paramètre de stratégie vous permet de définir si le client Gestion à distance de Windows (WinRM) ne devra pas utiliser l’authentification Digest.

Si vous activez ce paramètre de stratégie, le client WinRM n’utilisera pas l’authentification Digest.

Si vous désactivez ce paramètre de stratégie ou si vous ne le configurez pas, le client WinRM utilisera l’authentification Digest.</string>
      <string id="DisallowNegotiate">Ne pas autoriser l’authentification par négociation</string>
      <string id="DisallowKerberos">Ne pas autoriser l’authentification Kerberos</string>
      <string id="DisallowNegotiateClientHelp">Ce paramètre de stratégie vous permet de définir si le client Gestion à distance de Windows (WinRM) ne devra pas utiliser l’authentification par négociation.

Si vous activez ce paramètre de stratégie, le client WinRM n’utilisera pas l’authentification par négociation.

Si vous désactivez ce paramètre de stratégie ou si vous ne le configurez pas, le client WinRM utilisera l’authentification par négociation.</string>
      <string id="DisallowNegotiateServiceHelp">Ce paramètre de stratégie vous permet de définir si le service Gestion à distance de Windows (WinRM) n’acceptera pas l’authentification par négociation de la part d’un client distant.

Si vous activez ce paramètre de stratégie, le service WinRM n’acceptera pas l’authentification par négociation à partir d’un client distant.

Si vous désactivez ce paramètre de stratégie ou si vous ne le configurez pas, le service WinRM acceptera l’authentification par négociation à partir d’un client distant.</string>
      <string id="DisallowKerberosClientHelp">Ce paramètre de stratégie vous permet de définir si le client Gestion à distance de Windows (WinRM) ne devra pas utiliser directement l’authentification Kerberos.

Si vous activez ce paramètre de stratégie, le client Gestion à distance de Windows n’utilisera pas directement l’authentification Kerberos. Celle-ci pourra toujours être utilisée si le client Gestion à distance de Windows utilise l’authentification par négociation et si Kerberos est sélectionné.

Si vous désactivez ce paramètre de stratégie ou si vous ne le configurez pas, le client Gestion à distance de Windows utilisera directement l’authentification Kerberos.</string>
      <string id="DisallowKerberosServiceHelp">Ce paramètre de stratégie vous permet de définir si le service Gestion à distance de Windows (WinRM) n’acceptera pas d’informations d’identification Kerberos sur le réseau.

Si vous activez ce paramètre de stratégie, le service WinRM n’acceptera pas les informations d’identification Kerberos sur le réseau.

Si vous désactivez ce paramètre de stratégie ou si vous ne le configurez pas, le service WinRM acceptera l’authentification Kerberos à partir d’un client distant.</string>
      <string id="AllowCredSSP">Autoriser l'authentification CredSSP</string>
      <string id="AllowCredSSPClientHelp">
        Ce paramètre de stratégie vous permet de définir si le client Gestion à distance de Windows (WinRM) devra utiliser l’authentification CredSSP.

        Si vous activez ce paramètre de stratégie, le client WinRM utilisera l’authentification CredSSP. 

        Si vous désactivez ce paramètre de stratégie ou si vous ne le configurez pas, le client WinRM n’utilisera pas l’authentification CredSSP.
      </string>
      <string id="AllowCredSSPServiceHelp">
        Ce paramètre de stratégie vous permet de définir si le service Gestion à distance de Windows (WinRM) acceptera l’authentification CredSSP de la part d’un client distant.

        Si vous activez ce paramètre de stratégie, le service WinRM acceptera l’authentification CredSSP à partir d’un client distant.

        Si vous désactivez ce paramètre de stratégie ou si vous ne le configurez pas, le service WinRM n’acceptera pas l’authentification CredSSP à partir d’un client distant.
      </string>
      <string id="CBTHardeningLevel">Spécifiez le niveau de sécurisation renforcée de la liaison de canal</string>
      <string id="CBTHardeningLevelHelp">
        Ce paramètre de stratégie vous permet de définir le niveau de sécurisation renforcée que le service Gestion à distance de Windows (WinRM) appliquera pour les jetons de liaison de canal.

        Si vous activez ce paramètre de stratégie, le service WinRM appliquera le niveau de sécurisation renforcée défini pour déterminer s'il doit ou non accepter une demande qu'il a reçue, sur la base d'un jeton de liaison de canal fourni.

        Si vous désactivez ce paramètre de stratégie ou si vous ne le configurez pas, vous pouvez configurer le niveau de sécurisation renforcée localement sur chaque ordinateur.

        Si le paramètre Niveau de sécurisation renforcée a la valeur Strict, toute demande qui ne contient pas un jeton de liaison de canal valide sera rejetée.

        Si le paramètre Niveau de sécurisation renforcée a la valeur Approximatif (valeur par défaut), toute demande qui contient un jeton de liaison de canal non valide sera rejetée. En revanche, une demande ne contenant aucun jeton de liaison de canal sera acceptée (mais elle ne sera pas protégée contre les attaques par transfert d'informations d'identification).

        Si le paramètre Niveau de sécurisation renforcée a la valeur Aucun, toutes les demandes seront acceptées (mais elles ne seront pas protégées contre les attaques par transfert d'informations d'identification).
      </string>
        <string id="None">Aucun</string>
        <string id="Strict">Strict</string>
        <string id="Relaxed">Approximatif</string>
        <string id="WinRM">Gestion à distance de Windows (WinRM)</string>
      <string id="WinRMClient">Client Gestion à distance de Windows</string>
      <string id="WinRMService">Service Gestion à distance de Windows</string>
      <string id="TrustedHosts">Hôtes approuvés</string>
      <string id="TrustedHostsHelp">Ce paramètre de stratégie vous permet de définir si le client Gestion à distance de Windows (WinRM) utilisera la liste spécifiée dans TrustedHostsList afin de déterminer si l’hôte de destination est une entité approuvée.

Si vous activez ce paramètre de stratégie, le client WinRM utilisera la liste spécifiée dans TrustedHostsList afin de déterminer si l’hôte de destination est une entité approuvée. Le client Gestion à distance de Windows utilise cette liste lorsque ni HTTPS ni Kerberos ne sont utilisés pour authentifier l’identité de l’hôte.

Si vous désactivez ce paramètre de stratégie ou si vous ne le configurez pas, et si le client Gestion à distance de Windows a besoin d’utiliser la liste des hôtes approuvés, vous devez configurer la liste des hôtes approuvés localement sur chaque ordinateur.</string>
      <string id="HttpCompatibilityListener">Activer l'écouteur HTTP de compatibilité</string>
      <string id="HttpsCompatibilityListener">Activer l'écouteur HTTPS de compatibilité</string>
      <string id="HttpCompatibilityListenerHelp">Ce paramètre de stratégie active ou désactive l'écouteur HTTP créé en vue d'assurer la compatibilité descendante dans le service Gestion à distance de Windows (WinRM).
          
          En cas de migration de certains écouteurs du port 80 vers WinRM 2.0, le numéro du port d'écoute est remplacé par le numéro 5985.
          
          Il est possible de créer automatiquement un écouteur pour le port 80 afin d'assurer la compatibilité descendante.
          
          Lorsque ce paramètre est activé, cet écouteur est systématiquement affiché.
          
          En revanche, si vous désactivez ce paramètre, l'écouteur n'apparaît jamais.</string>
      <string id="HttpsCompatibilityListenerHelp">Ce paramètre de stratégie active ou désactive l'écouteur HTTPS créé en vue d'assurer la compatibilité descendante dans le service Gestion à distance de Windows (WinRM).
          
          En cas de migration de certains écouteurs du port 443 vers WinRM 2.0, le numéro du port d'écoute est remplacé par le numéro 5985.
          
          Il est possible de créer automatiquement un écouteur pour le port 443 afin d'assurer la compatibilité descendante.
          
          Lorsque ce paramètre est activé, cet écouteur est systématiquement affiché.
          
          En revanche, si vous désactivez ce paramètre, l'écouteur n'apparaît jamais.</string>
    </stringTable>
    <presentationTable>
      <presentation id="AllowAutoConfig">
        <textBox refId="AllowAutoConfig_IPv4Filter">
          <label>Filtre IPv4 :</label>
        </textBox>
        <textBox refId="AllowAutoConfig_IPv6Filter">
          <label>Filtre IPv6 :</label>
        </textBox>
        <text>Syntaxe :</text>
        <text>Tapez « * » afin d’autoriser les messages provenant de n’importe quelle adresse IP, ou laissez le</text>
        <text>champ vide pour ne pas écouter les adresses IP. Vous pouvez spécifier une</text>
        <text>ou plusieurs plages d’adresses IP.</text>
        <text/>
        <text>Exemple de filtre IPv4 :</text>
        <text>2.0.0.1-2.0.0.20, 24.0.0.1-24.0.0.22</text>
        <text>*</text>
        <text/>
        <text>Exemple de filtre IPv6 :</text>
        <text>3FFE:FFFF:7654:FEDA:1245:BA98:0000:0000-3FFE:FFFF:7654:FEDA:1245:BA98:3210:4562</text>
        <text>*</text>
      </presentation>
      <presentation id="TrustedHosts">
        <textBox refId="TrustedHosts_List">
          <label>TrustedHostsList :</label>
        </textBox>
        <text>Syntaxe :</text>
        <text>Configurez les hôtes approuvés dans une liste</text>
        <text>de noms d’hôtes. Vous pouvez utiliser des caractères génériques (*), mais un seul</text>
        <text>caractère générique est autorisé par liste de noms d’hôtes.</text>
        <text>Utilisez la valeur « &lt;local&gt; » (sans respecter la casse) pour indiquer</text>
        <text>tous les noms d’hôtes ne contenant pas de point (.).</text>
        <text>Définissez une liste vide pour indiquer qu’aucun hôte n’est approuvé.</text>
        <text>Utilisez un astérisque (*) pour indiquer que tous les hôtes sont approuvés.</text>
        <text>Si vous utilisez *, aucun autre caractère générique ne peut figurer dans la liste.</text>
        <text/>
        <text/>
        <text>Exemples :</text>
        <text>*.mondomaine.com indique que tous les ordinateurs de mondomaine.com sont approuvés</text>
        <text>2.0.* indique que toutes les adresses IP commençant par 2.0. sont approuvées</text>
      </presentation>
        <presentation id="CBTHardeningLevel">
            <dropdownList refId="HardeningLevelCombo" defaultItem="1">Niveau de sécurisation renforcée :</dropdownList>
        </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
