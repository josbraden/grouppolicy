<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>πληκτρολογήστε εδώ το όνομα εμφάνισης</displayName>
  <description>πληκτρολογήστε εδώ την περιγραφή</description>
  <resources>
    <stringTable>
      <string id="SNMP_Communities">Κοινότητες</string>
      <string id="SNMP_PermittedManagers">Διαχειριστές με άδεια πρόσβασης</string>
      <string id="SNMP_PermittedManagers_Help">Αυτή η ρύθμιση καθορίζει τη λίστα των επιτρεπόμενων κεντρικών υπολογιστών οι οποίο μπορούν να υποβάλλουν ερώτημα στον παράγοντα της υπηρεσίας Απλού πρωτοκόλλου διαχείρισης δικτύου (SNMP), ο οποίος εκτελείται στον υπολογιστή-πελάτη.

Το πρωτόκολλο SNMP είναι ένα πρωτόκολλο σχεδιασμένο να παρέχει στο χρήστη τη δυνατότητα απομακρυσμένης διαχείρισης ενός δικτύου υπολογιστών, κάνοντας ανίχνευση και ορίζοντας τις τιμές τερματικών και εποπτεύοντας τα συμβάντα δικτύου.

Ο διαχειριστής βρίσκεται στον κεντρικό υπολογιστή του δικτύου. Ο ρόλος του είναι να ανιχνεύει τους παράγοντες για ορισμένες ζητούμενες πληροφορίες.

Αν ενεργοποιήσετε αυτήν τη ρύθμιση, ο παράγοντας SNMP δέχεται αιτήματα μόνο από τη λίστα των διαχειριστών με άδεια πρόσβασης που έχετε ορίσει με αυτήν τη ρύθμιση.

Αν απενεργοποιήσετε αυτήν τη ρύθμιση ή δεν ρυθμίσετε τις παραμέτρους της, η υπηρεσία SNMP θα χρησιμοποιεί τους διαχειριστές με άδεια πρόσβασης που έχουν ρυθμιστεί στον τοπικό υπολογιστή.

Βέλτιστη πρακτική: Για λόγους ασφαλείας, προτείνεται να περιορίσετε το κλειδί HKML\SOFTWARE\Policies\SNMP\Parameters\PermittedManagers ώστε να επιτρέπεται πλήρης έλεγχος μόνο στην τοπική ομάδα διαχειριστών.

Σημείωση: Αυτή η ρύθμιση δεν έχει καμία ισχύ αν ο παράγοντας SNMP δεν είναι εγκατεστημένος στον υπολογιστή-πελάτη.

Επίσης, δείτε τις άλλες δύο ρυθμίσεις SNMP: "Ρύθμιση παραμέτρων παγίδευσης" και "Όνομα κοινότητας".</string>
      <string id="SNMP_SNMP">SNMP</string>
      <string id="SNMP_TrapDestinations_Help">Αυτή η ρύθμιση επιτρέπει τη ρύθμιση παραμέτρων παγίδευσης για τον παράγοντα της υπηρεσίας SNMP.

Το πρωτόκολλο SNMP είναι ένα πρωτόκολλο σχεδιασμένο να παρέχει στο χρήστη τη δυνατότητα απομακρυσμένης διαχείρισης ενός δικτύου υπολογιστών, κάνοντας ανίχνευση και ορίζοντας τις τιμές τερματικών και εποπτεύοντας τα συμβάντα δικτύου.

Αυτή η ρύθμιση σας επιτρέπει να ορίσετε το όνομα των κεντρικών υπολογιστών που λαμβάνουν μηνύματα παγίδευσης για την κοινότητα που αποστέλλονται από την υπηρεσία SNMP. Ένα μήνυμα παγίδευσης είναι μια ειδοποίηση ή σημαντικό συμβάν που επιτρέπει στον παράγοντα της υπηρεσίας SNMP να ειδοποιεί ασύγχρονα τα συστήματα διαχείρισης.

Αν ενεργοποιήσετε αυτήν τη ρύθμιση, η υπηρεσία SNMP αποστέλλει μηνύματα παγίδευσης στους κεντρικούς υπολογιστές εντός της "δημόσιας" κοινότητας.

Αν απενεργοποιήσετε αυτήν τη ρύθμιση ή δεν ρυθμίσετε τις παραμέτρους της, η υπηρεσία SNMP θα χρησιμοποιεί τις ρυθμίσεις παραμέτρων παγίδευσης του τοπικού υπολογιστή.

Σημείωση: Αυτή η ρύθμιση δεν έχει καμία ισχύ αν ο παράγοντας SNMP δεν είναι εγκατεστημένος στον υπολογιστή-πελάτη.

Επίσης, δείτε τις άλλες δύο ρυθμίσεις SNMP: "Διαχειριστές με άδεια πρόσβασης" και "Όνομα κοινότητας".</string>
      <string id="SNMP_Traps_Public">Παγιδεύσεις για δημόσια κοινότητα</string>
      <string id="SNMP_ValidCommunities_Help">Ρυθμίζει τις παραμέτρους της λίστας των κοινοτήτων που ορίζονται στην υπηρεσία SNMP.

Το πρωτόκολλο SNMP είναι ένα πρωτόκολλο σχεδιασμένο να παρέχει στο χρήστη τη δυνατότητα απομακρυσμένης διαχείρισης ενός δικτύου υπολογιστών, κάνοντας ανίχνευση και ορίζοντας τις τιμές τερματικών και εποπτεύοντας τα συμβάντα δικτύου.

Μια έγκυρη κοινότητα είναι μια κοινότητα που αναγνωρίζεται από την υπηρεσία SNMP, ενώ μια κοινότητα είναι μια ομάδα κεντρικών υπολογιστών (διακομιστών, σταθμών εργασίας, διανομέων και δρομολογητών) των οποίων η διαχείριση γίνεται μαζί μέσω SNMP. Η υπηρεσία SNMP είναι ένας διαχειριζόμενος κόμβος δικτύου που λαμβάνει πακέτα SNMP από το δίκτυο.

Αν ενεργοποιήσετε αυτήν τη ρύθμιση, ο παράγοντας της υπηρεσίας SNMP δέχεται αιτήματα μόνο από συστήματα διαχείρισης εντός των κοινοτήτων που αναγνωρίζει και επιτρέπεται μόνο η λειτουργία SNMP Read για την κοινότητα.

Αν απενεργοποιήσετε αυτήν τη ρύθμιση ή δεν ρυθμίσετε τις παραμέτρους της, η υπηρεσία SNMP θα χρησιμοποιεί τις έγκυρες κοινότητες που έχουν ρυθμιστεί στον τοπικό υπολογιστή.

Βέλτιστη πρακτική: Για λόγους ασφαλείας, προτείνεται να περιορίσετε το κλειδί HKLM\SOFTWARE\Policies\SNMP\Parameters\ValidCommunities ώστε να επιτρέπεται πλήρης έλεγχος μόνο στην τοπική ομάδα διαχειριστών.

Σημείωση: Είναι καλή πρακτική να χρησιμοποιήσετε ένα κρυπτογραφικό όνομα κοινότητας.

Σημείωση: Αυτή η ρύθμιση δεν έχει καμία ισχύ αν ο παράγοντας SNMP δεν είναι εγκατεστημένος στον υπολογιστή-πελάτη.

Επίσης, δείτε τις άλλες δύο ρυθμίσεις SNMP: "Διαχειριστές με άδεια πρόσβασης" και "Ρύθμιση παραμέτρων παγίδευσης".</string>
    </stringTable>
    <presentationTable>
      <presentation id="SNMP_Communities">
        <listBox refId="SNMP_CommunitiesListbox">Κοινότητες</listBox>
      </presentation>
      <presentation id="SNMP_PermittedManagers">
        <listBox refId="SNMP_PermittedManagersListbox">Διαχειριστές με άδεια πρόσβασης</listBox>
      </presentation>
      <presentation id="SNMP_Traps_Public">
        <listBox refId="SNMP_Traps_PublicListbox">Ρύθμιση παραμέτρων παγίδευσης</listBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
