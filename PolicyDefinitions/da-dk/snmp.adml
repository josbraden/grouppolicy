<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>indtast det viste navn her</displayName>
  <description>indtast beskrivelsen her</description>
  <resources>
    <stringTable>
      <string id="SNMP_Communities">Grupper</string>
      <string id="SNMP_PermittedManagers">Tilladte administratorer</string>
      <string id="SNMP_PermittedManagers_Help">Denne indstilling fastslår den tilladte liste over værter, der kan sende en forespørgsel til den SNMP-agent (Simple Network Management), som kører på klientcomputeren.

Simple Network Management Protocol er en protokol, der er udviklet til at give en bruger mulighed for at fjernstyre et computernetværk ved at anmode om og angive terminalværdier og overvåge netværkshændelser.

SNMP er placeret på værtscomputeren på netværket. SNMPs rolle er at anmode agenterne om bestemte oplysninger.

Hvis du aktiverer denne indstilling, accepterer SNMP-agenten kun anmodninger fra den liste over tilladte administratorer, du konfigurerer med denne indstilling.

Hvis du deaktiverer eller ikke konfigurerer denne indstilling, anvender SNMP-tjenesten i stedet de tilladte administratorer, der er konfigureret på den lokale computer.

Bedste fremgangsmåde: Af sikkerhedsmæssige årsager anbefales det, at du begrænser nøglen HKLM\SOFTWARE\Policies\SNMP\Parameters\PermittedManagers, så det kun er den lokale administratorgruppe, der har fuld kontrol.

Bemærk! Denne indstilling har ingen effekt, hvis SNMP-agenten ikke er installeret på klientcomputeren.

Se også de to andre SNMP-indstillinger: Trap-konfiguration og Gruppenavn.</string>
      <string id="SNMP_SNMP">SNMP</string>
      <string id="SNMP_TrapDestinations_Help">Du kan bruge denne indstilling til at angive trapkonfiguration for SNMP-agenten (Simple Network Management Protocol).

Simple Network Management Protocol er en protokol, der er udviklet til at give en bruger mulighed for at fjernstyre et computernetværk ved at anmode om og angive terminalværdier og overvåge netværkshændelser.

Denne indstilling giver dig mulighed for at konfigurere navnet på de værter, der modtager trapmeddelelser til gruppen, som er sendt af SNMP-tjenesten. En trapmeddelelse er en advarsel eller en vigtigt hændelse, som giver SNMP-agenten mulighed for at give asynkrone beskeder til administrationssystemer.

Hvis du aktiverer denne indstilling, sender SNMP-tjenesten trapmeddelelser til værterne i den offentlige gruppe.

Hvis du deaktiverer eller ikke konfigurerer denne indstilling, anvender SNMP-tjenesten i stedet den trapkonfiguration, der er konfigureret på den lokale computer.

Bemærk! Denne indstilling har ingen effekt, hvis SNMP-agenten ikke er installeret på klientcomputeren.

Se også de to andre SNMP-indstillinger: Tilladte administratorer og Gruppenavn.</string>
      <string id="SNMP_Traps_Public">Traps til offentlig gruppe</string>
      <string id="SNMP_ValidCommunities_Help">Konfigurerer en liste over grupper, der er defineret for SNMP-tjenesten (Simple Network Management Protocol).

SNMP er en protokol, der er udviklet til at give en bruger mulighed for at fjernstyre et computernetværk ved at anmode om og angive terminalværdier og overvåge netværkshændelser.

En gyldig gruppe er en gruppe, der genkendes af SNMP-tjenesten, mens en gruppe er en samling værter (servere, arbejdsstationer, hubber og routere), der administreres samlet af SNMP. SNMP-tjenesten er en administreret netværksnode, der modtager SNMP-pakker fra netværket.

Hvis du aktiverer denne indstilling, accepterer SNMP-agenten kun anmodninger fra administrationssystemer inden for de grupper, den genkender, og det er kun SNMP-handlingen Læsning, der er tilladt for gruppen.

Hvis du deaktiverer eller ikke konfigurerer denne indstilling, anvender SNMP-tjenesten i stedet de gyldige grupper, der er konfigureret på den lokale computer.

Bedste fremgangsmåde: Af sikkerhedsmæssige årsager anbefales det, at du begrænser nøglen HKLM\SOFTWARE\Policies\SNMP\Parameters\ValidCommunities, så det kun er den lokale administratorgruppe, der har fuld kontrol.

Bemærk! Det er en god idé at bruge et krypteret navn til gruppen.

Bemærk! Denne indstilling har ingen effekt, hvis SNMP-agenten ikke er installeret på klientcomputeren.

Se også de to andre SNMP-indstillinger: Tilladte administratorer og Trap-konfiguration.</string>
    </stringTable>
    <presentationTable>
      <presentation id="SNMP_Communities">
        <listBox refId="SNMP_CommunitiesListbox">Grupper</listBox>
      </presentation>
      <presentation id="SNMP_PermittedManagers">
        <listBox refId="SNMP_PermittedManagersListbox">Tilladte administratorer</listBox>
      </presentation>
      <presentation id="SNMP_Traps_Public">
        <listBox refId="SNMP_Traps_PublicListbox">Trap-konfiguration</listBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
