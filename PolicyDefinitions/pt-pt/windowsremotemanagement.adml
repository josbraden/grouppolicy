<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>introduzir o nome a apresentar aqui</displayName>
  <description>introduzir a descrição aqui</description>
  <resources>
    <stringTable>
      <string id="AllowAutoConfig">Permitir a configuração automática de serviços de escuta</string>
      <string id="AllowBasic">Permitir Autenticação básica</string>
      <string id="AllowBasicClientHelp">Esta definição de política permite-lhe gerir se o cliente de Gestão Remota do Windows (WinRM) utiliza a Autenticação básica.

Se activar esta definição de política, o cliente WinRM utilizará a Autenticação básica. Se WinRM estiver configurado para utilizar o transporte HTTP, então o nome de utilizador e a palavra-passe serão enviados pela rede como texto simples.

Se desactivar ou não configurar esta definição de política, então o cliente WinRM não vai utilizar a Autenticação básica.</string>
      <string id="AllowBasicServiceHelp">Esta definição de política permite-lhe gerir se o serviço de Gestão Remota do Windows (WinRM) aceita a Autenticação básica a partir de um cliente remoto.

Se activar esta definição de política, o serviço WinRM vai aceitar a Autenticação básica a partir de um cliente remoto.

Se desactivar ou não configurar esta definição de política, o serviço WinRM não vai aceitar a Autenticação básica a partir de um cliente remoto.</string>
      <string id="AllowUnencrypted">Permitir tráfego sem encriptação</string>
      <string id="AllowUnencryptedMessagesClientHelp">Esta definição de política permite-lhe gerir se o cliente de Gestão Remota do Windows (WinRM) envia e recebe mensagens sem encriptação pela rede. 

Se activar esta definição de política, então o cliente WinRM envia e recebe mensagens sem encriptação pela rede.

Se desactivar ou não configurar esta definição de política, o cliente WinRM envia e recebe apenas mensagens encriptadas pela rede.</string>
      <string id="AllowUnencryptedMessagesServiceHelp">Esta definição de política permite-lhe gerir se o serviço de Gestão Remota do Windows (WinRM) envia e recebe mensagens sem encriptação pela rede. 

Se activar esta definição de política, então o cliente WinRM envia e recebe mensagens sem encriptação pela rede.

Se desactivar ou não configurar esta definição de política, o cliente WinRM envia e recebe apenas mensagens encriptadas pela rede.</string>
      <string id="AutoConfigHelp">Esta definição de política permite-lhe gerir se o serviço Gestão Remota do Windows (WinRM) escuta automaticamente na rede a existência de pedidos no transporte HTTP através da porta HTTP predefinida.

Se activar esta definição de política, o serviço WinRM escutará automaticamente na rede a existência de pedidos no transporte HTTP através da porta HTTP predefinida.

Se desactivar ou não configurar esta definição de política, o serviço WinRM não escutará automaticamente na rede e o utilizador terá de criar manualmente serviços de escuta em cada computador.

Para permitir que o serviço WinRM receba pedidos através da rede, configure a definição de política da Firewall do Windows com excepções para a Porta 5985 (a porta predefinida para HTTP).

O serviço escuta nos endereços especificados pelos filtros IPv4 e IPv6. O filtro IPv4 especifica um ou mais intervalos de endereços IPv4 e o filtro IPv6 especifica um ou mais intervalos de endereços IPv6. Se especificado, o serviço enumera os endereços IP disponíveis no computador e utiliza apenas endereços que correspondam a um dos intervalos do filtro.

Deverá utilizar um asterisco (*) para indicar que o serviço escuta em todos os endereços IP disponíveis no computador. Quando * for utilizado, outros intervalos no filtro serão ignorados. Se o filtro for deixado em branco, o serviço não escutará em quaisquer endereços.

Por exemplo, se pretender que o serviço escute apenas em endereços IPv4, deixe o filtro IPv6 vazio.

Os intervalos são especificados utilizando a sintaxe IP1-IP2. Múltiplos intervalos são separados utilizando "," (vírgula) como o delimitador.

Filtros IPv4 de exemplo:\n2.0.0.1-2.0.0.20, 24.0.0.1-24.0.0.22
Filtros IPv6 de exemplo:\n3FFE:FFFF:7654:FEDA:1245:BA98:0000:0000-3FFE:FFFF:7654:FEDA:1245:BA98:3210:4562</string>
      <string id="DisallowDigest">Não permitir Autenticação de texto implícita</string>
      <string id="DisallowDigestClientHelp">Esta definição de política permite-lhe gerir se o cliente de Gestão Remota do Windows (WinRM) não vai utilizar a Autenticação de texto implícita.

Se activar esta definição de política, o cliente WinRM não utilizará a Autenticação de texto implícita.

Se desactivar ou não configurar esta definição de política, o cliente WinRM vai utilizar a Autenticação de texto implícita.</string>
      <string id="DisallowNegotiate">Não permitir Autenticação negociada</string>
      <string id="DisallowKerberos">Não permitir Autenticação Kerberos</string>
      <string id="DisallowNegotiateClientHelp">Esta definição de política permite-lhe gerir se o cliente de Gestão Remota do Windows (WinRM) não vai utilizar a Autenticação negociada.

Se activar esta definição de política, o cliente WinRM não utilizará a Autenticação negociada.

Se desactivar ou não configurar esta definição de política, o cliente WinRM vai utilizar a Autenticação negociada.</string>
      <string id="DisallowNegotiateServiceHelp">Esta definição de política permite-lhe gerir se o serviço de Gestão Remota do Windows (WinRM) não aceitará a Autenticação negociada a partir de um cliente remoto.

Se activar esta definição de política, o serviço WinRM não vai aceitar a Autenticação negociada a partir de um cliente remoto.

Se desactivar ou não configurar esta definição de política, o serviço WinRM vai aceitar a Autenticação negociada a partir de um cliente remoto.</string>
      <string id="DisallowKerberosClientHelp">Esta definição de política permite-lhe gerir se o cliente de Gestão Remota do Windows (WinRM) não vai utilizar a Autenticação Kerberos directamente.

Se activar esta definição de política, o cliente de Gestão Remota do Windows (WinRM) não vai utilizar directamente a Autenticação Kerberos. Kerberos poderá ainda ser utilizado se o cliente WinRM estiver a utilizar a Autenticação negociada e Kerberos estiver seleccionado.

Se desactivar ou não configurar esta definição de política, o cliente WinRM vai utilizar directamente a Autenticação Kerberos.</string>
      <string id="DisallowKerberosServiceHelp">Esta definição de política permite-lhe gerir se o serviço de Gestão Remota do Windows (WinRM) não vai aceitar as credenciais Kerberos pela rede.

Se activar esta definição de política, o serviço WinRM não vai aceitar as credenciais de Kerberos pela rede.

Se desactivar ou não configurar esta definição de política, então o serviço WinRM vai aceitar a Autenticação Kerberos a partir de um cliente remoto.</string>
      <string id="AllowCredSSP">Permitir autenticação CredSSP</string>
      <string id="AllowCredSSPClientHelp">
        Esta definição de política permite-lhe gerir se o cliente de Gestão Remota do Windows (WinRM) utiliza a autenticação de CredSSP.

        Se activar esta definição de política, o cliente WinRM utilizará a autenticação de CredSSP. 

        Se desactivar ou não configurar esta definição de política, o cliente WinRM não utilizará a autenticação de CredSSP.
      </string>
      <string id="AllowCredSSPServiceHelp">
        Esta definição de política permite-lhe gerir se o serviço de Gestão Remota do Windows (WinRM) aceita a autenticação de CredSSP a partir de um cliente remoto.

        Se activar esta definição de política, o serviço WinRM vai aceitar a autenticação de CredSSP a partir de um cliente remoto.

        Se desactivar ou não configurar esta definição de política, o serviço WinRM não vai aceitar a autenticação de CredSSP a partir de um cliente remoto.
      </string>
      <string id="CBTHardeningLevel">Especificar nível de protecção de token de enlaces de canal</string>
      <string id="CBTHardeningLevelHelp">
        Esta definição de política permite-lhe definir a protecção do serviço de Gestão Remota do Windows (WinRM) relativamente a tokens de enlaces de canal.

        Se activar esta definição de política, o serviço WinRM utiliza o nível especificado em HardeningLevel para determinar se deverá aceitar um pedido recebido, com base num token de enlaces de canal fornecido.

        Se desactivar ou não configurar esta definição de política, poderá configurar o nível de protecção localmente em cada computador.

       Se o HardeningLevel for definido para Restrito, qualquer pedido que não contenha um token de enlaces de canal válido será rejeitado.

        Se o HardeningLevel for definido para Impreciso (valor predefinido), qualquer pedido que contenha um token de enlaces de canal inválido será rejeitado. No entanto, um pedido que não contenha nenhum token de enlaces de canal será aceite (embora não fique protegido contra ataques de reencaminhamento de credenciais).

        Se o HardeningLevel for definido para Nenhum, todos os pedidos serão aceites (embora não fiquem protegidos contra ataques de reencaminhamento de credenciais).
      </string>
        <string id="None">Nenhum</string>
        <string id="Strict">Restrito</string>
        <string id="Relaxed">Imprecisas</string>
        <string id="WinRM">Gestão Remota do Windows (WinRM)</string>
      <string id="WinRMClient">Cliente WinRM</string>
      <string id="WinRMService">Serviço WinRM</string>
      <string id="TrustedHosts">Anfitriões Fidedignos</string>
      <string id="TrustedHostsHelp">Esta definição de política permite-lhe gerir se o cliente de Gestão Remota do Windows (WinRM) utiliza a lista especificada em TrustedHostsList para determinar se o anfitrião de destino é uma entidade fidedigna.

Se activar esta definição de política, o cliente WinRM utiliza a lista especificada em TrustedHostsList para determinar se o anfitrião de destino é uma entidade fidedigna. O cliente WinRM utiliza esta lista quando nem HTTPS nem Kerberos são utilizados para autenticar a identidade do anfitrião.

Se desactivar ou não configurar esta definição de política e o cliente WinRM necessitar de utilizar a lista de anfitriões fidedignos, terá de configurar a lista de anfitriões fidedignos localmente em cada computador.</string>
      <string id="HttpCompatibilityListener">Activar o Serviço de Escuta de HTTP de Compatibilidade</string>
      <string id="HttpsCompatibilityListener">Activar o Serviço de Escuta de HTTPS de Compatibilidade</string>
      <string id="HttpCompatibilityListenerHelp">Esta definição de política activa ou desactiva um serviço de escuta de HTTP criado para efeitos de retro-compatibilidade no serviço Gestão Remota do Windows (WinRM).
          
          Quando determinados serviços de escuta da porta 80 são migrados para o WinRM 2.0, o número de porta do serviço de escuta é alterado para 5985.
          
          Poderá ser automaticamente criado um serviço de escuta na porta 80 para garantir retro-compatibilidade.
          
          Quando esta definição está activada, este serviço de escuta aparece sempre.
          
          Quando esta definição está desactivada, este serviço de escuta nunca aparece.</string>
      <string id="HttpsCompatibilityListenerHelp">Esta definição de política activa ou desactiva um serviço de escuta de HTTPS criado para efeitos de retro-compatibilidade no serviço Gestão Remota do Windows (WinRM).
          
          Quando determinados serviços de escuta da porta 443 são migrados para o WinRM 2.0, o número de porta do serviço de escuta é alterado para 5986.
          
          Poderá ser automaticamente criado um serviço de escuta na porta 443 para garantir retro-compatibilidade.
          
          Quando esta definição está activada, este serviço de escuta aparece sempre.
          
          Quando esta definição está desactivada, este serviço de escuta nunca aparece.</string>
    </stringTable>
    <presentationTable>
      <presentation id="AllowAutoConfig">
        <textBox refId="AllowAutoConfig_IPv4Filter">
          <label>Filtro IPv4:</label>
        </textBox>
        <textBox refId="AllowAutoConfig_IPv6Filter">
          <label>Filtro IPv6:</label>
        </textBox>
        <text>Sintaxe:</text>
        <text>Escreva "*" para permitir mensagens de qualquer endereço IP ou deixe o</text>
        <text>campo vazio para não efectuar a escuta em nenhum endereço IP. Pode especificar um</text>
        <text>ou mais intervalos de endereços IP.</text>
        <text/>
        <text>Filtros IPv4 de exemplo:</text>
        <text>2.0.0.1-2.0.0.20, 24.0.0.1-24.0.0.22</text>
        <text>*</text>
        <text/>
        <text>Filtros ZPv6 de exemplo:</text>
        <text>3FFE:FFFF:7654:FEDA:1245:BA98:0000:0000-3FFE:FFFF:7654:FEDA:1245:BA98:3210:4562</text>
        <text>*</text>
      </presentation>
      <presentation id="TrustedHosts">
        <textBox refId="TrustedHosts_List">
          <label>TrustedHostsList:</label>
        </textBox>
        <text>Sintaxe:</text>
        <text>Configure os anfitriões fidedignos através de uma lista separada por vírgulas</text>
        <text>de nomes de anfitrião. Pode utilizar caracteres universais (*) mas só é permitido</text>
        <text>um carácter universal num padrão de nome de anfitrião.</text>
        <text>Utilize "&lt;local&gt;" (não sensível a maiúsculas e minúsculas) para indicar</text>
        <text>todos os nomes de anfitrião que não contêm um ponto (.).</text>
        <text>A lista pode estar vazia para indicar que nenhum anfitrião é fidedigno.</text>
        <text>Utilize um asterisco (*) para indicar que todos os anfitriões são fidedignos.</text>
        <text>Se utilizar *, então nenhum outro padrão pode ser apresentado na lista.</text>
        <text/>
        <text/>
        <text>Exemplos:</text>
        <text>*.meudomínio.com indica que todos os computadores no meudomínio.com são fidedignos</text>
        <text>2.0.* indica que todos os endereços IP que começam por 2.0 são fidedignos</text>
      </presentation>
        <presentation id="CBTHardeningLevel">
            <dropdownList refId="HardeningLevelCombo" defaultItem="1">Nível de Protecção:</dropdownList>
        </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
