<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>introduzir o nome a apresentar aqui</displayName>
  <description>introduzir a descrição aqui</description>
  <resources>
    <stringTable>
      <string id="DisableNamedPipeShutdownPolicyDescription">Desactivar a interface de encerramento remota legada</string>
      <string id="DisableNamedPipeShutdownPolicyDescription_Help">Esta política controla a interface de encerramento remota legada (pipe nomeado). A interface de encerramento remota de pipe nomeado é necessária para encerrar este sistema a partir de um sistema remoto do Windows XP ou do Windows Server 2003.

Se esta definição estiver activada, o sistema não cria a interface de encerramento remota de pipe nomeado.

Se esta definição estiver desactivada ou não for configurada, o sistema vai criar a interface de encerramento remota de pipe nomeado.</string>
      <string id="Shutdown">Opções de Encerramento</string>
      <string id="ShutdownTimeoutHungSessionsDescription">Tempo limite para sessões de início de sessão paradas durante o encerramento</string>
      <string id="ShutdownTimeoutHungSessionsDescription_Help">O número de minutos que o sistema vai aguardar pelas sessões de início de sessão paradas antes de avançar com o encerramento do sistema.

Se estas definições estiverem activadas, o sistema vai aguardar pelas sessões de início de sessão paradas durante o período de tempo em minutos especificado.

Se esta definição estiver desactivada ou não for configurada, o valor de tempo limite predefinido é de 3 minutos para estações de trabalho e de 15 minutos para servidores.</string>
    </stringTable>
    <presentationTable>
      <presentation id="ShutdownTimeoutHungSessionsDescription">
        <decimalTextBox refId="ShutdownSessionTimeout_Time" defaultValue="9999">Tempo limite de sessão parada em Minutos:</decimalTextBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
