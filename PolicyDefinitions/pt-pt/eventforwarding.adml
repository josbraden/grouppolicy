<?xml version="1.0"?>
<policyDefinitionResources revision="1.0" schemaVersion="1.0">
	<displayName>Reencaminhamento de Eventos</displayName>

	<description>Definições de Política para Reencaminhamento de Eventos</description>

	<resources>

		<stringTable>

			<string id="EventForwarding">Reencaminhamento de Eventos</string>

                        <string id="ForwarderResourceUsage">ForwarderResourceUsage</string>
                        <string id="ForwarderResourceUsage_Help">Controla a utilização de recursos para o reencaminhador. Cada definição é aplicada em todas as subscrições do reencaminhador.</string>
 
			<string id="SubscriptionManager">Configurar endereço de servidor, intervalo de actualização e autoridade de certificação do emissor de um Gestor de Subscrições de destino</string>

			<string id="Subscription_Help">Esta definição de política permite-lhe configurar o endereço de servidor, o intervalo de actualização e a autoridade de certificação (AC) do emissor de um Gestor de Subscrições de destino. O Gestor de Subscrições é o computador para o qual são reencaminhados os eventos.

Sintaxe: Option=Valor[,Option=Valor]*
Opções:
Server (obrigatório) - Endereço do computador para o qual devem ser reencaminhados os eventos. Quando o servidor utilizar o protocolo de transporte (HTTP) e a porta (5985) predefinidos, utilize "Server=&lt;FQDN&gt;" em que &lt;FQDN&gt; é o nome de domínio completamente qualificado do servidor. Caso contrário, especifique o URL completo. Por exemplo, ao utilizar o protocolo HTTPS, "Server=https://&lt;FQDN&gt;:5986/wsman/SubscriptionManager/WEC"
</string>

			<string id="SubscriptionManager_Listbox">SubscriptionManagers</string>
		</stringTable>

	
		<presentationTable>

			<presentation id="SubscriptionManager">
                                
				<listBox refId="SubscriptionManager_Listbox">SubscriptionManagers</listBox>

			</presentation>

                        <presentation id="ForwarderResourceUsage">

                                <decimalTextBox refId="MaxForwardingRate" spinStep="100">A taxa de reencaminhamento máxima (eventos/seg) permitida para o reencaminhador: </decimalTextBox>

                        </presentation>

		</presentationTable>

	</resources>

</policyDefinitionResources>
