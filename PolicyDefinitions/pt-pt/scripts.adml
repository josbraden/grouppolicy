<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>introduzir o nome a apresentar aqui</displayName>
  <description>introduzir a descrição aqui</description>
  <resources>
    <stringTable>
      <string id="MaxGPOScriptWaitPolicy">Tempo máximo de espera para scripts de Política de Grupo</string>
      <string id="MaxGPOScriptWaitPolicy_Help">Determina o tempo que o sistema aguarda pela execução dos scripts aplicados pela Política de Grupo.

Esta definição limita o tempo total permitido para que todos os scripts de início de sessão, fim de sessão, arranque e encerramento aplicados pela Política de Grupo terminem de ser executados. Se a execução dos scripts não tiver terminado quando o tempo especificado expirar, o sistema pára o processamento de scripts e regista um evento de erro.

Por predefinição, o sistema permite que o conjunto combinado de scripts seja executado durante 600 segundos (10 minutos) no máximo, mas poderá utilizar esta definição para ajustar este intervalo.

Para utilizar esta definição, na caixa Segundos, escreva um número entre 1 e 32.000 para o número de segundos que pretende que o sistema aguarde pela conclusão do conjunto de scripts. Para indicar ao sistema para esperar pela conclusão dos scripts, independentemente do tempo que demorar, prima 0.

Este intervalo é particularmente importante quando outras tarefas do sistema tiverem de aguardar pela conclusão dos scripts. Por predefinição, cada script de arranque deverá ser concluído antes da execução do seguinte. Além disso, poderá utilizar a definição "Executar os scripts de início de sessão de modo síncrono" para indicar ao sistema para aguardar pela conclusão dos scripts de início de sessão antes de carregar o ambiente de trabalho

Um intervalo excessivamente longo poderá atrasar o sistema e ser inconveniente para os utilizadores. No entanto, se o intervalo for demasiado curto, as tarefas previamente requisitadas poderão não ser concluídas e o sistema poderá parecer estar pronto prematuramente.</string>
      <string id="Run_Legacy_Logon_Script_Hidden">Executar os scripts de início de sessão legados em modo oculto</string>
      <string id="Run_Legacy_Logon_Script_Hidden_Explain">Oculta as instruções existentes em scripts de início de sessão escritos para o Windows NT 4.0 e versões anteriores.

Os scripts de início de sessão são ficheiros batch de instruções que são executados quando o utilizador inicia sessão. Por predefinição, o Windows 2000 apresenta as instruções existentes em scripts de início de sessão escritos para o Windows NT 4.0 e versões anteriores numa janela de comandos à medida que estes são executados, apesar de não apresentar os scripts de início de sessão escritos para o Windows 2000.

Se activar esta definição, o Windows 2000 não apresenta scripts de início de sessão escritos para o Windows NT 4.0 e versões anteriores.

Consulte também a definição "Executar os Scripts de Início de Sessão em Modo Visível".</string>

      <string id="Run_Logoff_Script_Visible">Executar os scripts de fim de sessão de modo visível</string>
      <string id="Run_Logoff_Script_Visible_Explain">Apresenta as instruções existentes nos scripts de fim de sessão à medida que estes são executados.

Os scripts de fim de sessão são ficheiros batch de instruções que são executados quando o utilizador termina sessão. Por predefinição, o sistema não apresenta as instruções existentes no script de fim de sessão.

Se activar esta definição, o sistema apresenta cada instrução existente no script de fim de sessão à medida que este é executado. As instruções são apresentadas numa janela de comandos. Esta definição foi concebida para utilizadores avançados.

Se desactivar esta definição ou não a configurar, as instruções não são apresentadas.</string>
      <string id="Run_Logon_Script_Sync">Executar os scripts de início de sessão de modo síncrono</string>
      <string id="Run_Logon_Script_Sync_Help">Indica ao sistema para aguardar que a execução dos scripts de início de sessão termine antes de iniciar o programa de interface do Explorador do Windows e criar o ambiente de trabalho.

Se activar esta definição, o Explorador do Windows não é iniciado até que a execução dos scripts de início de sessão termine. Esta definição garante que o processamento dos scripts de início de sessão fica concluído antes de o utilizador começar a trabalhar, mas pode atrasar a apresentação do ambiente de trabalho.

Se desactivar esta definição ou não a configurar, os scripts de início de sessão e o Explorador do Windows não são sincronizados e podem ser executados em simultâneo.

Esta definição aparece nas pastas Configuração do Computador e Configuração do Utilizador. A definição estabelecida em Configuração do Computador tem precedência sobre a definição estabelecida em Configuração do Utilizador.</string>
      <string id="Run_Logon_Script_Visible">Executar os scripts de início de sessão em modo visível</string>
      <string id="Run_Logon_Script_Visible_Explain">Apresenta as instruções existentes nos scripts de início de sessão à medida que estes são executados.

Os scripts de início de sessão são ficheiros batch de instruções que são executados quando o utilizador inicia sessão. Por predefinição, o sistema não apresenta as instruções existentes no script de início de sessão.

Se activar esta definição, o sistema apresenta cada instrução existente no script de início de sessão à medida que este é executado. As instruções são apresentadas numa janela de comandos. Esta definição foi concebida para utilizadores avançados.

Se desactivar esta definição ou não a configurar, as instruções não são apresentadas.</string>

      <string id="Run_Computer_PS_Scripts_First">Executar scripts Windows PowerShell primeiro quando o computador arrancar, encerrar</string>
      <string id="Run_User_PS_Scripts_First">Executar scripts Windows PowerShell primeiro quando o utilizador iniciar sessão, terminar sessão</string>
      <string id="Run_Computer_PS_Scripts_First_Explain">Esta definição de política determina se os scripts Windows PowerShell serão executados antes de scripts não PowerShell durante o arranque e encerramento do computador. Por predefinição, os scripts PowerShell são executados depois dos scripts não PowerShell. 
 
Se activar esta definição de política, em cada objecto de Política de Grupo (GPO) aplicável, os scripts PowerShell serão executados antes dos scripts não PowerShell durante o arranque e encerramento do computador. 

Por exemplo, considere o seguinte cenário: 

Existem três GPOs (GPO A, GPO B e GPO C). Esta definição de política está activada no GPO A. 

O GPO B e GPO C incluem os seguintes scripts de arranque de computador:

GPO B: B.cmd, B.ps1
GPO C: C.cmd, C.ps1

Considere também que existem dois computadores, ComputadorTI e ComputadorVendas. 
Para o ComputadorTI, são aplicados os GPOs A, B e C. Assim, os scripts para os GPOs B e C são executados pela seguinte ordem para o ComputadorTI:

No GPO B: B.ps1, B.cmd
No GPO C: C.ps1, C.cmd
 
Para o ComputadorVendas, são aplicados os GPOs B e C, mas não o GPO A. Assim, os scripts para os GPOs B e C são executados pela seguinte ordem para o ComputadorVendas:

No GPO B: B.cmd, B.ps1
No GPO C: C.cmd, C.ps1

Nota: Esta política determina a ordem pela qual os scripts de arranque e encerramento do computador são executados em todos os GPOs aplicáveis. Pode substituir esta definição de política para tipos de script específicos num GPO específico configurando as seguintes definições de política para o GPO:
 
Configuração do Computador\Políticas\Definições do Windows\Scripts (Arranque/Encerramento)\Arranque
Configuração do Computador\Políticas\Definições do Windows\Scripts (Arranque/Encerramento)\Encerrar
</string>
      <string id="Run_User_PS_Scripts_First_Explain">Esta definição de política determina se os scripts Windows PowerShell serão executados antes de scripts não PowerShell durante o início e fim de sessão do utilizador. Por predefinição, os scripts PowerShell são executados depois dos scripts não PowerShell. 
 
Se activar esta definição de política, em cada objecto de Política de Grupo (GPO) aplicável, os scripts PowerShell serão executados antes dos scripts não PowerShell durante o início e fim de sessão do utilizador. 

Por exemplo, considere o seguinte cenário: 

Existem três GPOs (GPO A, GPO B e GPO C). Esta definição de política está activada no GPO A. 

O GPO B e GPO C incluem os seguintes scripts de início de sessão do utilizador:

GPO B: B.cmd, B.ps1
GPO C: C.cmd, C.ps1

Considere também que existem dois utilizadores, Qin Hong e Tamara Johnston. 
Para o Qin, são aplicados os GPOs A, B e C. Assim, os scripts para os GPOs B e C são executados pela seguinte ordem para o Qin:

No GPO B: B.ps1, B.cmd
No GPO C: C.ps1, C.cmd
 
Para a Tamara, são aplicados os GPOs B e C, mas não o GPO A. Assim, os scripts para os GPOs B e C são executados pela seguinte ordem para Tamara:

No GPO B: B.cmd, B.ps1
No GPO C: C.cmd, C.ps1

Nota: Esta política determina a ordem pela qual os scripts de início e fim de sessão do utilizador são executados em todos os GPOs aplicáveis. Pode substituir esta definição de política para tipos de script específicos num GPO específico configurando as seguintes definições de política para o GPO:
 
Configuração do Utilizador\Políticas\Definições do Windows\Scripts (Início/Fim de Sessão)\Início de Sessão
Configuração do Utilizador\Políticas\Definições do Windows\Scripts (Início/Fim de Sessão)\Terminar Sessão

Esta definição aparece nas pastas Configuração do Computador e Configuração do Utilizador. A definição especificada na Configuração do Computador tem prioridade sobre a definição especificada na Configuração do Utilizador.
</string>
      <string id="Run_Shutdown_Script_Visible">Executar scripts de encerramento visíveis</string>
      <string id="Run_Shutdown_Script_Visible_Explain">Apresenta as instruções existentes nos scripts de encerramento de sessão à medida que estes são executados.

Os scripts de encerramento são ficheiros batch de instruções que são executados quando o utilizador reinicia ou encerra o sistema. Por predefinição, o sistema não apresenta as instruções existentes no script de encerramento de sessão.

Se activar esta definição, o sistema apresenta cada instrução existente no script de encerramento de sessão à medida que este é executado. As instruções são apresentadas numa janela de comandos.

Se desactivar esta definição ou não a configurar, as instruções não são apresentadas.</string>
      <string id="Run_Startup_Script_Sync">Executar os scripts de arranque de modo assíncrono</string>
      <string id="Run_Startup_Script_Sync_Help">Permite que o sistema execute scripts de arranque simultaneamente.

Scripts de arranque são ficheiros batch que são executados antes de o utilizador ser convidado a iniciar sessão. Por predefinição, o sistema aguarda a conclusão de todos os scripts de arranque antes de executar o script de arranque seguinte.

Se activar esta definição, o sistema não coordena a execução de scripts de arranque. Consequentemente, os scripts de arranque poderão ser executados simultaneamente.

Se desactivar esta definição ou não a configurar, um script de arranque só será executado quando a conclusão do script anterior estiver concluída.

Nota: A partir do sistema operativo Windows Vista, os scripts que estiverem configurados para serem executados em modo assíncrono deixam de ser apresentados durante o arranque, quer a definição de política "Executar os scripts de arranque em modo visível" esteja activa ou não.</string>
      <string id="Run_Startup_Script_Visible">Executar os scripts de arranque em modo visível</string>
      <string id="Run_Startup_Script_Visible_Explain">Apresenta as instruções existentes nos scripts de arranque à medida que estes são executados.

Scripts de arranque são ficheiros batch de instruções que são executados antes de o utilizador ser convidado a iniciar sessão. Por predefinição, o sistema não apresenta as instruções existentes no script de arranque.

Se activar esta definição, o sistema apresenta cada instrução existente no script de arranque à medida que este é executado. As instruções são apresentadas numa janela de comandos. Esta definição foi concebida para utilizadores avançados.

Se desactivar esta definição ou não a configurar, as instruções não são apresentadas.

Nota: A partir do sistema operativo Windows Vista, os scripts que estiverem configurados para serem executados em modo assíncrono deixam de ser apresentados no arranque, quer esta definição esteja ou não activa.</string>
      <string id="Allow_Logon_Script_NetbiosDisabled">Permitir scripts de início de sessão quando o NetBIOS ou o WINS estiver desactivado</string>
      <string id="Allow_Logon_Script_NetbiosDisabled_Explain">Esta definição de política permite a execução de scripts de início de sessão do utilizador quando o início de sessão é realizado entre florestas, os sufixos DNS não estão configurados e o NetBIOS ou WINS está desactivado. Esta definição de política afecta todas as contas de utilizador que iniciam sessão de forma interactiva no computador.

Se activar esta definição de política, os scripts de início de sessão do utilizador serão executados se o NetBIOS ou o WINS estiver desactivado durante os inícios de sessão entre florestas sem que os sufixos DNS estejam configurados.

Se desactivar esta definição de política ou não a configurar, nenhum dos registos interactivos entre florestas de contas de utilizador conseguirão executar os scripts de início de sessão caso o NetBIOS ou WINS esteja desactivado e os sufixos DNS não estejam configurados.</string>
      <string id="Scripts">Scripts</string>
    </stringTable>
    <presentationTable>
      <presentation id="MaxGPOScriptWaitPolicy">
        <decimalTextBox refId="MaxGPOScriptWait" defaultValue="600">Segundos:</decimalTextBox>
        <text>O intervalo é de 0 a 32000; utilize 0 para tempo de espera infinito</text>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
