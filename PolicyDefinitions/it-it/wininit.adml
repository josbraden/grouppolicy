<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>immettere qui il nome visualizzato</displayName>
  <description>immettere qui la descrizione</description>
  <resources>
    <stringTable>
      <string id="DisableNamedPipeShutdownPolicyDescription">Disattiva interfaccia di arresto remota legacy</string>
      <string id="DisableNamedPipeShutdownPolicyDescription_Help">Questa impostazione controlla l'interfaccia di arresto remota legacy (named pipe). L'interfaccia è necessaria per l'arresto del sistema in uso da un sistema Windows XP o Windows Server 2003 remoto.

Se si abilita questa impostazione, il sistema non creerà l'interfaccia di arresto remota named pipe.

Se si disabilita o non si configura questa impostazione, il sistema creerà l'interfaccia di arresto remota named pipe.</string>
      <string id="Shutdown">Opzioni di arresto</string>
      <string id="ShutdownTimeoutHungSessionsDescription">Timeout delle sessioni di accesso bloccate durante l'arresto</string>
      <string id="ShutdownTimeoutHungSessionsDescription_Help">Il numero di minuti di attesa delle sessioni di accesso bloccate prima che si proceda all'arresto del sistema.

Se si abilita questa impostazione, il sistema attenderà le sessioni di accesso bloccate per i minuti specificati.

Se si disabilita o non si configura questa impostazione, il valore di timeout predefinito sarà di 3 minuti per le workstation e di 15 minuti per i server.</string>
    </stringTable>
    <presentationTable>
      <presentation id="ShutdownTimeoutHungSessionsDescription">
        <decimalTextBox refId="ShutdownSessionTimeout_Time" defaultValue="9999">Timeout sessione bloccata in minuti:</decimalTextBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
