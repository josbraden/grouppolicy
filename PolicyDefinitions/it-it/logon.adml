<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>immettere qui il nome visualizzato</displayName>
  <description>immettere qui la descrizione</description>
  <resources>
    <stringTable>
      <string id="UseOEMBackground">Usa sempre sfondo di accesso personalizzato</string> 
      <string id="UseOEMBackground_Help">Ignora lo sfondo di accesso di Windows.

Questa impostazione può essere utilizzata per fare in modo che in Windows venga data preferenza a uno sfondo di accesso personalizzato.

Se si abilita questa impostazione, per la schermata di accesso verrà sempre tentato il caricamento di uno sfondo personalizzato anziché dello sfondo di accesso di Windows.

Se si disabilita o non si configura questa impostazione, verrà utilizzato lo sfondo di accesso di Windows predefinito o lo sfondo personalizzato.</string> 
      <string id="DisableExplorerRunLegacy">Non elaborare l'elenco di esecuzione precedente</string>
      <string id="DisableExplorerRunLegacy_Help">Ignora l'elenco di esecuzione personalizzato.

È possibile creare un elenco personalizzato di programmi e documenti aggiuntivi che il sistema avvia automaticamente se eseguito su Windows Vista, Windows XP Professional e Windows 2000 Professional. Questi programmi vengono aggiunti all'elenco di esecuzione standard dei programmi e servizi avviati dal sistema.

Se si abilita questa impostazione, il sistema ignorerà l'elenco di esecuzione per Windows Vista, Windows 2000 Professional e Windows XP Professional.

Se si disabilita o non si configura questa impostazione, Windows Vista aggiungerà qualsiasi elenco di esecuzione personalizzato configurato al proprio elenco di esecuzione.

Questa impostazione si trova nelle cartelle Configurazione computer e Configurazione utente. Se entrambe le impostazioni sono configurate, l'impostazione in Configurazione computer ha la priorità.

Nota: per creare un elenco di esecuzione personalizzato tramite un criterio, utilizzare l'impostazione "Esegui queste applicazioni all'avvio".

Vedere anche l'impostazione "Non elaborare l'elenco di esecuzione unica".</string>
      <string id="DisableExplorerRunOnceLegacy">Non elaborare l'elenco di esecuzione unica</string>
      <string id="DisableExplorerRunOnceLegacy_Help">Ignora gli elenchi personalizzati eseguibili una sola volta.

È possibile creare un elenco personalizzato di programmi e documenti aggiuntivi avviati automaticamente solo al successivo avvio del sistema, ma non in seguito. Questi programmi vengono aggiunti all'elenco standard dei programmi e servizi avviati dal sistema.

Se si abilita questa impostazione, il sistema ignorerà l'elenco di esecuzione unica.

Se si disabilita o non si configura questa impostazione, il sistema eseguirà i programmi inclusi nell'elenco di esecuzione unica.

Questa impostazione si trova nelle cartelle Configurazione computer e Configurazione utente. Se vengono configurate entrambe le impostazioni, l'impostazione di Configurazione computer risulterà prioritaria rispetto a quella di Configurazione utente.

Nota: gli elenchi di esecuzione unica personalizzati sono archiviati nella chiave del Registro di sistema HKEY_LOCAL_MACHINE\Software\Microsoft\Windows\CurrentVersion\RunOnce.

Vedere anche l'impostazione "Non elaborare l'elenco di esecuzione precedente".</string>
      <string id="DisableStatusMessages">Disattiva messaggi di stato relativi ad Avvio/Arresto del sistema/Accesso/Fine sessione</string>
      <string id="DisableStatusMessages_Help">Disabilita la visualizzazione dei messaggi sullo stato del sistema.

Se si abilita questa impostazione, non verranno visualizzati i messaggi che indicano che è in corso l'avvio o l'arresto del sistema oppure la connessione o la disconnessione.</string>
      <string id="Logon">Accesso</string>
      <string id="LogonType">Usa sempre schermata di accesso classica</string>
      <string id="LogonType_Help">Questa impostazione forza l'utente ad accedere al computer utilizzando la schermata di accesso classica. Per impostazione predefinita, un gruppo di lavoro è impostato in modo da utilizzare la schermata di accesso semplice. Questa impostazione funziona solo quando il computer non è parte di un dominio.</string>
      <string id="NoWelcomeTips">Non visualizzare la schermata iniziale della Guida introduttiva all'accesso</string>
      <string id="NoWelcomeTips_Help">Disattiva la visualizzazione della schermata iniziale.

Questa impostazione nasconde la schermata iniziale visualizzata ogni volta che l'utente accede a Windows 2000 Professional.

Per visualizzare la schermata iniziale, è possibile selezionarla dal menu Start oppure digitare "Welcome" nella finestra di dialogo Esegui.

Questa impostazione si applica solo a Windows 2000 Professional e non ha effetto sulla schermata "Configurazione server Windows 2000" di Windows 2000 Server.

Nota: questa impostazione si trova nelle cartelle Configurazione computer e Configurazione utente. Se vengono configurate entrambe le impostazioni, l'impostazione di Configurazione computer risulterà prioritaria rispetto a quella di Configurazione utente.

Suggerimento: per visualizzare la schermata iniziale, fare clic sul pulsante Start, scegliere Programmi, Accessori, Utilità di sistema e quindi "Guida introduttiva". Per disattivare la visualizzazione della schermata iniziale senza specificare un'impostazione, deselezionare la casella di controllo "Mostra questa schermata all'avvio" nella schermata iniziale.</string>
      <string id="Run">Esegui questi programmi all'accesso dell'utente</string>
      <string id="Run_Help">Specifica programmi o documenti aggiuntivi avviati automaticamente da Windows quando un utente accede al sistema.

Per specificare valori per questa impostazione, fare clic su Mostra. Nella colonna Valore della finestra di dialogo Mostra contenuto digitare il nome del file eseguibile del programma (con estensione exe) o del file del documento. Per specificare un altro nome, premere INVIO e digitare il nome. Se il file non si trova nella directory %Systemroot%, sarà necessario specificare il percorso completo del file.

Nota: questa impostazione si trova nelle cartelle Configurazione computer e Configurazione utente. Se vengono configurate entrambe le impostazioni, il sistema avvierà i programmi specificati nell'impostazione Configurazione computer prima di quelli specificati nell'impostazione Configurazione utente.

Vedere anche le impostazioni "Non elaborare l'elenco di esecuzione precedente" e "Non elaborare l'elenco di esecuzione unica".</string>
      <string id="SyncForegroundPolicy">Attendi sempre disponibilità rete all'avvio e all'accesso</string>
      <string id="SyncForegroundPolicy_Help">
Questa impostazione determina se l'elaborazione dei criteri di gruppo viene eseguita in modo sincrono, ovvero se i computer attendono l'inizializzazione completa della rete all'avvio e all'accesso utente. Per impostazione predefinita, l'elaborazione dei criteri di gruppo non è sincrona in quanto i computer client in genere non attendono l'inizializzazione completa della rete all'avvio e all'accesso. Gli utenti esistenti si connettono utilizzando le credenziali memorizzate nella cache, con tempi di accesso più brevi. I criteri di gruppo vengono applicati in background quando la rete diventa disponibile.

Si noti che a causa dell'aggiornamento in background, per estensioni quali Installazione software e Reindirizzamento cartelle sono necessari due accessi per applicare le modifiche. Per operare in modo sicuro, tali estensioni richiedono che nessun utente sia connesso. È pertanto necessario elaborarle in primo piano prima che gli utenti utilizzino attivamente il computer. È inoltre possibile che il rilevamento di modifiche apportate all'oggetto utente, ad esempio l'aggiunta di un percorso del profilo mobile, di una home directory o di uno script di accesso oggetto utente, richieda fino a due accessi.

Se un utente con un profilo mobile, una home directory o uno script di accesso oggetto utente accede a un computer, il computer attenderà sempre l'inizializzazione della rete prima di consentire l'accesso dell'utente. Se un utente accede al computer per la prima volta, il computer attenderà sempre l'inizializzazione della rete.

Se si abilita questa impostazione, i computer attenderanno l'inizializzazione completa della rete prima di consentire l'accesso degli utenti. I criteri di gruppo verranno applicati in primo piano in modo sincrono.

Nei server che eseguono Windows Server 2008 o versioni successive questa impostazione verrà ignorata durante l'elaborazione dei criteri di gruppo all'avvio del computer e l'elaborazione dei criteri di gruppo sarà sincrona, ovvero i server attenderanno l'inizializzazione della rete durante l'avvio del computer.

Se il server è configurato come descritto di seguito, questa impostazione verrà applicata durante l'elaborazione dei criteri di gruppo all'accesso dell'utente:
•  Il server è configurato come Terminal Server, ovvero il servizio ruolo Terminal Server è stato installato e configurato nel server) e
•  L'impostazione "Consenti elaborazione asincrona Criteri di gruppo utente quando l'accesso viene effettuato tramite Servizi terminal" è abilitata. Questa impostazione è disponibile in Configurazione computer\Criteri\Modelli amministrativi\Sistema\Criteri gruppo\.
Se questa configurazione non è implementata nel server, l'impostazione verrà ignorata. In questo caso, l'elaborazione dei criteri di gruppo all'accesso dell'utente sarà sincrona, ovvero i server attenderanno l'inizializzazione della rete durante l'accesso dell'utente.

Se si disabilita o non si configura questa impostazione e gli utenti accedono a un computer client o a un server che esegue Windows Server 2008 o versione successiva configurato come illustrato in precedenza, il computer non attenderà l'inizializzazione completa della rete. In questo caso, per l'accesso degli utenti vengono utilizzate le credenziali memorizzate nella cache. I criteri di gruppo vengono applicati in background in modo asincrono.

Note: 
se si desidera garantire che l'applicazione di Reindirizzamento cartelle, Installazione software o delle impostazioni del profilo utente mobile avvenga con un solo accesso, abilitare questa impostazione in modo che Windows attenda che la rete sia disponibile prima di applicare i criteri.
Se il criterio di reindirizzamento delle cartelle viene applicato durante il successivo accesso, i criteri di sicurezza verranno applicati in modo asincrono durante il ciclo di aggiornamento successivo, se è disponibile la connettività di rete.</string>
      <string id="VerboseStatus">Messaggi di stato dettagliati e normali</string>
      <string id="VerboseStatus_Help">Indica al sistema di visualizzare messaggi di stato molto dettagliati.

Se si abilita questa impostazione, verranno visualizzati messaggi di stato che includono informazioni dettagliate su tutti passaggi del processo di avvio, chiusura, connessione e disconnessione del sistema.

Questa impostazione è destinata a utenti esperti che necessitano di tale tipo di informazioni.

Nota: l'impostazione viene ignorata se è stata abilitata l'impostazione "Disattiva messaggi di stato relativi ad Avvio/Arresto del sistema/Accesso/Fine sessione".</string>
      <string id="HideFastUserSwitching">Nascondi punti di ingresso per Cambio rapido utente</string>
      <string id="HideFastUserSwitching_Help">Abilitando questo criterio, gli amministratori nascondono il pulsante Cambia utente nell'interfaccia utente di accesso, nel menu Start e in Gestione attività.</string>
      <string id="DisableStartupSound">Disattiva il suono di avvio di Windows</string>
      <string id="DisableStartupSound_Help">Disabilita il suono di avvio di Windows e ne evita la personalizzazione dall'applicazione Suono del Pannello di controllo.

Il suono di avvio di Microsoft Windows viene riprodotto all'avvio del sistema e può essere abilitato o disabilitato da Suono del Pannello di controllo.

L'abilitazione o la disabilitazione di questa impostazione consente di evitare la personalizzazione del comportamento predefinito del suono di avvio di Windows.

Se questa impostazione dei criteri è abilitata, il suono verrà disabilitato per tutti gli utenti.

Se questa impostazione è disabilitata, il suono verrà abilitato per tutti gli utenti.

Se questa impostazione dei criteri non è configurata, il suono di avvio di Windows verrà abilitato per tutti gli utenti per impostazione predefinita e sarà personalizzabile da Suono nel Pannello di controllo.

Questa impostazione non consente di evitare l'impostazione delle preferenze per altri suoni del sistema.</string>

    </stringTable>
    <presentationTable>
      <presentation id="Run_1">
        <listBox refId="RunListBox1">Elementi da eseguire all'accesso</listBox>
      </presentation>
      <presentation id="Run_2">
        <listBox refId="RunListBox2">Elementi da eseguire all'accesso</listBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
