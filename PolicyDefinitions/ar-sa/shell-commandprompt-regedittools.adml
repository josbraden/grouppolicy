<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>أدخل اسم العرض هنا</displayName>
  <description>أدخل الوصف هنا</description>
  <resources>
    <stringTable>
      <string id="DisableCMD">منع الوصول إلى موجه الأوامر</string>
      <string id="DisableCMD_Help">منع المستخدمين من تشغيل موجه الأوامر التفاعلي Cmd.exe. يحدد هذا الإعداد أيضاً ما إذا كان يمكن تشغيل الملفات الدفعية (.cmd و .bat) على الكمبيوتر أو لا.

في حالة تمكين هذا الإعداد ومحاولة المستخدم فتح نافذة الأمر يعرض النظام رسالة توضح أن إعدادًا يمنع هذا الإجراء.

ملاحظة: لا تمنع الكمبيوتر من تشغيل ملفات دفعية في حالة استخدام الكمبيوتر للبرامج النصية للملفات الدفعية لتسجيل الدخول أو تسجيل الخروج أو بدء التشغيل أو إيقاف التشغيل أو للمستخدمين الذين يستخدمون "خدمات سطح المكتب البعيد".</string>
      <string id="DisableCMD_NO">لا</string>
      <string id="DisableCMD_YES">نعم</string>
      <string id="DisableRegedit">منع الوصول إلى أدوات تحرير التسجيل</string>
      <string id="DisableRegedit_Help">تعطيل محرر تسجيل Windows ‏Regedit.exe.

في حالة تمكين هذا الإعداد ومحاولة المستخدم بدء تشغيل محرر التسجيل، تظهر رسالة توضح أن إعدادًا يمنع الإجراء.

لمنع المستخدمين من استخدام أدوات إدارية أخرى، استخدم الإعداد "تشغيل تطبيقات Windows المحددة فقط".</string>
      <string id="DisableRegeditMode_UIAndSilent">نعم</string>
      <string id="DisableRegeditMode_UIonly">لا</string>
  
   <string id="RestrictApps">تشغيل تطبيقات Windows المحددة فقط</string>
      <string id="RestrictApps_Help">يحدد برامج Windows التي يمتلك المستخدمون إذنًا لتشغيلها على الكمبيوتر.

إذا قمت بتمكين هذا الإعداد، يمكن للمستخدمين فقط تشغيل البرامج التي تضيفها إلى "قائمة التطبيقات المسموح بها".

يمنع هذا الإعداد فقط المستخدمين من تشغيل البرامج التي يتم بدء تشغيلها بواسطة عملية "مستكشف Windows". إنه لا يمنع المستخدمين من تشغيل البرامج، مثل "إدارة المهام"، التي يتم بدء تشغيلها بواسطة عملية نظام أو عمليات أخرى. أيضاً، إذا كان يتوفر للمستخدمين وصولاً إلى موجه الأوامر، Cmd.exe، فإن هذا الإعداد لا يمنعهم من بدء تشغيل البرامج في نافذة الأمر والتي لا يتم السماح بتشغيلها باستخدام "مستكشف Windows".

ملاحظة: إنه لمن الضروري أن تخضع تطبيقات الجهة الخارجية ذات شهادة Windows 2000 أو ما بعده لهذا الإعداد. ملاحظة: لإنشاء قائمة بالتطبيقات المسموح بها، انقر فوق "إظهار". وفي مربع الحوار "إظهار الملفات"، في عمود "القيمة"، أدخل اسم الملف التنفيذي للتطبيق (مثلاً، Winword.exe، أو Poledit.exe، أو Powerpnt.exe).</string>
      <string id="DisallowApps">لا تشغّل تطبيقات محددة لـ Windows</string>
      <string id="DisallowApps_Help">يمنع Windows من تشغيل البرامج التي تحددها في هذا الإعداد.

إذا قمت بتمكين هذا الإعداد، يتعذر على المستخدمين تشغيل البرامج التي تضيفها إلى قائمة التطبيقات غير المسموح بها.

يمنع هذا الإعداد فقط المستخدمين من تشغيل البرامج التي يتم بدء تشغيلها بواسطة عملية "مستكشف Windows". لا يمنع هذا الإعداد المستخدمين من تشغيل البرامج، مثل إدارة المهام، التي تم تشغيلها من قبل عملية النظام أو من قبل عمليات أخرى. أيضاً، إذا سمحت للمستخدمين بالوصول إلى موجه الأوامر، Cmd.exe، فإن هذا الإعداد لا يمنعها من تشغيل البرامج في نافذة الأوامر غير المسموح بتشغيله باستخدام مستكشف Windows. ملاحظة: لإنشاء قائمة التطبيقات غير المسموح بها، انقر فوق "إظهار". في مربع الحوار "إظهار المحتويات"، في عمود "القيمة"، أدخل الاسم التنفيذي للتطبيق (مثلاً، Winword.exe، وPoledit.exe, وPowerpnt.exe).</string>
      
  </stringTable>
    <presentationTable>
      <presentation id="DisableCMD">
        <dropdownList refId="DisableCMDScripts" noSort="true" defaultItem="1">تعطيل معالجة البرنامج النصي لموجه الأوامر أيضاً؟</dropdownList>
      </presentation>
      <presentation id="DisableRegedit">
        <dropdownList refId="DisableRegeditMode" noSort="true" defaultItem="0">تعطيل تشغيل regedit في الوضع الصامت؟</dropdownList>
      </presentation>
	<presentation id="RestrictApps">
        <listBox refId="RestrictApps_RestrictAppsList">قائمة التطبيقات المسموح بها</listBox>
      </presentation>
      <presentation id="DisallowApps">
        <listBox refId="DisallowAppsList">قائمة بالتطبيقات غير المسموح بها</listBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
