<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:xsd="http://www.w3.org/2001/XMLSchema" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>النسخ الاحتياطي لـ Windows</displayName>
  <description>النسخ الاحتياطي لـ Windows</description>
  <resources>
    <stringTable>
      <string id="AllowOnlySystemBackup">إتاحة فقط النسخ الاحتياطي للنظام </string>
      <string id="AllowOnlySystemBackupExplain">يتيح لك إعداد المنج هذا التحكم فيما إذا كانت النسخ الاحتياطية لوحدات تخزين النظام فقط متوفرة أو أنه يمكن النسخ الاحتياطي لكل من OS ووحدات تخزين البيانات.

في حالك قيامك بتمكين إعداد النهج هذا، يمكن لمسؤول الجهاز/عامل تشغيل النسخ الاحتياطي نسخ وحدات التخزين التي تستضيف مكونات OS فقط ولن يمكن نسخ أية بيانات أخرى غير وحدات التخزين. في حالة تعطيل إعداد النهج هذا أو عدم تكوينه، يمكن أن تحتوي النسخ الاحتياطية على كل من وحدات تخزين النظام والبيانات.</string>
      <string id="DisallowLocallyAttachedStorageAsBackupTarget">عدم السماح للتخزين المرفق محلياً بأن يكون هدفاً للنسخ الاحتياطي</string>
      <string id="DisallowLocallyAttachedStorageAsBackupTargetExplain">يتيح لك إعداد النهج هذا إمكانية التحكم فيما إذا كان يمكن تشغيل النسخ الاحتياطية لجهاز على التخزين المرفق محلياً أم لا.

في حالة قيامك بتمكين إعداد النهج هذا، فلن يمكن لمسؤول الجهاز/عامل تشغيل النسخ الاحتياطي استخدام Windows Server Backup لتشغيل النسخ الاحتياطية للتخزين المرفق محلياً أو للقرص. 

في حالة قيامك بتعطيل إعداد النهج هذا أو عدم تكوينه، فلن تكون هناك أية قيود على التخزين المرفق محلياً أو القرص المتخذ كهدف للنسخ الاحتياطي.</string>
      <string id="DisallowNetworkAsBackupTarget">عدم السماح للشبكة بأن تكون هدفاً للنسخ الاحتياطي</string>
      <string id="DisallowNetworkAsBackupTargetExplain">يتيح لك إعداد النهج هذا إمكانية التحكم فيما إذا كان يمكن تشغيل النسخ الاحتياطية لجهاز على مشاركة شبكة اتصال أم لا.

في حالة قيامك بتمكين إعداد النهج هذا، فلن يمكن لمسؤول الجهاز/عامل تشغيل النسخ الاحتياطي استخدام Windows Server Backup لتشغيل النسخ الاحتياطية لمشاركة شبكة اتصال. 

في حالة قيامك بتعطيل إعداد النهج هذا أو عدم تكوينه، فلن تكون هناك أية قيود على مشاركة شبكة اتصال المتخذة كهدف للنسخ الاحتياطي.</string>
      <string id="DisallowOpticalMediaAsBackupTarget">عدم السماح للوسائل المرئية بأن تكون هدفاً للنسخ الاحتياطي</string>
      <string id="DisallowOpticalMediaAsBackupTargetExplain">يتيح لك إعداد النهج هذا إمكانية التحكم فيما إذا كان يمكن تشغيل النُسخ الاحتياطية لجهاز على الوسائل المرئية أم لا.

في حالة قيامك بتمكين إعداد النهج هذا، فلن يمكن لمسؤول الجهاز/عامل تشغيل النسخ الاحتياطي استخدام Windows Server Backup لتشغيل النسخ الاحتياطية للوسائل المرئية.

في حالة قيامك بتعطيل إعداد النهج هذا أو عدم تكوينه، فلن تكون هناك أية قيود على الوسائل المرئية المتخذة كهدف للنسخ الاحتياطي.</string>
      <string id="DisallowRunOnceBackups">عدم السماح لنُسخ الاحتياطية التي يتم تشغيلها مرة واحدة</string>
      <string id="DisallowRunOnceBackupsexplain">يتيح لك إعداد النهج هذا إمكانية التحكم فيما إذا كان يمكن تشغيل النُسخ الاحتياطية التي يتم تشغيلها مرة واحدة على جهاز أم لا.

في حالة قيامك بتمكين إعداد النهج هذا، فلن يمكن لمسؤول الجهاز/عامل تشغيل النسخ الاحتياطي استخدام Windows Server Backup لتشغيل نُسخ احتياطية يتم تشغيلها لمرة واحدة غير مجدولة.

في حالة قيامك بتعطيل إعداد النهج هذا أو عدم تكوينه، فلن تكون هناك أية قيود على تشغيل النسخ الاحتياطية التي يتم تشغيلها لمرة واحدة.</string>
      <string id="supported_win_vista">Windows Server 2008 أو أحدث.</string>
      <string id="Backup">النسخ الاحتياطي</string>
      <string id="BackupServer">خادم</string>
      <string id="windowscomponents">مكونات Windows</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
