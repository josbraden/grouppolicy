<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>أدخل اسم العرض هنا</displayName>
  <description>أدخل الوصف هنا</description>
  <resources>
    <stringTable>
      <string id="WinMSG_NoAutoStartWindowsMsg_Comp">عدم بدء تشغيل Windows Messenger بشكل تلقائي في البداية</string>
      <string id="WinMSG_NoAutoStartWindowsMsg_Help">يتم تحميل Windows Messenger تلقائياً وتشغيله عندما يقوم مستخدم بتسجيل الدخول إلى كمبيوتر Windows XP. يمكنك استخدام هذا الإعداد لوقف التشغيل التلقائي لـ Windows Messenger عند تسجيل الدخول.

في حالة قيامك بتمكين هذا الإعداد، فلن يتم تحميل Windows Messenger تلقائياً عندما يقوم مستخدم بتسجيل الدخول.

في حالة قيامك بتعطيل هذا الإعداد أو عدم تكوينه، سيتم تحميل Windows Messenger تلقائياً عند تسجيل الدخول.

ملاحظة: ببساطة يمنع هذا الإعداد Windows Messenger من التشغيل بشكل أولي . وإذا قام المستخدم باستدعاء Windows Messenger واستخدامه ابتداءً من تلك النقطة، فعندها يتم تحميل Windows Messenger.

ويستطيع المستخدم تكوين هذا السلوك على التبويب 'تفضيلات' في القائمة 'أدوات' في واجهة المستخدم لـ Windows Messenger.

ملاحظة: إذا كنت لا تريد للمستخدمين أن يستخدموا Windows Messenger، فقم بتمكين الإعداد "عدم السماح بتشغيل Windows Messenger".

ملاحظة: يتوفر هذا الإعداد ضمن 'تكوين المستخدم' و'تكوين الكمبيوتر'. وإذا كان كلاهما موجوداً، فإن نسخة 'تكوين الكمبيوتر' لهذا الإعداد تكون لها الأسبقية.</string>
      <string id="WinMSG_NoAutoStartWindowsMsg_User">عدم بدء تشغيل Windows Messenger بشكل تلقائي في البداية</string>
      <string id="WinMSG_NoWindowsMsg_Comp">عدم السماح بتشغيل Windows Messenger</string>
      <string id="WinMSG_NoWindowsMsg_Help">يتيح لك إمكانية تعطيل Windows Messenger.

إذا قمت بتمكين هذا الإعداد، فلن يتم تشغيل Windows Messenger.

إذا قمت بتعطيل هذا الإعداد أو عدم تكوينه، يمكن تشغيل Windows Messenger.

ملاحظة: إذا قمت بتمكين هذا الإعداد، فلا يمكن لـ 'المساعدة عن بعد' كذلك استخدام Windows Messenger.

ملاحظة: يتوفر هذا الإعداد ضمن 'تكوين المستخدم' و'تكوين الكمبيوتر'. وإذا كان كلاهما موجوداً، فإن نسخة 'تكوين الكمبيوتر' لهذا الإعداد تكون لها الأسبقية.</string>
      <string id="WinMSG_NoWindowsMsg_User">عدم السماح بتشغيل Windows Messenger</string>
      <string id="WinMSG_WindowsMsg">Windows Messenger</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
