<?xml version="1.0"?>
<policyDefinitionResources revision="1.0" schemaVersion="1.0">
	<displayName>이벤트 전달</displayName>

	<description>이벤트 전달에 대한 정책 정의</description>

	<resources>

		<stringTable>

			<string id="EventForwarding">이벤트 전달</string>

                        <string id="ForwarderResourceUsage">ForwarderResourceUsage</string>
                        <string id="ForwarderResourceUsage_Help">전달자의 리소스 사용을 제어합니다. 각 설정은 전달자의 모든 가입에 적용됩니다.</string>
 
			<string id="SubscriptionManager">대상 가입 관리자의 서버 주소, 새로 고침 간격 및 발급자 인증 기관 구성</string>

			<string id="Subscription_Help">이 정책 설정으로 대상 가입 관리자의 서버 주소, 새로 고침 간격, 발급자 CA(인증 기관)를 구성할 수 있습니다. 가입 관리자는 이벤트가 전달되는 컴퓨터입니다.

구문: 옵션=값[,옵션=값]*
옵션:
서버(필수) - 이벤트가 전달되는 컴퓨터의 주소입니다. 서버가 기본 전송 프로토콜(HTTP) 및 포트(5985)를 사용하면 "Server=&lt;FQDN&gt;"을 사용하십시오. 여기서 &lt;FQDN&gt;은 서버의 정규화된 도메인 이름입니다. 그렇지 않은 경우 전체 URL을 지정하십시오. 예를 들어 HTTPS 프로토콜을 사용하면 "Server=https://&lt;FQDN&gt;:5986/wsman/SubscriptionManager/WEC"를 지정합니다.
</string>

			<string id="SubscriptionManager_Listbox">SubscriptionManagers</string>
		</stringTable>

	
		<presentationTable>

			<presentation id="SubscriptionManager">
                                
				<listBox refId="SubscriptionManager_Listbox">SubscriptionManagers</listBox>

			</presentation>

                        <presentation id="ForwarderResourceUsage">

                                <decimalTextBox refId="MaxForwardingRate" spinStep="100">전달자에 허용되는 최대 전달 속도(초당 이벤트 수): </decimalTextBox>

                        </presentation>

		</presentationTable>

	</resources>

</policyDefinitionResources>
