<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>Zde zadejte zobrazovaný název.</displayName>
  <description>Zde zadejte popis.</description>
  <resources>
    <stringTable>
      <string id="SNMP_Communities">Komunity</string>
      <string id="SNMP_PermittedManagers">Povolení správci</string>
      <string id="SNMP_PermittedManagers_Help">Toto nastavení určuje povolený seznam hostitelů, kteří mohou odeslat dotaz agentovi protokolu SNMP (Simple Network Management Protocol) spuštěnému v klientském počítači.

SNMP je protokol určený k tomu, aby poskytl uživateli schopnost vzdáleně řídit počítačovou síť cyklickým dotazováním a nastavením terminálových hodnot a sledováním událostí v síti.

Správce je umístěn v hostitelském počítači v síti. Role správce je dotazovat se cyklicky agentů na určité požadované informace.

Pokud toto nastavení povolíte, bude agent SNMP přijímat pouze požadavky ze seznamu povolených správců, který nakonfigurujete pomocí tohoto nastavení.

Jestliže toto nastavení zakážete nebo nenakonfigurujete, bude místo toho služba SNMP přebírat povolené správce nakonfigurované v místním počítači.

Doporučený postup: Z důvodu zabezpečení doporučujeme pomocí klíče registru HKLM\SOFTWARE\Policies\SNMP\Parameters\PermittedManagers nastavit omezení, aby úplnou kontrolu měla pouze skupina místních správců.

Poznámka: Toto nastavení je neúčinné, pokud v klientském počítači není nainstalován agent protokolu SNMP.

Viz také další dvě nastavení protokolu SNMP: Nastavení depeší a Název komunity.</string>
      <string id="SNMP_SNMP">Protokol SNMP</string>
      <string id="SNMP_TrapDestinations_Help">Toto nastavení umožňuje konfiguraci depeše pro agenta protokolu SNMP.

SNMP je protokol určený k tomu, aby poskytl uživateli schopnost vzdáleně řídit počítačovou síť cyklickým dotazováním a nastavením terminálových hodnot a sledováním událostí v síti.

Toto nastavení umožňuje konfigurovat názvy hostitelů, kteří přijímají zprávy depeší pro komunitu odeslané službou SNMP. Zpráva depeše je výstraha nebo důležitá událost, která umožní agentovi protokolu SNMP asynchronně upozornit systémy správy.

Pokud toto nastavení povolíte, bude služba SNMP odesílat zprávy depeší hostitelům ve veřejné komunitě.

Jestliže toto nastavení zakážete nebo nenakonfigurujete, bude místo toho služba SNMP přebírat konfiguraci depeší nastavenou v místním počítači.

Poznámka: Toto nastavení je neúčinné, pokud v klientském počítači není nainstalován agent protokolu SNMP.

Viz také další dvě nastavení protokolu SNMP: Povolení správci a Název komunity.</string>
      <string id="SNMP_Traps_Public">Depeše veřejné komunity</string>
      <string id="SNMP_ValidCommunities_Help">Konfiguruje seznam komunit definovaných pro službu SNMP (Simple Network Management Protocol).

SNMP je protokol umožňující uživateli vzdáleně spravovat počítačovou síť cyklickým dotazováním a nastavováním hodnot terminálu a sledováním síťových událostí.

Platná komunita je komunita rozpoznaná službou SNMP, zatímco komunita je skupina hostitelských zařízení (serverů, pracovních stanic, rozbočovačů a směrovačů), kterou jsou společně spravovány službou SNMP. Služba SNMP je uzel spravované sítě, který přijímá síťové pakety SNMP.

Pokud toto nastavení povolíte, pak agent služby SNMP bude přijímat pouze žádosti od rozpoznaných spravujících systémů v rámci komunity a komunitě je povolena pouze operace SNMP Read.

Pokud toto nastavení zakážete nebo nenakonfigurujete, pak služba SNMP namísto toho převezme platné komunity nakonfigurované v místním počítači.

Doporučený postup: Z důvodu zabezpečení doporučujeme pomocí klíče registru HKLM\SOFTWARE\Policies\SNMP\Parameters\ValidCommunities nastavit omezení, aby úplnou kontrolu měla pouze skupina místních správců.

Poznámka: Doporučujeme také použít pro komunitu kryptický název.

Poznámka: Toto nastavení je neúčinné, pokud v klientském počítači není nainstalován agent protokolu SNMP.

Viz také další dvě nastavení protokolu SNMP: Povolení správci a Nastavení depeší.</string>
    </stringTable>
    <presentationTable>
      <presentation id="SNMP_Communities">
        <listBox refId="SNMP_CommunitiesListbox">Komunity</listBox>
      </presentation>
      <presentation id="SNMP_PermittedManagers">
        <listBox refId="SNMP_PermittedManagersListbox">Povolení správci</listBox>
      </presentation>
      <presentation id="SNMP_Traps_Public">
        <listBox refId="SNMP_Traps_PublicListbox">Nastavení depeší</listBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
