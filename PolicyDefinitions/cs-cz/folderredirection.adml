<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>Zde zadejte zobrazovaný název.</displayName>
  <description>Zde zadejte popis.</description>
  <resources>
    <stringTable>
      <string id="Fdeploy_Cat">Přesměrování složky</string>
      <string id="LocalizeXPRelativePaths">Při přesměrování složek Nabídka Start a Dokumenty použít lokalizované názvy podsložek</string>
      <string id="LocalizeXPRelativePaths_Help">Nastavení této zásady umožňuje správci určit, zda má funkce Přesměrování složky při přesměrování složky Nabídka Start nebo složky Dokumenty ze starší verze systému použít pro podsložky Všechny programy, Po spuštění, Hudba, Obrázky a Filmy lokalizované názvy.

Pokud nastavení této zásady povolíte, systém Windows Vista a Windows 7 při přesměrování složky Nabídka Start nebo složky Dokumenty ze starší verze systému pro tyto podsložky použije lokalizované názvy.

Pokud nastavení této zásady zakážete nebo nenakonfigurujete, systém Windows Vista a Windows 7 při přesměrování složky Nabídka Start nebo složky Dokumenty ze starší verze systému pro tyto podsložky použije standardní anglické názvy.

Poznámka: Tato zásada je pro systémy Windows Vista a Windows 7 platná pouze při zpracování zásad přesměrování ze starších verzí systému, které pro tyto složky již byly nasazeny ve stávajícím lokalizovaném prostředí.</string>

 <string id="DisableFRAdminPin">Nepovolit automaticky zpřístupnění přesměrovaných složek offline</string>
      <string id="DisableFRAdminPin_Help">Ve výchozím nastavení jsou všechny přesměrované složky prostředí, například Kontakty, Dokumenty, Plocha, Oblíbené položky, Hudba, Obrázky, Filmy, Nabídka Start a AppData\Roaming, k dispozici v režimu offline. Pomocí tohoto nastavení můžete toto chování změnit, takže přesměrované složky prostředí nebudou automaticky k dispozici pro práci v režimu offline. Uživatelé však stále mohou sami soubory a složky zpřístupnit offline.

Pokud toto nastavení povolíte, budou uživatelé muset ručně vybrat soubory, které chtějí zpřístupnit offline.

Pokud toto nastavení zakážete nebo nenakonfigurujete, budou přesměrované složky prostředí automaticky zpřístupněny offline. Všechny podsložky v přesměrovaných složkách budou také zpřístupněny offline.

Poznámka: Toto nastavení nezabrání v automatickém ukládání souborů do mezipaměti, pokud je síťová sdílená složka nakonfigurována na automatické ukládání do mezipaměti, ani neovlivní dostupnost příkazu Zpřístupnit offline v uživatelském rozhraní.

Poznámka: Toto nastavení nepovolujte, pokud si nejste jisti, že uživatelé nebudou potřebovat přístup ke všem svým přesměrovaným souborům v případě, že nebude k dispozici síť nebo server, na kterém se nachází přesměrované soubory.</string>  
  </stringTable>
  </resources>
</policyDefinitionResources>
