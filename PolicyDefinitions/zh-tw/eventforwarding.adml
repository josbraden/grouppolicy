<?xml version="1.0"?>
<policyDefinitionResources revision="1.0" schemaVersion="1.0">
	<displayName>事件轉寄</displayName>

	<description>事件轉寄的原則定義</description>

	<resources>

		<stringTable>

			<string id="EventForwarding">事件轉寄</string>

                        <string id="ForwarderResourceUsage">ForwarderResourceUsage</string>
                        <string id="ForwarderResourceUsage_Help">控制轉寄站的資源使用量。每個設定都會套用到轉寄站的所有訂閱。</string>
 
			<string id="SubscriptionManager">設定伺服器位址、重新整理間隔以及目標訂閱管理員的簽發者憑證授權單位</string>

			<string id="Subscription_Help">這個原則設定可讓您設定伺服器位址、重新整理間隔以及目標訂閱管理員的簽發者憑證授權單位 (CA)。訂閱管理員是轉寄事件的目標電腦。

語法: 選項=值[,選項=值]*
選項:
Server (強制) - 轉寄事件的目標電腦位址。當伺服器使用預設傳輸通訊協定 (HTTP) 與連接埠 (5985) 時，請使用 "Server=&lt;FQDN&gt;"，其中 &lt;FQDN&gt; 是伺服器的完整網域名稱。其他狀況請指定完整的 URL。例如，使用 HTTPS 通訊協定時，請使用 "Server=https://&lt;FQDN&gt;:5986/wsman/SubscriptionManager/WEC"
</string>

			<string id="SubscriptionManager_Listbox">SubscriptionManagers</string>
		</stringTable>

	
		<presentationTable>

			<presentation id="SubscriptionManager">
                                
				<listBox refId="SubscriptionManager_Listbox">SubscriptionManagers</listBox>

			</presentation>

                        <presentation id="ForwarderResourceUsage">

                                <decimalTextBox refId="MaxForwardingRate" spinStep="100">轉寄站允許的最大轉寄速率 (事件/秒): </decimalTextBox>

                        </presentation>

		</presentationTable>

	</resources>

</policyDefinitionResources>
