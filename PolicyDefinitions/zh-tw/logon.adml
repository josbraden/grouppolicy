<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>在此輸入顯示名稱</displayName>
  <description>在此輸入描述</description>
  <resources>
    <stringTable>
      <string id="UseOEMBackground">永遠使用自訂登入背景</string> 
      <string id="UseOEMBackground_Help">忽略 Windows 登入背景。

這個原則設定可用來讓 Windows 優先使用自訂的登入背景。

如果啟用這個原則設定，登入畫面總是會嘗試載入自訂背景，而不是 Windows 的登入背景。

如果停用或未設定這個原則設定，Windows 會使用預設的 Windows 登入背景或自訂背景。</string> 
      <string id="DisableExplorerRunLegacy">不要處理舊版執行清單</string>
      <string id="DisableExplorerRunLegacy_Help">略過自訂執行清單。

您可以建立一份系統在 Windows Vista、Windows XP Professional 以及 Windows 2000 Professional 上執行時會自動啟動的自訂額外程式與文件清單。這些程式會被加入由系統啟動的程式與服務標準執行清單中。

如果您啟用這個原則設定，則系統會略過 Windows Vista、Windows 2000 Professional，以及 Windows XP Professional 的執行清單。

如果您停用或未設定這個原則設定，則 Windows Vista 會將所有已設定的自訂執行清單加入它自己的執行清單。

這項原則設定會同時出現在 [電腦設定] 及 [使用者設定] 資料夾中。如果兩個資料夾都已設定這項原則設定，則 [電腦設定] 中的原則設定將優先於 [使用者設定] 中的原則設定。

注意: 如果要使用原則來建立自訂執行清單，請使用 [啟動時執行這些應用程式] 原則設定。

另請參閱 [不要處理只執行一次清單] 原則設定。</string>
      <string id="DisableExplorerRunOnceLegacy">不要處理只執行一次清單</string>
      <string id="DisableExplorerRunOnceLegacy_Help">略過自訂的只執行一次清單。

您可以建立一份系統在下次啟動時會自動啟動 (但之後就不再自動啟動) 的自訂額外程式與文件清單。這些程式會被加入由系統啟動的程式與服務標準清單中。

如果您啟用這個設定，則系統會略過只執行一次清單。

如果您停用或未設定這個設定，則系統會執行在只執行一次清單中的程式。

這項設定會同時出現在 [電腦設定] 及 [使用者設定] 資料夾中。如果兩個資料夾都已設定這項設定，則 [電腦設定] 中的設定將優先於 [使用者設定] 中的設定。

注意: 自訂的只執行一次清單存放在 HKEY_LOCAL_MACHINE\Software\Microsoft\Windows\CurrentVersion\RunOnce 的登錄中。

另請參閱 [不要處理舊版執行清單] 設定。</string>
      <string id="DisableStatusMessages">移除開機 / 關機 / 登入 / 登出狀態訊息</string>
      <string id="DisableStatusMessages_Help">不顯示系統狀態訊息。

如果您啟用這個設定，則在系統啟動或關機，或者當使用者登入或登出時，系統將不會顯示訊息以提醒使用者等待。</string>
      <string id="Logon">登入</string>
      <string id="LogonType">永遠使用傳統登入</string>
      <string id="LogonType_Help">這個設定會強制使用者使用傳統登入畫面來登入電腦。預設狀況下，工作群組是設定成使用簡單登入畫面。這個設定只能在電腦未加入網域時使用。</string>
      <string id="NoWelcomeTips">登入時不要顯示 [開始使用] 的歡迎畫面</string>
      <string id="NoWelcomeTips_Help">不顯示歡迎畫面。

這個設定會隱藏每次使用者登入時顯示在 Windows 2000 Professional 的歡迎畫面。

使用者仍然可以從 [開始] 功能表選取歡迎畫面，或者在 [執行] 對話方塊中輸入 "Welcome"，以顯示歡迎畫面。

這個設定只會套用到 Windows 2000 Professional。它並不影響 Windows 2000 Server 上的 [在 Windows 2000 Server 設定您的伺服器] 畫面。

注意: 這項設定會同時出現在 [電腦設定] 及 [使用者設定] 資料夾中。如果兩個資料夾都已設定這項設定，則 [電腦設定] 中的設定將優先於 [使用者設定] 中的設定。

提示: 如果要顯示歡迎畫面，請按 [開始]，指向 [程式集]，指向 [附屬應用程式]，指向 [系統工具]，然後按 [開始使用]。如果您不要顯示歡迎畫面，但又不想指定設定，請清除歡迎畫面上的 [啟動時顯示這個畫面] 核取方塊。</string>
      <string id="Run">當使用者登入時執行這些程式</string>
      <string id="Run_Help">指定使用者登入系統時，由 Windows 自動啟動的額外程式或文件。

若要為這個設定指定值，請按一下 [顯示]。在 [顯示內容] 對話方塊的 [值] 欄中，輸入可執行程式 (.exe) 檔案或文件檔案的名稱。若要指定另一個名稱，請按 ENTER，然後輸入名稱。除非檔案位在 %Systemroot% 目錄中，否則必須指定該檔案的完整路徑。

注意: 這個設定會同時出現在 [電腦設定] 及 [使用者設定] 資料夾中。如果已進行這兩種設定，則系統會先啟動 [電腦設定] 設定中指定的程式，緊接著再啟動 [使用者設定] 設定中指定的程式。

請參閱「不要處理舊版執行清單」以及「不要處理只執行一次清單」設定。</string>
      <string id="SyncForegroundPolicy">永遠在電腦啟動及登入時等待網路啟動</string>
      <string id="SyncForegroundPolicy_Help">
這個原則設定會決定是否以同步方式處理群組原則 (也就是，電腦在電腦啟動及使用者登入期間是否會等待網路完全初始化)。根據預設，用戶端電腦上的群組原則處理為非同步方式; 用戶端電腦在啟動和登入時，通常不會等待網路完全初始化。現有使用者會使用快取的認證來登入，這樣可以縮短登入時間。在網路可用之後，群組原則會於背景套用。

請注意，因為這是背景執行更新作業，所以像是「軟體安裝」及「資料夾重新導向」等延伸需要兩次登入才能套用變更。為了能夠更安全地操作，這些延伸必須不能有任何使用者登入。因此，必須先在前景處理這些延伸，然後使用者才能使用電腦。此外，對使用者物件所做的變更 (例如新增漫遊設定檔路徑、主目錄，或使用者物件登入指令碼)，可能需要兩次登入才偵測得到。

如果具有漫遊設定檔、主目錄或使用者物件登入指令碼的使用者登入電腦，電腦一定會等待網路完全初始化後，才讓使用者登入。如果使用者之前從未登入這部電腦，電腦一定會等待網路完全初始化。

如果啟用這個原則設定，電腦會等待網路完全初始化後，才讓使用者登入。群組原則會在前景同步套用。

在執行 Windows Server 2008 或更新版本的電腦上，電腦啟動時的處理群組原則期間會忽略這個原則設定，而且群組原則會以同步方式處理 (這些伺服器在電腦啟動期間會等待網路完全初始化)。

如果依下列方式設定伺服器，在使用者登入時的群組原則處理期間，這個原則設定會生效:
•  伺服器設定為終端機伺服器 (也就是，會在伺服器上安裝和設定終端機伺服器角色服務); 而且
•  「透過終端機服務登入時，允許非同步使用者群組原則處理」原則設定已啟用。這個原則設定位在「電腦設定\原則\系統管理範本\系統\群組原則\」底下。
如果伺服器上未實作這個設定，就會忽略這個原則設定。在這種情況下，使用者登入時會以同步方式處理群組原則 (這些伺服器在使用者登入期間會等待網路完全初始化)。

如果停用或未設定這個原則設定，而且使用者登入執行 Windows Server 2008 或更新版本的用戶端電腦或伺服器，而且已依上述方式設定，則電腦通常不會等待網路完全初始化。在這種情況下，使用者會以快取的認證登入。群組原則會在背景同步套用。

注意:
如果要確保「資料夾重新導向」應用程式、「軟體安裝」、或漫遊使用者設定檔設定能在一次登入中完成，請啟用這個原則設定來確定 Windows 會等到網路可用之後才套用原則。
如果下次登入期間會套用「資料夾重新導向」，下次更新循環時會以非同步方式套用安全性原則 (如果可以使用網路連線)。</string>
      <string id="VerboseStatus">詳細資訊相對於一般狀態訊息</string>
      <string id="VerboseStatus_Help">指示系統顯示非常詳細的狀態訊息。

如果您啟用這個設定，則系統會顯示反映系統在啟動、關機、登出或登入過程中每一個步驟的狀態訊息。

這個設定是針對需要此項資訊的資深使用者而設計。

注意: 如果 [移除開機 / 關機 / 登入 / 登出狀態訊息] 設定已啟用，就會略過這項設定。</string>
      <string id="HideFastUserSwitching">隱藏快速切換使用者的進入點</string>
      <string id="HideFastUserSwitching_Help">系統管理員啟用此選項即可隱藏登入 UI、[開始] 功能表以及 [工作管理員] 中的 [切換使用者] 按鈕。</string>
      <string id="DisableStartupSound">關閉 Windows 啟動音效</string>
      <string id="DisableStartupSound_Help">關閉 Windows 啟動音效，並禁止自訂 [控制台] 中的 [聲音] 項目。

當系統啟動或冷開機時會播放 Microsoft Windows 啟動音效，且您可以在 [控制台] 的 [聲音] 項目中開啟或關閉啟動音效。

啟用或停用此設定會自動禁止使用者自訂 Windows 啟動音效的預設行為。

如果啟用此原則設定，將會關閉所有使用者的 Windows 啟動音效。

如果停用此原則設定，將會開啟所有使用者的 Windows 啟動音效。

如果未設定這個原則設定，預設會開啟所有使用者的 Windows 啟動音效和 [控制台] 之 [聲音] 項目的自訂功能。

此原則設定並不會禁止使用者設定其他系統音效的喜好設定。</string>

    </stringTable>
    <presentationTable>
      <presentation id="Run_1">
        <listBox refId="RunListBox1">在登入時執行的項目</listBox>
      </presentation>
      <presentation id="Run_2">
        <listBox refId="RunListBox2">在登入時執行的項目</listBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
