<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>ange visningsnamn här</displayName>
  <description>ange beskrivning här</description>
  <resources>
    <stringTable>
      <string id="DNS_Client">DNS-klient</string>
      <string id="DNS_Domain">Anslutningsspecifikt DNS-suffix</string>
      <string id="DNS_Domain_Help">Anger ett anslutningsspecifikt DNS-suffix. Den här inställningen ersätter anslutningsspecifika DNS-suffix som anges på de datorer som inställningen tillämpas på. Den ersätter både de som konfigureras lokalt och de som konfigureras genom DHCP.

Varning! Ett anslutningsspecifikt DNS-suffix som anges i den här inställningen tillämpas på alla nätverksanslutningar som används av datorer med flera IP-adresser som den här inställningen tillämpas på.

Om du vill använda inställningen klickar du på "Aktivera" och skriver en sträng som representerar det DNS-suffix som ska användas.

Om inställningen inte konfigureras tillämpas den inte på någon dator, utan varje dator använder parametrar som konfigureras lokalt eller genom DHCP.</string>
      <string id="DNS_NameServer">DNS-servrar</string>
      <string id="DNS_NameServer_Help">Definierar de DNS-servrar som datorer ska skicka frågor till när namn matchas.

Varning! Den lista över DNS-servrar som definieras i den här inställningen ersätter de DNS-servrar som konfigureras lokalt och genom DHCP. Listan över DNS-servrar används för alla nätverksanslutningar på datorer med fler än en IP-adress som den här inställningen tillämpas på.

Om du vill använda inställningen klickar du på "Aktivera" och skriver en lista över IP-adresser (i punktdecimalform) i fältet. Om du aktiverar den här inställningen måste du ange minst en IP-adress.

Om inställningen inte konfigureras tillämpas den inte på någon dator, utan varje dator använder parametrar som konfigureras lokalt eller genom DHCP.</string>
      <string id="DNS_PrimaryDnsSuffix">Primärt DNS-suffix</string>
      <string id="DNS_PrimaryDnsSuffix_Help">Anger primärt DNS-suffix (Domain Name System) för alla datorer som påverkas. Primärt DNS-suffix används för att registrera och matcha DNS-namn.

Med hjälp av den här inställningen kan du ange ett primärt DNS-suffix för en grupp datorer och förhindra att användare, även administratörer, ändrar inställningen.

Om inställningen inaktiveras eller inte konfigureras använder varje dator sitt lokala primära DNS-suffix, vilket vanligtvis är DNS-namnet i den Active Directory-domän som datorn är ansluten till. Administratörer kan dock använda "System" på Kontrollpanelen och ändra datorns primära DNS-suffix.

Om du vill använda inställningen anger du det fullständiga primära DNS-suffixet som du vill tilldela i fältet. Exempel: microsoft.com.

Den här inställningen inaktiverar inte dialogrutan "DSN-suffix och NetBIOS-datornamn" som används av administratörer för att ändra en datorn primära DNS-suffix. Om administratören ändå anger ett suffix, ignoreras detta så länge som den här inställningen tillämpas.

Viktigt! Om du gör ändringar i den här inställningen måste Windows startas om på de datorer som påverkas av inställningen innan ändringarna börjar gälla.

Obs! Du kan ändra primärt DNS-suffix för en dator utan att ange någon inställning genom att öppna "System" på Kontrollpanelen, klicka på fliken "Nätverksidentifikation", klicka på "Egenskaper", klicka på "Mer" och sedan ange ett suffix i fältet "Primärt DNS-suffix för den här datorn".

Mer information om DNS finns i "DNS (Domain Name System)" i direkthjälpen.</string>
      <string id="DNS_RegisterAdapterName">Registrera DNS-poster med anslutningsspecifika DNS-suffix</string>
      <string id="DNS_RegisterAdapterName_Help">Bestämmer om en dator, som utför dynamisk registrering, kan registrera A- och PTR-resursposter genom att slå samman datornamnet och ett anslutningsspecifik DNS-suffix samt registrera dessa poster genom att slå samman datornamnet och det primära DNS-suffixet.

Varning! Om du aktiverar den här gruppinställningen tillämpas den på alla nätverksanslutningar på datorer som har flera nätverkskort och som den här inställningen tillämpas på.

Standard är att en DNS-klient som utför dynamisk DNS-registrering registrerar A- och PTR-resursposter genom att slå samman sitt datornamn och det primära DNS-suffixet. En sammanslagning av datornamnet "mindator" och det primära DNS-suffixet "microsoft.com" skulle alltså ge "mindator.microsoft.com".

Om den här inställningen aktiveras, registrerar datorn även A- och PTR-resursposter med sitt anslutningsspecifika DNS-suffix utöver att registrera A- och PTR-resursposter med det primära DNS-suffixet. En sammanslagning av datornamnet "mindator" och det anslutningsspecifika DNS-suffixet "VPN-anslutning" skulle exempelvis användas för att registrera A- och PTR-resursposter, vilket ger "mindator.VPN-anslutning". Observera att om dynamisk DNS-registrering är inaktiverat på en dator som den här inställningen tillämpas på kommer datorn inte att försöka utföra dynamisk DNS-registrering av A- och PTR-resursposter som innehåller en sammanslagning av datornamnet och det anslutningsspecifika DNS-suffixet, oavsett vad den här inställningen anger. Om dynamisk DNS-registrering är aktiverat på en specifik nätverksanslutning som den här inställningen tillämpas på, kommer datorn inte att försöka utföra dynamisk DNS-registrering av A- och PTR-resursposter som innehåller en sammanslagning av datornamnet och det anslutningsspecifika DNS-suffixet för denna nätverksanslutning, oavsett vad den här inställningen anger.

Om inställningen inaktiveras kommer en DNS-klient inte att registrera A- och PTR-resursposter med sitt anslutningsspecifika DNS-suffix.

Om den här inställningen inte har konfigurerats används den inte på några datorer, utan datorerna använder den lokala konfigurationen.</string>
      <string id="DNS_RegisterReverseLookup">Registrera PTR-poster</string>
      <string id="DNS_RegisterReverseLookup_DoNotRegister">Registrera inte</string>
      <string id="DNS_RegisterReverseLookup_Help">Bestämmer om registrering av PTR-resursposter ska ske på datorer där den här principen tillämpas.

Som standard försöker DNS-klienter som konfigurerats för att utföra dynamisk DNS-registrering registrera PTR-resursposter endast om motsvarande A-resurspost registrerats korrekt. En A-resurspost mappar DNS-namnet för en värddator till datorns IP-adress och en PTR-resurspost mappar värd-IP-adressen till DNS-namnet för en värddator.

Aktivera inställningen genom att klicka på "Aktivera" och sedan välja ett av följande värden:

"Registrera inte" - datorer försöker aldrig registrera PTR-resursposter.

"Registrera" - datorer försöker registrera PTR-resursposter oavsett om A-resurspost registrerats eller inte.

"Registrera endast om A-resursposten lyckas" - datorer försöker registrera PTR-resursposter endast om motsvarande A-resursposter registrerats korrekt.

Om den här inställningen inte har konfigurerats används den inte på några datorer, utan datorerna använder den lokala konfigurationen.</string>
      <string id="DNS_RegisterReverseLookup_Register">Registrera</string>
      <string id="DNS_RegisterReverseLookup_RegisterOnlyIfASucceeds">Registrera endast om registrering av A-poster lyckas </string>
      <string id="DNS_RegistrationEnabled">Dynamisk uppdatering</string>
      <string id="DNS_RegistrationEnabled_Help">Anger om dynamisk uppdatering är aktiverad.

Datorer som konfigurerats för dynamiska uppdateringar registrerar och uppdaterar sina DNS-resursposter automatiskt med en DNS-server.

Om den här inställningen aktiveras kan de datorer som inställningen tillämpas på använda dynamisk DNS-registrering för varje nätverksanslutning, beroende på konfigurationen för de enskilda nätverksanslutningarna. För att dynamisk DNS-registrering ska vara aktiverat på en viss nätverksanslutning måste både datorspecifika och anslutningsspecifika konfigurationer tillåta dynamisk DNS-registrering. Med hjälp av den här inställningen styr du datorspecifik, egenskapsstyrande, dynamisk DNS-registrering. Om den här inställningen aktiveras tillåter du att dynamisk uppdatering ställs in för varje enskild nätverksanslutning separat.

Om den här inställningen inaktiveras kan de datorer som den tillämpas på inte använda dynamisk DNS-registrering för någon nätverksanslutning, oavsett hur de enskilda nätverksanslutningarna konfigureras.

Om den här inställningen inte har konfigurerats används den inte på några datorer, utan datorerna använder den lokala konfigurationen.</string>
      <string id="DNS_RegistrationOverwritesInConflict">Ersätt adresser som står in konflikt</string>
      <string id="DNS_RegistrationOverwritesInConflict_Help">Bestämmer om en DNS-klient som försöker registrera sin A-resurspost ska skriva över en befintlig A-resurspost som innehåller IP-adresser som står i konflikt.

Den här inställningen är avsedd för datorer som registrerar A-resursposter i DNS-zoner utan stöd för skyddade dynamiska uppdateringar. Skyddade dynamiska uppdateringar bevarar ägarskap för resursposter och tillåter inte att DNS-klienter skriver över poster som registrerats av andra datorer.

När en dynamisk uppdatering sker för en zon som inte stöder skyddade dynamiska uppdateringar, kan en DNS-klient upptäcka att en befintlig A-resurspost associerar klientens värd-DNS-namn med en IP-adress för en annan dator. Enligt standardkonfigurationen försöker DNS-klienten ersätta den befintliga A-resursposten med en A-resurspost som associerar DNS-namnet med klientens IP-adress.

Om den här inställningen aktiveras försöker DNS-klienter ersätta A-resursposter som står i konflikt vid dynamisk uppdatering.

Om inställningen inaktiveras kommer DNS-klienten fortfarande att försöka uppdatera A-resursposter dynamisk, men om DNS-klienten försöker uppdatera A-resursposter som innehåller konflikter kommer detta att misslyckas och ett fel skrivas i Loggboken.

Om den här inställningen inte har konfigurerats används den inte på några datorer, utan datorerna använder den lokala konfigurationen.</string>
      <string id="DNS_RegistrationRefreshInterval">Uppdateringsintervall för registreringar</string>
      <string id="DNS_RegistrationRefreshInterval_Help">Anger uppdateringsintervall för registreringar av A- och PTR-resursposter för datorer som den här inställningen tillämpas på. Inställningen kan endast tillämpas på datorer som använder dynamiska uppdateringar.

Datorer som använder Windows 2000 Professional och Windows XP Professional och som konfigurerats för att utföra dynamisk DNS-registrering av A- och PTR-poster, uppdaterar regelbundet sina poster med DNS-servrarna även informationen i posterna inte har ändrats. Den här omregistreringen är nödvändig för att visa att posterna är aktuella och används, för de DNS-servrar som konfigurerats för att automatiskt rensa bort föråldrade poster ur databasen.

Varning! Om DNS-resursposterna registreras i zoner där rensning används, bör värdet i den här inställningen aldrig vara högre än det uppdateringsintervall som konfigurerats för zonerna. Om ett värde som är högre än zonens uppdateringsintervall anges, kanske A- och PTR-resursposter tas bort i onödan.

Ange uppdateringsintervall för registreringar genom att klicka på "Aktivera" och ange ett värde som är större än 1800. Kom ihåg att uppdateringsintervallet för registreringar anges i sekunder. 1800 sekunder innebär alltså 30 minuter.

Om den här inställningen inte har konfigurerats används den inte på några datorer, utan datorerna använder den lokala konfigurationen.</string>
      <string id="DNS_RegistrationTtl">TTL-värde anges i A- och PTR-poster</string>
      <string id="DNS_RegistrationTtl_Help">Anger värde för fältet TTL (Time-To-Live) i A- och PTR-resursposter som registreras av datorer som den här inställningen tillämpas på.

Du kan ange TTL-värde genom att klicka på "Aktivera" och sedan ange ett värde i sekunder (till exempel 900 vilket innebär 15 minuter).

Om inställningen inte konfigureras, tillämpas den inte på någon dator.</string>
      <string id="DNS_SearchList">Söklista för DNS-suffix</string>
      <string id="DNS_SearchList_Help">Bestämmer vilket DNS-suffix som ska anslutas till ett icke-kvalificerat en-delsnamn innan en DNS-förfrågan sänds.

Ett icke-kvalificerat en-delsnamn innehåller inga punkter, t.ex. "exempel". Det skiljer sig från ett fullständigt kvalificerat domännamn, som "exempel.microsoft.com".

När den här inställningen är aktiverad och en användare skickar en fråga på ett en-delsnamn, som "exempel", lägger en lokal DNS-klient till ett suffix, exempelvis "microsoft.com", vilket resulterar i att den förfrågan som skickas till DNS-servern är "exempel.microsoft.com".

Om inställningen aktiveras kan DNS-suffixet anges innan förfrågan om ett icke-kvalificerat en-delsnamn skickas. DNS-suffixets värden kan anges med kommaavgränsade strängar, t.ex. "microsoft.com,serverua.microsoft.com,office.microsoft.com ". Ett DNS-suffix läggs till för varje skickad förfrågan. Om en förfrågan misslyckas, läggs ett nytt suffix till i stället för det misslyckade, och en ny förfrågan skickas. Värdena används i den ordning de visas i strängen, från vänster till höger.

Om den här inställningen har aktiverats måste du ange minst ett suffix.

Om inställningen inaktiveras läggs det primära DNS-suffixet och nätverksspecifika DNS-suffix till de icke-kvalificerade förfrågningarna.

Om den här inställningen inte har konfigurerats används den inte på några datorer, utan datorerna använder den lokala konfigurationen.</string>
      <string id="DNS_UpdateSecurityLevel">Säkerhetsnivå för uppdateringar</string>
      <string id="DNS_UpdateSecurityLevel_Help">Anger om de datorer som denna inställning tillämpas på använder skyddad dynamisk uppdatering eller vanlig dynamisk uppdatering vid registrering av DNS-poster.

Aktivera inställningen genom att klicka på "Aktivera" och sedan välja ett av följande värden:

"Oskyddade följda av skyddade" - med detta alternativ skickar datorer skyddade dynamiska uppdateringar endast när oskyddade dynamiska uppdateringar vägras.

"Endast oskyddade" - med detta alternativ skickar datorer endast oskyddade dynamiska uppdateringar.

"Endast skyddade" - med detta alternativ skickar datorer endast skyddade dynamiska uppdateringar.

Om den här inställningen inte har konfigurerats används den inte på några datorer, utan datorerna använder den lokala konfigurationen.</string>
      <string id="DNS_UpdateSecurityLevel_OnlySecure">Endast skyddade</string>
      <string id="DNS_UpdateSecurityLevel_OnlyUnsecure">Endast oskyddade</string>
      <string id="DNS_UpdateSecurityLevel_UnsecureFollowedBySecure">Oskyddade följda av skyddade</string>
      <string id="DNS_UpdateTopLevelDomainZones">Uppdatera domänzoner på den högsta nivån</string>
      <string id="DNS_UpdateTopLevelDomainZones_Help">Anger om datorerna där inställningen tillämpas får skicka dynamiska uppdateringar till zoner med ett en-delsnamn, så kallade toppnivådomänzoner, t.ex. "com".

Som standard gäller att en DNS-klient som konfigurerats att utföra dynamisk DNS-uppdatering skickar dynamiska uppdateringar till de DNS-zoner som är auktoritära för DNS-resursposterna, om inte de auktoritära zonerna är toppnivådomäner och rotzoner.

Om inställningen aktiveras skickar datorer där den här principen tillämpas dynamiska uppdateringar till de zoner som är auktoritära för de resursposter datorn behöver uppdatera, förutom rotzonen.

Om inställningen inaktiveras kommer datorer där inställningen tillämpas inte att skicka dynamiska uppdateringar till rot- och/eller toppnivådomänzonens auktoritär för de resursposter datorn behöver uppdatera.

Om den här inställningen inte har konfigurerats används den inte på några datorer, utan datorerna använder den lokala konfigurationen.</string>
      <string id="DNS_UseDomainNameDevolution">Överlåtande av primärt DNS-suffix</string>
      <string id="DNS_UseDomainNameDevolution_Help">Bestämmer om DNS-klienten lägger till primärt DNS-suffix när namnmatchning sker.

När en användare skickar en fråga efter ett en-delsnamn, t.ex. "exempel", lägger en lokal DNS-klient till ett suffix, t.ex. microsoft.com, vilket resulterar i att den förfrågan som skickas till DNS-servern är exempel.microsoft.com.

Om en söklista för DNS-suffix inte har angetts lägger DNS-klienten till det primära DNS-suffixet till en-delsnamnet, och om frågan inte kan matchas, läggs det anslutningsspecifika DNS-suffixet till namnet och en ny fråga skickas. Om ingen av frågorna kan matchas tar klienten bort det primära DNS-suffixet för datorn (den del av det primära DNS-suffixet som visas längst till vänster), lägger till det borttagna primära DNS-suffixet till en-delsnamnet och skickar en ny fråga till DNS-servern.

Om det primära DNS-suffixet ooo.aaa.microsoft.com exempelvis läggs till en-delsnamnet exempel och DNS-frågan efter exempel.ooo.aaa.microsoft.com misslyckas, tar DNS-klienten bort det primära DNS-suffixet (den del av namnet som visas längst till vänster) till den angivna suffixtilläggsnivån och skickar en ny fråga efter exempel.aaa.microsoft.com. Om detta misslyckas, kortas det primära DNS-suffixet ytterligare om det har angetts för den aktuella suffixtilläggsnivån och den nya frågan exempel.microsoft.com skapas. Om denna misslyckas kortas DNS-suffixet ytterligare om det har angetts för den aktuella suffixtilläggsnivån och frågan exempel.microsoft.com skapas. Det primära DNS-suffixet kan inte minskas längre än vad suffixtilläggsnivån tillåter.

Om den här inställningen aktiveras med lämplig suffixtilläggsnivå försöker DNS-klienter på de datorer där inställningen tillämpas att matcha namn som består av ett en-delsnamn och ett förkortat primärt DNS-suffix.

Om inställningen inaktiveras kommer DNS-klienter på de datorer där inställningen tillämpas inte att försöka matcha namn som består av ett en-delsnamn och ett förkortat primärt DNS-suffix.

Om den här inställningen inte har konfigurerats används den inte på några datorer, utan datorerna använder den lokala konfigurationen.</string>
      <string id="DNS_DomainNameDevolutionLevel">Nivå för överlåtande av primärt DNS-suffix</string>
      <string id="DNS_DomainNameDevolutionLevel_Help">Den här principinställningen avgör nivån på överlåtande av DNS-suffix som används av DNS-klienter vid överlåtande av primärt DNS-suffix när namnmatchning sker. När överlåtande av primärt DNS-suffix är aktiverat tas den del av det primära DNS-suffixet som visas längst till vänster bort i varje efterföljande frågeförsök när en fråga misslyckas för ett namn som ett primärt DNS-suffix har kopplats till. Nivån för överlåtandet indikerar det minsta antal etiketter som måste läggas till en frågesträng efter det att det primära DNS-suffixet har tagits bort.

När en användare skickar en fråga efter ett en-delsnamn, t.ex. "exempel", lägger en lokal DNS-klient till ett suffix, t.ex. microsoft.com till frågan innan den skickas till en DNS-server. I det här fallet blir resultatet frågan exempel.microsoft.com.
 
Om en söklista för DNS-suffix inte har angetts lägger DNS-klienten till det primära DNS-suffixet till en-delsnamnet. Om frågan inte kan matchas, läggs det anslutningsspecifika DNS-suffixet till namnet och en ny fråga skickas. Om ingen av frågorna kan matchas tar klienten bort det primära DNS-suffixet för datorn, lägger till det borttagna primära DNS-suffixet till en-delsnamnet och skickar en ny fråga till DNS-servern.

Om det primära DNS-suffixet aaa.bbb.ccc.ddd.microsoft.com exempelvis läggs till en-delsnamnet exempel (som inte har någon punkt i slutet), och om Överlåtande av primärt DNS-suffix är aktiverat och nivån är inställd på 3 skulle följande fråga köras: 

Exempel.aaa.bbb.ccc.ddd.microsoft.com (Om den här frågan inte kan matchas kommer DNS-suffixet för nästa fråga minskas till bbb.ccc.ddd.microsoft.com.)

Exempel.bbb.ccc.ddd.microsoft.com (Om den här frågan inte kan matchas kommer DNS-suffixet för nästa fråga minskas till ccc.ddd.microsoft.com.)

Exempel.ccc.ddd.microsoft.com (Om den här frågan inte kan matchas kommer DNS-suffixet för nästa fråga minskas till ddd.microsoft.com.)

Exempel.ddd.microsoft.com (Om den här frågan inte kan matchas är det inte möjligt att ställa fler frågor eftersom namnmatchningsnivån är inställd på 3 och det primära DNS-suffixet innehåller 3 etiketter.)
  
Om den här inställningen är aktiverad försöker DNS-klienter på de datorer som inställningen tillämpas på att matcha namn som är sammanslagna av en-delsnamnet som ska matchas och det förkortade primära DNS-suffixet. DNS-klienten minskar namnet vid varje frågeförsök tills namnet matchas, nivån på överlåtandet som specificerats i den här inställningen har nåtts eller att det primära DNS-suffixnamnet har två etiketter.
 
Om den här inställningen är inaktiverad kommer DNS-klienter på de datorer som inställningen tillämpas på inte försöka matcha namn som är sammanslagna av en-delsnamnet som ska matchas och ett förkortat primärt DNS-suffix. Om det finns en FRD (skogsrotdomän) konfigureras, om ingen söklista har konfigurerats och en fråga ställs om ett en-delsnamn kommer DNS-klienten matcha upp till skogsrotdomänen tills namnet matchas.</string>
      <string id="DNS_TurnOffMulticast">Inaktivera multicast-namnmatchning</string>
      <string id="DNS_TurnOffMulticast_Help">LLMNR (Local Link Multicast Name Resolution) är ett sekundärt namnmatchningsprotokoll. Frågor skickas via Local Link, ett enskilt undernät, från en klientdator som använder Multicast och som en annan klient på samma länk, som också har aktiverat LLMNR, kan skicka svar till. LLMNR tillhandahåller namnmatchning i situationer där konventionell DNS-namnmatchning inte är möjlig.

Om den här principinställningen aktiveras stängs Multicast-namnmatchning, eller LLMNR, av på datorn för alla tillgängliga men okonfigurerade nätverkskort.

Om inställningen inaktiveras aktiveras Multicast-namnmatchning, eller LLMNR, på datorn för alla tillgängliga men okonfigurerade nätverkskort.

Om inställningen inte konfigureras aktiveras Multicast-namnmatchning, eller LLMNR, som standard på datorn för alla tillgängliga men okonfigurerade nätverkskort.</string>
      <string id="DNS_AppendToMultiLabelName">Tillåt DNS-suffixtillägg på okvalificerade frågor för flerdelsnamn</string>
      <string id="DNS_AppendToMultiLabelName_Help">Anger om datorerna som den här inställningen tillämpas på får lägga till suffix i ett icke-kvalificerat flerdelsnamn innan DNS-frågor skickas, om den ursprungliga namnförfrågan misslyckas.

Ett namn som innehåller punkter, men inte avslutas med punkt, kallas för ett icke-kvalificerat flerdelsnamn, t.ex. "server.corp". Ett kvalificerat namn skulle avslutas med en punkt, t.ex. "server.corp.contoso.com.".

Om du aktiverar den här inställningen kan suffix bifogas till ett icke-kvalificerat flerdelsnamn, om ursprungliga namnförfrågan misslyckas. Exempelvis kommer en icke-kvalificerad flerdelsnamnförfrågan för "server.corp" att frågas först av DNS-klienten. Om svar på frågan hittas returneras det till klienten. Om förfrågan misslyckas bifogas det icke-kvalificerade flerdelsnamnet med DNS-suffix som är konfigurerade för datorn för frågor. Suffixen kan vara härledda från en kombination av den lokala DNS-klientens primära domänsuffix, ett anslutningsspecifikt domänsuffix och/eller söklistan för DNS-suffix.

Om den lokala DNS-klienten exempelvis får en fråga om "server.corp", och ett primärt domänsuffix är konfigurerat som "contoso.com", kommer DNS-klienten med den här inställningen att skicka en förfrågan på "server.corp.contoso.com." om den ursprungliga namnfrågan för "server.corp" misslyckas. 

Om du inaktiverar den här inställningen bifogas inte suffix till icke-kvalificerade flerdelsnamnfrågor om den ursprungliga namnförfrågan misslyckas. 

Om du inte konfigurerar den här inställningen använder datorerna den lokala DNS-klientkonfigurationen för att bestämma frågebeteendet för icke-kvalificerade flerdelsnamn.</string>
    </stringTable>
    <presentationTable>
      <presentation id="DNS_Domain">
        <textBox refId="DNS_DomainLabel">
          <label>DNS-suffix:</label>
        </textBox>
      </presentation>
      <presentation id="DNS_NameServer">
        <textBox refId="DNS_NameServerLabel">
          <label>IP-adresser:</label>
        </textBox>
      </presentation>
      <presentation id="DNS_PrimaryDnsSuffix">
        <textBox refId="DNS_PrimaryDnsSuffixBox">
          <label>Ange ett primärt DNS-suffix:</label>
        </textBox>
      </presentation>
      <presentation id="DNS_RegisterReverseLookup">
        <dropdownList refId="DNS_RegisterReverseLookup_Box" defaultItem="0">Registrera PTR-poster:</dropdownList>
      </presentation>
      <presentation id="DNS_RegistrationRefreshInterval">
        <decimalTextBox refId="DNS_RegistrationRefreshIntervalLabel" defaultValue="1800" spinStep="100">Sekunder:</decimalTextBox>
      </presentation>
      <presentation id="DNS_DomainNameDevolutionLevel">
        <decimalTextBox refId="DNS_DomainNameDevolutionLevelLabel" defaultValue="2" spinStep="1">Ange nivån för överlåtande av primärt DNS-suffix</decimalTextBox>
      </presentation>
      <presentation id="DNS_RegistrationTtl">
        <decimalTextBox refId="DNS_RegistrationTtlLabel" defaultValue="600" spinStep="100">Sekunder:</decimalTextBox>
      </presentation>
      <presentation id="DNS_SearchList">
        <textBox refId="DNS_SearchListLabel">
          <label>DNS-suffix:</label>
        </textBox>
      </presentation>
      <presentation id="DNS_UpdateSecurityLevel">
        <dropdownList refId="DNS_UpdateSecurityLevel_Box" defaultItem="2">Säkerhetsnivå för uppdateringar:</dropdownList>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
