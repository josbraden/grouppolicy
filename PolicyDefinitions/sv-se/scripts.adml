<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>ange visningsnamn här</displayName>
  <description>ange beskrivning här</description>
  <resources>
    <stringTable>
      <string id="MaxGPOScriptWaitPolicy">Maximal väntetid för grupprincipskript</string>
      <string id="MaxGPOScriptWaitPolicy_Help">Anger hur länge datorn ska vänta på att skript som tillämpas av grupprincipen ska köras färdigt.

Den här inställningen begränsar den sammanlagda tid som tillåts för att alla inloggnings-, start- och avstängningsskript som tillämpas av grupprincipen ska köras färdigt. Om skripten inte har slutförts efter den angivna tiden, avbryts skripten och en felhändelse loggas.

Som standard kan dessa skript köras i sammanlagt 600 sekunder (tio minuter), men den här inställningen kan användas för att ändra detta värde.

Du använder den här inställningen genom att skriva in ett värde mellan 1 och 32 000 som motsvarar det antal sekunder du vill att datorn ska vänta på att skripten ska köras färdigt. Om du vill att datorn alltid ska vänta tills alla skript slutförs, oavsett hur lång tid detta tar, kan du ange värdet 0.

Detta intervall är särskilt viktigt när andra aktiviteter måste vänta medan skripten slutförs. Som standard måste alla startskript slutföras innan nästa körs. Du kan använda inställningen "Kör inloggningsskript synkront" om du vill att datorn ska vänta på att inloggningsskripten ska slutföras innan skrivbordet läses in.

Ett mycket långt intervall kan fördröja datorn och vara irriterande för användarna. Å andra sidan, om intervallet är väldigt kort kan det hända att nödvändiga förberedande aktiviteter inte utförs och datorn verkar vara redo innan den verkligen är det.</string>
      <string id="Run_Legacy_Logon_Script_Hidden">Kör äldre inloggningsskript dolt</string>
      <string id="Run_Legacy_Logon_Script_Hidden_Explain">Döljer instruktionerna i inloggningsskript som skrivits för Windows NT 4.0 eller tidigare.

Inloggningsskript är kommandofiler med instruktioner som körs när användare loggar in. Som standard visas instruktionerna i inloggningsskript som skrivits för Windows NT 4.0 och tidigare.

Om den här inställningen aktiveras, visas inloggningsskript som skrivits för Windows NT 4.0 och tidigare i Windows 2000.

Se även inställningen "Kör inloggningsskript synligt".</string>

      <string id="Run_Logoff_Script_Visible">Kör utloggningsskript synligt</string>
      <string id="Run_Logoff_Script_Visible_Explain">Visar instruktionerna i utloggningsskript medan de körs.

Utloggningsskript är kommandofiler med instruktioner som körs när användare loggar ut. Som standard visas inte instruktionerna i skriptet.

Om den här inställningen aktiveras, visas alla instruktioner i skriptet medan det körs på datorn. Instruktionerna visas i ett kommandoradsfönster. Den här inställningen har utformats för avancerade användare.

Om du inaktiverar den här inställningen eller låter bli att konfigurera den visas inte instruktionerna.</string>
      <string id="Run_Logon_Script_Sync">Kör inloggningsskript synkront</string>
      <string id="Run_Logon_Script_Sync_Help">Gör att datorn väntar på att inloggningsskripten ska köras klart innan användargränssnittet startas och skrivbordet skapas.

Om den här inställningen aktiveras startas inte användargränssnittet förrän inloggningsskripten har körts klart. Den här inställningen ser till att inloggningsskript slutförs innan användaren börjar arbeta, men kan fördröja att skrivbordet visas.

Om den här inställningen inaktiveras eller inte konfigureras, synkroniseras inte inloggningsskript och användargränssnittet.

Den här inställningen finns både i mappen Datorkonfiguration och i mappen Användarkonfiguration. Inställningen i Datorkonfiguration gäller före inställningen i Användarkonfiguration.</string>
      <string id="Run_Logon_Script_Visible">Kör inloggningsskript synligt</string>
      <string id="Run_Logon_Script_Visible_Explain">Visar instruktionerna i inloggningsskript medan de körs.

Inloggningsskript är kommandofiler med instruktioner som körs när användare loggar in. Som standard visas inte instruktionerna i skriptet.

Om den här inställningen aktiveras visas alla instruktioner i skriptet medan det körs. Instruktionerna visas i ett kommandoradsfönster. Den här inställningen har utformats för avancerade användare.

Om du inaktiverar den här inställningen eller låter bli att konfigurera den visas inte instruktionerna.</string>

      <string id="Run_Computer_PS_Scripts_First">Kör Windows PowerShell-skript först när datorn startas eller stängs av</string>
      <string id="Run_User_PS_Scripts_First">Kör Windows PowerShell-skript först vid inloggning eller utloggning</string>
      <string id="Run_Computer_PS_Scripts_First_Explain">Den här principinställningen avgör om Windows PowerShell-skript ska köras före skript som inte är PowerShell-skript vid start eller avstängning av datorn. Som standard körs PowerShell-skript efter skript som inte är PowerShell-skript. 
 
Om du aktiverar den här principinställningen i alla tillämpliga GPO (Group Policy Object), körs PowerShell-skript före skript som inte är PowerShell-skript vid start eller avstängning av datorn. 

Föreställ dig t.ex. följande scenario: 

Det finns tre GPO (GPO A, GPO B och GPO C). Den här principinställningen är aktiverad i GPO A. 

GPO B och GPO C inkluderar följande startskript för datorer:

GPO B: B.cmd, B.ps1
GPO C: C.cmd, C.ps1

Föreställ dig även att det finns två datorer, DesktopIT och DesktopSales. 
GPO A, B och C tillämpas på DesktopIT. Därför körs skripten för GPO B och C i följande ordning för DesktopIT:

I GPO B: B.ps1, B.cmd
i GPO C: C.ps1, C.cmd
 
GPO B och C tillämpas för DesktopSales men inte GPO A. Därför körs skripten för GPO B och C i följande ordning för DesktopSales:

I GPO B: B.cmd, B.ps1
I GPO C: C.cmd, C.ps1

Obs! Den här principinställningen avgör i vilken ordning start- och avslutningsskript för datorer körs i alla tillämpliga GPO. Du kan åsidosätta den här principinställningen för specifika skripttyper i en enskild GPO genom att konfigurera följande principinställningar för den GPO:n:
 
Datorkonfiguration\Principer\Windows-inställningar\Skript (Start/Avslutning)\Start
Datorkonfiguration\Principer\Windows-inställningar\Skript (Start/Avslutning)\Avstängning
</string>
      <string id="Run_User_PS_Scripts_First_Explain">Den här principinställningen avgör om Windows PowerShell-skript ska köras före skript som inte är PowerShell-skript vid inloggning och utloggning. Som standard körs PowerShell-skript efter skript som inte är PowerShell-skript. 
 
Om du aktiverar den här principinställningen i alla tillämpliga GPO (Group Policy Object), körs PowerShell-skript före skript som inte är PowerShell-skript vid inloggning eller utloggning. 

Föreställ dig t.ex. följande scenario: 

Det finns tre GPO (GPO A, GPO B och GPO C). Den här principinställningen är aktiverad i GPO A. 

GPO B och GPO C inkluderar följande startskript för datorer:

GPO B: B.cmd, B.ps1
GPO C: C.cmd, C.ps1

Föreställ dig även att det finns två användare, QinHong och Tamara Johnston. 
GPO A, B och C tillämpas för Qin. Därför körs skripten för GPO B och C i följande ordning för Qin:

I GPO B: B.ps1, B.cmd
I GPO C: C.ps1, C.cmd
 
GPO B och C tillämpas för Tamara men inte GPO A. Därför körs skripten för GPO B och C i följande ordning för Tamara:

I GPO B: B.cmd, B.ps1
I GPO C: C.cmd, C.ps1

Obs! Den här principinställningen avgör i vilken ordning inloggnings- och utloggningsskript körs i alla tillämpliga GPO. Du kan åsidosätta den här principinställningen för specifika skripttyper i en enskild GPO genom att konfigurera följande principinställningar för den GPO:n:
 
Användarkonfiguration\Principer\Windows-inställningar\Skript (in-/utloggning)\Inloggning
Användarkonfiguration\Principer\Windows-inställningar\Skript (in-/utloggning)\Utloggning

Den här inställningen finns i mapparna Datorkonfiguration och Användarkonfiguration. Inställningen i Datorkonfiguration gäller före inställningen i Användarkonfiguration.
</string>
      <string id="Run_Shutdown_Script_Visible">Kör avstängningsskript synligt</string>
      <string id="Run_Shutdown_Script_Visible_Explain">Visar instruktionerna i avstängningsskript medan de körs.

Avstängningsskript är kommandofiler med instruktioner som körs när användare startar om eller stänger av datorn. Som standard visas inte instruktionerna i skriptet.

Om den här inställningen aktiveras visas alla instruktioner i skriptet medan det körs. Instruktionerna visas i ett kommandoradsfönster.

Om du inaktiverar den här inställningen eller låter bli att konfigurera den visas inte instruktionerna.</string>
      <string id="Run_Startup_Script_Sync">Kör startskript asynkront</string>
      <string id="Run_Startup_Script_Sync_Help">Låter datorn köra startskript samtidigt.

Startskript är kommandofiler som körs innan användaren kan logga in. Som standard väntar datorn på att varje startskript ska slutföras innan nästa skript börjar köras.

Om den här inställningen aktiveras koordineras inte körningen av skripten på datorn. Detta innebär att startskript kan köras samtidigt.

Om den här inställningen inaktiveras eller inte konfigureras, körs startskripten i följd, och inget startskript kan starta förrän det föregående skriptet har slutförts.

Obs! Från och med operativsystemet Windows Vista visas inte längre skript som är konfigurerade för asynkron körning när datorn startas, oavsett om principinställningen "Kör startskript synligt" är aktiverad eller inte.</string>
      <string id="Run_Startup_Script_Visible">Kör startskript synligt</string>
      <string id="Run_Startup_Script_Visible_Explain">Visar instruktionerna i startskript medan de körs.

Startskript är kommandofiler med instruktioner som körs innan användaren kan logga in. Som standard visas inte instruktionerna i skriptet.

Om den här inställningen aktiveras, visas alla instruktioner i skriptet medan det körs. Instruktionerna visas i ett kommandoradsfönster. Den här inställningen har utformats för avancerade användare.

Om du inaktiverar den här inställningen eller låter bli att konfigurera den visas inte instruktionerna.

Obs! Från och med operativsystemet Windows Vista visas inte längre skript som är konfigurerade för asynkron körning när datorn startas, oavsett om denna inställning är aktiverad eller inte.</string>
      <string id="Allow_Logon_Script_NetbiosDisabled">Tillåt inloggningsskript när NetBIOS eller WINS är inaktiverat</string>
      <string id="Allow_Logon_Script_NetbiosDisabled_Explain">Den här principinställningen tillåter körning av användarinloggningsskript när DNS-inloggningssuffix mellan skogar inte konfigurerats och NetBIOS eller WINS är inaktiverat. Den här principinställningen påverkar alla användarkonton som loggar in interaktivt på datorn.

Om du aktiverar den här principinställningen körs användarinloggningsskript om NetBIOS eller WINS är inaktiverat under inloggning mellan skogar där DNS-suffix inte konfigurerats.

Om du inaktiverar eller låter bli att konfigurera den här principinställningen kan inte interaktiv loggning av användarkonton mellan skogar köra inloggningsskript om NetBIOS eller WINS har inaktiverats och DNS-suffix inte har konfigurerats.</string>
      <string id="Scripts">Skript</string>
    </stringTable>
    <presentationTable>
      <presentation id="MaxGPOScriptWaitPolicy">
        <decimalTextBox refId="MaxGPOScriptWait" defaultValue="600">Sekunder:</decimalTextBox>
        <text>Intervall är 0 till 32000. Använd 0 för oändlig väntetid</text>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
