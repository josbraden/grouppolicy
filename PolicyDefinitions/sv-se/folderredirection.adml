<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>ange visningsnamn här</displayName>
  <description>ange beskrivning här</description>
  <resources>
    <stringTable>
      <string id="Fdeploy_Cat">Mappomdirigering</string>
      <string id="LocalizeXPRelativePaths">Använd lokaliserade undermappsnamn vid omdirigering av Start-menyn och Mina dokument</string>
      <string id="LocalizeXPRelativePaths_Help">Administratören kan använda den här principinställningen för att definiera om Mappomdirigering ska använda lokaliserade namn för undermapparna Alla program, Autostart, Min musik, Mina bilder och Mina videoklipp vid omdirigering av den överordnade mappen för Start-menyn respektive den äldre mappen Mina dokument.

Om du aktiverar den här principinställningen använder Windows Vista och Windows 7 lokaliserade mappnamn för dessa undermappar vid omdirigering av Start-menyn eller den äldre mappen Mina dokument.

Om du inaktiverar eller inte konfigurerar den här principinställningen använder Windows Vista och Windows 7 standardnamn på engelska för dessa undermappar vid omdirigering av Start-menyn eller den äldre mappen Mina dokument.

Obs! Den här principen är endast giltig i Windows Vista och Windows 7 när den bearbetar en äldre omdirigeringsprincip som redan har installerats för dessa mappar i den befintliga lokaliserade miljön.</string>

 <string id="DisableFRAdminPin">Gör inte omdirigerade mappar automatiskt tillgängliga offline</string>
      <string id="DisableFRAdminPin_Help">Alla omdirigerade skalmappar t.ex. Kontakter, Dokument, Skrivbord, Favoriter, Musik, Bilder, Videoklipp, Start-menyn och Start-menyn finns som standard tillgängliga offline. Med den här inställningen kan du ändra detta så att omdirigerade mappar inte automatiskt är tillgängliga offline. Men användare kan ändå välja att själva göra filer och mappar tillgängliga offline.

Om du aktiverar denna inställning måste användare manuellt markera de filer de vill göra tillgängliga offline.

Om du inaktiverar denna inställning eller inte konfigurerar den, görs omdirigerade skalmappar automatiskt tillgängliga offline. Undermapparna i de omdirigerade mapparna görs också tillgängliga offline.

Obs! Om nätverksresursen konfigurerats för automatisk lagring i cacheminnet förhindrar denna inställning inte att filer automatiskt lagras i cacheminnet, och den påverkar inte heller om alternativet "Gör tillgänglig offline" finns tillgängligt i användargränssnittet.

Obs! Aktivera inte den här inställningen om du inte är säker på att användare inte behöver komma åt filer i de omdirigerade mapparna om nätverket eller den server där filerna finns skulle bli otillgängliga.</string>  
  </stringTable>
  </resources>
</policyDefinitionResources>
