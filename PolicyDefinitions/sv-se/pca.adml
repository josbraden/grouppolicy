<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>ange visningsnamn här</displayName>
  <description>ange beskrivning här</description>
  <resources>
    <stringTable>


      <!-- Overall category text -->
      <string id="PcaScenarioCategory">Diagnostik för programkompatibilitet</string>
      

      <!-- Generic WDI text -->
      <string id="WdiScenarioExecutionPolicyLevelResolution">Identifiering, felsökning och lösning</string>
      <string id="WdiScenarioExecutionPolicyLevelTsOnly">Endast identifiering och felsökning</string>


      <!-- Individual scenario text -->
      <string id="DetectInstallFailuresText">Identifiera programinstallationsfel</string>
      <string id="DetectUpdateFailuresText">Identifiera program som inte kan starta installationsprogram under UAC</string>
      <string id="DetectDeprecatedComponentFailuresText">Identifiera programfel orsakade av inaktuella Windows-DLL-filer</string>
      <string id="DetectDeprecatedCOMComponentFailuresText">Identifiera programfel orsakade av inaktuella COM-objekt</string>
      <string id="DetectUndetectedInstallersText">Identifiera installationsprogram som behöver köras som administratör</string>
      <string id="DetectBlockedDriversText">Meddela blockerade drivrutiner</string>
      <!-- End individual scenario text -->


      <!-- Begin install failure scenario description -->
      <string id="DetectInstallFailuresExplain">Den här principinställningen konfigurerar Program Compatibility Assistant (PCA) så att den diagnostiserar misslyckade programinstallationer. 

Om du aktiverar den här principinställningen är PCA konfigurerad för att identifiera fel i exekveringen av installationsprogram genom heuristisk sökning. När möjliga fel identifieras ger PCA:n användaren möjlighet att starta om installationsprogrammet i Microsoft Windows XP-kompatibelt läge.

Om du inaktiverar den här principinställningen är PCA konfigurerad för att identifiera fel i exekveringen av installationsprogram.

Om du inte konfigurerar den här principinställningen aktiveras det här diagnostiska scenariet som standard.

Obs! Om du inaktiverar principinställningen Inaktivera Program Compatibility Assistant har den här principinställningen inte någon effekt. DPS (Diagnostic Policy Service) och tjänsten Program Compatibility Assistant måste köras för att PCA ska köras. De här tjänsterna kan konfigureras med snapin-modulen Tjänster i Microsoft Management Console.</string>
      <!-- End install failure scenario description -->
      

      <!-- Begin update failure scenario description -->
      <string id="DetectUpdateFailuresExplain">Den här principinställningen konfigurerar Program Compatibility Assistant (PCA) så att den diagnostiserar programfel under User Account Control (UAC). 

Om du aktiverar den här inställningen identifieras program som har misslyckats med att starta underordnade processer som är installationsprogram (oftast uppdateringar). När det här felet identifieras tillämpas kompatibilitetsläget ELEVATECREATEPROCESS som gör att program kan starta installationsprogrammet med administratörbehörighet nästa gång programmet körs.

Om du inaktiverar den här principinställningen identifierar inte PCA program som misslyckas med att starta installationsprogram under UAC.

Om du inte konfigurerar den här inställningen identifierar PCA program som inte lyckades starta underordnade processer som är installationsprogram (oftast uppdateringar).

Obs! Om du inaktiverar principinställningen Inaktivera Program Compatibility Assistant har den här principinställningen inte någon effekt. DPS (Diagnostic Policy Service) och tjänsten Program Compatibility Assistant måste köras för att PCA ska köras. De här tjänsterna kan konfigureras med snapin-modulen Tjänster i Microsoft Management Console.</string>
      <!-- End update failure scenario description -->
      

      <!-- Begin deprecated component failure scenario description -->
      <string id="DetectDeprecatedComponentFailuresExplain">Den här principinställningen avgör om assistenten för programkompatibilitet diagnostiserar fel vid inläsning av DLL-filer. 

Om du aktiverar den här inställningen identifierar assistenten för programkompatibilitet program som försöker läsa in gamla Microsoft Windows-DLL-filer som inte ingår i den här versionen av Windows. När det här felet identifieras och programmet har avslutats meddelas användaren om problemet och han kan besöka Microsofts webbplats för lösningar.

Om du inaktiverar den här inställningen identifieras inte program som försöker läsa in gamla Windows-DLL-filer.

Om du inte konfigurerar den här inställningen identifieras program som försöker installera gamla Windows-DLL-filer.

Obs! Om du inaktiverar principinställningen Inaktivera Program Compatibility Assistant har den här principinställningen inte någon effekt. DPS (Diagnostic Policy Service) och tjänsten Program Compatibility Assistant måste köras för att PCA ska köras. De här tjänsterna kan konfigureras med snapin-modulen Tjänster i Microsoft Management Console.</string>
      <!-- End deprecated component failure scenario description -->


      <!-- Begin deprecated COM component failure scenario description -->
      <string id="DetectDeprecatedCOMComponentFailuresExplain">Den här principinställningen avgör om PCA (Program Compatibility Assistant) diagnostiserar fel vid inläsning av DLL-filer eller när COM-objekt i program skapas. 

Om du aktiverar den här principinställningen identifierar PCA program som försöker skapa gamla COM-objekt som har tagits bort i den här versionen av Windows. När det här felet har identifierats och programmet har avslutats, meddelas användaren om problemet så att användaren kan gå till Microsofts webbplats för lösningar.

Om du inaktiverar den här inställningen identifieras inte program som försöker skapa gamla COM-objekt.

Om du inte konfigurerar den här principinställningen identifieras program som försöker skapa gamla COM-objekt.

Obs! Om du inaktiverar principinställningen Inaktivera Program Compatibility Assistant har den här principinställningen inte någon effekt. DPS (Diagnostic Policy Service) och tjänsten Program Compatibility Assistant måste köras för att PCA ska köras. De här tjänsterna kan konfigureras med snapin-modulen Tjänster i Microsoft Management Console.</string>
      <!-- End deprecated COM component failure scenario description -->


      <!-- Begin undetected installer failure scenario description -->
      <string id="DetectUndetectedInstallersExplain">Inställningen avgör om Program Compatibility Assistant (PCA) diagnostiserar fel med installationsprogram som inte identifieras som att de körs som administratörer. 

Om du aktiverar den här inställningen konfigureras PCA för att identifiera installationsprogram som inte har behörighet att köras som administratörer av User Access Control (UAC). När möjliga fel identifieras kan användaren starta om installationsprogrammet som adminstratör.

Om du inaktiverar den här inställningen identifierar PCA inte installationsprogram som inte har behörighet att köras som administratörer av User Access Control (UAC).

Om du inte konfigurerar den här inställningen konfigureras PCA för identifiering av installationsprogram som inte har behörighet att köras som administratörer av UAC.


Obs! Om du inaktiverar principinställningen Inaktivera Program Compatibility Assistant har den här principinställningen inte någon effekt. DPS (Diagnostic Policy Service) och tjänsten Program Compatibility Assistant måste köras för att PCA ska köras. De här tjänsterna kan konfigureras med snapin-modulen Tjänster i Microsoft Management Console.</string>
      <!-- End undetected installer failure scenario description -->


      <!-- Begin blocked drivers scenario description -->
      <string id="DetectBlockedDriversExplain">Den här inställningen avgör om Program Compatibility Assistant (PCA) diagnostiserar drivrutiner som blockeras på grund av kompatibilitetsproblem. 

Om du aktiverar den här inställningen meddelas användaren om problem med blockerade drivrutiner och han kan gå till Microsofts webbplats för att hitta lösningar.

Om du inaktiverar den här inställningen meddelas användaren inte om problem med blockerade drivrutiner. Obs! Om den här principinställningen är inaktiverad får användaren inte lösningar på blockerade drivrutiner.

Om du inte konfigurerar den här inställningen meddelas användaren om problem med blockerade drivrutiner och han kan gå till Microsofts webbplats för att hitta lösningar.

Obs! Om du inaktiverar principinställningen Inaktivera Program Compatibility Assistant har den här principinställningen inte någon effekt. DPS (Diagnostic Policy Service) och tjänsten Program Compatibility Assistant måste köras för att PCA ska köras. De här tjänsterna kan konfigureras med snapin-modulen Tjänster i Microsoft Management Console.</string>
      <!-- End blocked drivers scenario description -->
    </stringTable>

    <presentationTable>
      <presentation id="DetectUpdateFailuresTable">
        <dropdownList refId="DetectUpdateFailuresLevel" noSort="true" defaultItem="1">Körningsnivå för scenario</dropdownList>
      </presentation>
      <presentation id="DetectDeprecatedComponentFailuresTable">
        <dropdownList refId="DetectDeprecatedComponentFailuresLevel" noSort="true" defaultItem="1">Körningsnivå för scenario</dropdownList>
      </presentation>
      <presentation id="DetectDeprecatedCOMComponentFailuresTable">
        <dropdownList refId="DetectDeprecatedCOMComponentFailuresLevel" noSort="true" defaultItem="1">Körningsnivå för scenario</dropdownList>
      </presentation>
      <presentation id="DetectUndetectedInstallersTable">
        <dropdownList refId="DetectUndetectedInstallersLevel" noSort="true" defaultItem="1">Körningsnivå för scenario</dropdownList>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
