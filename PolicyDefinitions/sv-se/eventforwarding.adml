<?xml version="1.0"?>
<policyDefinitionResources revision="1.0" schemaVersion="1.0">
	<displayName>Vidarebefordran av händelser</displayName>

	<description>Principdefinitioner för vidarebefordran av händelser</description>

	<resources>

		<stringTable>

			<string id="EventForwarding">Vidarebefordran av händelser</string>

                        <string id="ForwarderResourceUsage">ForwarderResourceUsage</string>
                        <string id="ForwarderResourceUsage_Help">Kontrollerar resursanvändandet för vidarebefordraren. Varje inställning tillämpas för alla vidarebefordrarens prenumerationer.</string>
 
			<string id="SubscriptionManager">Konfigurera serveradressen, uppdateringsintervallet och certifikatutfärdaren för en specifik prenumerationshanteraren</string>

			<string id="Subscription_Help">Med den här principinställningen konfigurerar du serveradressen, uppdateringsintervallet och certifikatutfärdaren för en specifik prenumerationshanterare. Prenumerationshanteraren är den dator som händelser vidarebefordras till.

Syntax: Alternativ=Värde[,Alternativ=Värde]*
Alternativ:
Server (förinställd) - Adress till datorn som händelser ska vidarebefordras till. När servern använder standardtransportprotokollet (HTTP) och porten (5895) använder du Server=&lt;FQDN&gt;, där &lt;FQDN&gt; är serverns fullständiga domännamn. I alla andra fall måste den fullständiga URL:en anges. Till exempel när HTTPS-protokollet används, Server=https://&lt;FQDN&gt;:5986/wsman/SubscriptionManager/WEC
</string>

			<string id="SubscriptionManager_Listbox">SubscriptionManagers</string>
		</stringTable>

	
		<presentationTable>

			<presentation id="SubscriptionManager">
                                
				<listBox refId="SubscriptionManager_Listbox">SubscriptionManagers</listBox>

			</presentation>

                        <presentation id="ForwarderResourceUsage">

                                <decimalTextBox refId="MaxForwardingRate" spinStep="100">Maximal tillåten hastighet för vidarebefordringar (händelser/sek.) för vidarebefordraren: </decimalTextBox>

                        </presentation>

		</presentationTable>

	</resources>

</policyDefinitionResources>
