<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>ange visningsnamn här</displayName>
  <description>ange beskrivning här</description>
  <resources>
    <stringTable>
      <string id="DisableBackupRestore">Förhindra återställning av tidigare versioner från säkerhetskopior</string>
      <string id="DisableBackupRestore_Help">Med den här inställningen kan du inaktivera knappen "Återställ" på egenskapssidan för tidigare versioner då användaren har valt en tidigare version av en lokal fil och den lagras på en säkerhetskopia.

Om den här inställningen är aktiverad är knappen "Återställ" inaktiverad då användaren markerar en tidigare version som finns på en säkerhetskopia.

Om den här inställningen är inaktiverad är knappen "Återställ" aktiverad då en tidigare version som finns på en säkerhetskopia markeras. Om användaren klickar på "Återställ" försöker Windows återställa filen från säkerhetskopian.

Om den här inställningen inte har konfigurerats är standardvärdet att den är inaktiverad. Knappen "Återställ" går då att välja om den tidigare versionen gäller en lokal fil som har sparats på en säkerhetskopia.</string>
      <string id="DisableLocalPage">Dölj listan med tidigare versioner av lokala filer</string>
      <string id="DisableLocalPage_Help">Du kan använda den här principinställningen för att dölja listan med tidigare versioner av filer som finns på lokala diskar. De tidigare versionerna kan komma från återställningspunkter på disken eller från ett säkerhetskopieringsmedium.

Om du aktiverar den här principinställningen kan användarna inte visa eller återställa tidigare versioner av filer på lokala diskar.

Om du inaktiverar den här principinställningen kan användarna visa och återställa tidigare versioner av filer på lokala diskar.

Om du inte konfigurerar den här principinställningen är den som standard inaktiverad.</string>
      <string id="DisableLocalRestore">Förhindra återställning av tidigare lokala versioner</string>
      <string id="DisableLocalRestore_Help">Med den här inställningen kan du inaktivera knappen "Återställ" på egenskapssidan för tidigare versioner då användaren har valt en tidigare version av en lokal fil.

Om den här inställningen är aktiverad är knappen "Återställ" inaktiverad då användaren markerar en tidigare version av en lokal fil.

Om den här inställningen är inaktiverad är knappen "Återställ" aktiverad då en tidigare version av en lokal fil markeras. Om användaren klickar på "Återställ" försöker Windows återställa filen från den lokala disken.

Om den här inställningen inte har konfigurerats är standardvärdet att den är inaktiverad. Knappen "Återställ" går då att välja om den tidigare versionen gäller en lokal fil.</string>
      <string id="DisableRemotePage">Dölj listan med tidigare versioner av fjärrlagrade filer </string>
      <string id="DisableRemotePage_Help">Du kan använda den här principinställningen för att dölja listan med tidigare versioner av filer som finns på filresurser. De tidigare versionerna kan komma från återställningspunkter på disken eller från filresursen.

Om du aktiverar den här principinställningen kan användarna inte visa eller återställa tidigare versioner av filer på filresurser.

Om du inaktiverar den här principinställningen kan användarna visa och återställa tidigare versioner av filer på filresurser.

Om du inte konfigurerar den här principinställningen är den som standard inaktiverad.</string>
      <string id="DisableRemoteRestore">Förhindra återställning av tidigare fjärrlagrade versioner</string>
      <string id="DisableRemoteRestore_Help">Med den här inställningen kan du inaktivera knappen "Återställ" på egenskapssidan då användaren har valt en tidigare version av en fil på en filresurs.

Om den här inställningen är aktiverad är knappen "Återställ" inaktiverad då användaren markerar en tidigare version av en fil på en filresurs.

Om den här inställningen är inaktiverad är knappen "Återställ" aktiverad då en tidigare version av en fil på en filresurs markeras. Om användaren klickar på "Återställ" försöker Windows återställa filen från filresursen.

Om den här inställningen inte har konfigurerats är standardvärdet att den är inaktiverad. Knappen "Återställ" går då att välja om den tidigare versionen gäller en fil på en filresurs.</string>
      <string id="HideBackupEntries">Dölj tidigare versioner av filer på säkerhetskopieringsmedier</string>
      <string id="HideBackupEntries_Help">Den här inställningen innebär att du kan dölja de poster i listan med tidigare versioner av en fil som finns på ett säkerhetskopieringsmedium. De tidigare versionerna kan komma från återställningspunkter på disken eller från säkerhetskopieringsmediet.

Om du aktiverar den här inställningen visas inga tidigare versioner som finns på säkerhetskopior för användarna och de kan bara se tidigare versioner som finns som återställningspunkter på disken.

Om du inaktiverar den här inställningen visas både tidigare versioner som finns på säkerhetskopior och tidigare versioner som finns som återställningspunkter på disken för användarna.

Om du inte konfigurerar den här inställningen är den som standard inaktiverad.</string>
      <string id="PreviousVersions">Tidigare versioner</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
