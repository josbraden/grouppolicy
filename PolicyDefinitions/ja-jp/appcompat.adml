<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>表示名をここに入力する</displayName>
  <description>説明をここに入力する</description>
  <resources>
    <stringTable>
      <string id="AppCompat">アプリケーションの互換性</string>
      <string id="AppCompat_Prevent16Bit_Help">MS-DOS サブシステム (ntvdm.exe) をユーザーすべてに対して実行できないようにするかどうかを指定します。この設定は、オペレーティング システムでの 16 ビット アプリケーションの起動に影響します。既定では、MS-DOS サブシステムはすべてのユーザーに対して実行されます。

この設定を使って、リソースの使用量を減らし 16 ビット アプリケーションを実行できないように、MS-DOS サブシステムをオフにできます。16 ビット アプリケーションまたは 16 ビット コンポーネントを持つアプリケーションを実行するには、ntvdm.exe の実行が許可されていなければなりません。MS-DOS サブシステムは最初の 16 ビット アプリケーションの起動時に開始します。プロセスの実行中、これ以降の 16 ビット アプリケーションはさらに速く起動しますが、全体のシステム リソースの使用量は増加します。

状態が有効に設定されている場合、ntvdm.exe が実行できないため、16 ビット アプリケーションはすべて実行できなくなります。また、16 ビット インストーラーまたは他の 16 ビット コンポーネントのある 32 ビット アプリケーションは実行できません。

状態が無効に設定されている場合は、既定の設定が適用され、すべてのユーザーに対して MS-DOS サブシステムが実行されます。

状態が未構成に設定されている場合は、既定の設定が適用されてすべてのユーザーに対して ntvdm.exe が実行されます。しかし、管理者がレジストリ DWORD 値 HKLM\System\CurrentControlSet\Control\WOW\DisallowedPolicyDefault を 1 に設定した場合、既定の設定が変更されて 16 ビット アプリケーションがすべて実行できなくなります。

注: この設定は [コンピューターの構成] および [ユーザーの構成] の両方に表示されます。両方の設定が構成されている場合は、[コンピューターの構成] の設定が優先されます。</string>
      <string id="AppCompat_Prevent16BitMach_Help">MS-DOS サブシステム (ntvdm.exe) をこのコンピューターで実行できないようにするかどうかを指定します。この設定は、オペレーティング システムでの 16 ビット アプリケーションの起動に影響します。既定では、MS-DOS サブシステムはこのコンピューターのすべてのユーザーに対して実行します。

この設定を使って、リソースの使用量を減らし 16 ビット アプリケーションを実行できないように、MS-DOS サブシステムをオフにできます。16 ビット アプリケーションまたは 16 ビット コンポーネントを持つアプリケーションを実行するには、ntvdm.exe の実行が許可されていなければなりません。MS-DOS サブシステムは最初の 16 ビット アプリケーションの起動時に開始します。MS-DOS サブシステムの実行中、これ以降の 16 ビット アプリケーションはさらに速く起動しますが、全体のシステム リソースの使用量は増加します。

状態が有効に設定されている場合、ntvdm.exe が実行できないため、16 ビット アプリケーションはすべて実行できなくなります。また、16 ビット インストーラーまたは他の 16 ビット コンポーネントのある 32 ビット アプリケーションは実行できません。

状態が無効に設定されている場合は、既定の設定が適用され、このコンピューターのすべてのユーザーに対して MS-DOS サブシステムが実行されます。

状態が未構成に設定されている場合は、既定の設定が適用されてすべてのユーザーに対して ntvdm.exe が実行されます。ただし、管理者がレジストリ DWORD 値 HKLM\System\CurrentControlSet\Control\WOW\DisallowedPolicyDefault を 1 に設定した場合、既定の設定が変更されて 16 ビット アプリケーションがすべて実行できなくなります。

注: この設定は、[コンピューターの構成] および [ユーザーの構成] の両方に表示されます。両方の設定が構成されている場合は、[コンピューターの構成] の設定が優先されます。</string>
      <string id="AppCompat_RemoveProgramCompatPropPage_Help">このポリシーによってプログラムの互換性プロパティ ページのシェルの拡張機能の表示が制御されます。このシェルの拡張機能は、プログラムのショートカットまたは実行可能ファイルのプロパティ ショートカット メニューにあります。

互換性プロパティ ページには、レガシ アプリケーションに影響する最も一般的な問題の解決用に、選択してアプリケーションに適用できるオプション一覧が表示されています。このポリシー設定を有効にするとショートカット メニューからプロパティ ページが削除されますが、このインターフェイスを使用しているアプリケーションに適用した以前の互換性の設定には影響しません。</string>
        <string id="AppCompat_TurnOffApplicationImpactTelemetry_Help">このポリシーでは、システムにおけるアプリケーション遠隔測定エンジンの状態を制御します。

アプリケーション遠隔測定は、特定の Windows システム コンポーネントについてのアプリケーションによる使用状況を匿名で追跡するメカニズムです。

[有効] を選択してアプリケーション遠隔測定を無効にすると、使用状況データの収集が停止します。

カスタマー エクスペリエンス向上プログラムが無効になっている場合、アプリケーション遠隔測定はこのポリシーの設定に関係なく無効になります。

遠隔測定を無効にすると、新しく起動されるアプリケーションの遠隔測定が行われなくなります。遠隔測定による収集がすべてのアプリケーションについて確実に停止するようにするには、コンピューターを再起動してください。</string>
      <string id="AppCompat_TurnOffEngine_Help"> このポリシーにより、システム内のアプリケーション互換性エンジンの状態が制御されます。

エンジンはローダーの一部であり、システム上でアプリケーションが起動するたびに互換性データベースを検索します。アプリケーションに一致するものが見つかると、ランタイムの解決策または互換性の修正が提供されるか、またはアプリケーションに既知の問題がある場合、アプリケーションのヘルプ メッセージが表示されます。

アプリケーション互換性エンジンをオフにすると、システムのパフォーマンスが上がります。ただし、これにより多くの一般のレガシ アプリケーションの互換性が低下し、既知の互換性のないアプリケーションのインストールがブロックされなくなります (たとえば、古いウイルス対策アプリケーションがインストールされている場合はブルー スクリーンが表示される可能性があります)。

Windows リソース保護機能および Windows のユーザー アカウント制御機能は、アプリケーション互換性エンジンを使用してアプリケーションの問題を軽減します。このエンジンが無効にされている場合、これらの軽減機能はアプリケーションとそのインストーラーに対して適用されないため、アプリケーションはインストールに失敗したり適切に実行されない可能性があります。

このオプションは、速いパフォーマンスを要求し使用中のアプリケーションの互換性を認識しているサーバー管理者に役立ちます。特に、アプリケーションを 1 秒間に数百回起動する可能性がありローダーのパフォーマンスが重要となる Web サーバーで役に立ちます。

注: 多くのシステム プロセスは、パフォーマンス上の理由からこの設定値をキャッシュします。この設定に変更する場合は、システムを再起動してこれらの変更を正確に反映させてください。</string>
      <string id="AppCompat_TurnOffProgramCompatibilityAssistant_Help">このポリシーにより、システム内のプログラム互換性アシスタントの状態が制御されます。
      
PCA により、ユーザーが開始したプログラムに実行時の互換性に関する既知の問題がないかどうかが監視されます。潜在的な問題がアプリケーションで検出された場合、PCA はポインターを使用して、推奨される解決策をユーザーに対して表示します。PCA によるさまざまな問題検出のシナリオと、そのシナリオを構成するポリシーについての詳細は、「システム\トラブルシューティングと診断\アプリケーションの互換性診断」にあるポリシーを参照してください。 

PCA は既定で有効になっています。

このポリシー設定を有効にすると、PCA は無効になります。このオプションは、より高速のパフォーマンスを必要とし、使用中のアプリケーションの互換性を認識しているシステム管理者に役立ちます。注: PCA を無効にすると、アプリケーションの実行時に、互換性に関する既知の問題の解決策がユーザーに対して表示されなくなります。

このポリシー設定を無効にした場合、または構成しなかった場合、PCA は有効になります。 

注: PCA を実行するには、診断ポリシー サービス (DPS) およびプログラム互換性アシスタントが実行されている必要があります。これらのサービスは、Microsoft 管理コンソールのサービス スナップインを使用して構成することができます。</string>
      <string id="AppCompat_TurnOffUserActionRecord_Help">このポリシーでは、システムにおける問題ステップ記録ツール機能の状態を制御します。

問題ステップ記録ツールを使用すると、問題の発生前にユーザーが実行した手順をキャプチャすることができます。問題ステップ記録ツールによって生成される出力データは、Windows エラー報告などのフィードバック シナリオで収集されるデータとは異なる種類のものであり、開発者が問題を把握するのに役立ちます。キャプチャされるデータには、ユーザーがキーボードやマウスで入力した情報、対話操作に使用された UI オブジェクトのメタデータ、およびスクリーン ショットなど、ユーザー操作に関する内容が含まれます。問題ステップ記録ツールには、データ収集を必要に応じていつでも有効または無効にできる固有のオプションがあります。

既定では、問題ステップ記録ツールは、問題レポート シナリオで有効にすることができます。

このポリシー設定を有効にした場合、このコンピューターで問題ステップ記録ツール機能が無効になります。

このポリシー設定を無効にした場合、または構成しなかった場合は、問題ステップ記録ツールが有効になります。</string>

      <string id="AppCompat_TurnOffSwitchBack_Help">このポリシーでは、システムにおけるスイッチバック互換性エンジンの状態を制御します。

スイッチバックは、古いアプリケーションには古い動作を、新しいアプリケーションには新しい動作を提供することで、古いアプリケーションの互換性に対する汎用的な対処法を提供するメカニズムです。

スイッチバックは既定では有効になっています。

このポリシー設定を有効にした場合、スイッチバックは無効になります。スイッチバックを無効にすると、古いアプリケーションの互換性が低下する可能性があります。このオプションは、速いパフォーマンスを要求し使用中のアプリケーションの互換性を認識しているサーバー管理者に役立ちます。

このポリシー設定を無効にした場合、または構成しなかった場合は、スイッチバックは有効になります。

この設定を変更した後は、システムを再起動してこれらの変更を正確に反映させてください。</string>
      <string id="AppCompat_TurnOffProgramInventory_Help">このポリシーでは、システムにおけるプログラム インベントリ コレクターの状態を制御します。
      
PDU では、システム上のプログラムやファイルのインベントリを作成し、それらのファイルに関する情報を Microsoft に送信します。この情報は、ファイルとプログラムの関連付けやアプリケーションの互換性の問題の診断に役立てられます。

PDU は既定では有効になっています。

このポリシー設定を有効にした場合、インベントリ コレクションは無効になり、Microsoft にデータが送信されなくなります。この設定を有効にすると、PCA によるインストール データの収集も無効になります。

このポリシー設定を無効にした場合、または構成しなかった場合は、インベントリ コレクションは有効になります。</string>

      <string id="AppCompatPrevent16Bit">16 ビット アプリケーションにアクセスできないようにする</string>
      <string id="AppCompatPrevent16BitMach">16 ビット アプリケーションにアクセスできないようにする</string>
      <string id="AppCompatRemoveProgramCompatPropPage">プログラムの互換性プロパティ ページを削除する</string>
      <string id="AppCompatTurnOffApplicationImpactTelemetry">アプリケーション遠隔測定をオフにする</string>
      <string id="AppCompatTurnOffEngine">アプリケーションの互換性エンジンをオフにする</string>
      <string id="AppCompatTurnOffProgramCompatibilityAssistant">プログラム互換性アシスタントを終了する</string>
      <string id="AppCompatTurnOffSwitchBack">スイッチバック互換性エンジンをオフにする</string>
      <string id="AppCompatTurnOffUserActionRecord">問題ステップ記録ツールをオフにする</string>
      <string id="AppCompatTurnOffProgramInventory">プログラム インベントリをオフにする</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
