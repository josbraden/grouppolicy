<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>表示名をここに入力する</displayName>
  <description>説明をここに入力する</description>
  <resources>
    <stringTable>


      <!-- Overall category text -->
      <string id="PcaScenarioCategory">アプリケーションの互換性の診断</string>
      

      <!-- Generic WDI text -->
      <string id="WdiScenarioExecutionPolicyLevelResolution">検出、トラブルシューティング、および解決</string>
      <string id="WdiScenarioExecutionPolicyLevelTsOnly">検出とトラブルシューティングのみ</string>


      <!-- Individual scenario text -->
      <string id="DetectInstallFailuresText">アプリケーション インストールの失敗を検出する</string>
      <string id="DetectUpdateFailuresText">UAC で管理されたインストーラーを起動できないアプリケーションを検出する</string>
      <string id="DetectDeprecatedComponentFailuresText">使用されていない Windows DLL によって発生したアプリケーション エラーを検出する</string>
      <string id="DetectDeprecatedCOMComponentFailuresText">使用されていない COM オブジェクトによって発生したアプリケーション エラーを検出する</string>
      <string id="DetectUndetectedInstallersText">管理者として実行する必要があるアプリケーション インストーラーを検出する</string>
      <string id="DetectBlockedDriversText">ブロックされたドライバーを通知する</string>
      <!-- End individual scenario text -->


      <!-- Begin install failure scenario description -->
      <string id="DetectInstallFailuresExplain">このポリシー設定では、プログラム互換性アシスタント (PCA) が構成され、アプリケーションのインストールで発生した失敗が診断されます。 

このポリシー設定を有効にすると、アプリケーションのインストーラー実行で発生した失敗をヒューリスティックを使用して検出するように PCA が構成されます。失敗の可能性が検出されると、Microsoft Windows XP 互換性モードでインストーラーを再起動するオプションが、PCA によってユーザーに提供されます。

このポリシー設定を無効にすると、プログラムのインストーラー実行で発生した失敗を検出するように PCA が構成されません。

このポリシー設定を構成しない場合、既定では PCA でこの診断シナリオが有効になります。

注: "プログラム互換性アシスタントを終了する" ポリシー設定を無効にすると、このポリシー設定も無効になります。PCA を実行するには、診断ポリシー サービス (DPS) とプログラム互換性アシスタントを実行している必要があります。これらのサービスは、Microsoft 管理コンソールのサービス スナップインで構成することができます。</string>
      <!-- End install failure scenario description -->
      

      <!-- Begin update failure scenario description -->
      <string id="DetectUpdateFailuresExplain">このポリシー設定により、プログラム互換性アシスタント (PCA) が構成され、ユーザー アカウント制御 (UAC) で管理されたプログラムの失敗が診断されます。 

このポリシー設定を有効にすると、インストーラー (通常は更新プログラム) である子プロセスの起動に失敗したプログラムが PCA で検出されます。この失敗が検出されると、PCA により ELEVATECREATEPROCESS 互換性モードが適用されます。これにより、次回にプログラムを実行するときに、管理者特権を使用した場合と同じようにプログラムでインストーラーを正常に起動することができます。

このポリシー設定を無効にすると、UAC で管理されたインストーラーの起動に失敗したアプリケーションが PCA で検出されなくなります

このポリシー設定を構成しない場合、インストーラー (通常は更新プログラム) である子プロセスの起動に失敗したプログラムが PCA で検出されます。

注: "プログラム互換性アシスタントを終了する" ポリシー設定を無効にすると、このポリシー設定も無効になります。PCA を実行するには、診断ポリシー サービス (DPS) とプログラム互換性アシスタントを実行している必要があります。これらのサービスは、Microsoft 管理コンソールのサービス スナップインで構成することができます。</string>
      <!-- End update failure scenario description -->
      

      <!-- Begin deprecated component failure scenario description -->
      <string id="DetectDeprecatedComponentFailuresExplain">このポリシー設定では、プログラム互換性アシスタント (PCA) でプログラムの DLL 読み込みの失敗を診断するかどうかを決定します。

このポリシー設定を有効にすると、このバージョンの Windows で削除されたレガシ Microsoft Windows DLL の読み込みを試行しているプログラムが、PCA によって検出されます。この失敗が検出された場合、PCA によりプログラム終了後にこの問題がユーザーに通知され、問題解決のための Microsoft Web サイトを確認するオプションが提供されます。

このポリシー設定を無効にすると、レガシ Windows DLL の読み込みを試行したプログラムが、PCA によって検出されなくなります。

このポリシー設定を構成しない場合、レガシ Windows DLL の読み込みを行ったプログラムが、PCA によって検出されます。

注: [プログラム互換性アシスタントを終了する] ポリシー設定を無効にすると、このポリシー設定も無効になります。PCA を実行するには、診断ポリシー サービス (DPS) とプログラム互換性アシスタントを実行している必要があります。これらのサービスは、Microsoft 管理コンソールのサービス スナップインで構成することができます。</string>
      <!-- End deprecated component failure scenario description -->


      <!-- Begin deprecated COM component failure scenario description -->
      <string id="DetectDeprecatedCOMComponentFailuresExplain">このポリシー設定では、プログラム互換性アシスタント (PCA) でプログラムの DLL 読み込みの失敗または COM オブジェクト作成の失敗を診断するかどうかを決定します。

このポリシー設定を有効にすると、このバージョンの Windows で削除されたレガシ COM オブジェクトの作成を試行しているプログラムが、PCA によって検出されます。この失敗が検出された場合、PCA によりプログラム終了後にこの問題がユーザーに通知され、問題解決のための Microsoft Web サイトを確認するオプションが提供されます。

このポリシー設定を無効にすると、レガシ COM オブジェクトの作成を試行したプログラムが、PCA によって検出されなくなります。

このポリシー設定を構成しない場合、レガシ COM オブジェクトの作成を試行したプログラムが、PCA によって検出されます。

注: [プログラム互換性アシスタントを終了する] ポリシー設定を無効にすると、このポリシー設定も無効になります。PCA を実行するには、診断ポリシー サービス (DPS) とプログラム互換性アシスタントを実行している必要があります。これらのサービスは、Microsoft 管理コンソールのサービス スナップインで構成することができます。</string>
      <!-- End deprecated COM component failure scenario description -->


      <!-- Begin undetected installer failure scenario description -->
      <string id="DetectUndetectedInstallersExplain">このポリシー設定では、管理者として実行していることが検出されていないアプリケーション インストーラーで発生した失敗を、プログラム互換性アシスタント (PCA) で診断するかどうかが決定されます。 

このポリシー設定を有効にすると、ユーザー アクセス制御 (UAC) で管理者として実行するための特権を持たないアプリケーション インストーラーを検出するように PCA が構成されます。発生する可能性のある失敗が検出されると、管理者としてインストーラーを再起動するオプションが PCA によってユーザーに提供されます。

このポリシー設定を無効にすると、UAC で管理者として実行するための特権を持たないアプリケーション インストーラーが、PCA によって検出されなくなります。

このポリシー設定を構成しない場合、UAC で管理者として実行するための特権を持たないアプリケーション インストーラーを検出するように PCA が構成されます。


注: "プログラム互換性アシスタントを終了する" ポリシー設定を無効にすると、このポリシー設定も無効になります。PCA を実行するには、診断ポリシー サービス (DPS) とプログラム互換性アシスタントを実行している必要があります。これらのサービスは、Microsoft 管理コンソールのサービス スナップインで構成することができます。</string>
      <!-- End undetected installer failure scenario description -->


      <!-- Begin blocked drivers scenario description -->
      <string id="DetectBlockedDriversExplain">このポリシー設定では、互換性の問題によりブロックされたドライバーをプログラム互換性アシスタント (PCA) で診断するかどうかが決定されます。 

このポリシー設定を有効にすると、ブロックされたドライバーの問題が PCA によってユーザーに通知され、問題解決のための Microsoft Web サイトを確認するオプションが提供されます。

このポリシー設定を無効にすると、ブロックされたドライバーの問題が、PCA によってユーザーに通知されなくなります。注: このポリシー設定が無効な状態の場合、ブロックされたドライバーの解決策がユーザーに対して表示されなくなります。

このポリシー設定を構成しない場合、ブロックされたドライバーの問題が PCA によってユーザーに通知され、問題解決のための Microsoft Web サイトを確認するオプションが提供されます。

注: "プログラム互換性アシスタントを終了する" ポリシー設定を無効にすると、このポリシー設定も無効になります。PCA を実行するには、診断ポリシー サービス (DPS) とプログラム互換性アシスタントを実行している必要があります。これらのサービスは、Microsoft 管理コンソールのサービス スナップインで構成することができます。</string>
      <!-- End blocked drivers scenario description -->
    </stringTable>

    <presentationTable>
      <presentation id="DetectUpdateFailuresTable">
        <dropdownList refId="DetectUpdateFailuresLevel" noSort="true" defaultItem="1">シナリオ実行レベル</dropdownList>
      </presentation>
      <presentation id="DetectDeprecatedComponentFailuresTable">
        <dropdownList refId="DetectDeprecatedComponentFailuresLevel" noSort="true" defaultItem="1">シナリオ実行レベル</dropdownList>
      </presentation>
      <presentation id="DetectDeprecatedCOMComponentFailuresTable">
        <dropdownList refId="DetectDeprecatedCOMComponentFailuresLevel" noSort="true" defaultItem="1">シナリオ実行レベル</dropdownList>
      </presentation>
      <presentation id="DetectUndetectedInstallersTable">
        <dropdownList refId="DetectUndetectedInstallersLevel" noSort="true" defaultItem="1">シナリオ実行レベル</dropdownList>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
