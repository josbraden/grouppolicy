<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2008 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>自然認証構成の設定</displayName>
  <description>自然認証構成の設定</description>
  <resources>
    <stringTable>
      <string id="Logon">ログオン</string>
      <string id="NaAuth_DynamicLock">動的ロックを構成する</string>
      <string id="NaAuth_DynamicLock_Help">この設定では、動的ロックの有効化または無効化を決定します。
 
この設定を有効にした場合は、管理対象デバイスのすべてのユーザーに対して動的ロックが有効になり、これらのユーザーのアカウントで動的ロックの無効化が許可されなくなります。
 
この設定を無効にした場合は、管理対象デバイスのすべてのユーザーに対して動的ロックが無効になり、これらのユーザーのアカウントで動的ロックの有効化が許可されなくなります。
 
この設定を構成しなかった場合は、ユーザーのアカウントで動的ロックの有効化と無効化が許可されます。
 
この設定が構成されていない管理対象デバイスおよび非管理対象デバイスのアカウントに対しては、既定で動的ロックが有効になります。</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
