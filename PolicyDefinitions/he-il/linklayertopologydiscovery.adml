<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>הזן שם תצוגה כאן</displayName>
  <description>הזן תיאור כאן</description>
  <resources>
    <stringTable>
      <string id="LLTD_Category">גילוי טופולוגיית שכבת קישור</string>
      <string id="LLTD_Category_Help">קביעת תצורה של כל רכיבי גילוי טופולוגיה של שכבת קישור.</string>
      <string id="LLTD_EnableLLTDIO">הפעל את מנהל ההתקן של ממפה I/O ‏(LLTDIO)‎</string>
      <string id="LLTD_EnableLLTDIO_Help">הגדרת מדיניות זו משנה את אופן תפעול מנהל ההתקן של פרוטוקול הרשת של ממפה I/O.

LLTDIO מאפשר למחשב לגלות את הטופולוגיה של רשת שאליה הוא מחובר. הוא מאפשר למחשב גם ליזום בקשות של איכות שירות כגון הערכת רוחב פס וניתוח בריאות רשת.

אם תהפוך הגדרת מדיניות זו לזמינה, אפשרויות נוספות יהיו זמינות לשם ביצוע כיוונון עדין של הבחירה שלך. באפשרותך לבחור באפשרות "אפשר פעולה בתוך התחום" כדי לאפשר ל- LLTDIO לפעול בממשק רשת המחובר לרשת מנוהלת. לעומת זאת, אם ממשק הרשת מחובר לרשת שאינה מנוהלת, באפשרותך לבחור באפשרויות "אפשר פעולה בתוך רשת ציבורית" ו"מנע פעולה בתוך רשת ציבורית" במקום זאת.

אם תהפוך הגדרת מדיניות זו ללא זמינה או לא תקבע את תצורתה, יחול אופן הפעולה של LLTDIO שנקבע כברירת מחדל.</string>
      <string id="LLTD_EnableRspndr">הפעל את מנהל ההתקן של רכיב Responder (RSPNDR)‎</string>
      <string id="LLTD_EnableRspndr_Help">הגדרת מדיניות זו משנה את אופן תפעול מנהל ההתקן של פרוטוקול הרשת של רכיב Responder.

רכיב Responder מאפשר למחשב להשתתף בבקשות גילוי טופולוגיה של שכבת קישור כך שהמערכת תוכל לגלות ולאתר אותו ברשת. הוא מאפשר למחשב גם ליזום בקשות של איכות שירות כגון הערכת רוחב פס וניתוח בריאות רשת.

אם תהפוך הגדרת מדיניות זו לזמינה, אפשרויות נוספות יהיו זמינות לשם ביצוע כיוונון עדין של הבחירה שלך. באפשרותך לבחור באפשרות "אפשר פעולה בתוך התחום" כדי לאפשר ל- Responder לפעול בממשק רשת המחובר לרשת מנוהלת. לעומת זאת, אם ממשק הרשת מחובר לרשת שאינה מנוהלת, באפשרותך לבחור באפשרויות "אפשר פעולה בתוך רשת ציבורית" ו"מנע פעולה בתוך רשת ציבורית" במקום זאת.

אם תהפוך הגדרת מדיניות זו ללא זמינה או שלא תקבע את תצורתה, יחול אופן הפעולה של רכיב Responder שנקבע כברירת מחדל.</string>
    </stringTable>
    <presentationTable>
      <presentation id="LLTD_EnableLLTDIO">
        <checkBox refId="LLTD_EnableLLTDIO_AllowOnDomain">אפשר פעולה בתוך התחום</checkBox>
        <checkBox refId="LLTD_EnableLLTDIO_AllowOnPublicNet">אפשר פעולה בתוך רשת ציבורית</checkBox>
        <checkBox refId="LLTD_EnableLLTDIO_ProhibitOnPrivateNet">מנע פעולה בתוך רשת ציבורית</checkBox>
      </presentation>
      <presentation id="LLTD_EnableRspndr">
        <checkBox refId="LLTD_EnableRspndr_AllowOnDomain">אפשר פעולה בתוך התחום</checkBox>
        <checkBox refId="LLTD_EnableRspndr_AllowOnPublicNet">אפשר פעולה בתוך רשת ציבורית</checkBox>
        <checkBox refId="LLTD_EnableRspndr_ProhibitOnPrivateNet">מנע פעולה בתוך רשת ציבורית</checkBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
