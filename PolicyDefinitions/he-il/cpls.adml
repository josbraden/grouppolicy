<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>הזן שם תצוגה כאן</displayName>
  <description>הזן תיאור כאן</description>
  <resources>
    <stringTable>
      <string id="Users">חשבונות משתמשים</string>
      <string id="Users_Help">הכללת הגדרות השולטות באופן הפעולה של חשבונות משתמשים</string>
      <string id="UseDefaultTile">החלת תמונת הכניסה של המשתמש המהווה ברירת מחדל, על כל המשתמשים</string>
      <string id="UseDefaultTile_Help">הגדרת מדיניות זו מאפשרת למנהל המערכת להתאים את כל תמונות הכניסה עבור כל המשתמשים במערכת לתקן של תמונת כניסה המהווה ברירת מחדל. תכלית אחת של הגדרת מדיניות זו היא לאחד את כל תמונות הכניסה לסמל של חברה. 

הערה: תמונת המשתמש בכניסה המהווה ברירת מחדל מאוחסנת ב- ‎%PROGRAMDATA%\Microsoft\User Account Pictures\user.bmp. תמונת האורח המהווה ברירת מחדל מאוחסנת ב- ‎%PROGRAMDATA%\Microsoft\User Account Pictures\guest.bmp. אם תמונת ברירת המחדל אינה קיימת, מוצגת מסגרת ריקה.

אם תהפוך הגדרת מדיניות זו לזמינה, תמונת הכניסה של המשתמש המהווה ברירת מחדל תוצג עבור כל המשתמשים במערכת ולא תתאפשר כל התאמה אישית.

אם תהפוך את הגדרת מדיניות זו ללא זמינה או אם לא תקבע את תצורתה, משתמשים יוכלו להתאים אישית את תמונות הכניסה שלהם.</string>
    
    </stringTable>
  </resources>
</policyDefinitionResources>
