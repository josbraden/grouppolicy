<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>הזן שם תצוגה כאן</displayName>
  <description>הזן תיאור כאן</description>
  <resources>
    <stringTable>
      <string id="DisableCMD">מנע גישה לשורת הפקודה</string>
      <string id="DisableCMD_Help">מניעת משתמשים מהפעלת שורת הפקודה האינטראקטיבית, Cmd.exe. בנוסף לכך, הגדרה זו קובעת אם ניתן להפעיל קבצי אצווה (‎.cmd ו- ‎.bat) במחשב.

אם תהפוך הגדרה זו לזמינה והמשתמש ינסה לפתוח חלון פקודה, המערכת תציג הודעה שמסבירה שהגדרה מנעה את הפעולה.

הערה: אל תמנע מהמחשב להפעיל קבצי אצווה אם המחשב משתמש בפריטי Script של קובץ אצווה לכניסה, יציאה, הפעלה או כיבוי, או למשתמשים שמשתמשים בשירותי שולחן עבודה מרוחק.</string>
      <string id="DisableCMD_NO">לא</string>
      <string id="DisableCMD_YES">כן</string>
      <string id="DisableRegedit">מנע גישה אל כלי עריכת רישום</string>
      <string id="DisableRegedit_Help">הפיכת עורך הרישום של Windows‏, Regedit.exe, ללא זמין.

אם הגדרה זו זמינה והמשתמש מנסה להפעיל עורך רישום, מוצגת הודעה שמסבירה שהגדרה מונעת פעולה זו.

כדי למנוע מהמשתמשים להפעיל כלי ניהול אחרים, השתמש בהגדרה 'הפעל רק יישומים מוגדרים של Windows'.</string>
      <string id="DisableRegeditMode_UIAndSilent">כן</string>
      <string id="DisableRegeditMode_UIonly">לא</string>
  
   <string id="RestrictApps">הפעל רק יישומים מוגדרים של Windows</string>
      <string id="RestrictApps_Help">הגבלת תוכניות Windows הניתנות להפעלה על-ידי משתמשים במחשב.

אם תהפוך הגדרה זו לזמינה, משתמשים יוכלו להפעיל רק תוכניות שתוסיף ל'רשימת התוכניות המותרות'.

הגדרה זו רק מונעת ממשתמשים להפעיל תוכניות שהופעלו על-ידי התהליך סייר Windows. היא אינה מונעת ממשתמשים להפעיל תוכניות כגון מנהל המשימות, המופעלות באמצעות תהליך המערכת או באמצעות תהליכים אחרים. בנוסף, אם למשתמשים יש גישה לשורת הפקודה, Cmd.exe, הגדרה זו לא תמנע מהם להפעיל בחלון הפקודה תוכניות שאין להם הרשאה לפתוח בסייר Windows.

הערה: השימוש בהגדרה זו מהווה דרישה עבור יישומים של ספקים חיצוניים עם אישור עבור Windows 2000 או גירסאות מתקדמות יותר. הערה: כדי ליצור רשימת יישומים מותרים, לחץ על 'הצג'. בתיבת הדו-שיח 'הצגת תכנים', בעמודה 'ערך', הקלד את שם קובץ ההפעלה של היישום (לדוגמה Winword.exe‏, Poledit.exe‏, Powerpnt.exe).</string>
      <string id="DisallowApps">אל תפעיל את יישומי Windows שצוינו</string>
      <string id="DisallowApps_Help">מניעה מ- Windows להפעיל את התוכניות שתציין בהגדרה זו.

אם תהפוך הגדרה זו לזמינה, משתמשים לא יוכלו להפעיל רק תוכניות שתוסיף ל'רשימת התוכניות הבלתי מותרות'.

הגדרה זו רק מונעת ממשתמשים להפעיל תוכניות שהופעלו על-ידי התהליך סייר Windows. היא אינה מונעת מהמשתמשים להפעיל תוכניות, כגון מנהל המשימות, המופעלות דרך תהליך המערכת או תהליכים אחרים. כמו כן, אם תאפשר למשתמשים לגשת לשורת הפקודה, Cmd.exe, הגדרה זו לא תמנע מהם להפעיל בחלון הפקודה תוכניות שאין רשות להפעילן באמצעות סייר Windows. הערה: כדי ליצור רשימת יישומים שאינם מותרים, לחץ על 'הצג'. בתיבת הדו-שיח 'הצגת תכנים', בעמודה 'ערך', הקלד את שם קובץ ההפעלה של היישום (לדוגמה Winword.exe‏, Poledit.exe‏, Powerpnt.exe).</string>
      
  </stringTable>
    <presentationTable>
      <presentation id="DisableCMD">
        <dropdownList refId="DisableCMDScripts" noSort="true" defaultItem="1">האם ברצונך להפוך ללא זמין גם את עיבוד קובץ ה- Script של שורת הפקודה?</dropdownList>
      </presentation>
      <presentation id="DisableRegedit">
        <dropdownList refId="DisableRegeditMode" noSort="true" defaultItem="0">האם להפוך ללא זמינה הפעלה שקטה של regedit?</dropdownList>
      </presentation>
	<presentation id="RestrictApps">
        <listBox refId="RestrictApps_RestrictAppsList">רשימת היישומים המותרים</listBox>
      </presentation>
      <presentation id="DisallowApps">
        <listBox refId="DisallowAppsList">רשימת יישומים לא זמינים</listBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
