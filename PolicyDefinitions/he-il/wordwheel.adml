<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>הזן שם תצוגה כאן</displayName>
  <description>הזן תיאור כאן</description>
  <resources>
    <stringTable>
      <string id="CustomSearch">ספק חיפוש באינטרנט של 'חיפוש מיידי' מותאם אישית</string>
      <string id="CustomSearch_Explain">הגדר את שם התפריט וכתובת ה- URL עבור ספק החיפוש באינטרנט המותאם אישית.

אם תהפוך הגדרה זו לזמינה, שם התפריט וכתובת ה- URL שצוינו ישמשו עבור חיפושים באינטרנט.

אם תהפוך הגדרה זו ללא זמינה או אם לא תקבע את תצורתה, ייעשה שימוש בספק החיפוש באינטרנט המשמש כברירת מחדל.</string>
      <string id="NoSearchInternetInWordWheel">הסתר את הקישור "חפש באינטרנט" ברשימה הנפתחת של התיבה 'חיפוש'.</string>
      <string id="NoSearchInternetInWordWheel_Explain">הגדרת מדיניות זו מאפשרת לך לקבוע אם הקישור "חפש באינטרנט" לא יופיע בתפריט 'התחלה' וברשימה הנפתחת של התיבה 'חיפוש מיידי'.

אם תהפוך הגדרת מדיניות זו לזמינה, הקישור "חפש באינטרנט" יוסתר מתפריט 'התחלה' ומרשימה הנפתחת של התיבה 'חיפוש מיידי'.

אם תהפוך הגדרת מדיניות זו ללא זמינה או אם לא תקבע את תצורתה, הקישור "חפש באינטרנט" יופיע בתפריט 'התחלה' וברשימה הנפתחת של התיבה 'חיפוש מיידי'.</string>
      <string id="WordWheelAlwaysUseExplicitCommit">דרוש פעולת Commit מפורשת לפני ביצוע חיפושים.</string>
      <string id="WordWheelAlwaysUseExplicitCommit_Explain">הגדרת מדיניות זו מאפשרת לך לקבוע אם יש לדרוש פעולת Commit מסוימת לפני ביצוע חיפושים.

אם תהפוך הגדרת מדיניות זו לזמינה, פעולת Commit מסוימת נדרשת לפני ביצוע חיפושים.

אם תהפוך הגדרת מדיניות זו ללא זמינה או לא תקבע את תצורתה, פעולת Commit מסוימת אינה נדרשת.</string>
      <string id="WordwheelPolicies">חיפוש מיידי</string>
    </stringTable>
    <presentationTable>
      <presentation id="CustomSearch">
        <textBox refId="CustomSearch_NamePrompt">
          <label>המחרוזת או משאב ה- DLL מהם יש לטעון את המחרוזת המוצגת בתפריט 'חיפוש מיידי'.</label>
        </textBox>
        <textBox refId="CustomSearch_URLPrompt">
          <label>כתובת ה- URL בה יש להשתמש בעת הפעלת החיפוש באינטרנט המותאם אישית, כאשר מונח החיפוש מצוין באמצעות ‎"%w".</label>
        </textBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
