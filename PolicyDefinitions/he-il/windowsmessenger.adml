<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>הזן שם תצוגה כאן</displayName>
  <description>הזן תיאור כאן</description>
  <resources>
    <stringTable>
      <string id="WinMSG_NoAutoStartWindowsMsg_Comp">אל תפעיל את Windows Messenger באופן אוטומטי מראש</string>
      <string id="WinMSG_NoAutoStartWindowsMsg_Help">Windows Messenger נטען ומופעל באופן אוטומטי כאשר משתמש נכנס למחשב של Windows XP. באפשרותך להשתמש בהגדרה זו כדי לבטל את ההפעלה האוטומטית של Windows Messenger בעת הכניסה למחשב.

אם תהפוך הגדרה זו לזמינה, Windows Messenger לא ייטען אוטומטית בעת כניסה של משתמש.

אם תהפוך הגדרה זו ללא זמינה או אם לא תקבע את תצורתה, Windows Messenger ייטען אוטומטית בעת הכניסה למחשב.

הערה: הגדרה זו מונעת הפעלה של Windows Messenger מראש. אם המשתמש מפעיל את Windows Messenger ומשתמש בו החל מנקודה זו, Windows Messenger ייטען.

המשתמש יכול גם לקבוע את התצורה של אופן פעולה זה בכרטיסיה 'העדפות' בתפריט 'כלים' בממשק המשתמש של Windows Messenger.

הערה: אם אינך מעוניין לאפשר למשתמשים להשתמש ב- Windows Messenger, הפוך את ההגדרה "אל תאפשר הפעלה של Windows Messenger" לזמינה.

הערה: הגדרה זו זמינה הן תחת 'תצורת מחשב' והן תחת 'תצורת משתמש'. אם שניהם קיימים, הגירסה 'תצורת מחשב' של ההגדרה מקבלת קדימות.</string>
      <string id="WinMSG_NoAutoStartWindowsMsg_User">אל תפעיל את Windows Messenger באופן אוטומטי מראש</string>
      <string id="WinMSG_NoWindowsMsg_Comp">אל תאפשר הפעלה של Windows Messenger</string>
      <string id="WinMSG_NoWindowsMsg_Help">מתן אפשרות להפוך את Windows Messenger ללא זמין.

אם תהפוך הגדרה זו לזמינה, Windows Messenger לא יפעל.

אם תהפוך הגדרה זו ללא זמינה או אם לא תקבע את תצורתה, תהיה אפשרות להשתמש ב- Windows Messenger.

הערה: אם תהפוך הגדרה זו לזמינה, גם ל'סיוע מרחוק' לא תהיה אפשרות להשתמש ב- Windows Messenger.

הערה: הגדרה זו זמינה הן תחת 'תצורת מחשב' והן תחת 'תצורת משתמש'. אם שניהם קיימים, הגירסה 'תצורת מחשב' של ההגדרה מקבלת קדימות.</string>
      <string id="WinMSG_NoWindowsMsg_User">אל תאפשר הפעלה של Windows Messenger</string>
      <string id="WinMSG_WindowsMsg">Windows Messenger</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
