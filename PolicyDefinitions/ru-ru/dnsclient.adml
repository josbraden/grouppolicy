<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>введите выводимое имя</displayName>
  <description>введите описание</description>
  <resources>
    <stringTable>
      <string id="DNS_Client">DNS-клиент</string>
      <string id="DNS_Domain">DNS-суффикс подключения</string>
      <string id="DNS_Domain_Help">Задает DNS-суффикс подключения. Этот параметр заменяет DNS-суффиксы подключения на компьютерах, к которым применим данный параметр, настроенных как локально, так и с использованием DHCP.

Внимание! DNS-суффикс подключения, указанный в этом параметре, применяется для всех сетевых подключений многосетевых компьютеров, к которым применяется этот параметр.

Чтобы использовать этот параметр, выберите «Включить» и введите в соответствующее поле строку, представляющую DNS-суффикс.

Если этот параметр не задан, то он не применяется ни к каким компьютерам и компьютеры используют свои локальные или настроенные с помощью DHCP параметры.</string>
      <string id="DNS_NameServer">DNS-серверы</string>
      <string id="DNS_NameServer_Help">Указывает DNS-серверы, к которым компьютер посылает запросы при попытках разрешения имен.

Внимание! Список DNS-серверов, указанный в этом параметре, заменяет DNS-серверы, настроенные как локально, так и с использованием DHCP. Список DNS-серверов применяется ко всем сетевым подключениям многосетевых компьютеров, к которым применяется этот параметр.

Чтобы использовать этот параметр, выберите «Включить» и введите в соответствующем поле список разделенных пробелом IP-адресов (в десятичном формате с точкой). Если этот параметр включен, должен быть указан как минимум один IP-адрес.

Если параметр не задан, то он не применяется ни к каким компьютерам и компьютеры используют свои локальные или настроенные с помощью DHCP параметры.</string>
      <string id="DNS_PrimaryDnsSuffix">Основной DNS-суффикс</string>
      <string id="DNS_PrimaryDnsSuffix_Help">Задает основной суффикс системы доменных имен (DNS) для группы компьютеров. Основной DNS-суффикс используется при регистрации и разрешении DNS-имен.

Этот параметр позволяет задать основной DNS-суффикс для группы компьютеров и запрещает пользователям, в том числе администраторам, изменять его.

Если данный параметр отключен или не определен, все компьютеры используют свой локальный основной DNS-суффикс (обычно это DNS-имя домена Active Directory), к которому присоединен компьютер. Однако администраторы могут использовать компонент «Система» панели управления для изменения основного DNS-суффикса компьютера.

Для использования данного параметра в предлагаемом поле следует ввести нужный основной DNS-суффикс полностью. Например, «microsoft.com».

Параметр не отключает диалоговое окно «DNS-суффикс и NetBIOS-имя компьютера», которое администраторы используют для изменения основного DNS-суффикса компьютера. Однако при включении данного параметра суффиксы, введенные администраторами, игнорируются, пока параметр включен.

Внимание! Для вступления в силу изменений параметра требуется перезапустить Windows на всех компьютерах, к которым применяется параметр.

Примечание. Для изменения основного DNS-суффикса на компьютере без задания значения параметра щелкните значок «Система» в панели управления, выберите вкладку «Сетевая идентификация», нажмите кнопку «Свойства», затем «Другие» и введите суффикс в поле «Основной DNS-суффикс этого компьютера».

Дополнительные сведения о DNS см. в разделе справки «Система доменных имен (DNS)»</string>
      <string id="DNS_RegisterAdapterName">Регистрировать DNS-записи с DNS-суффиксом подключения</string>
      <string id="DNS_RegisterAdapterName_Help">Указывает, может ли выполняющий динамическую регистрацию компьютер регистрировать A- и PTR-записи ресурсов, выполняя сцепление имени компьютера с DNS-суффиксом подключения, в дополнение к регистрации этих записей со сцеплением имени компьютера с основным DNS-суффиксом.

Внимание! Включение данного группового параметра распространяется на все сетевые подключения многосетевых компьютеров, к которым он применяется.

По умолчанию DNS-клиент, выполняющий динамическую регистрацию DNS, регистрирует A- и PTR-записи ресурсов, выполняя сцепление имени компьютера с основным DNS-суффиксом. Например, сцепление имени компьютера «mycomputer» и основного DNS-суффикса «microsoft.com» дает в результате «mycomputer.microsoft.com».

Если этот параметр включен, компьютер будет регистрировать A- и PTR-записи ресурсов, сцепленные с DNS-суффиксом подключения, в дополнение к регистрации A- и PTR-записей ресурсов с основным DNS-суффиксом. Например, сцепление имени компьютера «mycomputer» и DNS-суффикса подключения «VPNconnection» будет использоваться при регистрации A- и PTR-записей ресурсов, давая в результате «mycomputer.VPNconnection». Необходимо отметить, что если на компьютере, к которому применяется данный параметр, отключена динамическая регистрация DNS, то независимо от значения параметра компьютер не будет пытаться выполнить динамическую регистрацию DNS A- и PTR-записей ресурсов, представляющих собой имена компьютеров, сцепленные с DNS-суффиксом подключения. Если динамическая регистрация DNS отключена на конкретном сетевом подключении компьютера, к которому применяется данный параметр, то независимо от значения параметра компьютер не будет пытаться выполнить динамическую регистрацию DNS A- и PTR-записей ресурсов, представляющих собой имена компьютеров, сцепленные с DNS-суффиксом подключения, на этом сетевом подключении.

Если этот параметр отключен, DNS-клиент не будет регистрировать A- и PTR-записи ресурсов с DNS-суффиксом подключения.

Если параметр не задан, он не применяется ни к каким компьютерам и компьютеры используют свою локальную настройку.</string>
      <string id="DNS_RegisterReverseLookup">Регистрировать PTR-записи</string>
      <string id="DNS_RegisterReverseLookup_DoNotRegister">Не регистрировать</string>
      <string id="DNS_RegisterReverseLookup_Help">Определяет, будет ли включена регистрация PTR-записей ресурсов для компьютеров, к которым применяется данный параметр.

По умолчанию DNS-клиенты, выполняющие динамическую регистрацию DNS, пытаются регистрировать PTR-записи ресурсов только после успешной регистрации соответствующей A-записи ресурса. A-записи ресурсов сопоставляют DNS-имя узла его IP-адресу, а PTR-записи ресурсов — IP-адрес сайта его DNS-имени.

Чтобы включить параметр, выберите «Включить», а затем одно из следующих значений:

Не регистрировать — компьютеры никогда не пытаются регистрировать PTR-записи ресурсов.

Регистрировать — компьютеры пытаются регистрировать PTR-записи ресурсов независимо от успеха или неудачи регистрации A-записей.

Регистрировать только после успешной регистрации A-записи — компьютеры пытаются регистрировать PTR-записи ресурсов только после успешной регистрации соответствующей A-записи ресурса.

Если параметр не задан, он не применяется ни к каким компьютерам и компьютеры используют свою локальную настройку.</string>
      <string id="DNS_RegisterReverseLookup_Register">Регистрировать</string>
      <string id="DNS_RegisterReverseLookup_RegisterOnlyIfASucceeds">Регистрировать только после успешной регистрации A-записи </string>
      <string id="DNS_RegistrationEnabled">Динамическое обновление</string>
      <string id="DNS_RegistrationEnabled_Help">Определяет, включено ли динамическое обновление

Компьютеры, настроенные на динамическое обновление, автоматически регистрируют и обновляют свои записи ресурсов DNS на DNS-сервере.

Если данный параметр включен, то компьютеры, к которым он применяется, могут использовать динамическую DNS-регистрацию на каждом из своих сетевых подключений, в зависимости от настройки этого подключения. Чтобы динамическая DNS-регистрация могла быть включена на конкретном сетевом подключении, необходимо и достаточно, чтобы настройки и компьютера, и подключения поддерживали динамическую DNS-регистрацию. Данный параметр определяет свойство компьютера, управляющее динамической регистрацией DNS. Если параметр включен, то разрешается устанавливать динамическое обновление индивидуально для каждого из сетевых подключений.

Если параметр отключен, компьютеры, к которым он применяется, не могут использовать динамическую регистрацию DNS ни для каких подключений, независимо от настройки отдельных сетевых подключений.

Если параметр не задан, он не применяется ни к каким компьютерам и компьютеры используют свою локальную настройку.</string>
      <string id="DNS_RegistrationOverwritesInConflict">Заменять адреса при конфликтах</string>
      <string id="DNS_RegistrationOverwritesInConflict_Help">Определяет, сможет ли DNS-клиент, пытающийся зарегистрировать свою A-запись ресурса, перезаписать уже существующую А-запись или несколько A-записей ресурсов, содержащих конфликтующие IP-адреса.

Данный параметр предназначается для компьютеров, которые регистрируют A-записи ресурсов в DNS-зонах, не поддерживающих безопасное динамическое обновление. Безопасное динамическое обновление сохраняет владельца записей ресурсов и не позволяет DNS-клиенту перезаписывать записи, зарегистрированные другими компьютерами.

Во время динамического обновления зоны, не поддерживающей безопасного динамического обновления, DNS-клиент может обнаружить, что уже существует A-запись ресурса, в которой клиентское DNS-имя сайта сопоставлено IP-адресу другого компьютера.  В соответствии с конфигурацией по умолчанию DNS-клиент попытается заменить существующую A-запись ресурса на A-запись ресурса, сопоставляющую DNS-имя IP-адресу клиента.

Если данный параметр включен, DNS-клиенты во время динамического обновления будут пытаться заменять конфликтующие A-записи ресурсов.

Если параметр отключен, DNS-клиенты продолжают выполнять динамическое обновление A-записей ресурсов, но если DNS-клиент попытается обновить A-запись ресурса, конфликтующую с существующими записями, эта попытка завершится неудачей и ошибка будет записана в журнал событий.

Если параметр не задан, он не применяется ни к каким компьютерам и компьютеры используют свою локальную настройку.</string>
      <string id="DNS_RegistrationRefreshInterval">Интервал обновления регистрации</string>
      <string id="DNS_RegistrationRefreshInterval_Help">Задает интервал обновления регистрации A- и PTR-записей ресурсов для компьютеров, к которым применяется данный параметр. Параметр может применяться только к компьютерам, использующим динамическое обновление.

Компьютеры с операционной системой Windows 2000 Professional и Windows XP Professional, настроенные на выполнение динамической регистрации DNS для A- и PTR-записей ресурсов, периодически перерегистрируют свои записи на DNS-серверах, даже если данные их записей не изменились. Перерегистрация требуется для того, чтобы указать DNS-серверам, выполняющим автоматическое удаление (сборку мусора) устаревших записей, что эти записи являются действующими и должны быть сохранены в базе данных.

Внимание! Если записи ресурсов DNS зарегистрированы в зонах, где включена сборка мусора, значение данной установки никогда не должно превышать интервал обновления, заданный для этих зон. Задание интервалу обновления регистрации значения, большего, чем интервал обновления DNS-зон, может привести к нежелательному удалению A- и PTR-записей ресурсов.

Чтобы указать интервал обновления регистрации, выберите «Включить» и введите значение, большее 1800. Имейте в виду, что значение задает интервал обновления регистрации в секундах, например, 1800 секунд составляют 30 минут.

Если параметр не задан, он не применяется ни к каким компьютерам и компьютеры используют свою локальную настройку.</string>
      <string id="DNS_RegistrationTtl">Установить TTL для A- и PTR-записей</string>
      <string id="DNS_RegistrationTtl_Help">Задает значение поля срока жизни (TTL) для A- и PTR-записей ресурсов, зарегистрированных компьютерами, к которым применяется данный параметр.

Чтобы задать TTL, выберите «Включить» и введите значение в секундах (например, значение 900 соответствует 15 минутам).

Если параметр не задан, то он не применяется ни к каким компьютерам.</string>
      <string id="DNS_SearchList">Порядок просмотра DNS-суффиксов</string>
      <string id="DNS_SearchList_Help">Задает DNS-суффиксы для присоединения их к неполному однокомпонентному имени перед выдачей DNS-запроса для такого имени.

Неполное однокомпонентное имя не содержит разделяющих точек, например: «example». Этим оно отличается от полного имени домена, такого, как «example.microsoft.com».

Если данный параметр включен и пользователь выдает запрос на однокомпонентное имя, например «example», локальный DNS-клиент присоединяет суффикс, например «microsoft.com», формируя в результате запрос «example.microsoft.com», перед тем, как отправить его на DNS-сервер.

Если параметр включен, перед отправкой запроса на неполное однокомпонентное имя можно указать DNS-суффиксы для присоединения.  Значения DNS-суффиксов в этом параметре могут быть заданы с помощью строк с запятой-разделителем, например «microsoft.com,serverua.microsoft.com,office.microsoft.com».  Один DNS-суффикс присоединяется при каждой отправке запроса.  Если запрос неудачен, на место неудачного суффикса подставляется следующий DNS-суффикс и выдается новый запрос.  Значения используются в порядке появления их в строке, слева направо.

Если параметр включен, должен быть указан как минимум один суффикс.

Если данный параметр отключен, к неквалифицированным запросам присоединяются основной DNS-суффикс и DNS-суффикс подключения.

Если параметр не задан, он не применяется ни к каким компьютерам и компьютеры используют свою локальную настройку.</string>
      <string id="DNS_UpdateSecurityLevel">Уровень безопасности обновления</string>
      <string id="DNS_UpdateSecurityLevel_Help">Указывает, используют ли компьютеры, к которым применяется этот параметр, безопасные динамические обновления или стандартные динамические обновления для регистрации своих DNS-записей.

Чтобы включить параметр, выберите «Включить», а затем одно из следующих значений:

Небезопасное, затем безопасное — если выбрано это значение, компьютеры посылают безопасное динамическое обновление только в том случае, когда в небезопасном обновлении отказано.

Только небезопасные — если выбрано это значение, компьютеры посылают только небезопасные динамические обновления.

Только безопасные — если выбрано это значение, компьютеры посылают только безопасные динамические обновления.

Если параметр не задан, он не применяется ни к каким компьютерам, и компьютеры используют свою локальную настройку.</string>
      <string id="DNS_UpdateSecurityLevel_OnlySecure">Только безопасные</string>
      <string id="DNS_UpdateSecurityLevel_OnlyUnsecure">Только небезопасные</string>
      <string id="DNS_UpdateSecurityLevel_UnsecureFollowedBySecure">Небезопасные, затем безопасные</string>
      <string id="DNS_UpdateTopLevelDomainZones">Обновлять зоны доменов верхнего уровня</string>
      <string id="DNS_UpdateTopLevelDomainZones_Help">Определяет, могут ли компьютеры, к которым применяется данный параметр, посылать динамические обновления в зоны, имена которых состоят из одного компонента, известные также как зоны доменов верхнего уровня, например «com».

По умолчанию DNS-клиент, настроенный на выполнение динамических обновлений DNS, отправляет динамические обновления в зоны DNS, управляющие его DNS-записями ресурсов, если только управляющие зоны не являются зонами доменов верхнего уровня или корневой зоной.

Если данный параметр включен, то компьютеры, к которым он применяется, отправляют динамические обновления к любой зоне, управляющей записями ресурсов, которые компьютеру требуется обновить, за исключением корневой зоны.

Если параметр отключен, то компьютеры, к которым он применяется, не отправляют динамические обновления в корневую зону и зоны доменов верхнего уровня, управляющие записями ресурсов, которые требуется обновить.

Если параметр не задан, он не применяется ни к каким компьютерам и компьютеры используют свою локальную настройку.</string>
      <string id="DNS_UseDomainNameDevolution">Регрессирование основного DNS-суффикса</string>
      <string id="DNS_UseDomainNameDevolution_Help">Определяет, будет ли DNS-клиент выполнять регрессию основного DNS-суффикса при разрешении имен.

Когда пользователь выдает запрос для однокомпонентного имени, например "example", перед отправкой запроса на DNS-сервер локальный DNS-клиент присоединяет к нему суффикс, например "microsoft.com", формируя в результате запрос "example.microsoft.com".

Если не указан список поиска DNS-суффиксов, DNS-клиент присоединяет основной DNS-суффикс к однокомпонентному имени, а затем, если запрос неудачен, присоединяет DNS-суффиксы подключения к новому запросу. Если ни один из этих запросов не будет успешен, то клиент выполняет регрессию основного DNS-суффикса компьютера (отбрасывает самый левый компонент основного DNS-суффикса), присоединяет этот укороченный основной DNS-суффикс к однокомпонентному имени и вновь отправляет запрос к DNS-серверу.

Например, если основной DNS-суффикс "ooo.aaa.microsoft.com" был присоединен к не заканчивающемуся точкой однокомпонентному имени "example" и DNS-запрос для "example.ooo.aaa. microsoft.com" неудачен, DNS-клиент регрессирует основной DNS-суффикс (отбрасывает самый левый компонент) и отправляет запрос "example.aaa.microsoft.com".  Если и этот запрос неудачен, основной DNS-суффикс регрессируется далее, если такая возможность имеется, и подается запрос "example.microsoft.com". Если и этот запрос неудачен, регрессирование производится далее, если такая возможность имеется, и подается запрос "example.microsoft.com".   Основной DNS-суффикс не будет далее регрессироваться, поскольку в DNS-суффиксе остались только два компонента: "microsoft.com".

Если данный параметр включен при соответствующей возможности регрессии, DNS-клиенты на компьютерах, к которым он применяется, будут пытаться выполнить разрешение имен, представляющих собой сцепление однокомпонентного разрешаемого имени и регрессированного основного DNS-суффикса.

Если параметр отключен, DNS-клиенты на компьютерах, к которым он применяется, не будут пытаться выполнять разрешение имен, представляющих собой сцепление однокомпонентного разрешаемого имени и регрессированного основного DNS-суффикса.

Если параметр не задан, он не применяется ни к каким компьютерам и компьютеры используют свою локальную настройку.</string>
      <string id="DNS_DomainNameDevolutionLevel">Уровень регрессии основного DNS-суффикса</string>
      <string id="DNS_DomainNameDevolutionLevel_Help">Этот параметр политики определяет уровень регрессии DNS-суффикса, используемый клиентами DNS, если они выполняют регрессию основного DNS-суффикса в процессе разрешения имен. Если регрессия DNS-суффикса включена, самый левый компонент DNS-суффикса отбрасывается при каждой последовательной попытке запроса, если запрос завершается с ошибкой для имени, к которому присоединен основной DNS-суффикс. Уровень регрессии определяет минимальное число компонентов, добавляемых к строке запроса после регрессии основного DNS-суффикса.

Когда пользователь отправляет запрос на однокомпонентное имя, например "example", перед отправкой запроса на DNS-сервер локальный DNS-клиент перед отправкой запроса на DNS-сервер добавляет к нему суффикс, например "microsoft.com". В этом случае формируется запрос "example.microsoft.com".
 
Если список поиска DNS-суффикса не указан, клиент DNS присоединяет DNS-суффикс к однокомпонентному имени. Если этот запрос завершается неудачей, DNS-суффикс, специфичный для этого подключения, присоединяется к новому запросу. Если ни один из запросов не разрешается, клиент выполняет регрессию основного DNS-суффикса, присоединяет регрессированный основной DNS-суффикс к однокомпонентному имени и передает новый запрос на DNS-сервер.

Например, если основной DNS-суффикс aaa.bbb.ccc.ddd.microsoft.com присоединяется к однокомпонентному имени "example” (в котором нет точки в конце), регрессия DNS-суффикса включена и уровень  равен 3, выполняются следующие запросы:  

Пример:aaa.bbb.ccc.ddd.microsoft.com (если запрос завершается неудачей, в следующем запросе основной DNS-суффикс регрессируется до bbb.ccc.ddd.microsoft.com.)

Пример:bbb.ccc.ddd.microsoft.com (если запрос завершается неудачей, в следующем запросе основной DNS-суффикс регрессируется до ccc.ddd.microsoft.com.)

Пример:ccc.ddd.microsoft.com (если запрос завершается неудачей, в следующем запросе основной DNS-суффикс регрессируется до ddd.microsoft.com.)

Пример:ddd.microsoft.com (если запрос завершается неудачей, последующие запросы невозможны, так как уровень регрессии равен 3, а основной DNS-суффикс содержит 3 компонента.)
  
При включении этого параметра политики, клиенты DNS на компьютерах, к которым он применяется, попытаются разрешить имена, являющиеся конкатенацией однокомпонентного имени, которое следует разрешить, и регрессированного основного DNS-суффикса. Клиенты DNS выполняют регрессию основного DNS-суффикса в каждой попытке запроса до тех пор, пока имя не будет успешно разрешено, достигнут указанный уровень регрессии или в основном DNS-суффиксе два компонента.
 
Если параметр отключен или не задан, DNS-клиенты на компьютерах, к которым он применяется, не будут пытаться выполнять разрешение имен, присоединяя однокомпонентное имя к регрессированному основному DNS-суффиксу. Если существует корневой домен леса (FRD), список поиска не настроен и запрос используется для однокомпонентного имени, то клиенты DNS выполняют регрессию до FRD, пока имя не будет успешно разрешено.</string>
      <string id="DNS_TurnOffMulticast">Отключить многоадресное разрешение имен</string>
      <string id="DNS_TurnOffMulticast_Help">Local Link Multicast Name Resolution (LLMNR, многоадресное разрешение имен локальных ссылок) — это дополнительный протокол разрешения имен. Запросы посылаются через подсеть локальной связи из клиентской машины с использованием Multicast, на них может ответить другой клиент той же подсети, у которого также включено LLMNR. LLMNR предоставляет разрешение имен в сценариях, в которых обычное разрешение DNS-имен невозможно.

При включении этого параметра политики многоадресное разрешение имен или LLMNR будет отключено для машины через все доступные, но ненастроенные сетевые адаптеры.

При отключении этого параметра политики многоадресное разрешение имен или LLMNR будет включено для машины через все доступные, но ненастроенные сетевые адаптеры.

Если данный параметр политики не был определен, многоадресное разрешение имен или LLMNR будет включено по умолчанию для машины через все доступные, но ненастроенные сетевые адаптеры.</string>
      <string id="DNS_AppendToMultiLabelName">Разрешить добавление DNS-суффиксов к запросам несоответствующих составных имен.</string>
      <string id="DNS_AppendToMultiLabelName_Help">Определяет, могут ли компьютеры, к которым применяется этот параметр, добавлять суффиксы к несоответствующим составным именам перед отправкой повторных DNS-запросов, если запрос по первоначальному имени неудачен.

Имя, содержащее точки, но не заканчивающееся точкой, называется несоответствующим составным именем, например «server.corp». Полностью соответствующее имя должно заканчиваться точкой, например «server.corp.contoso.com.».

Если параметр включен, то разрешено добавление суффиксов к несоответствующим составным именам при неудаче запроса с использованием первоначального имени. Например, сначала клиент посылает запрос с использованием несоответствующего составного имени «server.corp». Если запрос будет успешным, клиенту будет возвращен ответ. Если запрос не будет успешным, к несоответствующему составному имени будут добавлены DNS-суффиксы, указанные для использования компьютером. Эти суффиксы могут быть получены из комбинации первичных доменных суффиксов локального DNS-клиента, DNS-суффикса подключения и (или) из списка поиска суффиксов DNS.

Например, если локальный DNS-клиент получает запрос для «server.corp», а первичный доменный суффикс настроен на «contoso.com», и запрос на «server.corp» будет неудачен, то DNS-клиент пошлет запрос на адрес «server.corp.contoso.com.». 

Если параметр отключен, то добавление суффиксов к несоответствующим составным именам при неудаче запроса с использованием первоначального имени запрещено. 

Если параметр не настроен, компьютеры будут использовать настройки локальных DNS-клиентов для определения поведения в отношении запросов с несоответствующими составными именами.</string>
    </stringTable>
    <presentationTable>
      <presentation id="DNS_Domain">
        <textBox refId="DNS_DomainLabel">
          <label>DNS-суффикс:</label>
        </textBox>
      </presentation>
      <presentation id="DNS_NameServer">
        <textBox refId="DNS_NameServerLabel">
          <label>IP-адреса:</label>
        </textBox>
      </presentation>
      <presentation id="DNS_PrimaryDnsSuffix">
        <textBox refId="DNS_PrimaryDnsSuffixBox">
          <label>Введите основной DNS-суффикс:</label>
        </textBox>
      </presentation>
      <presentation id="DNS_RegisterReverseLookup">
        <dropdownList refId="DNS_RegisterReverseLookup_Box" defaultItem="0">Регистрировать PTR-записи:</dropdownList>
      </presentation>
      <presentation id="DNS_RegistrationRefreshInterval">
        <decimalTextBox refId="DNS_RegistrationRefreshIntervalLabel" defaultValue="1800" spinStep="100">Секунды:</decimalTextBox>
      </presentation>
      <presentation id="DNS_DomainNameDevolutionLevel">
        <decimalTextBox refId="DNS_DomainNameDevolutionLevelLabel" defaultValue="2" spinStep="1">Установка уровня регрессии основного DNS-суффикса</decimalTextBox>
      </presentation>
      <presentation id="DNS_RegistrationTtl">
        <decimalTextBox refId="DNS_RegistrationTtlLabel" defaultValue="600" spinStep="100">Секунды:</decimalTextBox>
      </presentation>
      <presentation id="DNS_SearchList">
        <textBox refId="DNS_SearchListLabel">
          <label>DNS-суффиксы:</label>
        </textBox>
      </presentation>
      <presentation id="DNS_UpdateSecurityLevel">
        <dropdownList refId="DNS_UpdateSecurityLevel_Box" defaultItem="2">Уровень безопасности обновления:</dropdownList>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
