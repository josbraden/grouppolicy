<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>введите отображаемое имя</displayName>
  <description>введите описание</description>
  <resources>
    <stringTable>
      <string id="WdiScenarioCategory">Отказоустойчивая куча</string>
      <string id="WdiScenarioExecutionPolicy">Настройка уровня выполнения сценария</string>
      <string id="WdiScenarioExecutionPolicyExplain">Если этот параметр политики включен, служба политики диагностики (DPS) обнаружит, устранит неполадки и попытается автоматически устранить все повреждения кучи.

Если этот параметр политики отключен, Windows не может обнаружить, устранить неполадки и попытаться автоматически устранить все повреждения кучи, которые обрабатываются службой политики диагностики (DPS).

Если этот параметр политики не задан, служба DPS включит отказоустойчивую кучу для устранения неполадок по умолчанию.

Этот параметр политики вступает в силу только в том случае, если политика выполнения диагностического сценария не настроена.

Чтобы этот параметр политики вступил в силу, не требуется перезапуск системы или службы, поскольку изменения применяются немедленно.

Этот параметр политики вступает в силу только в том случае, если запущена служба политики диагностики (DPS). Если служба остановлена или отключена, сценарии диагностики не выполняются.  Службу DPS можно настроить с помощью оснастки "Службы" консоли управления (MMC).</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
