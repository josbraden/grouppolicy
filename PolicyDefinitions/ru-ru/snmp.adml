<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>введите выводимое имя</displayName>
  <description>введите описание</description>
  <resources>
    <stringTable>
      <string id="SNMP_Communities">Сообщества</string>
      <string id="SNMP_PermittedManagers">Разрешенные диспетчеры</string>
      <string id="SNMP_PermittedManagers_Help">Этот параметр определяет список разрешенных сайтов, которые могут отправлять запросы агенту SNMP, выполняющемуся на клиентском компьютере.

Протокол SNMP - это протокол, позволяющий пользователю удаленно управлять компьютерной сетью путем опроса и установки параметров терминалов и наблюдения за сетевыми событиями.

Диспетчер находится на главном компьютере сети. Роль диспетчера - выполнять опрос агентов, собирая нужные сведения.

Если этот параметр включен, то агент SNMP принимает запросы только из списка разрешенных диспетчеров, который можно задать с помощью этого параметра.

Если этот параметр отключен или не задан, то служба SNMP использует список разрешенных диспетчеров, заданный на локальном компьютере.

Советы и рекомендации. В целях обеспечения безопасности рекомендуется задать соответствующее значение в разделе реестра HKLM\SOFTWARE\Policies\SNMP\Parameters\PermittedManagers, чтобы разрешить полный доступ только локальной группе администраторов.

Примечание. Этот параметр не оказывает влияния, если агент SNMP не установлен на клиентском компьютере.

См. также описание двух других параметров SNMP: «Конфигурация ловушки» и «Сетевое имя».</string>
      <string id="SNMP_SNMP">SNMP</string>
      <string id="SNMP_TrapDestinations_Help">Этот параметр разрешает конфигурацию ловушек для агента SNMP.

Протокол SNMP - это протокол, позволяющий пользователю удаленно управлять компьютерной сетью путем опроса и установки параметров терминалов и наблюдения за сетевыми событиями.

Этот параметр позволяет настраивать имена сайтов, которые получают сообщения-ловушки для сообщества, посылаемые службой SNMP. Сообщение-ловушка - это сигнал или значимое событие, которое позволяет агенту SNMP уведомлять системы управления в асинхронном режиме.

Если этот параметр включен, то служба SNMP посылает сообщения-ловушки сайтам в публичном сообществе.

Если этот параметр отключен или не задан, то служба SNMP использует конфигурацию ловушек, заданную на локальном компьютере.

Примечание. Этот параметр не оказывает влияния, если агент SNMP не установлен на клиентском компьютере.

См. также описание двух других параметров SNMP: «Разрешенные диспетчеры» и «Сетевое имя».</string>
      <string id="SNMP_Traps_Public">Ловушки для публичного сообщества</string>
      <string id="SNMP_ValidCommunities_Help">Задает список сообществ, определенных для службы SNMP.

Протокол SNMP - это протокол, позволяющий пользователю удаленно управлять компьютерной сетью путем опроса и установки параметров терминалов и наблюдения за сетевыми событиями.

Допустимое сообщество - это сообщество, распознаваемое службой SNMP, являющееся группой сайтов (серверов, рабочих станций, концентраторов и маршрутизаторов), которые управляются с помощью SNMP. Служба SNMP является управляемым сетевым узлом, который получает пакеты SNMP из сети.

Если этот параметр включен, то агент SNMP принимает запросы только от систем управления внутри распознаваемых сообществ, и для сообщества разрешена только операция чтения SNMP.

Если этот параметр отключен или не задан, то служба SNMP использует список допустимых сообществ, заданный на локальном компьютере.

Советы и рекомендации. В целях обеспечения безопасности рекомендуется задать соответствующее значение в разделе реестра HKLM\SOFTWARE\Policies\SNMP\Parameters\ValidCommunities, чтобы разрешить полный доступ только локальной группе администраторов.

Примечание. Рекомендуется использовать зашифрованное сетевое имя.

Примечание. Этот параметр не оказывает влияния, если агент SNMP не установлен на клиентском компьютере.

См. также описание двух других параметров SNMP: «Разрешенные диспетчеры» и «Конфигурация ловушки».</string>
    </stringTable>
    <presentationTable>
      <presentation id="SNMP_Communities">
        <listBox refId="SNMP_CommunitiesListbox">Сообщества</listBox>
      </presentation>
      <presentation id="SNMP_PermittedManagers">
        <listBox refId="SNMP_PermittedManagersListbox">Разрешенные диспетчеры</listBox>
      </presentation>
      <presentation id="SNMP_Traps_Public">
        <listBox refId="SNMP_Traps_PublicListbox">Конфигурация ловушки</listBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
