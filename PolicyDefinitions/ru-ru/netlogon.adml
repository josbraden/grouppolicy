<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>введите выводимое имя</displayName>
  <description>введите описание</description>
  <resources>
    <stringTable>
      <string id="Netlogon">Сетевой вход в систему</string>
      <string id="Netlogon_AllowSingleLabelDnsDomain">Обнаружение контроллеров домена, несущих домен с однокомпонентным DNS-именем</string>
      <string id="Netlogon_AllowSingleLabelDnsDomain_Help">Указывает, будут ли компьютеры, к которым применяется этот параметр, пытаться выполнять разрешение DNS-имен для однокомпонентных доменных имен.

По умолчанию, когда компьютер (точнее говоря, локатор контроллеров домена, выполняющийся на этом компьютере) пытается найти контроллер домена, на котором располагается служба каталогов Active Directory для домена, заданного однокомпонентным именем, этот компьютер использует только разрешение имен NetBIOS, а не разрешение имен DNS, если только этот компьютер не присоединен к лесу Active Directory, в котором по крайней мере один домен имеет однокомпонентное DNS-имя. 

Если этот параметр включен, компьютеры, к которым применяется эта политика, будут пытаться найти контроллер домена, на котором располагается служба каталогов Active Directory домена, задаваемого однокомпонентным именем, используя разрешение имен DNS.

Если этот параметр отключен, компьютеры, к которым он применяется, будут пытаться найти контроллер домена, на котором располагается служба каталогов Active Directory для домена, заданного однокомпонентным именем, используя только разрешение имен NetBIOS, а не разрешение имен DNS, при условии, что компьютер не ищет домен с однокомпонентным DNS-именем, существующим в лесу Active Directory, к которому присоединен этот компьютер.

Если параметр не задан, он не применяется ни к каким компьютерам, и компьютеры используют свои локальные настройки.</string>
      <string id="Netlogon_AutoSiteCoverage">Автоматическое обслуживание сайта DNS SRV-записями локатора контроллеров домена</string>
      <string id="Netlogon_AutoSiteCoverage_Help">Определяет, будут ли контроллеры домена (DC) динамически регистрировать специфические для сайтов SRV-записи ресурсов локатора DC для ближайших сайтов, не имеющих DC для того же домена (или не имеющих глобального каталога для того же леса). Эти DNS-записи динамически регистрируются службой NetLogon и используются для поиска контроллеров домена.

Если этот параметр включен, контроллеры домена, к которым он применяется, динамически регистрируют специфические для сайтов DNS SRV-записи ресурсов локатора контроллеров домена для ближайших сайтов, не имеющих контроллеров домена для того же домена (или не имеющих глобального каталога для того же леса).

Если параметр отключен, контроллеры домена не будут регистрировать специфические для сайтов DNS SRV-записи ресурсов локатора контроллеров домена ни для каких других сайтов, кроме собственного.

Если параметр не задан, он не применяется ни к каким контроллерам домена, и контроллеры домена используют свои локальные настройки.</string>
      <string id="Netlogon_AvoidPdcOnWan">Обращение к PDC при сбое входа в систему</string>
      <string id="Netlogon_AvoidPdcOnWan_Help">Определяет, должен ли контроллер домена проверять на основном контроллере домена пароль, введенный клиентом, если самому контроллеру домена не удалось проверить этот пароль.

Обращение к PDC полезно, если пароль пользователя был недавно изменен и изменение еще не распространено на этот DC. Пользователи могут захотеть отключить эту возможность, если основной контроллер домена PDC расположен на медленном WAN-подключении.

Чтобы включить эту возможность, выберите «Включить».

Чтобы отключить эту возможность, выберите «Отключить».</string>
      <string id="Netlogon_BackgroundRetryInitialPeriod">Начальный интервал попыток обнаружения контроллеров домена для фоновых клиентов</string>
      <string id="Netlogon_BackgroundRetryInitialPeriod_Help">Если приложения, периодически ведущие поиск контроллера домена, не могут его найти, значение, заданное этим параметром, определяет интервал времени (в секундах) до выполнения первой повторной попытки.

По умолчанию значение параметра — 10 минут (10*60). Максимальное значение — 49 дней (0x49*24*60*60=4233600). Минимальное значение — 0.

Данный параметр влияет только на вызывающих DsGetDcName, установивших флаг DS_BACKGROUND_ONLY.

Если значение этого параметра меньше значения, указанного в разделе реестра NegativeCachePeriod, используется последнее.

Внимание! Если значение этого параметра слишком большое, клиент не будет вести поиск первоначально не доступных контроллеров домена. Если значение слишком мало, периодические попытки поиска отсутствующих контроллеров домена могут вызвать лишний сетевой трафик.</string>
      <string id="Netlogon_BackgroundRetryMaximumPeriod">Максимальный интервал повторных попыток обнаружения контроллера домена для фоновых клиентов</string>
      <string id="Netlogon_BackgroundRetryMaximumPeriod_Help">Если приложения, ведущие периодический поиск контроллера домена, не могут его найти, значение, заданное этим параметром, определяет максимальный интервал, допустимый между повторными попытками.

Например, интервалы повторения могут быть установлены в 10, 20 и 40 минут, но достигнув значения, установленного в данном параметре, интервал не будет изменяться для всех последующих попыток, пока не будет достигнуто значение, установленное в параметре «Окончательный интервал попыток обнаружения контроллера домена».

По умолчанию значение параметра — 60 минут (60*60). Максимальное значение — 49 дней (0x49*24*60*60=4233600). Минимальное значение — 0.

Если для данного параметра указано значение, меньшее значения параметра «Начальный интервал попыток обнаружения контроллеров домена», используется последнее.

Внимание! Если значение для этого параметра очень велико, клиент может предпринимать попытки обнаружения контроллера домена с очень большим интервалом.

Если значение очень мало и контроллер домена недоступен, частые попытки могут вызвать лишний сетевой трафик.</string>
      <string id="Netlogon_BackgroundRetryQuitTime">Окончательный интервал попыток обнаружения контроллера домена для фоновых клиентов</string>
      <string id="Netlogon_BackgroundRetryQuitTime_Help">Если приложения, ведущие периодический поиск контроллера домена, не могут его найти, значение, заданное этим параметром, определяет момент запрета дальнейших попыток. Например, интервалы повторения могут быть установлены в соответствии с максимальным интервалом попыток обнаружения, но по достижении значения, указанного в данном параметре, дальнейшие попытки выполняться не будут. Если значение этого параметра меньше значения максимального интервала попыток обнаружения, используется последнее.

По умолчанию значение параметра — не прекращать попытки обнаружения (0). Максимальное значение — 49 дней (0x49*24*60*60=4233600). Минимальное значение — 0.

Внимание! Если значение данного параметра очень мало, клиент может слишком быстро прекратить попытки обнаружения контроллера домена.</string>
      <string id="Netlogon_BackgroundSuccessfulRefreshPeriod">Периодическое обновление кэша положительных ответов DC для фонового вызова</string>
      <string id="Netlogon_BackgroundSuccessfulRefreshPeriod_Help">Определяет, как часто следует выполнять обновление записей кэша контроллеров домена. Этот параметр применяется к вызывающим программам, периодически пытающимся обнаружить контроллеры домена, перед возвращением им информации о контроллерах домена. По умолчанию значение параметра — бесконечность (4294967200). Максимальное значение данного параметра — (4294967200), однако максимальное число, не считающееся бесконечным, — 49 дней (49*24*60*60=4233600). Любое большее значение считается бесконечным. Минимальное значение параметра — обновлять всегда (0).</string>
      <string id="Netlogon_DC_Locator_DNS_Records">DNS-записи локатора контроллеров домена</string>
      <string id="Netlogon_DebugFlag">Уровень вывода данных отладки в файл журнала</string>
      <string id="Netlogon_DebugFlag_Help">Задает для службы сетевого входа в систему уровень вывода данных отладки.

Служба сетевого входа в систему выводит данные отладки в файл журнала netlogon.log в каталоге %windir%\debug. По умолчанию данные отладки в журнал не записываются.

Если включить этот параметр и указать ненулевое значение, данные отладки будут записываться в файл журнала.  Чем выше значение, тем более подробными будут записи в журнале; значение 536936447 обычно используется как оптимальное.

При нулевом значении данного параметра действует описанное выше поведение по умолчанию.

Если параметр отключен или не задан, действует описанное выше поведение по умолчанию.</string>
      <string id="Netlogon_DnsAvoidRegisterRecords">DNS-записи локатора контроллеров домена, не регистрируемые контроллерами доменов</string>
      <string id="Netlogon_DnsAvoidRegisterRecords_Help">Определяет, какие DNS-записи локатора контроллеров домена не регистрируются службой сетевого входа Netlogon.

Чтобы включить этот параметр, выберите «Включить» и укажите список разделенных пробелами мнемоник (инструкций) для DNS-записей локатора контроллеров домена, не регистрируемых контроллерами домена, к которым применяется параметр.

Мнемоники можно выбрать в следующем списке:

Мнемоника       Тип    DNS-запись

LdapIpAddress    A     &lt;DnsDomainName&gt; Ldap             SRV   _ldap._tcp.&lt;DnsDomainName&gt; LdapAtSite       SRV   _ldap._tcp.&lt;SiteName&gt;._sites.&lt;DnsDomainName&gt; Pdc              SRV   _ldap._tcp.pdc._msdcs.&lt;DnsDomainName&gt; Gc               SRV   _ldap._tcp.gc._msdcs.&lt;DnsForestName&gt; GcAtSite         SRV   _ldap._tcp.&lt;SiteName&gt;._sites.gc._msdcs.&lt;DnsForestName&gt; DcByGuid         SRV   _ldap._tcp.&lt;DomainGuid&gt;.domains._msdcs.&lt;DnsForestName&gt; GcIpAddress      A     _gc._msdcs.&lt;DnsForestName&gt; DsaCname         CNAME &lt;DsaGuid&gt;._msdcs.&lt;DnsForestName&gt; Kdc              SRV   _kerberos._tcp.dc._msdcs.&lt;DnsDomainName&gt; KdcAtSite        SRV    _kerberos._tcp.dc._msdcs.&lt;SiteName&gt;._sites.&lt;DnsDomainName&gt; Dc               SRV   _ldap._tcp.dc._msdcs.&lt;DnsDomainName&gt; DcAtSite         SRV   _ldap._tcp.&lt;SiteName&gt;._sites.dc._msdcs.&lt;DnsDomainName&gt; Rfc1510Kdc       SRV   _kerberos._tcp.&lt;DnsDomainName&gt; Rfc1510KdcAtSite SRV   _kerberos._tcp.&lt;SiteName&gt;._sites.&lt;DnsDomainName&gt; GenericGc        SRV   _gc._tcp.&lt;DnsForestName&gt; GenericGcAtSite  SRV   _gc._tcp.&lt;SiteName&gt;._sites.&lt;DnsForestName&gt; Rfc1510UdpKdc    SRV   _kerberos._udp.&lt;DnsDomainName&gt; Rfc1510Kpwd      SRV   _kpasswd._tcp.&lt;DnsDomainName&gt; Rfc1510UdpKpwd   SRV   _kpasswd._udp.&lt;DnsDomainName&gt;

Если данный параметр отключен, контроллеры домена, настроенные на динамическую регистрацию DNS-записей локатора контроллеров домена, регистрируют все DNS-записи локатора контроллеров домена.

Если параметр не применяется к контроллерам домена, контроллеры домена используют свою локальную конфигурацию.</string>
      <string id="Netlogon_DnsRefreshInterval">Интервал обновления DNS-записей локатора контроллеров домена </string>
      <string id="Netlogon_DnsRefreshInterval_Help">Задает интервал обновления DNS-записей локатора контроллеров домена, к которым применяется этот параметр. Эти DNS-записи регистрируются службой сетевого входа NetLogon динамически и используются алгоритмом локатора для обнаружения контроллеров домена. Параметр может применяться только к контроллерам домена, использующим динамическое обновление.

Контроллеры домена, настроенные на динамическую регистрацию DNS-записей локатора контроллеров домена, периодически перерегистрируют свои записи на DNS-серверах, даже если данные их записей не изменились. Если полномочные DNS-серверы настроены на выполнение автоматической очистки устаревших записей, перерегистрация нужна, чтобы указать таким серверам действующие записи, сохраняемые в базе данных.

Внимание! Если записи ресурсов DNS зарегистрированы в зонах, где включена сборка мусора, значение данной настройки никогда не должно превышать интервал обновления, заданный для этих зон. Значение интервала обновления DNS-записей локатора контроллеров домена, большее интервала обновления DNS-зон, может привести к нежелательному удалению DNS-записей.

Чтобы указать интервал обновления записей контроллера домена, выберите «Включить» и введите значение, большее 1800. Имейте в виду, что значение задает интервал обновления регистрации в секундах (например, 1800 секунд составляют 30 минут).

Если параметр не задан, он не применяется ни к каким контроллерам домена, и контроллеры домена используют свои локальные настройки.</string>
      <string id="Netlogon_DnsTtl">Набор TTL DNS-записей локатора контроллеров домена</string>
      <string id="Netlogon_DnsTtl_Help">Задает значение поля «срок жизни» (TTL, Time-To-Live) для регистрируемых NetLogon SRV-записей ресурсов. Эти DNS-записи динамически регистрируются службой NetLogon и используются для поиска контроллеров домена.

Чтобы задать TTL для DNS-записи локатора контроллеров домена, выберите «Включить» и введите значение в секундах (например, значение «900» соответствует 15 минутам).

Если параметр не задан, он не применяется ни к каким контроллерам домена, и контроллеры домена используют свои локальные настройки.</string>
      <string id="Netlogon_ExpectedDialupDelay">Ожидаемая задержка при удаленном входе</string>
      <string id="Netlogon_ExpectedDialupDelay_Help">Задает дополнительное время ожидания ответа контроллера домена при удаленном входе в сеть.

Чтобы задать ожидаемую задержку при удаленном входе, выберите «Включить» и введите нужное значение в секундах (например, значение «60» — это 1 минута).

Если параметр не задан, он не применяется ни к каким компьютерам, и компьютеры используют свои локальные настройки.</string>
      <string id="Netlogon_GcSiteCoverage">Сайты, обслуживаемые DNS SRV-записями локатора глобального каталога</string>
      <string id="Netlogon_GcSiteCoverage_Help">Задает сайты, для которых глобальные каталоги (GC, global catalogs) должны регистрировать специфические для сайтов DNS SRV-записи ресурсов локатора глобального каталога. Эти записи регистрируются в дополнение к специфическим для сайтов SRV-записям, зарегистрированным на сайте, где располагается глобальный каталог, и записям, регистрируемым глобальным каталогом, настроенным на регистрацию DNS SRV-записей локатора глобального каталога для ближайших к нему сайтов без глобального каталога. 

DNS-записи локатора GC и специфические для сайтов SRV-записи динамически регистрируются службой NetLogon и используются для поиска глобального каталога. Сайт Active Directory — это одна или несколько соединенных быстрой связью подсетей TCP/IP, позволяющих администраторам настраивать доступ и репликацию Active Directory. Глобальный каталог — это контроллер домена, который содержит частичную реплику всех доменов Active Directory.

Чтобы задать сайты, обслуживаемые DNS SRV-записями локатора GC, выберите «Включить» и введите имена сайтов, разделенные пробелами.

Если параметр не задан, он не применяется ни к каким глобальным каталогам, и глобальные каталоги используют свои локальные настройки.</string>
      <string id="Netlogon_LdapSrvPriority">Установка приоритета в DNS SRV-записях локатора контроллеров домена</string>
      <string id="Netlogon_LdapSrvPriority_Help">Задает поле приоритета в SRV-записях ресурсов, регистрируемых контроллерами домена, к которым применяется данный параметр. Эти DNS-записи динамически регистрируются службой NetLogon и используются для поиска контроллеров домена.

Поле приоритета SRV-записи задает предпочтение для целевых узлов (указанных в поле цели SRV-записей). DNS-клиенты, запрашивающие SRV-записи ресурсов, пытаются обратиться к первому достижимому узлу с наименьшим номером в поле приоритета.

Чтобы задать приоритет в DNS SRV-записи ресурсов локатора контроллеров домена, выберите «Включить» и введите значение. Диапазон допустимых значений — от 0 до 65535.

Если параметр не задан, он не применяется ни к каким контроллерам домена, и контроллеры домена используют свои локальные настройки.</string>
      <string id="Netlogon_LdapSrvWeight">Установка веса в DNS SRV-записях локатора контроллеров домена</string>
      <string id="Netlogon_LdapSrvWeight_Help">Задает поле веса в SRV-записях ресурсов, регистрируемых контроллерами домена, к которым применяется данный параметр. Эти DNS-записи динамически регистрируются службой NetLogon и используются для поиска контроллеров домена.

Поле веса в SRV-записи может использоваться в дополнение к значению приоритета для обеспечения механизма балансировки нагрузки, если в поле цели в SRV-записях указано несколько серверов одинакового приоритета. Вероятность, с которой DNS-клиент случайным образом выбирает целевой узел для обращения, пропорциональна значению в поле веса SRV-записи.

Чтобы задать вес DNS SRV-записи локатора контроллеров домена, выберите «Включить» и введите значение. Диапазон допустимых значений — от 0 до 65535.

Если параметр не задан, он не применяется ни к каким контроллерам домена, и контроллеры домена используют свои локальные настройки.</string>
      <string id="Netlogon_MaximumLogFileSize">Максимальный размер файла журнала</string>
      <string id="Netlogon_MaximumLogFileSize_Help">Задает в байтах максимальный размер файла журнала netlogon.log в каталоге %windir%\debug при включенном ведении журнала.

По умолчанию максимальный размер файла журнала 20 Мбайт. Если данный параметр включен, задается указанный максимальный размер файла журнала.  По достижении этого размера файл журнала сохраняется с именем netlogon.bak, а файл netlogon.log очищается. Необходимо указывать значение, соответствующее доступному свободному месту.

Если данная политика отключена или не задана, действует описанное выше поведение по умолчанию.</string>
      <string id="Netlogon_NdncSiteCoverage">Сайты, обслуживаемые DNS SRV-записями локатора раздела каталога приложений</string>
      <string id="Netlogon_NdncSiteCoverage_Help">Указывает сайты, для которых контроллеры домена с разделом каталога приложений должны регистрировать специфические для сайтов и для раздела каталога приложений DNS SRV-записи ресурсов локатора DC. Эти записи регистрируются в дополнение к специфическим для сайтов SRV-записям, зарегистрированным на сайте, где располагается DC, и записям, регистрируемым DC, настроенным на регистрацию DNS SRV-записей локатора DC для ближайших к нему сайтов без DC. 

DNS-записи локатора раздела каталога приложений и специфические для сайта SRV-записи динамически регистрируются службой NetLogon и используются для поиска специфического для раздела каталога приложений DC. Сайт Active Directory — это одна или несколько соединенных быстрой связью подсетей TCP/IP, позволяющих администраторам настраивать доступ и репликацию Active Directory.

Чтобы задать сайты, обслуживаемые DNS SRV-записями локатора DC раздела каталога приложений, выберите «Включить» и введите имена сайтов, разделенные пробелами.

Если параметр не задан, он не применяется ни к каким контроллерам домена, и контроллеры домена используют свои локальные настройки.</string>
      <string id="Netlogon_NegativeCachePeriod">Настройка кэша отрицательных ответов обнаружения контроллеров домена</string>
      <string id="Netlogon_NegativeCachePeriod_Help">Задает интервал времени (в секундах), в течение которого локатор контроллеров домена помнит, что контроллер домена не удалось найти в домене. Повторная попытка обнаружить контроллер домена в течение этого интервала немедленно завершается ошибкой без попытки поиска.

По умолчанию значение параметра 45 секунд. Максимальное значение параметра — 7 дней (7*24*60*60). Минимальное значение — 0.

Внимание! Если значение этого параметра слишком большое, клиент не будет вести поиск первоначально не доступных контроллеров домена. Если значение слишком мало, клиенты будут пытаться искать контроллеры домена, даже если их нет.</string>
      <string id="Netlogon_NetlogonShareCompatibilityMode">Совместимость общего сетевого доступа</string>
      <string id="Netlogon_NetlogonShareCompatibilityMode_Help">Этот параметр определяет, будет ли общий ресурс сетевого доступа, созданный службой сетевого входа в систему на контроллере домена, поддерживать семантику общего доступа к файлам со старыми приложениями.

Если этот параметр включен, общий ресурс сетевого доступа будет поддерживать семантику общего доступа к файлам, удовлетворяющую запросы монопольного доступа для чтения файлов на общем ресурсе, даже если у обращающегося есть только права чтения.

Если этот параметр отключен или не задан, общий ресурс сетевого доступа предоставляет совместный доступ на чтение файлов общего ресурса при запросе монопольного доступа с правами обращающегося только на чтение.

По умолчанию общий ресурс сетевого доступа предоставляет совместный доступ на чтение файлов общего ресурса, если запрашивается монопольный доступ.

Примечание. Общий ресурс сетевого доступа создается службой сетевого входа в систему для использования клиентскими компьютерами домена. По умолчанию поведение общего ресурса сетевого доступа не позволит приложению с разрешением только чтения файлов блокировать файлы, запрашивая монопольный доступ на чтение, что может помешать обновлению параметров групповой политики клиентов домена.  Если этот параметр включен, приложение, блокирующее файлы общего ресурса сетевого доступа с разрешениями только чтения, сможет запрещать клиентам групповой политики чтение файлов; в общем случае это снижает доступность общего ресурса сетевого доступа в домене.

Если параметр включен, администратор домена должен обеспечить, чтобы утвержденные администратором приложения были единственными, использующими возможность монопольного чтения в домене.</string>
      <string id="Netlogon_NonBackgroundSuccessfulRefreshPeriod">Периодическое обновление кэша положительных ответов DC для нефонового вызова</string>
      <string id="Netlogon_NonBackgroundSuccessfulRefreshPeriod_Help">Определяет частоту обновления записей кэша контроллеров домена. Этот параметр применяется к вызывающим программам, периодически пытающимся обнаружить контроллеры домена, перед возвращением им информации о контроллерах домена. Данный параметр влияет только на вызывающих DsGetDcName, не установивших флаг DS_BACKGROUND_ONLY.

По умолчанию значение параметра — 30 минут (1800*60). Максимальное значение данного параметра — (4294967200), однако максимальное число, не считающееся бесконечным, — 49 дней (49*24*60*60=4233600). Любое большее значение считается бесконечным. Минимальное значение параметра — обновлять всегда (0).</string>
      <string id="Netlogon_ScavengeInterval">Интервал очистки</string>
      <string id="Netlogon_ScavengeInterval_Help">Определяет интервал выполнения службой сетевого входа Netlogon следующих операций очистки:

- Проверяет, не требуется ли обновление пароля на безопасном канале, и при необходимости обновляет его.

- На контроллерах домена (DC) обнаруживает ранее не обнаруженные DC.

- На основных контроллерах домена (PDC) выполняет попытку добавления NetBIOS-имени &lt;DomainName&gt;[1B], если оно еще не было добавлено.

Ни одна из этих операций не является критической. В большинстве случаев оптимальное значение - 15 минут. Например, если DC отделен от доверенного домена дорогостоящей линией связи (такой как ISDN), этот параметр можно увеличить во избежание частых автоматически выполняемых операций обнаружения DC в доверенном домене.

Для включения этого параметра выберите «Включить» и укажите интервал времени в секундах.</string>
      <string id="Netlogon_SiteCoverage">Сайты, обслуживаемые DNS SRV-записями локатора контроллеров домена</string>
      <string id="Netlogon_SiteCoverage_Help">Задает сайты, для которых контроллеры домена должны регистрировать специфические для сайтов DNS SRV-записи ресурсов локатора контроллеров домена. Эти записи регистрируются в дополнение к специфическим для сайтов SRV-записям, зарегистрированным на сайте, где располагается DC, и записям, регистрируемым DC, настроенным на регистрацию DNS SRV-записей локатора DC для ближайших к нему сайтов без DC. 

DNS-записи локатора DC динамически регистрируются службой NetLogon и используются для поиска контроллеров домена. Сайт Active Directory — это одна или несколько соединенных быстрой связью подсетей TCP/IP, позволяющих администраторам настраивать доступ и репликацию Active Directory.

Чтобы задать сайты, обслуживаемые DNS SRV-записями локатора GC, выберите «Включить» и введите имена сайтов, разделенные пробелами.

Если параметр не задан, он не применяется ни к каким контроллерам домена, и контроллеры домена используют свою локальные настройки.</string>
      <string id="Netlogon_SiteName">Имя сайта</string>
      <string id="Netlogon_SiteName_Help">Задает сайт Active Directory, к которому принадлежат компьютеры.

Сайт Active Directory — это одна или несколько соединенных быстрой связью подсетей TCP/IP, позволяющих администраторам настраивать доступ и репликацию Active Directory.

Чтобы задать имя сайта для этого параметра, выберите «Включить» и введите имя сайта. Если сайт, к которому принадлежит компьютер, не задан, компьютер автоматически ищет свой сайт в Active Directory.

Если параметр не задан, он не применяется ни к каким компьютерам, и компьютеры используют свои локальные настройки.</string>
      <string id="Netlogon_SysvolShareCompatibilityMode">Совместимость общего ресурса Sysvol</string>
      <string id="Netlogon_SysvolShareCompatibilityMode_Help">Этот параметр определяет, будет ли общий ресурс Sysvol, созданный службой сетевого входа в систему на контроллере домена, поддерживать семантику общего доступа к файлам со старыми приложениями.

Если этот параметр включен, общий ресурс Sysvol будет поддерживать семантику общего доступа к файлам, удовлетворяющую запросы монопольного доступа для чтения файлов на общем ресурсе, даже если у обращающегося есть только права чтения.

Если этот параметр отключен или не задан, общий ресурс Sysvol предоставляет совместный доступ на чтение к файлам общего ресурса при запросе монопольного доступа с правами обращающегося только на чтение.

По умолчанию общий ресурс Sysvol предоставляет совместный доступ на чтение файлов общего ресурса при запросе монопольного доступа.

Примечание. Общий ресурс Sysvol создается службой сетевого входа в систему для использования клиентами групповой политики домена. По умолчанию поведение общего ресурса Sysvol не позволит приложению с разрешением только чтения файлов блокировать файлы, запрашивая монопольный доступ на чтение, что может помешать обновлению параметров групповой политики клиентов домена. Если этот параметр включен, приложение, блокирующее файлы общего ресурса Sysvol с разрешениями только чтения, сможет запрещать клиентам групповой политики чтение файлов; в общем случае это снижает доступность общего ресурса Sysvol в домене.

Если параметр включен, администратор домена должен обеспечить, чтобы утвержденные администратором приложения были единственными, использующими возможность монопольного чтения в домене.</string>
      <string id="Netlogon_UseDynamicDns">Динамическая регистрация DNS-записей локатора контроллеров домена</string>
      <string id="Netlogon_UseDynamicDns_Help">Определяет, доступна ли динамическая регистрация DNS-записей ресурсов локатора контроллера домена (DC). Эти DNS-записи регистрируются службой сетевого входа NetLogon динамически и используются алгоритмом локатора для обнаружения контроллеров домена.

Если этот параметр включен, контроллеры домена, к которым он применяется, динамически регистрируют DNS-записи ресурсов локатора контроллера домена с помощью динамических сетевых подключений, на которых разрешено обновление DNS.

Если параметр отключен, контроллеры домена не будут регистрировать DNS-записи ресурсов локатора контроллера домена.

Если параметр не задан, он не применяется ни к каким контроллерам домена, и контроллеры домена используют свою локальные настройки.</string>
      <string id="Netlogon_TryNextClosestSite">Попробовать следующий ближайший сайт.</string>
      <string id="Netlogon_TryNextClosestSite_Help">Cлужба локатора контроллеров домена (служба локатора DC) используется клиентами для поиска контроллеров домена для их домена Active Directory. По умолчанию локатор контроллеров домена настроен на поиск контроллера домена в том же сайте.  Если ничего не будет найдено, локатором контроллеров домена может быть возвращен контроллер домена в другом сайте, который может находиться в нескольких переходах от текущего. Расстояние между двумя сайтами определяется ценой связи между ними. Сайт находится ближе, если стоимость связи с ним ниже, чем стоимость связи с другим. Функция «Попробовать следующий ближайший сайт» разрешает локатору контроллеров домена попытаться обнаружить контроллер домена в ближайшем (исходя из стоимости связи) сайте, если в текущем сайте контроллер домена не найден. В ситуациях с несколькими сайтами переход к данной функции во время обнаружения контроллера домена делает движение трафика в сети более эффективным.

При включении этого параметра политики функция «Попробовать следующий ближайший сайт» будет включена для машины через все доступные, но ненастроенные сетевые адаптеры.

При отключении этого параметра политики функция «Попробовать следующий ближайший сайт» не будет использоваться по умолчанию для машины через все доступные, но ненастроенные сетевые адаптеры. Однако если запрос локатора контроллеров доменов сделан с явным использованием флага DS_TRY_NEXTCLOSEST_SITE, то функция «Попробовать следующий ближайший сайт» будет поддерживаться.

Если этот параметр политики не задан, функция «Попробовать следующий ближайший сайт» не будет использоваться по умолчанию для машины через все доступные, но ненастроенные сетевые адаптеры. Если флаг DS_TRY_NEXTCLOSEST_SITE используется явно, функция «Попробовать следующий ближайший сайт» также используется.</string>
      <string id="Netlogon_ForceRediscoveryInterval">Установить интервал повторного поиска контроллера домена</string>
      <string id="Netlogon_ForceRediscoveryInterval_Help">Cлужба локатора контроллеров домена (служба локатора DC) используется клиентами для поиска контроллеров домена для их домена Active Directory. При обнаружении локатором контроллеров домена, они записываются в кэш для повышения эффективности алгоритма обнаружения. Пока кэшированный контроллер домена удовлетворяет требованиям и функционирует, локатор контроллеров домена будет его использовать. При появлении нового контроллера он станет доступным клиентам только после повторного обнаружения контроллеров локатором контроллеров домена. Чтобы реагировать на изменения состояния сети, локатор контроллеров домена будет по умолчанию выполнять повторное обнаружение через определенные промежутки времени и поддерживать сбалансированное распределение клиентов среди всех доступных контроллеров во всех доменах или лесах. По умолчанию интервал между повторными проверками локатором контроллеров домена составляет 12 часов. Повторный поиск контроллеров домена можно запустить, если в запросе локатору контроллеров домена используется флаг DS_FORCE_REDISCOVERY.  При повторном поиске счетчик времени в записях о контроллерах доменов в кэше обнуляется.

Если этот параметр включен, локатор контроллеров домена на компьютере будет проводить повторный поиск через определенные интервалы времени. Во избежание повышенной загруженности сети минимальный интервал составляет 3600 секунд (1 час). Максимально возможный интервал составляет 4294967200 секунд, а любое значение, превышающее 4294967 секунд (~49 дней), будет рассматриваться как бесконечность.

Если этот параметр политики отключен, повторный поиск контроллеров домена будет проводиться по умолчанию через каждые 12 часов.

Если этот параметр политики не задан, повторный поиск контроллеров домена будет проводиться по умолчанию через каждые 12 часов до тех пор, пока не поменяется значение параметра локального компьютера в системном реестре.</string>
      <string id="Netlogon_AddressTypeReturned">Тип возвращаемого адреса контроллера домена</string>
      <string id="Netlogon_AddressTypeReturned_Help">API-интерфейсы локатора контроллера домена (DC) возвращают вместе с другими данными и IP-адрес контроллера домена. До появления поддержки IPv6 типом возвращаемого IP-адреса контроллера домена был IPv4. Но с появлением поддержки IPv6 API-интерфейсы получили возможность возвращать IPv6-адреса контроллеров домена. Возвращаемый IPv6-адрес контроллера домена может не поддерживаться должным образом некоторыми приложениями. Во избежание неполадок в этих случаях и применяется данная политика.

По умолчанию API-интерфейсы могут возвращать IPv4 или IPv6-адреса контроллеров домена. Но если в некоторых приложениях возникают неполадки из-за IPv6-адресов, данный параметр может быть использован для отключения поведения по умолчанию и принудительного возврата только IPv4-адресов контроллеров домена. После решения проблемы параметр можно использовать для возврата к поведению по умолчанию.

При включенном параметре политики API-интерфейсы локатора контроллеров домена могут возвращать IPv4 или IPv6-адреса контроллеров домена. Это поведение по умолчанию локатора контроллеров доменов.

При отключенном параметре политики API-интерфейсы локатора контроллеров домена будут возвращать только IPv4-адреса контроллеров домена (если такие есть). Если контроллер домена поддерживает и Pv4, и IPv6-адреса, API-интерфейсы локатора контроллеров домена будут возвращать IPv4-адреса. Но если контроллер домена поддерживает только IPv6-адреса, произойдет сбой работы API-интерфейсов локатора контроллеров домена.

Если этот параметр не настроен, API-интерфейсы локатора контроллеров домена могут возвращать IPv4 или IPv6-адреса. Это поведение по умолчанию локатора контроллеров доменов.</string>
      <string id="Netlogon_AllowNT4Crypto">Разрешать алгоритмы шифрования, совместимые с Windows NT 4.0</string>
      <string id="Netlogon_AllowNT4Crypto_Help">Этот параметр определяет, разрешает ли служба сетевого входа в систему применять более старые криптографические алгоритмы, чем используемые в Windows NT 4.0. Криптографические алгоритмы, используемые в Windows NT 4.0 и более ранних версиях ОС, не столь надежны, как новые алгоритмы, применяемые в Windows 2000, Windows XP, Windows Server 2003, Windows Vista и в данной версии Windows.

По умолчанию служба сетевого входа в систему не разрешает применять устаревшие криптографические алгоритмы и не включает их в согласование. Поэтому компьютеры под управлением ОС Windows NT 4.0 не смогут подключаться к этому контроллеру домена.
 
Если этот параметр включен, служба сетевого входа в систему разрешает согласование и использование устаревших криптографических алгоритмов, совместимых с Windows NT 4.0. Однако применение устаревших алгоритмов создает потенциальную угрозу безопасности.</string>
      <string id="Netlogon_IgnoreIncomingMailslotMessages">Не обрабатывать в слотах входящие сообщения, которые используются для поиска контроллеров доменов по NetBIOS-именам доменов</string>
      <string id="Netlogon_IgnoreIncomingMailslotMessages_Help">Этот параметр политики позволяет управлять обработкой входящих сообщений в слотах сообщений с помощью локального контроллера домена.

Примечание. Чтобы найти удаленный контроллер домена по однокомпонентному NetBIOS-имени домена, локатор контроллеров доменов сначала получает список контроллеров с WINS-сервера, указанного в его параметрах локального клиента. Затем локатор передает сообщение из слота каждому удаленному контроллеру домена для получения дополнительных сведений. Местоположение контроллера домена будет определено, только если удаленный контроллер ответит на это сообщение.

Этот параметр рекомендуется использовать для уменьшения контактной зоны атак на контроллер домена в среде без WINS, в среде только с IPv6 и в других случаях, когда не требуется поиск контроллера по NetBIOS-имени домена. Этот параметр политики не влияет на поиск контроллеров домена по DNS-именам.

Если этот параметр включен, контроллер домена не выполняет обработку входящих сообщений в слотах сообщений, используемых для поиска контроллера домена по NetBIOS-имени.

Если отключить или не настроить этот параметр, контроллер домена обработает входящие сообщения в слотах сообщений. Это поведение по умолчанию локатора контроллеров доменов.</string>
    </stringTable>
    <presentationTable>
      <presentation id="Netlogon_BackgroundRetryInitialPeriod">
        <decimalTextBox refId="Netlogon_BackgroundRetryInitialPeriodLabel" defaultValue="600" spinStep="600">Секунды:</decimalTextBox>
      </presentation>
      <presentation id="Netlogon_BackgroundRetryMaximumPeriod">
        <decimalTextBox refId="Netlogon_BackgroundRetryMaximumPeriodLabel" defaultValue="3600" spinStep="3600">Секунды:</decimalTextBox>
      </presentation>
      <presentation id="Netlogon_BackgroundRetryQuitTime">
        <decimalTextBox refId="Netlogon_BackgroundRetryQuitTimeLabel" defaultValue="0" spinStep="100">Секунды:</decimalTextBox>
      </presentation>
      <presentation id="Netlogon_BackgroundSuccessfulRefreshPeriod">
        <decimalTextBox refId="Netlogon_BackgroundSuccessfulRefreshPeriodLabel" defaultValue="4294967200" spinStep="1000">Секунды:</decimalTextBox>
      </presentation>
      <presentation id="Netlogon_DebugFlag">
        <decimalTextBox refId="Netlogon_DebugFlagLabel" defaultValue="536936447">Уровень:</decimalTextBox>
      </presentation>
      <presentation id="Netlogon_DnsAvoidRegisterRecords">
        <textBox refId="Netlogon_DnsAvoidRegisterRecordsLabel">
          <label>Мнемоники (инструкции):</label>
        </textBox>
      </presentation>
      <presentation id="Netlogon_DnsRefreshInterval">
        <decimalTextBox refId="Netlogon_DnsRefreshIntervalLabel" defaultValue="1800" spinStep="100">Секунды:</decimalTextBox>
      </presentation>
      <presentation id="Netlogon_DnsTtl">
        <decimalTextBox refId="Netlogon_DnsTtlLabel" defaultValue="600" spinStep="100">Секунды:</decimalTextBox>
      </presentation>
      <presentation id="Netlogon_ExpectedDialupDelay">
        <decimalTextBox refId="Netlogon_ExpectedDialupDelayLabel" defaultValue="0" spinStep="100">Секунды:</decimalTextBox>
      </presentation>
      <presentation id="Netlogon_GcSiteCoverage">
        <textBox refId="Netlogon_GcSiteCoverageLabel">
          <label>Сайты:</label>
        </textBox>
      </presentation>
      <presentation id="Netlogon_LdapSrvPriority">
        <decimalTextBox refId="Netlogon_LdapSrvPriorityLabel" defaultValue="0" spinStep="100">Приоритет:</decimalTextBox>
      </presentation>
      <presentation id="Netlogon_LdapSrvWeight">
        <decimalTextBox refId="Netlogon_LdapSrvWeightLabel" defaultValue="100" spinStep="100">Вес:</decimalTextBox>
      </presentation>
      <presentation id="Netlogon_MaximumLogFileSize">
        <decimalTextBox refId="Netlogon_MaximumLogFileSizeLabel" defaultValue="536936447">Байты:</decimalTextBox>
      </presentation>
      <presentation id="Netlogon_NdncSiteCoverage">
        <textBox refId="Netlogon_NdncSiteCoverageLabel">
          <label>Сайты:</label>
        </textBox>
      </presentation>
      <presentation id="Netlogon_NegativeCachePeriod">
        <decimalTextBox refId="Netlogon_NegativeCachePeriodLabel" defaultValue="45" spinStep="60">Секунды:</decimalTextBox>
      </presentation>
      <presentation id="Netlogon_NonBackgroundSuccessfulRefreshPeriod">
        <decimalTextBox refId="Netlogon_NonBackgroundSuccessfulRefreshPeriodLabel" defaultValue="1800" spinStep="1000">Секунды:</decimalTextBox>
      </presentation>
      <presentation id="Netlogon_ScavengeInterval">
        <decimalTextBox refId="Netlogon_ScavengeIntervalLabel" defaultValue="900" spinStep="900">Секунды:</decimalTextBox>
      </presentation>
      <presentation id="Netlogon_SiteCoverage">
        <textBox refId="Netlogon_SiteCoverageLabel">
          <label>Сайты:</label>
        </textBox>
      </presentation>
      <presentation id="Netlogon_SiteName">
        <textBox refId="Netlogon_SiteNameLabel">
          <label>Сайт:</label>
        </textBox>
      </presentation>
      <presentation id="Netlogon_ForceRediscoveryInterval">
        <decimalTextBox refId="Netlogon_ForceRediscoveryIntervalLabel" defaultValue="43200" spinStep="100">Секунды:</decimalTextBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
