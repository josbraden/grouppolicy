<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>Файл шаблона групповой политики TCP/IP</displayName>
  <description>В этом ADMX-файле описывается шаблон политики для компонентов TCP/IP</description>
  <resources>
    <stringTable>
      <string id="TCPIP">Параметры TCP/IP</string>
      <string id="Ipv6Transition">Технологии туннелирования IPv6</string>

      <string id="ISATAP_State">Состояние ISATAP</string>
      <string id="ISATAP_Router_Name">Имя маршрутизатора ISATAP</string>
      <string id="6to4_State">Состояние 6to4</string>
      <string id="6to4_Router_Name">Имя ретранслятора 6to4</string>
      <string id="6to4_Router_Name_Resolution_Interval">Интервал разрешения имен ретранслятора 6to4</string>
      <string id="Teredo_State">Состояние Teredo</string>
      <string id="Teredo_Server_Name">Имя сервера Teredo</string>
      <string id="Teredo_Refresh_Rate">Частота обновления Teredo</string>
      <string id="Teredo_Client_Port">Порт клиента Teredo</string>
      <string id="Teredo_Default_Qualified">Квалификация Teredo по умолчанию</string>

      <string id="DefaultState">Состояние по умолчанию</string>
      <string id="EnabledState">Включенное состояние</string>
      <string id="DisabledState">Отключенное состояние</string>
      <string id="TeredoClient">Клиент</string>
      <string id="TeredoEnterpriseClient">Корпоративный клиент</string>
      <string id="IPHTTPS_ClientState">Состояние IP-HTTPS</string>
      <string id="IPHTTPS_ClientUrl">URL-адрес IP-HTTPS</string>

      <string id="ISATAP_State_Help">C помощью этого параметра политики можно настроить протокол автоматической внутрисайтовой адресации туннелей (ISATAP), который представляет собой технологию автоматического туннелирования "адрес-маршрутизатор", "сайт-сайт", "сайт-маршрутизатор" и "маршрутизатор-сайт", которая используется для обеспечения одноадресных подключений по протоколу IPv6 между сайтами IPv6 в интрасети IPv4.

Если этот параметр отключен или не настроен, используется параметр, установленный на локальном сайте.

Если этот параметр включен, можно настроить один из следующих параметров ISATAP:

Состояние политики по умолчанию - если имя маршрутизатора ISATAP успешно разрешено, на сайте для протокола ISATAP настраиваются локальный адрес канала и адрес для каждого префикса, полученного от маршрутизатора ISATAP посредством автоматической настройки адреса без отслеживания состояния. Если имя маршрутизатора ISATAP не удалось разрешить, на сайте недоступно подключение по протоколу ISATAP с использованием соответствующего IPv4-адреса.

Включенное состояние политики - если имя ISATAP успешно разрешено, на сайте для протокола ISATAP настраиваются локальный адрес канала и адрес для каждого  префикса, полученного от маршрутизатора ISATAP посредством автоматической настройки адреса без отслеживания состояния. Если имя ISATAP не удалось разрешить, на сайте настраивается локальный адрес канала для интерфейса ISATAP.

Отключенное состояние политики - на сайте отсутствуют интерфейсы ISATAP.

</string>

      <string id="ISATAP_Router_Name_Help">C помощью этого параметра политики можно указать имя маршрутизатора или IPv4-адрес маршрутизатора ISATAP.

Если этот параметр включен, можно указать имя маршрутизатора или IPv4-адрес маршрутизатора ISATAP. Если в текстовое поле введен IPv4-адрес маршрутизатора ISATAP, службы DNS не требуются.

Если этот параметр отключен или не настроен, используется параметр, установленный на локальном сайте.

</string>


      <string id="6to4_State_Help">С помощью этого параметра политики можно настроить интерфейс 6to4, который представляет собой технологию назначения адресов и автоматического туннелирования между маршрутизаторами, используемую для обеспечения одноадресных подключений по протоколу IPv6 между сайтами и сайтами IPv6 в глобальной сети IPv4. В интерфейсе 6to4 используется глобальный префикс адреса 2002:WWXX:YYZZ::/48, в котором буквы определяют шестнадцатеричное представление глобального IPv4-адреса (w.x.y.z), назначенного сайту.

Если этот параметр отключен или не настроен, используется параметр, установленный на локальном сайте.

Если этот параметр включен, можно настроить один из следующих параметров 6to4:

Состояние политики по умолчанию - интерфейс 6to4 включен в том случае, если сайт использует только подключения по локальному адресу канала IPv6 и IPv4-адрес. Если глобальные IPv6- и IPv4-адреса не назначены, интерфейс 6to4 на сайте не используется. Если не назначен глобальный IPv6-адрес, но назначен глобальный IPv4-адрес, на сайте используется интерфейс 6to4.

Включенное состояние политики - если назначен глобальный IPv4-адрес, на сайте используется интерфейс 6to4. Если глобальный IPv4-адрес не назначен, интерфейс 6to4 на сайте не используется.

Отключенное состояние политики - интерфейс 6to4 отключен, и подключения через него недоступны.

</string>

      <string id="6to4_Router_Name_Help">С помощью этого параметра политики можно настроить имя ретранслятора 6to4 для сайта 6to4. Ретранслятор 6to4 используется в качестве шлюза по умолчанию для передачи сетевого трафика IPv6 с сайта 6to4. Значение имени ретранслятора 6to4 не применяется при отсутствии подключения 6to4 на сайте.

Если этот параметр включен, можно указать имя ретранслятора для сайта 6to4.

Если этот параметр отключен или не настроен, используется параметр, установленный на локальном сайте, а имя ретранслятора для сайта 6to4 указать нельзя.

</string>

      <string id="6to4_Router_Name_Resolution_Interval_Help">С помощью этого параметра политики можно указать интервал, с которым осуществляется разрешение имен ретранслятора. Значение интервала разрешения имен ретранслятора 6to4 не применяется при отсутствии подключения 6to4 на сайте.

Если этот параметр включен, можно указать значение, определяющее частоту периодического разрешения имен ретранслятора.

Если этот параметр отключен или не настроен, используется параметр, установленный на локальном сайте.

</string>

      <string id="Teredo_State_Help">С помощью этого параметра политики можно настроить интерфейс Teredo, который представляет собой технологию назначения адресов и автоматического туннелирования, используемую для обеспечения одноадресных подключений по протоколу IPv6 в глобальной сети IPv4.

Если этот параметр отключен или не настроен, используются параметры, установленные на локальном сайте.

Если этот параметр включен, можно настроить один из следующих параметров Teredo:


По умолчанию - по умолчанию используется состояние "Клиент".

Отключено - интерфейсы Teredo на сайте не используются.

Клиент - интерфейс Teredo на сайте используется только в том случае, если сайт находится в сети, которая не содержит контроллер домена.

Корпоративный клиент - интерфейс Teredo всегда используется даже в том случае, если сайт находится в сети, которая содержит контроллер домена.

</string>

      <string id="Teredo_Server_Name_Help">С помощью этого параметра политики можно указать имя сервера Teredo. Это имя используется на клиентских компьютерах Teredo, на которых применен этот параметр политики.

Если этот параметр включен, можно указать имя сервера Teredo, которое применяется на клиентах Teredo.

Если этот параметр отключен или не настроен, для определения имени сервера Teredo используются локальные параметры, установленные на компьютере.

</string>

      <string id="Teredo_Refresh_Rate_Help">С помощью этого параметра политики можно настроить частоту обновления Teredo.

Примечание. Клиенты Teredo периодически (по умолчанию каждые 30 секунд) отправляют на сервер Teredo один пакет запроса маршрута. Сервер Teredo отправляет ответный пакет объявления маршрута (RA). На основании этих периодических пакетов обновляется сопоставление IP-адресов и портов UDP в таблице преобразования устройства преобразования сетевых адресов на клиенте Teredo.

Если этот параметр включен, можно указать частоту обновления.  Если выбран интервал обновления больше установленного для сопоставления портов в устройстве преобразования сетевых адресов на клиенте Teredo, возможно прекращение работы интерфейса Teredo или появление периодических сбоев подключения.

Если этот параметр отключен или не настроен, используется значение частоты обновления, установленное в локальных параметрах на компьютере. По умолчанию используется значение 30 секунд.

</string>

<string id="Teredo_Client_Port_Help">С помощью этого параметра политики можно выбрать порт UDP, который будет использоваться клиентом Teredo для отправки пакетов. Если установлено значение по умолчанию, равное 0, порт автоматически выбирается операционной системой (рекомендуется). Если выбран порт UDP, уже используемый системой, клиент Teredo не будет инициализирован.

Если этот параметр включен, можно настроить порт UDP для клиента Teredo.

Если этот параметр отключен или не настроен, используется параметр, установленный на локальном сайте.

</string>

<string id="Teredo_Default_Qualified_Help">С помощью этого параметра можно задать готовность интерфейса Teredo к связи (квалифицировать интерфейс). По умолчанию если интерфейс Teredo не используется, он переходит в неактивное состояние. В процессе квалификации интерфейс выводится из неактивного состояния.

Если этот параметр отключен или не настроен, используется параметр, установленный на локальном сайте.

Этот параметр содержит только одно состояние:

Включенное состояние политики - если квалификация по умолчанию включена, попытка квалификации интерфейса Teredo выполняется немедленно; в случае успеха интерфейс остается в квалифицированном состоянии.
</string>

<string id="IPHTTPS_ClientState_Help">С помощью этого параметра политики можно настроить технологию туннелирования IP-HTTPS, с помощью которой обеспечиваются IP-подключения к удаленным сетям на базе протокола HTTPS.

Если этот параметр отключен или не настроен, используются параметры, установленные на локальном сайте.

Если этот параметр включен, можно указать URL-адрес сервера IP-HTTPS. В этом случае можно настроить один из следующих параметров IP-HTTPS:

Состояние политики по умолчанию - интерфейс IP-HTTPS используется при недоступности других параметров подключения.

Включенное состояние политики - интерфейс IP-HTTPS используется всегда, даже при доступности других параметров подключения.

Отключенное состояние политики - на сайте отсутствуют интерфейсы IP-HTTPS.

</string>
 <string id="Parameters">Параметры</string>
 <string id="Windows_Scaling_Heuristics_State">Состояние масштабируемой эвристики Windows</string>
 <string id="Windows_Scaling_Heuristics_Help">Масштабируемая эвристика Windows - это алгоритм идентификации проблем подключения и пропускной способности, вызываемых многими брандмауэрами и другими промежуточными устройствами, неправильно интерпретирующими параметр масштабирования Windows.

Если этот параметр в gpedit определен как "не задан", будет действовать локальный параметр.

Этот параметр содержит два значения эвристики масштабирования Windows: "Включено" или "Отключено".

Состояние "Включено": эвристика масштабирования Windows будет включена, и система попытается определить проблемы подключения и пропускной способности, а затем принять соответствующие меры.

Состояние "Выключено": эвристика масштабирования Windows будет отключена, и система не будет пытаться определить проблемы подключения и пропускной способности, вызванные брандмауэрами или другими промежуточными устройствами.

 </string>

</stringTable>

<presentationTable>

      <presentation id="StateSelection">
        <dropdownList refId="StateSelect" noSort="true" defaultItem="0">Выберите из следующих состояний:</dropdownList>
      </presentation>

      <presentation id="RouterName">
        <textBox refId="RouterNameBox">
          <label>Введите имя маршрутизатора или ретранслятора:</label>
        </textBox>
      </presentation>

      <presentation id="RouterNameResolutionInterval">
        <text>Этот параметр позволяет настроить интервал разрешения имени ретранслятора 6to4</text>
        <decimalTextBox refId="RouterNameResolutionIntervalBox" defaultValue="1440">Мин.: </decimalTextBox>
      </presentation>

      <presentation id="TeredoServerName">
        <textBox refId="TeredoServerNameBox">
          <label>Введите имя сервера Teredo:</label>
        </textBox>
      </presentation>

      <presentation id="TeredoRefreshRate">
        <text>Этот параметр позволяет настроить частоту обновления клиента Teredo</text>
        <text>Диапазон значений: от 1 до 86 400 секунд (1 сутки).</text>
        <decimalTextBox refId="TeredoRefreshRateBox" defaultValue="30">Сек.: </decimalTextBox>
      </presentation>

      <presentation id="TeredoClientPort">
        <text>Этот параметр позволяет настроить порт клиента Teredo</text>
        <text>Диапазон от 0 до 65535. Значение по умолчанию (рекомендуемое) 0, что позволяет локальной системе выбрать порт.</text>
        <decimalTextBox refId="TeredoClientPortBox" defaultValue="0"> </decimalTextBox>
      </presentation>
      <presentation id="IPHTTPS_ClientState">
        <textBox refId="IPHTTPSClientUrlBox">
          <label>Введите URL-адрес IPHTTPS:</label>
        </textBox>
        <dropdownList refId="StateSelect" noSort="true" defaultItem="0">Выберите состояние интерфейса из следующих вариантов:</dropdownList>
      </presentation>
</presentationTable>

</resources>
</policyDefinitionResources>
