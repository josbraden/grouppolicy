<?xml version="1.0"?>
<policyDefinitionResources revision="1.0" schemaVersion="1.0">
	<displayName>Пересылка событий</displayName>

	<description>Определения политики для пересылки событий</description>

	<resources>

		<stringTable>

			<string id="EventForwarding">Пересылка событий</string>

                        <string id="ForwarderResourceUsage">ForwarderResourceUsage</string>
                        <string id="ForwarderResourceUsage_Help">Управляет использованием ресурсов сервера пересылки.   Каждый параметр применяется ко всем подпискам сервера пересылки.</string>
 
			<string id="SubscriptionManager">Настроить адрес сервера, интервал обновления и центр, выдающий сертификат для диспетчера подписки</string>

			<string id="Subscription_Help">Этот параметр политики позволяет настроить адрес сервера, интервал обновления и центр, выдающий сертификат для диспетчера подписки. Диспетчер подписки - это компьютер, на который пересылаются события.

Использование: Option=Value[,Option=Value]*
Параметры:
Сервер (обязательно) - адрес компьютера, на который необходимо пересылать события. Если сервер использует транспортный протокол (HTTP) и порт (5985), применяемые по умолчанию, введите Server=&lt;имя_сервера&gt;, где &lt;имя_сервера&gt; - полное доменное имя (FQDN). В противном случае, укажите полный URL-адрес. Например, при использовании протокола HTTPS - Server=https://&lt;имя_сервера&gt;:5986/wsman/SubscriptionManager/WEC
</string>

			<string id="SubscriptionManager_Listbox">SubscriptionManagers</string>
		</stringTable>

	
		<presentationTable>

			<presentation id="SubscriptionManager">
                                
				<listBox refId="SubscriptionManager_Listbox">SubscriptionManagers</listBox>

			</presentation>

                        <presentation id="ForwarderResourceUsage">

                                <decimalTextBox refId="MaxForwardingRate" spinStep="100">Максимальная скорость пересылки (событий/сек), разрешенная для сервера пересылки: </decimalTextBox>

                        </presentation>

		</presentationTable>

	</resources>

</policyDefinitionResources>
