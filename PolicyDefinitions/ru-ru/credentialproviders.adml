<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>введите выводимое имя</displayName>
  <description>введите описание</description>
  <resources>
    <stringTable>
      <string id="DefaultLogonDomain">Установка домена по умолчанию для входа</string>
      <string id="DefaultLogonDomain_Help">Данный параметр политики определяет используемый по умолчанию для входа домен, который может быть отличным от домена, к которому присоединен компьютер. Без этой политики, если пользователь не указал домен для входа, домен, к которому присоединен компьютер, будет считаться доменом по умолчанию. Например, если компьютер входит в домен Fabrikam, по умолчанию доменом для входа пользователей будет Fabrikam.

Если включить данный параметр политики, будет установлен определенный домен по умолчанию, и он может отличаться от домена, к которому присоединен компьютер.

Если параметр отключен или не задан, по умолчанию всегда будет использоваться домен, к которому присоединен компьютер.</string>
      <string id="ExcludedCredentialProviders">Исключить поставщиков учетных данных</string>
      <string id="ExcludedCredentialProviders_Help">Данный параметр позволяет администратору запретить использование указанных поставщиков учетных данных во время проверки подлинности.

Примечание. Поставщики учетных данных используются для обработки и проверки учетных данных пользователей в процессе входа или в случае, если требуется проверка подлинности. В Windows Vista существует два типа поставщиков учетных данных по умолчанию: пароль и смарт-карта. Администратор может установить дополнительных поставщиков учетных данных для поддержки различных способов проверки учетных данных (например, для поддержки биометрической проверки подлинности).

Если включить данную политику, администратор сможет указать CLSID поставщиков учетных данных, чтобы исключить их из списка установленных поставщиков учетных данных, доступных для проверки подлинности.

Если этот параметр отключен или не задан, все установленные поставщики учетных данных будут доступны для использования в целях проверки подлинности.</string>
      <string id="Logon">Вход</string>
    </stringTable>
    <presentationTable>
      <presentation id="DefaultLogonDomain">
        <textBox refId="DefaultLogonDomain_Message">
          <label>Домен для входа по умолчанию:</label>
        </textBox>
        <text>Введите имя домена</text>
      </presentation>
      <presentation id="ExcludedCredentialProviders">
        <textBox refId="ExcludedCredentialProviders_Message">
          <label>Исключить следующих поставщиков учетных данных:</label>
        </textBox>
        <text>Введите через запятую CLSID поставщиков учетных данных, которых не следует использовать в процессе проверки подлинности.

Например: {ba0dd1d5-9754-4ba3-973c-40dce7901283},{383f1aa4-65dd-45bc-9f5a-ddd2f222f07d}</text>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
