<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>escriba aquí el nombre</displayName>
  <description>escriba aquí la descripción</description>
  <resources>
    <stringTable>


      <!-- Overall category text -->
      <string id="PcaScenarioCategory">Diagnósticos de compatibilidad de aplicaciones</string>
      

      <!-- Generic WDI text -->
      <string id="WdiScenarioExecutionPolicyLevelResolution">Detección, solución de problemas y resolución</string>
      <string id="WdiScenarioExecutionPolicyLevelTsOnly">Solo detección y solución de problemas</string>


      <!-- Individual scenario text -->
      <string id="DetectInstallFailuresText">Detectar errores de instalación de aplicaciones</string>
      <string id="DetectUpdateFailuresText">Detectar aplicaciones que no pueden iniciar instaladores en UAC</string>
      <string id="DetectDeprecatedComponentFailuresText">Detectar errores de aplicaciones provocados por archivos DLL de Windows desusados</string>
      <string id="DetectDeprecatedCOMComponentFailuresText">Detectar errores de aplicación provocados por objetos COM desusados</string>
      <string id="DetectUndetectedInstallersText">Detectar instaladores de aplicaciones que necesitan ejecutarse como administrador</string>
      <string id="DetectBlockedDriversText">Notificar acerca de controladores bloqueados</string>
      <!-- End individual scenario text -->


      <!-- Begin install failure scenario description -->
      <string id="DetectInstallFailuresExplain">Esta configuración de directiva configura el Asistente para la compatibilidad de programas (PCA) para que diagnostique errores relativos a instalaciones de aplicaciones. 

Si habilita esta configuración de directiva, PCA queda configurado para detectar errores en la ejecución de instaladores de aplicaciones mediante heurística. Cuando se detecten posibles errores, PCA dará al usuario la opción de reiniciar el instalador con el modo de compatibilidad de Microsoft Windows XP.

Si deshabilita esta configuración de directiva, PCA no queda configurado para detectar errores en la ejecución de instaladores de programas.

Si no establece esta configuración de directiva, PCA habilitará este escenario de diagnóstico de manera predeterminada.

Nota: si deshabilita la configuración de directiva "Desactivar el Asistente para la compatibilidad de programas", esta configuración de directiva no tendrá efecto. El Servicio de directivas de diagnóstico (DPS) y el Servicio Asistente para la compatibilidad de programas deben estar ejecutándose para que PCA se ejecute. Estos servicios se pueden configurar mediante el complemento Servicios para Microsoft Management Console.</string>
      <!-- End install failure scenario description -->
      

      <!-- Begin update failure scenario description -->
      <string id="DetectUpdateFailuresExplain">Esta configuración de directiva configura el Asistente para la compatibilidad de programas (PCA) para que diagnostique errores relativos a los programas de Control de cuentas de usuario (UAC). 

Si habilita esta configuración de directiva, PCA detecta los programas que no pudieron iniciar procesos secundarios que constituyen instaladores (normalmente programas de actualización). Cuando se detecte este error, PCA aplicará el modo de compatibilidad ELEVATECREATEPROCESS, que permite al programa iniciar correctamente el instalador al igual que con privilegios de administrador la próxima vez que se ejecute el programa.

Si deshabilita esta configuración de directiva, PCA no detectará aplicaciones que no puedan iniciar instaladores ejecutados en UAC.

Si no establece esta configuración de directiva, PCA detecta los programas que no pudieron iniciar procesos secundarios que constituyen instaladores (normalmente programas de actualización).

Nota: si deshabilita la configuración de directiva "Desactivar el Asistente para la compatibilidad de programas", esta configuración de directiva no tendrá efecto. El Servicio de directivas de diagnóstico (DPS) y el Servicio Asistente para la compatibilidad de programas deben estar ejecutándose para que PCA se ejecute. Estos servicios se pueden configurar mediante el complemento Servicios para Microsoft Management Console.</string>
      <!-- End update failure scenario description -->
      

      <!-- Begin deprecated component failure scenario description -->
      <string id="DetectDeprecatedComponentFailuresExplain">Esta configuración de directiva determina si el Asistente para la compatibilidad de programas (PCA) diagnosticará la carga de archivos DLL. 

Si habilita esta configuración de directiva, PCA detecta los programas que intentan cargar archivos DLL de Microsoft Windows heredados que se eliminaron de esta versión de Windows. Cuando se detecte este error, después de que finalice el programa, PCA notificará al usuario este problema y dará la opción de visitar el sitio web de Microsoft para buscar soluciones.

Si deshabilita esta configuración de directiva, PCA no detecta los programas que intentan cargar archivos DLL de Windows heredados.

Si no establece esta configuración de directiva, PCA detecta los programas que intentan cargar archivos DLL de Windows heredados.

Nota: si deshabilita la configuración de directiva "Desactivar el Asistente para la compatibilidad de programas", esta configuración de directiva no tendrá efecto. El Servicio de directivas de diagnóstico (DPS) y el Servicio Asistente para la compatibilidad de programas deben estar ejecutándose para que PCA se ejecute. Estos servicios se pueden configurar mediante el complemento Servicios para Microsoft Management Console.</string>
      <!-- End deprecated component failure scenario description -->


      <!-- Begin deprecated COM component failure scenario description -->
      <string id="DetectDeprecatedCOMComponentFailuresExplain">Esta configuración de directiva determina si el Asistente para la compatibilidad de programas (PCA) diagnosticará los errores de carga de archivos DLL o de creación de objetos COM en programas. 

Si habilita esta configuración de directiva, PCA detectará los programas que intenten crear objetos COM heredados que se quitaron de esta versión de Windows. Cuando se detecte este error, y después de finalizar el programa, PCA notificará este problema al usuario y proporcionará una opción para buscar soluciones en el sitio web de Microsoft.

Si deshabilita esta configuración de directiva, PCA no detectará los programas que intenten crear objetos COM heredados.

Si no define esta configuración de directiva, PCA detectará los programas que intenten crear objetos COM heredados.

Nota: si deshabilita la configuración de directiva "Desactivar el Asistente para la compatibilidad de programas", esta configuración de directiva no surtirá efecto. El Servicio de directivas de diagnóstico (DPS) y el Servicio Asistente para la compatibilidad de programas deben estar en ejecución para que se ejecute PCA. Estos servicios se pueden configurar mediante el complemento Servicios en Microsoft Management Console.</string>
      <!-- End deprecated COM component failure scenario description -->


      <!-- Begin undetected installer failure scenario description -->
      <string id="DetectUndetectedInstallersExplain">Esta configuración de directiva determina si el Asistente para la compatibilidad de programas (PCA) diagnosticará los errores de los instaladores de aplicaciones que se detecta que no se ejecutan como administrador. 

Si habilita esta configuración de directiva, PCA queda configurado para detectar instaladores de aplicaciones que no tienen privilegios para ejecutarse como administrador por parte de Control de cuentas de usuario (UAC). Cuando se detecten posibles errores, PCA dará al usuario la opción de reiniciar el instalador como administrador.

Si deshabilita esta configuración de directiva, PCA no detectará instaladores de aplicaciones que no tienen privilegios para ejecutarse como administrador por parte de UAC.

Si no establece esta configuración de directiva, PCA quedará configurado para detectar instaladores de aplicaciones que no tienen privilegios para ejecutarse como administrador por parte de UAC.


Nota: si deshabilita la configuración de directiva "Desactivar el Asistente para la compatibilidad de programas", esta configuración de directiva no tendrá efecto. El Servicio de directivas de diagnóstico (DPS) y el Servicio Asistente para la compatibilidad de programas deben estar ejecutándose para que PCA se ejecute. Estos servicios se pueden configurar mediante el complemento Servicios para Microsoft Management Console.</string>
      <!-- End undetected installer failure scenario description -->


      <!-- Begin blocked drivers scenario description -->
      <string id="DetectBlockedDriversExplain">Esta configuración de directiva determina si el Asistente para la compatibilidad de programas (PCA) diagnosticará los controladores bloqueados debido a problemas de compatibilidad. 

Si habilita esta configuración de directiva, PCA notificará al usuario los problemas de controladores bloqueados con la opción de visitar el sitio web de Microsoft para buscar soluciones.

Si deshabilita esta configuración de directiva, PCA no notificará al usuario los problemas de controladores bloqueados. Nota: Si esta configuración de directiva se encuentra deshabilitada, el usuario no recibirá soluciones para los controladores bloqueados.

Si no establece esta configuración de directiva, PCA notificará al usuario los problemas de controladores bloqueados con la opción de visitar el sitio web de Microsoft para buscar soluciones.

Nota: si deshabilita la configuración de directiva "Desactivar el Asistente para la compatibilidad de programas", esta configuración de directiva no tendrá efecto. El Servicio de directivas de diagnóstico (DPS) y el Servicio Asistente para la compatibilidad de programas deben estar ejecutándose para que PCA se ejecute. Estos servicios se pueden configurar mediante el complemento Servicios para Microsoft Management Console.</string>
      <!-- End blocked drivers scenario description -->
    </stringTable>

    <presentationTable>
      <presentation id="DetectUpdateFailuresTable">
        <dropdownList refId="DetectUpdateFailuresLevel" noSort="true" defaultItem="1">Nivel de ejecución de escenario</dropdownList>
      </presentation>
      <presentation id="DetectDeprecatedComponentFailuresTable">
        <dropdownList refId="DetectDeprecatedComponentFailuresLevel" noSort="true" defaultItem="1">Nivel de ejecución de escenario</dropdownList>
      </presentation>
      <presentation id="DetectDeprecatedCOMComponentFailuresTable">
        <dropdownList refId="DetectDeprecatedCOMComponentFailuresLevel" noSort="true" defaultItem="1">Nivel de ejecución de escenario</dropdownList>
      </presentation>
      <presentation id="DetectUndetectedInstallersTable">
        <dropdownList refId="DetectUndetectedInstallersLevel" noSort="true" defaultItem="1">Nivel de ejecución de escenario</dropdownList>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
