<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>escriba aquí el nombre</displayName>
  <description>escriba aquí la descripción</description>
  <resources>
    <stringTable>
      <string id="Rpc">Llamada a procedimiento remoto</string>
      <string id="RpcEEInfoOff">Desactivado</string>
      <string id="RpcEEInfoOffWithExc">Desactivado con excepciones</string>
      <string id="RpcEEInfoOn">Activado</string>
      <string id="RpcEEInfoOnWithExc">Activado con excepciones</string>
      <string id="RpcEnableAuthEpResolution">Autenticación de clientes del Asignador de extremos de RPC</string>
      <string id="RpcEnableAuthEpResolution_Help">Si se habilita esta opción, se pedirá autenticación a los clientes de RPC que necesitan comunicarse con el Servicio de asignador de extremos, siempre y cuando la llamada RPC para la que el extremo necesita resolverse cuente con información de autenticación.

Si se deshabilita esta opción, los clientes de RPC que necesiten comunicarse con el Servicio de asignador de extremos no se autenticarán. El Servicio de asignador de extremos instalado en los equipos que ejecuten Windows NT 4.0 (todos los Service Pack) no puede procesar la información de autenticación proporcionada de esta forma. Esto significa que la habilitación de esta opción en un equipo cliente evitará que los clientes se comuniquen con un servidor Windows NT 4.0 mediante RPC, si se necesita la resolución de extremos.

De manera predeterminada, los clientes de RPC no usarán la autenticación para comunicarse con el Servicio de asignación de extremos de servidores de RPC cuando soliciten el extremo de un servidor.

Nota: no se aplicará esta directiva hasta que se reinicie el sistema. </string>
      <string id="RpcExtendedErrorInformation">Propagación de información de errores ampliada</string>
      <string id="RpcExtendedErrorInformation_Help">Hace que el tiempo de ejecución de RPC genere información de error ampliada cuando se produce un error.

La información de error ampliada incluye la hora local en la que se produjo el error, la versión de RPC, y el nombre de equipo en el que ocurrió el error o se propagó. Los programas pueden recuperar la información de error ampliada con las interfaces de programación de aplicaciones (API) estándar de Windows.

Si deshabilita esta opción o no la configura, el tiempo de ejecución de RPC solo genera un código de estado para indicar una condición de error.

Para usar esta opción, habilítela y, a continuación, seleccione un tipo de respuesta de error en el cuadro desplegable.

--  "Desactivado" deshabilita la información de error ampliada para todos los procesos. RPC solo genera un código de error.

--  "Activado con excepciones" habilita la información de error ampliada, pero le permite deshabilitarla para procesos seleccionados. Para deshabilitar la información de error ampliada de un proceso mientras la opción está activa, el comando que inicia el proceso debe empezar con una de las cadenas en el campo Excepción de información de error ampliada.

--  "Desactivado con excepciones" deshabilita la información de error ampliada, pero le permite habilitarla para procesos seleccionados. Para habilitar la información de error ampliada de un proceso mientras la opción está activa, el comando que inicia el proceso debe empezar con una de las cadenas en el campo Excepción de información de error ampliada.

--  "Activado" deshabilita la información de error ampliada para todos los procesos.

Nota: para obtener más información acerca del campo Excepción de información de error ampliada, consulte el kit de desarrollo de software (SDK) de la plataforma Windows 2000.

Nota: la información de error ampliada tiene un formato compatible con sistemas operativos anteriores de Microsoft y otros sistemas operativos, pero solo los nuevos sistemas operativos de Microsoft pueden leer y responder a la información.

Nota: la opción predeterminada "Desactivado" está diseñada para sistemas en los que la información de error ampliada se considera confidencial y no debe estar disponible de forma remota.

Nota: no se aplicará esta configuración de directiva hasta que se reinicie el sistema.</string>
      <string id="RpcIDFOff">Desactivado</string>
      <string id="RpcIDFOn">Activado</string>
      <string id="RpcIgnoreDelegationFailure">Omitir error de delegación</string>
      <string id="RpcIgnoreDelegationFailure_Help">Hace que el tiempo de ejecución de RPC omita los errores de delegación si ésta se pidió.

La familia de servidores de Windows Server 2003 incluye un nuevo modelo de delegación: delegación restringida. En este modelo, el sistema de seguridad no informa de que la delegación está habilitada en un contexto de seguridad cuando un cliente se conecta a un servidor. Se recomienda que los usuarios que llamen a RPC y COM usen la marca RPC_C_QOS_CAPABILITIES_IGNORE_DELEGATE_FAILURE; no obstante, es posible que algunas aplicaciones escritas para el modelo de delegación tradicional no usen esta marca y encuentren el error RPC_S_SEC_PKG_ERROR cuando se conecten a un servidor que usa delegación restringida.

Si deshabilita esta opción, no la configura o la establece en "Desactivada", el tiempo de ejecución de RPC generará errores RPC_S_SEC_PKG_ERROR en aplicaciones que pidan delegación y se conecten a servidores que usan delegación restringida. Si establece esta opción en "Activada", el tiempo de ejecución de RPC aceptará contextos de seguridad que no son compatibles con la delegación, así como contextos de seguridad que sí lo sean.

--  "Desactivada" hace que el tiempo de ejecución de RPC genere un error RPC_S_SEC_PKG_ERROR si el cliente pide delegación; no obstante, el contexto de seguridad creado no es compatible con la delegación.

-- "Activada" hace que el tiempo de ejecución de RPC acepte contextos de seguridad que no son compatibles con la delegación, incluso cuando ésta se ha pedido.

Nota: no se aplicará esta configuración de directiva hasta que se reinicie el sistema.</string>
      <string id="RpcMinimumHttpConnectionTimeout">Tiempo mínimo de espera de conexión inactiva para conexiones RPC/HTTP</string>
      <string id="RpcMinimumHttpConnectionTimeout_Help">Hace que el tiempo de ejecución de RPC asuma el tiempo de espera especificado como el tiempo de espera de conexión inactiva, incluso cuando el servidor IIS en el que se ejecuta el proxy HTTP de RPC está configurado con un tiempo de espera mayor. Si el servidor IIS en el que se ejecuta el proxy HTTP de RPC está configurado con un tiempo de espera menor de conexión inactiva, se usará el tiempo de espera del servidor IIS. El tiempo de espera está especificado en segundos.

Esta opción es útil en los casos en que un agente de red (como un proxy HTTP o un enrutador) use un tiempo de espera de conexión inactiva menor que el servidor IIS en el que se ejecuta el proxy HTTP de RPC. En tales casos, los clientes de RPC a través de HTTP pueden encontrar errores, ya que las conexiones agotarán su tiempo de espera antes de lo esperado. Mediante esta opción, puede forzar que el tiempo de ejecución y el proxy de RPC usen un tiempo de espera menor.

Esta opción solo es aplicable cuando el cliente de RPC, el servidor de RPC y el proxy HTTP de RPC ejecuten productos de la familia de servidores de Windows Server 2003, o bien Windows XP SP1 o versiones posteriores. Si el cliente o el servidor de RPC, o bien el proxy HTTP de RPC se ejecutan en una versión anterior de Windows, se omitirá esta opción.

El valor mínimo permitido para esta opción es de 90 segundos. El valor máximo es de 7200 (2 horas).

Si deshabilita esta opción o no la configura, se usará el tiempo de espera de conexión inactiva en el servidor IIS en el que se ejecuta el proxy HTTP de RPC.

Nota: no se aplicará esta configuración de directiva hasta que se reinicie el sistema.</string>
      <string id="RpcRestrictRemoteClients">Restricciones para clientes de RPC no autenticados</string>
      <string id="RpcRestrictRemoteClients_Help">Si habilita esta opción, el tiempo de ejecución de RPC en un servidor de RPC impide que los clientes de RPC no autenticados se conecten a los servidores de RPC que se ejecuten en un equipo. Un cliente se considera como autenticado cuando usa una canalización con nombre para comunicarse con el servidor o cuando usa seguridad de RPC. Las interfaces de RPC que solicitaron específicamente ser accesibles a clientes no autenticados pueden quedar exentas de esta restricción, dependiendo del valor seleccionado para esta directiva.

Si deshabilita esta opción o no la configura, se usará el valor de Autenticado para Windows XP y el valor de Ninguna para los SKU de servidor compatibles con esta configuración de directiva. Si la habilita, están disponibles los siguientes valores:

--  "Ninguna" permite que todos los clientes de RPC se conecten a los servidores de RPC que se ejecutan en el equipo donde se aplica la directiva.

--  "Autenticado" permite que solo los clientes de RPC autenticados (según la definición anterior) se conecten a los servidores de RPC que se ejecutan en el equipo donde se aplica la directiva. Se otorgará una exención a las interfaces que solicitaron estar exentas de esta restricción.

--  "Autenticado sin excepciones" permite que solo los clientes de RPC autenticados (según la definición anterior) se conecten a los servidores de RPC que se ejecuten en el equipo donde se aplica la directiva. No se permite ninguna excepción.

Nota: no se aplicará esta configuración de directiva hasta que se reinicie el sistema.</string>
      <string id="RpcRestrictRemoteClientsAuth">Autenticado</string>
      <string id="RpcRestrictRemoteClientsHigh">Autenticado sin excepciones</string>
      <string id="RpcRestrictRemoteClientsNone">Ninguna</string>
      <string id="RpcStateInfoAuto1">Auto1</string>
      <string id="RpcStateInfoAuto2">Auto2</string>
      <string id="RpcStateInfoBoth">Completa</string>
      <string id="RpcStateInfoNone">Ninguna</string>
      <string id="RpcStateInformation">Información de estado de solución de problemas de RPC</string>
      <string id="RpcStateInformation_Help">

Determina si el tiempo de ejecución de RPC mantiene la información de estado de RPC para el sistema, y cuánta información mantiene. La información de estado básica, que consta solo de los datos de estado más necesarios habitualmente, se requiere para solucionar problemas de RPC.

Si habilita esta opción, puede usar el cuadro desplegable para determinar los equipos que mantienen la información de estado de RPC. Si la opción está deshabilitada o no configurada, RPC usa la opción Auto2.

-- "Ninguna" indica que el sistema no mantiene ninguna información de estado RPC. Nota: puesto que la información de estado básica requerida para solucionar problemas tiene efectos insignificantes en el rendimiento y solo usa unos 4 KB de memoria, esta opción no se recomienda para la mayoría de las instalaciones.

--  "Auto1" hace que RPC mantenga información de estado básica solo si el equipo tiene al menos 64 MB de memoria.

--  "Auto2" hace que RPC mantenga información de estado básica solo si el equipo tiene al menos 128 MB de memoria y ejecuta Windows 2000 Server, Windows 2000 Advanced Server o Windows 2000 Datacenter Server. 

--  "Servidor" hace que RPC mantenga información de estado básica en el equipo, sin tener en cuenta su capacidad.

--  "Completa" hace que RPC mantenga información de estado de RPC completa, sin tener en cuenta su capacidad. Puesto que esta opción puede afectar al rendimiento, se recomienda para su uso solo cuando investigue un problema de RPC.

Nota: para recuperar la información de estado de RPC que mantiene un sistema, debe usar una herramienta de depuración.

Nota: no se aplicará esta directiva hasta que se reinicie el sistema.</string>
      <string id="RpcStateInfoServer">Servidor</string>
    </stringTable>
    <presentationTable>
      <presentation id="RpcExtendedErrorInformation">
        <dropdownList refId="RpcExtendedErrorInformationList" defaultItem="0">Propagación de información de errores ampliada:</dropdownList>
        <textBox refId="RpcExtErrorExceptions">
          <label>Excepciones de información de errores ampliada:</label>
        </textBox>
      </presentation>
      <presentation id="RpcIgnoreDelegationFailure">
        <dropdownList refId="RpcIgnoreDelegationFailureList" defaultItem="0">Se omite error de delegación:</dropdownList>
      </presentation>
      <presentation id="RpcMinimumHttpConnectionTimeout">
        <decimalTextBox refId="RpcMinimumHttpConnectionTimeoutValue" defaultValue="900" spinStep="10">Tiempo mínimo de espera de conexión inactiva (en segundos):</decimalTextBox>
      </presentation>
      <presentation id="RpcRestrictRemoteClients">
        <dropdownList refId="RpcRestrictRemoteClientsList" defaultItem="1">Restricción de clientes sin autenticar en tiempo de ejecución de RPC que se va a aplicar:</dropdownList>
      </presentation>
      <presentation id="RpcStateInformation">
        <dropdownList refId="RpcStateInformationList" defaultItem="2">Información de estado en tiempo de ejecución de RPC que se va a mantener:</dropdownList>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
