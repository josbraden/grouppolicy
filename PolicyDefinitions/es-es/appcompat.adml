<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>escriba aquí el nombre</displayName>
  <description>escriba aquí la descripción</description>
  <resources>
    <stringTable>
      <string id="AppCompat">Compatibilidad de aplicaciones</string>
      <string id="AppCompat_Prevent16Bit_Help">Especifica si se impide que todos los usuarios ejecuten el subsistema MS-DOS (ntvdm.exe). Esta opción afecta al inicio de aplicaciones de 16 bits en el sistema operativo. De manera predeterminada, se ejecuta el subsistema MS-DOS para todos los usuarios.

Puede usar esta opción para desactivar el subsistema MS-DOS, lo que reducirá el uso de recursos e impedirá que los usuarios ejecuten aplicaciones de 16 bits. Para ejecutar aplicaciones de 16 bits o aplicaciones con componentes de 16 bits, ntvdm.exe debe poder ejecutarse. El subsistema MS-DOS se inicia cuando se ejecuta la primera aplicación de 16 bits. Mientras se lleve a cabo el proceso, las posteriores aplicaciones de 16 bits se iniciarán más rápidamente, pero el uso global de recursos del sistema aumentará.

Si el estado se establece en Habilitado, no se puede ejecutar ntvdm.exe, lo que impide la ejecución de cualquier aplicación de 16 bits. Además, tampoco se podrán ejecutar aplicaciones de 32 bits con instaladores u otros componentes de 16 bits.

Si el estado se establece en Deshabilitado, se aplica la opción predeterminada y el subsistema MS-DOS se ejecuta para todos los usuarios.

Si el estado se establece en No configurado, se aplica la opción predeterminada y se ejecuta ntvdm.exe para todos los usuarios. Sin embargo, si un administrador establece el valor DWORD del Registro HKLM\System\CurrentControlSet\Control\WOW\DisallowedPolicyDefault en 1, la opción predeterminada cambia para impedir que se ejecuten todas las aplicaciones de 16 bits.

Nota: esta opción aparece en Configuración del equipo y Configuración de usuario. Si ambas están configuradas, Configuración del equipo queda invalidada.</string>
      <string id="AppCompat_Prevent16BitMach_Help">Especifica si se impide que se ejecute el subsistema MS-DOS (ntvdm.exe) en este equipo. Esta configuración afecta al inicio de aplicaciones de 16 bits en el sistema operativo. De manera predeterminada, se ejecuta el subsistema MS-DOS para todos los usuarios del equipo.

Puede usar esta opción para desactivar el subsistema MS-DOS, lo que reducirá el uso de recursos e impedirá que los usuarios ejecuten aplicaciones de 16 bits. Para ejecutar aplicaciones de 16 bits o aplicaciones con componentes de 16 bits, ntvdm.exe debe poder ejecutarse. El subsistema MS-DOS se inicia cuando se ejecuta la primera aplicación de 16 bits. Mientras se ejecuta el subsistema MS-DOS, las posteriores aplicaciones de 16 bits se iniciarán más rápidamente, pero el uso global de recursos del sistema aumentará.

Si el estado se establece en Habilitado, no se puede ejecutar ntvdm.exe, lo que impide la ejecución de cualquier aplicación de 16 bits. Además, tampoco se podrán ejecutar aplicaciones de 32 bits con instaladores u otros componentes de 16 bits.

Si el estado se establece en Deshabilitado, se aplica la opción predeterminada y el subsistema MS-DOS se ejecuta para todos los usuarios del equipo.

Si el estado se establece en No configurado, se aplica la opción predeterminada y se ejecuta ntvdm.exe para todos los usuarios. Sin embargo, si un administrador establece el valor DWORD del Registro HKLM\System\CurrentControlSet\Control\WOW\DisallowedPolicyDefault en 1, la opción predeterminada cambia para impedir que se ejecuten todas las aplicaciones de 16 bits.

Nota: esta configuración aparece en Configuración del equipo y Configuración de usuario. Si ambas están configuradas, Configuración del equipo queda invalidada.</string>
      <string id="AppCompat_RemoveProgramCompatPropPage_Help">Esta directiva controla la visibilidad de la extensión de shell de la página de propiedades de compatibilidad de programas. Esta extensión de shell se puede ver en el menú contextual de propiedades de cualquier acceso directo de programa o archivo ejecutable.

La página de propiedades de compatibilidad muestra una lista de opciones que se pueden seleccionar y aplicar a la aplicación para resolver los problemas más comunes que afectan a las aplicaciones heredadas. Si se habilita esta configuración de directiva, se quita la página de propiedades de los menús contextuales, sin efecto alguno sobre la configuración de compatibilidad anterior aplicada a la aplicación mediante esta interfaz.</string>
        <string id="AppCompat_TurnOffApplicationImpactTelemetry_Help">La directiva controla el estado del motor de telemetría de aplicaciones en el sistema.

La telemetría de aplicaciones es un mecanismo que realiza un seguimiento del uso anónimo de componentes específicos del sistema de Windows por aplicaciones.

Si se desactiva la telemetría de aplicaciones seleccionando "habilitar", se detendrá la recopilación de datos de uso.

Si se desactiva el Programa para la mejora de la experiencia del usuario, la telemetría de aplicaciones se desactivará independientemente de cómo esté establecida esta directiva.

Si se deshabilita la telemetría, esto afectará a todas las aplicaciones que se inicien a partir de ese momento. Para asegurarse de que la recopilación de telemetría se ha detenido para todas las aplicaciones, reinicie el equipo.</string>
      <string id="AppCompat_TurnOffEngine_Help"> Esta directiva controla el estado del motor de compatibilidad de aplicaciones en el sistema.

El motor forma parte del cargador y consulta una base de datos de compatibilidad cada vez que se inicia una aplicación en el sistema. Si se encuentra una coincidencia para la aplicación, proporciona soluciones en tiempo de ejecución o correcciones de compatibilidad, o bien muestra un mensaje de Ayuda de la aplicación si ésta tiene un problema conocido.

Si desactiva el motor de compatibilidad de aplicaciones, mejorará el rendimiento del sistema. Sin embargo, empeorará la compatibilidad de muchas aplicaciones heredadas populares y no se bloqueará la instalación de aplicaciones incompatibles conocidas. Esto, por ejemplo, puede producir la aparición de una pantalla azul si se instala una aplicación antivirus antigua.

Las características de Protección de recursos de Windows y Control de cuentas de usuario usan el motor de compatibilidad de aplicaciones para proporcionar mitigaciones a los problemas con las aplicaciones. Si el motor está desactivado, estas mitigaciones no se aplicarán a las aplicaciones y sus instaladores, y estas aplicaciones pueden generar errores en el momento de la instalación o la ejecución.

Esta opción resulta útil para los administradores de servidor que requieren un mejor rendimiento y conocen la compatibilidad de las aplicaciones que usan. Es de especial utilidad para un servidor web donde las aplicaciones se pueden iniciar cientos de veces por segundo y donde el rendimiento del cargador es esencial.

NOTA: muchos procesos de sistema almacenan en caché el valor de esta configuración por motivos de rendimiento. Si se hacen cambios en esta opción, reinicie el sistema para asegurarse de estos cambios se reflejan con precisión.</string>
      <string id="AppCompat_TurnOffProgramCompatibilityAssistant_Help">Esta directiva controla el estado del Asistente para la compatibilidad de programas en el sistema.
      
PCA supervisa los programas iniciados por si se producen problemas de compatibilidad conocidos en tiempo de ejecución. Cuando se detecta un problema potencial, PCA avisa al usuario mediante punteros a las soluciones recomendadas. Para obtener más información acerca de los diversos escenarios detección de problemas que abarca PCA y las directivas para configurarlos, consulte las directivas en Sistema-&gt;Solución de problemas y herramientas de diagnóstico-&gt;Diagnóstico de compatibilidad de aplicaciones. 

PCA está activado de manera predeterminada.

Si se habilita esta configuración de directivas, PCA se desactivará. Esta opción es útil para los administradores de sistemas que requieren mejor rendimiento y tener un conocimiento claro de la compatibilidad de las aplicaciones que usan. Nota: Si se desactiva PCA, no se presentarán al usuario soluciones a los problemas de compatibilidad conocidos cuando se ejecutan aplicaciones.

Si se deshabilita o no se configura esta opción de directivas, PCA se activará. 

Nota: el Servicio de directivas de diagnóstico (DPS) y el Servicio Asistente para la compatibilidad de programas deben estar en ejecución para que se ejecute PCA. Estos servicios se pueden configurar mediante el complemento Servicios en Microsoft Management Console (MMC).</string>
      <string id="AppCompat_TurnOffUserActionRecord_Help">Esta directiva controla el estado de la característica Grabación de acciones de usuario en el sistema.

Grabación de acciones de usuario permite la captura de los pasos dados por el usuario antes de que se produzca un problema. Los datos de salida generados por Grabación de acciones de usuario constituirán otro tipo de recopilación de datos habilitada mediante escenarios de envío de comentarios, como Informe de errores de Windows, que ayudan a los programadores a entender problemas. Los datos capturados contendrán descripciones de las acciones del usuario, con información como las entradas del usuario con el teclado y el mouse, metadatos sobre los objetos de la interfaz de usuario con los que se ha interactuado y capturas de pantalla. Grabación de acciones de usuario proporciona opciones específicas para activar o desactivar la recopilación de datos cuando sea necesario.

De forma predeterminada, Grabación de acciones de usuario estará disponible para que los clientes la activen en un escenario de informe de problemas.

Si habilita esta configuración de directiva, la característica Grabación de acciones de usuario quedará deshabilitada en este equipo.

Si no habilita o no establece esta configuración de directiva, Grabación de acciones de usuario estará disponible para los clientes.</string>

      <string id="AppCompat_TurnOffSwitchBack_Help">Esta directiva controla el estado del motor de compatibilidad de Switchback en el sistema. 

Switchback es un mecanismo que mitiga la compatibilidad genérica de aplicaciones más antiguas proporcionando un comportamiento más antiguo a aplicaciones antiguas y un comportamiento más nuevo a aplicaciones nuevas. 

Switchback está activado de forma predeterminada.

Si habilita esta configuración de directiva, Switchback se desactivará. La desactivación de Switchback puede degradar la compatibilidad de aplicaciones más antiguas. Esta opción es útil para los administradores de servidores que requieren rendimiento y tienen un conocimiento claro de la compatibilidad de las aplicaciones que usan. 

Si deshabilita o no establece esta configuración de directiva, Switchback se activará.

Después de cambiar esta configuración, vuelva a arrancar el sistema para asegurarse de que el sistema refleja correctamente los cambios.</string>
      <string id="AppCompat_TurnOffProgramInventory_Help">Esta directiva controla el estado del recopilador del inventario de programas en el sistema.
      
PDU realiza un inventario de programas y archivos en el sistema y envía la información a Microsoft. Esta información se usa para ayudar a asociar archivos con programas y diagnosticar problemas de compatibilidad de aplicaciones.

PDU está activado de forma predeterminada.

Si habilita esta configuración de directiva, la recopilación del inventario se desactivará y no se enviarán datos a Microsoft. Al habilitar esta configuración, también se desactiva la recopilación de datos de instalación mediante PCA.

Si deshabilita o no establece esta configuración de directiva, la recopilación del inventario se activará.</string>

      <string id="AppCompatPrevent16Bit">Impedir el acceso a aplicaciones de 16 bits</string>
      <string id="AppCompatPrevent16BitMach">Impedir el acceso a aplicaciones de 16 bits</string>
      <string id="AppCompatRemoveProgramCompatPropPage">Quitar página de propiedades de compatibilidad de programas</string>
      <string id="AppCompatTurnOffApplicationImpactTelemetry">Desactivar la telemetría de aplicaciones</string>
      <string id="AppCompatTurnOffEngine">Desactivar el motor de compatibilidad de aplicaciones</string>
      <string id="AppCompatTurnOffProgramCompatibilityAssistant">Desactivar el Asistente para la compatibilidad de programas</string>
      <string id="AppCompatTurnOffSwitchBack">Desactivar el motor de compatibilidad de SwitchBack</string>
      <string id="AppCompatTurnOffUserActionRecord">Desactivar la grabación de acciones de usuario</string>
      <string id="AppCompatTurnOffProgramInventory">Desactivar el inventario de programas</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
