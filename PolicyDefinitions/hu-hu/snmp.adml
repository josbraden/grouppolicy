<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>itt adja meg a megjelenítendő nevet</displayName>
  <description>itt adja meg a leírást</description>
  <resources>
    <stringTable>
      <string id="SNMP_Communities">Logikai csoportok</string>
      <string id="SNMP_PermittedManagers">Engedélyezett kezelők</string>
      <string id="SNMP_PermittedManagers_Help">Ez a beállítás adja meg azoknak az állomásoknak a listáját, amelyek lekérdezést nyújthatnak be az ügyfélszámítógépen futó SNMP-ügynökhöz.

Az SNMP protokoll lehetővé teszi, hogy a felhasználók a terminális elemek értékeinek lekérdezésével és megadásával, valamint hálózati események figyelésével távolról felügyelhessenek számítógép-hálózatokat.

A kezelő a hálózati gazdaszámítógépen található. A kezelő feladata, hogy megszerezze a kért információkat az ügynököktől.

Ha engedélyezi a beállítást, az SNMP-ügynök csak a beállításban megadott listában szereplő Engedélyezett kezelőktől fogad el kéréseket.

Ha letiltja, vagy nem konfigurálja a beállítást, az SNMP szolgáltatás a helyi számítógépen megadott Engedélyezett kezelők listát veszi figyelembe.

Gyakorlati tanács: Biztonsági szempontok miatt célszerű beállítani, hogy a HKLM\SOFTWARE\Policies\SNMP\Parameters\PermittedManagers kulcs kizárólag a helyi rendszergazdacsoportnak nyújtson korlátlan hozzáférést.

Megjegyzés: Ez a beállítás hatástalan, ha nincs SNMP-ügynök telepítve az ügyfélszámítógépen.

További információt a másik két SNMP beállítás leírásánál talál: „Trap-beállítás” és „Logikai csoport neve”.</string>
      <string id="SNMP_SNMP">SNMP</string>
      <string id="SNMP_TrapDestinations_Help">Ez a beállítás az SNMP-ügynök TRAP-beállításainak megadását teszi lehetővé.

Az SNMP protokoll lehetővé teszi, hogy a felhasználók terminális elemek értékeinek lekérdezésével és megadásával, valamint hálózati események figyelésével távolról felügyelhessenek számítógép-hálózatokat.

A beállítással megadható azoknak az állomásoknak a neve, amelyek fogadják a logikai csoport számára az SNMP-szolgáltatás által küldött trapüzeneteket. A trapüzenet olyan riasztás vagy kiemelt esemény, melyről az SNMP-ügynök aszinkron módon is értesítheti a felügyeleti rendszereket.

Ha engedélyezi a beállítást, az SNMP szolgáltatás trapüzeneteket küld azoknak az állomásoknak, amelyek a „nyilvános” logikai csoportban találhatók.

Ha letiltja, vagy nem konfigurálja a beállítást, az SNMP szolgáltatás a helyi számítógépen megadott trapbeállításokat veszi figyelembe.

Megjegyzés: Ez a beállítás hatástalan, ha nincs SNMP-ügynök telepítve az ügyfélszámítógépen.

További információt a másik két SNMP beállítás leírásánál talál: „Engedélyezett kezelők” és „Logikai csoport neve”.</string>
      <string id="SNMP_Traps_Public">Nyilvános logikai csoportok trapjei</string>
      <string id="SNMP_ValidCommunities_Help">Beállítja az SNMP szolgáltatáshoz megadott logikai csoportok listáját.

Az SNMP protokoll lehetővé teszi, hogy a felhasználók terminális elemek értékeinek lekérdezésével és megadásával, valamint hálózati események figyelésével távolról felügyelhessenek számítógép-hálózatokat.

Érvényesnek számít az a logikai csoport, amelyet az SNMP-szolgáltatás felismer, a logikai csoport pedig olyan állomásokból (kiszolgálókból, munkaállomásokból, elosztókból és útválasztókból) álló csoport, amelyeket az SNMP együttesen kezel. Az SNMP-szolgáltatás kezelt hálózati csomópont, amely a hálózatról SNMP-csomagokat kap.

Amennyiben engedélyezi ezt a beállítást, akkor az SNMP-ügynök csak azoknak a logikai csoportoknak a kezelőrendszereitől fogad el kéréseket, amelyeket felismer, és csak olvasási SNMP-műveleteket engedélyez a logikai csoportnak.

Ha letiltja vagy nem konfigurálja ezt a beállítást, az SNMP-szolgáltatás a helyi gépen beállított érvényes logikai csoportokat fogja figyelembe venni.

Gyakorlati tanács: Biztonsági szempontok miatt célszerű beállítani, hogy a HKLM\SOFTWARE\Policies\SNMP\Parameters\ValidCommunities kulcs kizárólag a helyi rendszergazdacsoportnak nyújtson korlátlan hozzáférést.

Megjegyzés: Célszerű titkosított logikaicsoport-nevet használni.

Megjegyzés: Ez a beállítás hatástalan, ha nincs SNMP-ügynök telepítve az ügyfélszámítógépen.

További információt a másik két SNMP beállítás leírásánál talál: „Engedélyezett kezelők” és „Trap-beállítások”.</string>
    </stringTable>
    <presentationTable>
      <presentation id="SNMP_Communities">
        <listBox refId="SNMP_CommunitiesListbox">Logikai csoportok</listBox>
      </presentation>
      <presentation id="SNMP_PermittedManagers">
        <listBox refId="SNMP_PermittedManagersListbox">Engedélyezett kezelők</listBox>
      </presentation>
      <presentation id="SNMP_Traps_Public">
        <listBox refId="SNMP_Traps_PublicListbox">Trap konfigurációja</listBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
