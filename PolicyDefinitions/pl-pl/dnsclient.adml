<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>tutaj wprowadź wyświetlaną nazwę</displayName>
  <description>tutaj wprowadź opis</description>
  <resources>
    <stringTable>
      <string id="DNS_Client">Klient DNS</string>
      <string id="DNS_Domain">Sufiks DNS konkretnego połączenia</string>
      <string id="DNS_Domain_Help">Określa sufiks DNS konkretnego połączenia. To ustawienie zastępuje sufiksy DNS konkretnego połączenia ustawione na komputerach, które skonfigurowano lokalnie oraz przy użyciu protokołu DHCP, do których stosowane jest to połączenie.

Ostrzeżenie: określony w tym ustawieniu sufiks DNS konkretnego połączenia jest stosowany do wszystkich połączeń sieciowych używanych przez komputery wieloadresowe, do których jest stosowane to ustawienie.

Aby użyć tego ustawienia, należy kliknąć opcję Włącz, a następnie wprowadzić w dostępnym polu wartość ciągu reprezentującą sufiks DNS.

Nieskonfigurowanie tego ustawienia spowoduje, że nie będzie ono stosowane do żadnych komputerów i komputery będą korzystać ze swoich parametrów lokalnych lub parametrów konfiguracji DHCP.</string>
      <string id="DNS_NameServer">Serwery DNS</string>
      <string id="DNS_NameServer_Help">Definiuje serwery DNS, do których komputer wysyła zapytania w czasie próby rozpoznania nazw.

Ostrzeżenie: lista serwerów DNS zdefiniowanych w tym ustawieniu zastępuje serwery DNS skonfigurowane lokalnie oraz serwery skonfigurowane przy użyciu protokołu DHCP. Lista serwerów DNS jest stosowana do wszystkich połączeń sieciowych komputerów wieloadresowych, do których jest stosowane to ustawienie.

Aby użyć tego ustawienia, należy kliknąć opcję Włącz, a następnie w dostępnym polu wprowadzić rozdzielaną spacjami listę adresów IP (w formacie dziesiętnym z kropkami). W przypadku włączenia tego ustawienia należy wprowadzić co najmniej jeden adres IP.

Nieskonfigurowanie tego ustawienia spowoduje, że nie będzie ono stosowane do żadnych komputerów i komputery będą korzystać ze swoich parametrów lokalnych lub parametrów konfiguracji DHCP.</string>
      <string id="DNS_PrimaryDnsSuffix">Sufiks podstawowej domeny DNS</string>
      <string id="DNS_PrimaryDnsSuffix_Help">Określa sufiks podstawowej domeny DNS (Domain Name System) dla wszystkich odnośnych komputerów. Sufiks podstawowej domeny DNS jest używany podczas rejestracji nazw DNS i rozpoznawania nazw DNS.

To ustawienie umożliwia określenie sufiksu podstawowej domeny DNS dla grupy komputerów i zapobiega jego zmienianiu przez użytkowników, w tym również przez administratorów.

Wyłączenie lub nieskonfigurowanie tego ustawienia spowoduje, że każdy komputer będzie korzystać ze swojego lokalnego sufiksu podstawowej domeny DNS, którym jest zwykle nazwa DNS domeny Active Directory, do której ten komputer jest przyłączony. Administratorzy mogą jednak skorzystać z apletu System w Panelu sterowania, aby zmienić sufiks podstawowej domeny DNS komputera.

Aby użyć tego ustawienia, należy w dostępnym polu wpisać cały sufiks podstawowej domeny DNS, który ma zostać przypisany. Na przykład: microsoft.com.

To ustawienie nie powoduje wyłączenia okna dialogowego Sufiks domeny DNS i nazwa NetBIOS komputera, którego administratorzy używają do zmiany sufiksu podstawowej domeny DNS. Jeśli jednak to ustawienie będzie włączone i sufiks zostanie wprowadzony przez administratorów, zostanie on zignorowany.

Ważne: aby zmiany wprowadzone w tym ustawieniu zaczęły obowiązywać, należy ponownie uruchomić system Windows na wszystkich komputerach, których dotyczy to ustawienie.

Uwaga: aby zmienić sufiks podstawowej domeny DNS komputera bez określania ustawienia, należy kliknąć aplet System w Panelu sterowania, kliknąć kartę Identyfikacja sieciowa, kliknąć opcję Właściwości, kliknąć przycisk Więcej, a następnie w polu „Sufiks podstawowej domeny DNS tego komputera” wprowadzić sufiks.

Więcej informacji na temat systemu DNS można znaleźć w temacie Pomocy „System nazw domen”.</string>
      <string id="DNS_RegisterAdapterName">Rejestruj rekordy DNS za pomocą sufiksu DNS konkretnego połączenia</string>
      <string id="DNS_RegisterAdapterName_Help">Określa, czy komputer wykonujący dynamiczną rejestrację może rejestrować rekordy zasobów A i PTR z użyciem złączenia własnej nazwy komputera i sufiksu DNS konkretnego połączenia oprócz rejestrowania tych rekordów z użyciem złączenia własnej nazwy komputera i sufiksu podstawowej domeny DNS.

Ostrzeżenie: włączenie tego ustawienia grupowego dotyczy wszystkich połączeń sieciowych komputerów wieloadresowych, do których jest stosowane to ustawienie.

Domyślnie klient DNS przeprowadzający dynamiczną rejestrację w usłudze DNS rejestruje rekordy zasobów A i PTR z zastosowaniem złączenia własnej nazwy komputera i sufiksu podstawowej domeny DNS. Na przykład w wyniku złączenia nazwy komputera, takiego jak „mojkomputer”, i sufiksu podstawowej domeny DNS, takiej jak „microsoft.com”, powstanie ciąg „mojkomputer.microsoft.com”.

Gdyby to ustawienie zostało włączone, oprócz zarejestrowania rekordów zasobów A i PTR z zastosowaniem sufiksu podstawowej domeny DNS komputer zarejestrowałby rekordy zasobów A i PTR z zastosowaniem sufiksu domeny DNS konkretnego połączenia. Na przykład złączenie nazwy komputera „mojkomputer” i sufiksu DNS konkretnego połączenia „polaczenieVPN” byłoby użyte podczas rejestrowania rekordów zasobów A i PTR i w rezultacie powstałby ciąg „mojkomputer.polaczenieVPN”. Należy pamiętać, że jeśli dynamiczne rejestrowanie w usłudze DNS zostanie wyłączone na komputerze, na którym ustawienie jest stosowane, niezależnie od wartości tego ustawienia komputer nie podejmie próby przeprowadzenia dynamicznej rejestracji w usłudze DNS rekordów A i PTR, zawierających złączenie własnej nazwy komputera i sufiksu DNS konkretnego połączenia. Jeśli dynamiczne rejestrowanie w usłudze DNS zostanie wyłączone dla konkretnego połączenia sieciowego komputera, do którego jest stosowane to ustawienie, niezależnie od wartości tego ustawienia komputer nie podejmie próby przeprowadzenia dynamicznej rejestracji w usłudze DNS rekordów A i PTR zawierających złączenie własnej nazwy komputera i sufiksu DNS konkretnego połączenia.

Wyłączenie tego ustawienia spowoduje, że klient DNS nie zarejestruje rekordów zasobów A i PTR z zastosowaniem sufiksu DNS konkretnego połączenia.

Jeśli to ustawienie nie zostanie skonfigurowane, nie będzie stosowane na żadnych komputerach, a komputery będą używać swojej lokalnej konfiguracji.</string>
      <string id="DNS_RegisterReverseLookup">Rejestruj rekordy PTR</string>
      <string id="DNS_RegisterReverseLookup_DoNotRegister">Nie rejestruj</string>
      <string id="DNS_RegisterReverseLookup_Help">Określa, czy rejestracja rekordów zasobów PTR jest włączona dla komputerów, do których jest stosowana ta zasada.

Domyślnie klienci DNS skonfigurowani do przeprowadzania dynamicznej rejestracji w usłudze DNS podejmują próbę zarejestrowania rekordów zasobów PTR tylko wtedy, gdy zarejestrowanie odpowiadających im rekordów zasobów A zakończyło się pomyślnie. Rekordy zasobów „A” mapują nazwę DNS hosta na adres IP hosta, natomiast rekordy zasobów „PTR” mapują adres IP hosta na nazwę DNS hosta.

Aby włączyć to ustawienie, należy kliknąć opcję Włącz, a następnie wybrać jedną z następujących wartości:

Nie rejestruj — komputery nigdy nie będą podejmować próby zarejestrowania rekordów zasobów PTR.

Zarejestruj — komputery będą podejmować próbę zarejestrowania rekordów zasobów PTR bez względu na to, czy rejestracja rekordów A zakończyła się pomyślnie.

Rejestruj tylko wtedy, gdy rejestracja rekordów A powiedzie się — komputery będą podejmować próbę zarejestrowania rekordów zasobów PTR tylko wtedy, gdy rejestracja odpowiadających im rekordów A zakończyła się pomyślnie.

Jeśli to ustawienie nie zostanie skonfigurowane, nie będzie stosowane na żadnych komputerach, a komputery będą używać swojej lokalnej konfiguracji.</string>
      <string id="DNS_RegisterReverseLookup_Register">Zarejestruj</string>
      <string id="DNS_RegisterReverseLookup_RegisterOnlyIfASucceeds">Rejestruj tylko wtedy, gdy rejestracja rekordów A się powiedzie </string>
      <string id="DNS_RegistrationEnabled">Aktualizacja dynamiczna</string>
      <string id="DNS_RegistrationEnabled_Help">Określa, czy funkcja aktualizacji dynamicznej jest włączona.

Komputery skonfigurowane do obsługi aktualizacji dynamicznej automatycznie rejestrują i aktualizują swoje rekordy zasobów DNS na serwerze DNS.

Włączenie tego ustawienia spowoduje, że komputery, do których jest stosowane to ustawienie, będą mogły korzystać z dynamicznej rejestracji w usłudze DNS dla każdego ze swoich połączeń sieciowych w zależności od konfiguracji poszczególnych połączeń. Aby włączyć dynamiczną rejestrację w usłudze DNS dla określonego połączenia sieciowego, jest niezbędne i wystarczające, aby konfiguracja komputera i konfiguracja sieci zezwalały na dynamiczną rejestrację w usłudze DNS. To ustawienie steruje właściwościami komputera mającymi wpływ na dynamiczną rejestrację w usłudze DNS. Włączenie tego ustawienia spowoduje, że aktualizację dynamiczną będzie można ustawiać indywidualnie dla poszczególnych połączeń sieciowych.

Wyłączenie tego ustawienia spowoduje, że komputery, do których jest stosowane to ustawienie, nie będą mogły korzystać z dynamicznej rejestracji w usłudze DNS dla żadnego ze swoich połączeń sieciowych bez względu na konfigurację poszczególnych połączeń.

Jeśli to ustawienie nie zostanie skonfigurowane, nie będzie stosowane na żadnych komputerach, a komputery będą używać swojej lokalnej konfiguracji.</string>
      <string id="DNS_RegistrationOverwritesInConflict">Zamień adresy będące w konflikcie</string>
      <string id="DNS_RegistrationOverwritesInConflict_Help">Określa, czy klient DNS, który podejmuje próbę zarejestrowania rekordu zasobów A, powinien zastępować istniejące rekordy zasobów A, które zawierają adresy IP powodujące konflikty.

To ustawienie jest przeznaczone dla komputerów rejestrujących rekordy zasobów A w strefach DNS, które nie obsługują funkcji zabezpieczonej aktualizacji dynamicznej. Funkcja zabezpieczonej aktualizacji dynamicznej zachowuje własność rekordów zasobów i nie zezwala klientowi DNS na zastępowanie rekordów, które są zarejestrowane przez inne komputery.

Podczas dynamicznej aktualizacji strefy, która nie korzysta z funkcji zabezpieczonej aktualizacji dynamicznej, klient DNS może odkryć, że istniejący rekord zasobów A kojarzy nazwę DNS hosta klienta z adresem IP innego komputera. Zgodnie z konfiguracją domyślną klient DNS podejmuje próbę zastąpienia istniejącego rekordu zasobów A rekordem zasobów A kojarzącym nazwę DNS z adresem IP klienta.

Włączenie tego ustawienia spowoduje, że podczas aktualizacji dynamicznej klienci DNS będą podejmować próbę zastąpienia rekordów zasobów A powodujących konflikty.

Wyłączenie tego ustawienia spowoduje, że klient DNS będzie nadal przeprowadzać aktualizację dynamiczną rekordów zasobów A, ale jeśli klient DNS podejmie próbę zaktualizowania rekordów zasobów zawierających konflikty, próba zakończy się niepowodzeniem i w dzienniku Podglądu zdarzeń zostanie zarejestrowany błąd.

Jeśli to ustawienie nie zostanie skonfigurowane, nie będzie stosowane na żadnych komputerach, a komputery będą używać swojej lokalnej konfiguracji.</string>
      <string id="DNS_RegistrationRefreshInterval">Interwał odświeżania rejestracji</string>
      <string id="DNS_RegistrationRefreshInterval_Help">Określa interwał odświeżania rejestracji rekordów zasobów A i PTR dla komputerów, do których to ustawienie jest stosowane. To ustawienie może być stosowane tylko do komputerów korzystających z aktualizacji dynamicznej.

Komputery z zainstalowanym systemem Windows 2000 Professional lub Windows XP Professional, skonfigurowane do przeprowadzania dynamicznej rejestracji rekordów zasobów A i PTR w usłudze DNS, okresowo ponownie rejestrują swoje rekordy na serwerach DNS, nawet jeśli dane w tych rekordach nie uległy zmianie. Rejestracja jest wymagana, aby wskazać serwerom DNS, skonfigurowanym do automatycznego usuwania (oczyszczania) starych rekordów, że te rekordy są bieżące i powinny zostać zachowane w bazie danych.

Ostrzeżenie: Jeśli rekordy zasobów usługi DNS są zarejestrowane w strefach o włączonym oczyszczaniu, wartość tego ustawienia nie powinna być nigdy większa niż interwał odświeżania skonfigurowany dla tych stref. Ustawienie dłuższego interwału odświeżania rejestracji niż interwał odświeżania stref DNS może spowodować niepożądane usunięcie rekordów zasobów A i PTR.

Aby określić interwał odświeżania rejestracji, należy kliknąć opcję Włącz, a następnie wprowadzić wartość większą niż 1800. Należy pamiętać, że ta wartość określa interwał odświeżania rejestracji w sekundach, na przykład wartość 1800 oznacza 30 minut.

Jeśli to ustawienie nie zostanie skonfigurowane, nie będzie stosowane na żadnych komputerach, a komputery będą używać swojej lokalnej konfiguracji.</string>
      <string id="DNS_RegistrationTtl">Ustawiony czas wygaśnięcia (TTL, Time to live) w rekordach A i PTR</string>
      <string id="DNS_RegistrationTtl_Help">Określa wartość pola Czas wygaśnięcia w rekordach zasobów A i PTR zarejestrowanych przez komputery, do których jest stosowane to ustawienie.

Aby określić czas wygaśnięcia, należy kliknąć opcję Włącz, a następnie wprowadzić wartość w sekundach (na przykład wartość 900 oznacza 15 minut).

Nieskonfigurowanie tego ustawienia spowoduje, że nie zostanie ono zastosowane do żadnego komputera.</string>
      <string id="DNS_SearchList">Lista przeszukiwania sufiksów DNS</string>
      <string id="DNS_SearchList_Help">Określa sufiksy DNS, które mają zastać dołączone do niekwalifikowanej nazwy o pojedynczej etykiecie przed przesłaniem zapytania dotyczącego tej nazwy do usługi DNS.

Niekwalifikowana nazwa o pojedynczej etykiecie nie zawiera kropek, jak w nazwie „przyklad”. Taka nazwa różni się od w pełni kwalifikowanej nazwy domeny, takiej jak „przyklad.microsoft.com”.

Po włączeniu tego ustawienia, gdy użytkownik przesyła zapytanie dotyczące nazwy o pojedynczej etykiecie, takiej jak „przyklad”, przed wysłaniem zapytania do serwera DNS lokalny klient DNS dołącza do tej nazwy sufiks, taki jak „microsoft.com”, w wyniku czego powstaje zapytanie dotyczące nazwy „przyklad.microsoft.com”.

Włączenie tego ustawienia spowoduje, że przed przesłaniem zapytania dotyczącego niekwalifikowanej nazwy o pojedynczej etykiecie będzie można określić sufiksy DNS, które mają zostać dołączone do tej nazwy. Wartości sufiksów DNS w tym ustawieniu mogą zostać ustawione za pomocą ciągów rozdzielanych przecinkami, na przykład „microsoft.com,serverua.microsoft.com,office.microsoft.com”. W przypadku każdego przesłania zapytania dołączany jest jeden sufiks DNS. Jeśli zapytanie nie powiedzie się, w miejsce nieprawidłowego sufiksu dodawany jest nowy sufiks DNS i przesyłane jest nowe zapytanie. Wartości są używane w takiej kolejności, w jakiej pojawiają się w ciągu, począwszy od wartości znajdującej się najdalej po lewej stronie i kontynuując do strony prawej.

W przypadku włączenia tego ustawienia należy określić co najmniej jeden sufiks.

Wyłączenie tego ustawienia spowoduje, że do zapytań dotyczących nazw niekwalifikowanych będzie dołączany sufiks podstawowej domeny DNS oraz sufiksy DNS konkretnego połączenia.

Jeśli to ustawienie nie zostanie skonfigurowane, nie będzie stosowane na żadnych komputerach, a komputery będą używać swojej lokalnej konfiguracji.</string>
      <string id="DNS_UpdateSecurityLevel">Poziom zabezpieczeń aktualizacji</string>
      <string id="DNS_UpdateSecurityLevel_Help">Określa, czy komputery, do których jest stosowane to ustawienie, korzystają z zabezpieczonej aktualizacji dynamicznej czy standardowej aktualizacji dynamicznej w celu przeprowadzenia rejestracji rekordów DNS.

Aby włączyć to ustawienie, należy kliknąć opcję Włącz, a następnie wybrać jedną z następujących wartości:

Zabezpieczone po niezabezpieczonych — wybranie tej opcji spowoduje, że komputery będą wysyłać zabezpieczone aktualizacje dynamiczne tylko w przypadku odmowy wysłania niezabezpieczonych aktualizacji dynamicznych.

Tylko niezabezpieczone — wybranie tej opcji spowoduje, że komputery będą wysyłać tylko niezabezpieczone aktualizacje dynamiczne.

Tylko zabezpieczone — wybranie tej opcji spowoduje, że komputery będą wysyłać tylko zabezpieczone aktualizacje dynamiczne.

Jeśli to ustawienie nie zostanie skonfigurowane, nie będzie stosowane na żadnych komputerach, a komputery będą używać swojej lokalnej konfiguracji.</string>
      <string id="DNS_UpdateSecurityLevel_OnlySecure">Tylko zabezpieczone</string>
      <string id="DNS_UpdateSecurityLevel_OnlyUnsecure">Tylko niezabezpieczone</string>
      <string id="DNS_UpdateSecurityLevel_UnsecureFollowedBySecure">Zabezpieczone po niezabezpieczonych</string>
      <string id="DNS_UpdateTopLevelDomainZones">Aktualizuj strefy domen najwyższego poziomu</string>
      <string id="DNS_UpdateTopLevelDomainZones_Help">Określa, czy komputery, do których jest stosowane to ustawienie, mogą wysyłać aktualizacje dynamiczne do stref mających nazwy o pojedynczej etykiecie, zwanych również strefami domen najwyższego poziomu, na przykład „com”.

Domyślnie klient DNS skonfigurowany do przeprowadzania aktualizacji dynamicznych DNS wysyła takie aktualizacje do strefy DNS, która jest autorytatywna dla swoich rekordów zasobów DNS, o ile ta strefa autorytatywna nie jest strefą domeny najwyższego poziomu i strefą główną.

Włączenie tego ustawienia spowoduje, że komputery, do których jest stosowana ta zasada, będą wysyłać aktualizacje dynamiczne do dowolnej strefy, która jest autorytatywna dla rekordów zasobów, które komputer musi zaktualizować, z wyjątkiem strefy głównej.

Wyłączenie tego ustawienia spowoduje, że komputery, do których jest stosowana ta zasada, nie będą wysyłać aktualizacji dynamicznych do stref głównych i/lub stref domeny najwyższego poziomu, które są autorytatywne dla rekordów zasobów, które komputer musi zaktualizować.

Jeśli to ustawienie nie zostanie skonfigurowane, nie będzie stosowane na żadnych komputerach, a komputery będą używać swojej lokalnej konfiguracji.</string>
      <string id="DNS_UseDomainNameDevolution">Dewolucja sufiksów podstawowej domeny DNS</string>
      <string id="DNS_UseDomainNameDevolution_Help">Określa, czy w procesie rozpoznawania nazw klient DNS wykonuje dewolucję sufiksów podstawowej domeny DNS.

Gdy użytkownik przesyła zapytanie dotyczące nazwy o jednej etykiecie, takiej jak „przyklad”, przed wysłaniem zapytania do serwera DNS lokalny klient DNS dołącza do tej nazwy sufiks, taki jak „microsoft.com”, w wyniku czego powstaje zapytanie dotyczące nazwy „przyklad.microsoft.com”.

Jeżeli nie została określona lista przeszukiwania sufiksów DNS, klient DNS dołącza do nazwy o jednej etykiecie sufiks podstawowej domeny DNS; jeśli wykonywanie zapytania zakończy się niepowodzeniem, dla nowego zapytania jest dołączany sufiks DNS specyficzny dla połączenia. Jeżeli żadne z tych zapytań nie zwróci wyników, klient wykona dewolucję sufiksu podstawowej domeny DNS komputera (opuści znajdującą się po lewej stronie etykietę sufiksu podstawowej domeny DNS), dołączy ten przekazany sufiks podstawowej domeny DNS do nazwy o jednej etykiecie i prześle to nowe zapytanie do serwera DNS.

Jeżeli na przykład sufiks podstawowej domeny DNS ooo.aaa.microsoft.com zostanie dołączony do niezakończonej kropką nazwy o jednej etykiecie „przyklad”, a zapytanie  DNS dotyczące nazwy przyklad.ooo.aaa.microsoft.com nie zwróci wyników, klient DNS przeprowadzi dewolucję sufiksu podstawowej domeny DNS (opuści etykietę znajdującą się po lewej stronie) i prześle zapytanie dotyczące nazwy przyklad.aaa.microsoft.com. Jeżeli to zapytanie nie zwróci wyników, zostanie przeprowadzona dalsza dewolucja sufiksu podstawowej domeny DNS, o ile jest możliwe osiągnięcie określonego poziomu dewolucji, i zostanie przesłane zapytanie dotyczące nazwy przyklad.microsoft.com. Jeżeli to zapytanie nie zwróci wyników, dewolucja będzie kontynuowana w przypadku, gdy nie osiągnięto określonego poziomu dewolucji, i zostanie przesłane zapytanie dotyczące nazwy przyklad.microsoft.com. Sufiks podstawowej domeny DNS nie może być dewoluowany poniżej poziomu dewolucji.

Jeżeli to ustawienie zostanie włączone z odpowiednim poziomem dewolucji, klienci DNS na komputerach, na których jest stosowane to ustawienie, będą podejmować próby rozpoznawania nazw stanowiących złączenie nazwy o jednej etykiecie, która ma zostać rozpoznana, i zdewoluowanego sufiksu podstawowej domeny DNS.

Jeżeli to ustawienie zostanie wyłączone, klienci DNS na komputerach, na których jest stosowane to ustawienie, nie będą podejmować prób rozpoznawania nazw stanowiących złączenie nazwy o jednej etykiecie, która ma zostać rozpoznana, i zdewoluowanego sufiksu podstawowej domeny DNS.

Jeżeli to ustawienie nie zostanie skonfigurowane, nie będzie stosowane na żadnych komputerach, a komputery będą używać swojej lokalnej konfiguracji.</string>
      <string id="DNS_DomainNameDevolutionLevel">Poziom dewolucji sufiksów podstawowej domeny DNS</string>
      <string id="DNS_DomainNameDevolutionLevel_Help">To ustawienie zasad określa poziom dewolucji sufiksów podstawowej domeny DNS, którego będą używać klienci DNS, gdy będą wykonywać dewolucję sufiksów podstawowej domeny DNS w ramach procesu rozpoznawania nazw. Gdy jest włączona dewolucja sufiksów domeny DNS, etykieta znajdująca się z lewej strony sufiksu podstawowej domeny DNS jest odrzucana po każdej pomyślnie zakończonej próbie wykonania zapytania, które nie zwróci wyniku dla nazwy, do której został dołączony sufiks podstawowej domeny DNS. Poziom dewolucji wskazuje minimalną liczbę etykiet, które należy dodać do ciągu zapytania po zdewoluowaniu sufiksu podstawowej domeny DNS.

Gdy użytkownik przesyła zapytanie dotyczące nazwy o jednej etykiecie, takiej jak „przyklad”, przed wysłaniem zapytania do serwera DNS lokalny klient DNS dołącza do tej nazwy sufiks, taki jak „microsoft.com”. W tym przypadku wynikiem tej akcji będzie zapytanie dotyczące nazwy przyklad.microsoft.com.
 
Jeżeli nie została określona lista przeszukiwania sufiksów DNS, klient DNS dołącza do nazwy o jednej etykiecie sufiks podstawowej domeny DNS. Jeżeli to zapytanie nie zwróci wyników, dla nowego zapytania jest dołączany sufiks DNS specyficzny dla połączenia. Jeżeli żadne z tych zapytań nie zwróci wyników, klient wykona dewolucję sufiksu podstawowej domeny DNS komputera, dołączy ten zdewoluowany sufiks podstawowej domeny DNS do nazwy o jednej etykiecie i prześle to nowe zapytanie do serwera DNS.

Jeżeli na przykład sufiks podstawowej domeny DNS aaa.bbb.ccc.ddd.microsoft.com zostanie dołączony do nazwy o jednej etykiecie „przyklad” (bez kropki na końcu) i jeśli jest włączona dewolucja sufiksów domeny DNS, a jej poziom jest równy 3, zostaną uruchomione następujące zapytania:  

przyklad.aaa.bbb.ccc.ddd.microsoft.com (jeśli to zapytanie nie zwróci wyników, dla następnego zapytania sufiks podstawowej domeny DNS zostanie zdewoluowany do postaci bbb.ccc.ddd.microsoft.com)

przyklad.bbb.ccc.ddd.microsoft.com (jeśli to zapytanie nie zwróci wyników, dla następnego zapytania sufiks podstawowej domeny DNS zostanie zdewoluowany do postaci ccc.ddd.microsoft.com)

przyklad.ccc.ddd.microsoft.com (jeśli to zapytanie nie zwróci wyników, dla następnego zapytania sufiks podstawowej domeny DNS zostanie zdewoluowany do postaci ddd.microsoft.com)

przyklad.ddd.microsoft.com (jeśli to zapytanie nie zwróci wyników, nie będą tworzone kolejne zapytania, ponieważ dla poziomu dewolucji ustawiono wartość 3, a sufiks podstawowej domeny DNS zawiera 3 etykiety)
  
Jeżeli to ustawienie zasad zostanie włączone, klienci DNS na komputerach, do których jest stosowane to ustawienie, będą podejmować próby rozpoznawania nazw będących połączeniem nazwy o jednej etykiecie, która ma zostać rozpoznana, oraz zdewoluowanego sufiksu podstawowej domeny DNS. Klienci DNS będą dewoluować sufiks podstawowej domeny DNS przy każdej próbie wykonania zapytania do czasu, gdy nazwa zostanie pomyślnie rozpoznana, zostanie osiągnięty poziom dewolucji określony w tym ustawieniu lub sufiks podstawowej domeny DNS będzie zawierał dwie etykiety.
 
Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, klienci DNS na komputerach, do których jest stosowane to ustawienie, nie będą podejmować prób rozpoznawania nazw będących połączeniem nazwy o jednej etykiecie, która ma zostać rozpoznana, i zdewoluowanego sufiksu podstawowej domeny DNS. Jeżeli istnieje domena główna lasu, nie skonfigurowano listy wyszukiwania, a zapytanie dotyczy nazwy o jednej etykiecie, do czasu pomyślnego rozpoznania nazwy klient DNS będzie wykonywał dewolucję do nazwy domeny głównej lasu.</string>
      <string id="DNS_TurnOffMulticast">Wyłącz rozpoznawanie nazw z obsługą multiemisji</string>
      <string id="DNS_TurnOffMulticast_Help">Protokół LLMNR (Local Link Multicast Name Resolution) jest pomocniczym protokołem umożliwiającym rozpoznawanie nazw. Zapytania są wysyłane za pośrednictwem łącza lokalnego (pojedyncza podsieć) z komputera klienckiego korzystającego z multiemisji; odpowiedzi może udzielić inny klient korzystający z tego samego łącza, który ma również włączoną obsługę protokołu LLMNR. Protokół LLMNR zapewnia rozpoznawanie nazw w scenariuszach, w których przeprowadzanie konwencjonalnego rozpoznawania nazw DNS nie jest możliwe.

Włączenie tego ustawienia zasad spowoduje, że funkcja rozpoznawania nazw z obsługą multiemisji (protokół LLMNR) zostanie wyłączona dla komputera na wszystkich dostępnych, ale nieskonfigurowanych kartach sieciowych.

Wyłączenie tego ustawienia zasad spowoduje, że funkcja rozpoznawania nazw z obsługą multiemisji (protokół LLMNR) zostanie włączona dla komputera na wszystkich dostępnych, ale nieskonfigurowanych kartach sieciowych.

Nieskonfigurowanie tego ustawienia zasad spowoduje, że funkcja rozpoznawania nazw z obsługą multiemisji (protokół LLMNR) zostanie domyślnie włączona dla komputera na wszystkich dostępnych, ale nieskonfigurowanych kartach sieciowych.</string>
      <string id="DNS_AppendToMultiLabelName">Zezwalaj na dołączanie sufiksu DNS do zapytań z niekwalifikowanymi nazwami o wielu etykietach</string>
      <string id="DNS_AppendToMultiLabelName_Help">To ustawienie określa, czy podlegające mu komputery mogą dołączać sufiksy do niekwalifikowanej nazwy o wielu etykietach przed wysłaniem kolejnych zapytań DNS, jeżeli zapytanie z nazwą oryginalną nie powiedzie się.

Niekwalifikowana nazwa o wielu etykietach to taka nazwa, która zawiera kropki, ale nie kończy się kropką, np. „serwer.corp”. Nazwa w pełni kwalifikowana ma na końcu kropkę, np.: „serwer.corp.contoso.com.”.

Po włączeniu tego ustawienia dopuszczalne będzie dołączanie sufiksów do niekwalifikowanej nazwy o wielu etykietach, jeżeli nie powiedzie się zapytanie z oryginalną nazwą. Przykład: klient DSN najpierw przeprowadzi zapytanie, używając niekwalifikowanej nazwy o wielu etykietach  „serwer.corp”. Jeżeli zapytanie zakończy się powodzeniem, do klienta zostanie zwrócona odpowiedź. Jeżeli zapytanie nie powiedzie się, to do niekwalifikowanej nazwy z wieloma etykietami będą dołączane sufiksy DNS, skonfigurowane na danym komputerze. Sufiksy te mogą pochodzić z kombinacji sufiksu domeny podstawowej lokalnego klienta DNS, sufiksu domeny właściwej dla połączenia albo z listy wyszukiwania sufiksów DNS.

Przykład: jeżeli lokalny klient DNS otrzyma zapytanie o nazwę „serwer.corp”, a sufiks domeny podstawowej to „contoso.com”, to opisywane ustawienie spowoduje, że w razie niepowodzenia zapytania o nazwę „serwer.corp”, klient DNS wyśle zapytanie o nazwę „serwer.corp.contoso.com”. 

Jeżeli to ustawienie nie zostanie włączone, to w razie niepowodzenia zapytania z oryginalną nazwą, do niekwalifikowanej nazwy o wielu etykietach nie są dołączane żadne sufiksy. 

Jeżeli to ustawienie nie zostanie skonfigurowane, komputery będą korzystać z konfiguracji lokalnego klienta DNS, która określi zachowanie zapytań z niekwalifikowanymi nazwami o wielu etykietach.</string>
    </stringTable>
    <presentationTable>
      <presentation id="DNS_Domain">
        <textBox refId="DNS_DomainLabel">
          <label>Sufiks DNS:</label>
        </textBox>
      </presentation>
      <presentation id="DNS_NameServer">
        <textBox refId="DNS_NameServerLabel">
          <label>Adresy IP:</label>
        </textBox>
      </presentation>
      <presentation id="DNS_PrimaryDnsSuffix">
        <textBox refId="DNS_PrimaryDnsSuffixBox">
          <label>Wprowadź sufiks podstawowej domeny DNS:</label>
        </textBox>
      </presentation>
      <presentation id="DNS_RegisterReverseLookup">
        <dropdownList refId="DNS_RegisterReverseLookup_Box" defaultItem="0">Rejestruj rekordy PTR:</dropdownList>
      </presentation>
      <presentation id="DNS_RegistrationRefreshInterval">
        <decimalTextBox refId="DNS_RegistrationRefreshIntervalLabel" defaultValue="1800" spinStep="100">Sekundy:</decimalTextBox>
      </presentation>
      <presentation id="DNS_DomainNameDevolutionLevel">
        <decimalTextBox refId="DNS_DomainNameDevolutionLevelLabel" defaultValue="2" spinStep="1">Ustaw poziom dewolucji sufiksów podstawowej domeny DNS</decimalTextBox>
      </presentation>
      <presentation id="DNS_RegistrationTtl">
        <decimalTextBox refId="DNS_RegistrationTtlLabel" defaultValue="600" spinStep="100">Sekundy:</decimalTextBox>
      </presentation>
      <presentation id="DNS_SearchList">
        <textBox refId="DNS_SearchListLabel">
          <label>Sufiksy DNS:</label>
        </textBox>
      </presentation>
      <presentation id="DNS_UpdateSecurityLevel">
        <dropdownList refId="DNS_UpdateSecurityLevel_Box" defaultItem="2">Poziom zabezpieczeń aktualizacji:</dropdownList>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
