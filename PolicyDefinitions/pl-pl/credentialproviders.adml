<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>tutaj wprowadź wyświetlaną nazwę</displayName>
  <description>tutaj wprowadź opis</description>
  <resources>
    <stringTable>
      <string id="DefaultLogonDomain">Przypisz domyślną domenę logowania</string>
      <string id="DefaultLogonDomain_Help">To ustawienie zasad określa domyślną domenę logowania, która może się różnić od domeny, do której należy komputer. Gdy ta zasada nie obowiązuje, jeśli podczas logowania użytkownik nie określi domeny logowania, nastąpi zalogowanie w domenie, do której należy komputer. Na przykład, jeśli komputer należy do domeny Fabrikam, domyślną domeną logowania będzie właśnie Fabrikam.

Jeśli to ustawienie zasad zostanie włączone, domyślną domeną logowania będzie wskazana domena, która może się od różnić od domeny, do której należy komputer.

Jeśli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, domyślną domeną logowania będzie zawsze domena komputera.</string>
      <string id="ExcludedCredentialProviders">Wyklucz dostawców poświadczeń</string>
      <string id="ExcludedCredentialProviders_Help">To ustawienie zasad umożliwia administratorowi wykluczanie
określonych dostawców poświadczeń w trakcie procedury uwierzytelniania.

Uwaga: dostawcy poświadczeń odpowiadają za wykonywanie operacji na poświadczeniach
użytkownika i sprawdzanie ich poprawności w trakcie logowania oraz w sytuacjach, gdy jest wymagane uwierzytelnianie.
System Windows Vista jest domyślnie wyposażony w dwa mechanizmy dostawców poświadczeń:
hasła i karty inteligentne. Administrator może zainstalować dodatkowych dostawców,
którzy będą odpowiadać za obsługę różnych innych zbiorów poświadczeń
(np. związanych z uwierzytelnianiem przy użyciu biometrii).

Jeśli ta zasada zostanie włączona, administrator może określić identyfikatory CLSID
zainstalowanych dostawców poświadczeń, którzy mają być wykluczeni
z procesów uwierzytelniania.

Jeśli ta zasada zostanie wyłączona lub nie zostanie skonfigurowana, wszyscy dostawcy będą dostępni na potrzeby uwierzytelniania.</string>
      <string id="Logon">Logowanie</string>
    </stringTable>
    <presentationTable>
      <presentation id="DefaultLogonDomain">
        <textBox refId="DefaultLogonDomain_Message">
          <label>Domyślna domena logowania:</label>
        </textBox>
        <text>Wprowadź nazwę żądanej domeny.</text>
      </presentation>
      <presentation id="ExcludedCredentialProviders">
        <textBox refId="ExcludedCredentialProviders_Message">
          <label>Wyklucz następujących dostawców poświadczeń:</label>
        </textBox>
        <text>Wprowadź identyfikatory CLSID (rozdzielając je przecinkami) dostawców poświadczeń,
którzy mają być wykluczeni z procesu uwierzytelniania.

Na przykład: {ba0dd1d5-9754-4ba3-973c-40dce7901283},{383f1aa4-65dd-45bc-9f5a-ddd2f222f07d}</text>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
