<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>tutaj wprowadź wyświetlaną nazwę</displayName>
  <description>tutaj wprowadź opis</description>
  <resources>
    <stringTable>
      <string id="WinMSG_NoAutoStartWindowsMsg_Comp">Na początku nie uruchamiaj automatycznie programu Windows Messenger</string>
      <string id="WinMSG_NoAutoStartWindowsMsg_Help">Program Windows Messenger jest automatycznie ładowany i uruchamiany po zalogowaniu użytkownika do komputera z systemem Windows XP. Tego ustawienia można użyć, aby zapobiec automatycznemu uruchamianiu programu Windows Messenger po zalogowaniu.

Jeśli to ustawienie zostanie włączone, program Windows Messenger nie będzie automatycznie ładowany po zalogowaniu użytkownika.

Jeśli to ustawienie zostanie wyłączone lub pozostanie nieskonfigurowane, program Windows Messenger będzie automatycznie ładowany po zalogowaniu.

Uwaga: To ustawienie zapobiega początkowemu uruchamianiu programu Windows Messenger. Jeśli użytkownik później wywoła program Windows Messenger i zacznie go używać, począwszy od tej chwili program Windows Messenger będzie ładowany.

Użytkownik może również skonfigurować takie zachowanie na karcie Preferencje w menu Narzędzia w interfejsie użytkownika programu Windows Messenger.

Uwaga: Aby użytkownicy nie mogli używać programu Windows Messenger, należy włączyć ustawienie „Nie zezwalaj na uruchamianie programu Windows Messenger”.

Uwaga: To ustawienie jest dostępne w folderach Konfiguracja komputera i Konfiguracja użytkownika. Jeśli oba będą obecne, pierwszeństwo będzie miała wersja ustawienia z folderu Konfiguracja komputera.</string>
      <string id="WinMSG_NoAutoStartWindowsMsg_User">Na początku nie uruchamiaj automatycznie programu Windows Messenger</string>
      <string id="WinMSG_NoWindowsMsg_Comp">Nie zezwalaj na uruchamianie programu Windows Messenger</string>
      <string id="WinMSG_NoWindowsMsg_Help">Umożliwia wyłączenie programu Windows Messenger.

Jeśli to ustawienie zostanie włączone, program Windows Messenger nie będzie uruchamiany.

Jeśli to ustawienie zostanie wyłączone lub nie będzie skonfigurowane, program Windows Messenger może być używany.

Uwaga: Jeśli to ustawienie zostanie włączone, użytkownicy Pomocy zdalnej również nie będą mogli korzystać z programu Windows Messenger.

Uwaga: To ustawienie jest dostępne w folderach Konfiguracja komputera i Konfiguracja użytkownika. Jeśli oba będą obecne, pierwszeństwo będzie miała wersja ustawienia z folderu Konfiguracja komputera.</string>
      <string id="WinMSG_NoWindowsMsg_User">Nie zezwalaj na uruchamianie programu Windows Messenger</string>
      <string id="WinMSG_WindowsMsg">Windows Messenger</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
