<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>tutaj wprowadź wyświetlaną nazwę</displayName>
  <description>tutaj wprowadź opis</description>
  <resources>
    <stringTable>
      <string id="DisableNamedPipeShutdownPolicyDescription">Wyłącz starszy interfejs zdalnego zamykania systemu</string>
      <string id="DisableNamedPipeShutdownPolicyDescription_Help">Ta zasada steruje starszym interfejsem zdalnego zamykania systemu (za pomocą nazwanego potoku). Interfejs zdalnego zamykania systemu za pomocą nazwanego potoku jest wymagany do zamykania tego systemu ze zdalnych systemów Windows XP lub Windows Server 2003.

Jeśli to ustawienie jest włączone, w systemie nie zostaje utworzony interfejs zdalnego zamykania systemu za pomocą nazwanego potoku.

Jeśli to ustawienie jest wyłączone lub nie zostało skonfigurowane, w systemie zostanie utworzony interfejs zdalnego zamykania systemu za pomocą nazwanego potoku.</string>
      <string id="Shutdown">Opcje zamykania systemu</string>
      <string id="ShutdownTimeoutHungSessionsDescription">Limit czasu dla zawieszonych sesji logowania podczas zamykania systemu</string>
      <string id="ShutdownTimeoutHungSessionsDescription_Help">Liczba minut, jaką system oczekuje na zawieszoną sesję logowania przez kontynuowaniem zamykania systemu.

Jeśli to ustawienie jest włączone, system będzie czekać na zawieszoną sesję logowania przez określoną liczbę minut.

Jeśli to ustawienie jest wyłączone lub nie zostało skonfigurowane, domyślną wartością limitu czasu są 3 minuty dla stacji roboczych i 15 minut dla serwerów.</string>
    </stringTable>
    <presentationTable>
      <presentation id="ShutdownTimeoutHungSessionsDescription">
        <decimalTextBox refId="ShutdownSessionTimeout_Time" defaultValue="9999">Limit czasu zawieszonej sesji w minutach:</decimalTextBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
