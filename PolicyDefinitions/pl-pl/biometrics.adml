<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2008 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>Ustawienia konfiguracji biometrii</displayName>
  <description>Ustawienia konfiguracji biometrii</description>
  <resources>
    <stringTable>
      <string id="BiometricsConfiguration">Biometria</string>
      <string id="Biometrics_EnableBio">Zezwalaj na używanie biometrii</string>
      <string id="Biometrics_EnableBio_Help">To ustawienie zasad umożliwia włączenie lub wyłączenie uruchamiania Usługi biometrycznej systemu Windows na tym komputerze.
        
Jeśli to ustawienie zasad zostanie włączone lub pozostanie nieskonfigurowane, Usługa biometryczna systemu Windows będzie dostępna, a użytkownicy będą mogli uruchamiać w systemie Windows aplikacje używające biometrii. Jeśli chcesz włączyć możliwość logowania przy użyciu biometrii, musisz też skonfigurować ustawienie zasad Zezwalaj użytkownikom na logowanie przy użyciu biometrii.

Jeśli to ustawienie zasad zostanie wyłączone, Usługa biometryczna systemu Windows będzie niedostępna, a użytkownicy nie będą mogli używać funkcji biometrii w systemie Windows.

Uwaga: użytkownicy logujący się przy użyciu biometrii powinni utworzyć dysk odzyskiwania hasła, dzięki któremu będzie można zapobiec utracie danych w przypadku zapomnienia poświadczeń logowania.
      
      </string>
      <string id="Biometrics_EnableCredProv">Zezwalaj użytkownikom na logowanie przy użyciu biometrii</string>
      <string id="Biometrics_EnableCredProv_Help">To ustawienie zasad określa, czy użytkownicy mogą logować się lub podwyższać poziom uprawnień Kontroli konta użytkownika przy użyciu biometrii.  Domyślnie użytkownicy lokalni będą mogli logować się do komputera lokalnego, ale aby użytkownicy domeny mogli logować się do domeny, należy włączyć ustawienie zasad Zezwalaj użytkownikom domeny na logowanie przy użyciu biometrii.

Jeśli to ustawienie zasad zostanie włączone lub pozostanie nieskonfigurowane, wszyscy użytkownicy będą mogli logować się do komputera lokalnego z systemem Windows i podwyższać poziom uprawnień przy użyciu funkcji Kontrola konta użytkownika za pomocą biometrii.

Jeśli to ustawienie zasad zostanie wyłączone, żaden użytkownik nie będzie mógł używać biometrii w celu logowania się do komputera lokalnego z systemem Windows.

Uwaga: użytkownicy logujący się przy użyciu biometrii powinni utworzyć dysk odzyskiwania hasła, dzięki któremu będzie można zapobiec utracie danych w przypadku zapomnienia poświadczeń logowania.

</string>
      <string id="Biometrics_EnableDomainCredProv">Zezwalaj użytkownikom domeny na logowanie przy użyciu biometrii</string>
      <string id="Biometrics_EnableDomainCredProv_Help">To ustawienie zasad określa, czy użytkownicy z kontami w domenie mogą logować się lub podwyższać poziom uprawnień przy użyciu funkcji Kontrola konta użytkownika za pomocą biometrii.

Domyślnie użytkownicy domeny nie mogą używać biometrii w celu logowania. Jeśli to ustawienie zasad zostanie włączone, użytkownicy domeny będą mogli logować się do komputera z systemem Windows przyłączonego do domeny przy użyciu biometrii.  W zależności od używanej biometrii włączenie tego ustawienia zasad może osłabić zabezpieczenia użytkowników logujących się przy użyciu biometrii.

Jeśli to ustawienie zasad zostanie wyłączone lub pozostanie nieskonfigurowane, użytkownicy domeny nie będą mogli logować się do komputera z systemem Windows przy użyciu biometrii.

Uwaga: użytkownicy logujący się przy użyciu biometrii powinni utworzyć dysk odzyskiwania hasła, dzięki któremu będzie można zapobiec utracie danych w przypadku zapomnienia poświadczeń logowania.

</string>
      <string id="Biometrics_FUSTimeout">Określ limit czasu zdarzeń szybkiego przełączania użytkowników</string>
      <string id="Biometrics_FUSTimeout_Help">To ustawienie zasad określa liczbę sekund, przez którą oczekujące zdarzenie szybkiego przełączania użytkowników pozostanie aktywne przed zainicjowaniem przełączenia.  Domyślnie zdarzenie szybkiego przełączania użytkowników jest aktywne przez 10 sekund, a potem staje się nieaktywne.

Jeśli to ustawienie zasad zostanie włączone, będzie można skonfigurować limit czasu zdarzeń szybkiego przełączania użytkowników w celu określenia liczby sekund, przez którą te zdarzenia będą aktywne. Ta wartość nie może przekroczyć 60 sekund.

Jeśli to ustawienie zasad zostanie wyłączone lub pozostanie nieskonfigurowane, limity czasu zdarzeń szybkiego przełączania użytkowników będą równe wartości domyślnej 10 sekund.

</string>
    </stringTable>
    <presentationTable>
      <presentation id="Biometrics_FUSTimeout">
          <decimalTextBox refId="Pol_Biometrics_FUSTimeout" defaultValue="10">Limit czasu w sekundach:</decimalTextBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
