<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>tutaj wprowadź wyświetlaną nazwę</displayName>
  <description>tutaj wprowadź opis</description>
  <resources>
    <stringTable>
      <string id="DFSDiscoverDC">Określ, jak często klient DFS odnajduje kontrolery domeny</string>
      <string id="DFSDiscoverDC_Help">Umożliwia skonfigurowanie częstotliwości podejmowania przez klienta rozproszonego systemu plików (DFS, Distributed File System) próby odnalezienia kontrolerów domeny w sieci.

Domyślnie klient DFS podejmuje próbę odnalezienia kontrolerów domeny co 15 minut.

Jeśli to ustawienie zostanie włączone, będzie można skonfigurować częstotliwość podejmowania przez klienta DFS próby odnalezienia kontrolerów domeny. Wartość ta jest podawana w minutach.

Jeśli to ustawienie zostanie wyłączone lub nie zostanie skonfigurowane, zostanie zastosowana wartość wynosząca 15 minut.

Uwaga: Minimalna wartość, jaką można wybrać, to 15 minut. Jeśli użytkownik spróbuje ustawić tę wartość na mniej niż 15 minut, zostanie zastosowana wartość domyślna wynosząca 15 minut.</string>
    </stringTable>
    <presentationTable>
      <presentation id="DFSDiscoverDC">
        <decimalTextBox refId="DFSDiscoverDialog" defaultValue="15">Czas w minutach:</decimalTextBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
