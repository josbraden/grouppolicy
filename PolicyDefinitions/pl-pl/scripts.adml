<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>tutaj wprowadź wyświetlaną nazwę</displayName>
  <description>tutaj wprowadź opis</description>
  <resources>
    <stringTable>
      <string id="MaxGPOScriptWaitPolicy">Maksymalny czas oczekiwania na skrypty zasad grupy</string>
      <string id="MaxGPOScriptWaitPolicy_Help">Określa, jak długo system czeka na skrypty stosowane przez zasady grupy do uruchamiania.

To ustawienie ogranicza całkowity dopuszczalny czas zakończenia działania dla wszystkich skryptów logowania, uruchamiania i zamykania stosowanych przez zasady grupy. Jeśli skrypty nie zakończą działania przed upływem określonego czasu, system zatrzyma przetwarzanie skryptów i zapisze zdarzenie błędu.

Domyślnie system pozwala łączonemu zestawowi skryptów działać do 600 sekund (10 minut), ale można użyć tego ustawienia, aby dopasować ten interwał.

Aby użyć tego ustawienia, w polu Sekundy należy wpisać liczbę z przedziału od 1 do 32 000, określającą czas oczekiwania systemu na zakończenie zestawu skryptów. Aby nakazać systemowi dowolnie długie oczekiwanie na zakończenie skryptów, należy wpisać 0.

Ten interwał jest szczególnie ważny, kiedy inne zadania systemu muszą czekać na ukończenie skryptów. Domyślnie każdy skrypt uruchamiania musi zostać ukończony, zanim zadziała następny skrypt. Można też użyć ustawienia „Uruchom skrypty logowania synchronicznie”, aby nakazać systemowi oczekiwanie na ukończenie skryptów logowania, zanim system załaduje pulpit.

Nadmiernie długi interwał może spowolnić system i być niewygodny dla użytkowników. Jednak jeśli interwał jest zbyt krótki, wstępne zadania mogą nie zostać wykonane i system może wydawać się gotowy przedwcześnie.</string>
      <string id="Run_Legacy_Logon_Script_Hidden">Uruchom starsze skrypty logowania w ukryciu</string>
      <string id="Run_Legacy_Logon_Script_Hidden_Explain">Ukrywa instrukcje w skryptach logowania napisanych dla systemu Windows NT w wersji 4.0 i starszej.

Skrypty logowania są plikami wsadowymi instrukcji uruchamianymi podczas logowania się użytkownika. Domyślnie system Windows 2000 wyświetla instrukcje w skryptach logowania napisanych dla systemu Windows NT w wersji 4.0 i nowszych, ale nie wyświetla skryptów logowania napisanych dla systemu Windows 2000.

Jeśli to ustawienie zostanie włączone, system Windows 2000 nie będzie wyświetlał skryptów logowania napisanych dla systemu Windows NT w wersji 4.0 i starszych.

Zobacz też ustawienie „Uruchom skrypty logowania jawnie”.</string>

      <string id="Run_Logoff_Script_Visible">Uruchom skrypty wylogowywania jawnie</string>
      <string id="Run_Logoff_Script_Visible_Explain">Wyświetla instrukcje w skryptach wylogowywania podczas ich działania.

Skrypty wylogowywania są plikami wsadowymi instrukcji, które działają, gdy użytkownik wylogowuje się. Domyślnie system nie wyświetla instrukcji w skrypcie wylogowywania.

Jeśli to ustawienie zostanie włączone, system wyświetli każdą instrukcję w skrypcie wylogowywania podczas jego działania. Instrukcje pojawiają się w oknie wiersza polecenia. To ustawienie jest przeznaczone dla zaawansowanych użytkowników.

Jeśli to ustawienie zostanie wyłączone lub nie zostanie skonfigurowane, instrukcje będą pomijane.</string>
      <string id="Run_Logon_Script_Sync">Uruchom skrypty logowania synchronicznie</string>
      <string id="Run_Logon_Script_Sync_Help">Nakazuje systemowi czekać na zakończenie działania skryptów logowania przed uruchomieniem przez system programu interfejsu Eksploratora Windows i utworzeniem pulpitu.

Jeśli to ustawienie zostanie włączone, Eksplorator Windows nie zostanie uruchomiony, dopóki skrypty logowania nie zakończą działania. To ustawienie zapewnia, że przetwarzanie skryptu logowania zostanie ukończone przed rozpoczęciem pracy użytkownika, ale może opóźnić wyświetlenie pulpitu.

Jeśli to ustawienie zostanie wyłączone lub nie zostanie skonfigurowane, skrypty logowania i Eksplorator Windows nie będą synchronizowane i można je będzie uruchamiać jednocześnie.

To ustawienie występuje w folderach Konfiguracja komputera i Konfiguracja użytkownika. Ustawienie określone w folderze Konfiguracja komputera ma pierwszeństwo przed ustawieniem określonym w folderze Konfiguracja użytkownika.</string>
      <string id="Run_Logon_Script_Visible">Uruchom skrypty logowania jawnie</string>
      <string id="Run_Logon_Script_Visible_Explain">Wyświetla instrukcje w skryptach logowania podczas ich działania.

Skrypty logowania są plikami wsadowymi instrukcji uruchamianymi podczas logowania się użytkownika. Domyślnie system nie wyświetla instrukcji w skrypcie logowania.

Jeśli to ustawienie zostanie włączone, system wyświetli każdą instrukcję w skrypcie logowania podczas jego działania. Instrukcje pojawiają się w oknie wiersza polecenia. To ustawienie jest przeznaczone dla zaawansowanych użytkowników.

Jeśli to ustawienie zostanie wyłączone lub nie zostanie skonfigurowane, instrukcje będą pomijane.</string>

      <string id="Run_Computer_PS_Scripts_First">Uruchamiaj skrypty środowiska Windows PowerShell jako pierwsze przy uruchamianiu i zamykaniu komputera</string>
      <string id="Run_User_PS_Scripts_First">Uruchamiaj skrypty środowiska Windows PowerShell jako pierwsze przy logowaniu i wylogowywaniu użytkownika</string>
      <string id="Run_Computer_PS_Scripts_First_Explain">To ustawienie zasad określa, czy skrypty środowiska Windows PowerShell będą uruchamiane przed innymi skryptami podczas uruchamiania i zamykania komputera. Domyślnie skrypty środowiska PowerShell są uruchamiane po innych skryptach. 
 
Jeżeli to ustawienie zasad zostanie włączone, w obrębie każdego odpowiedniego obiektu zasad grupy (obiektu GPO) skrypty środowiska PowerShell będą uruchamiane przed innymi skryptami podczas uruchamiania i zamykania komputera. 

Na przykład można przeanalizować następujący scenariusz: 

Istnieją trzy obiekty GPO (GPO A, GPO B i GPO C). To ustawienie zasad jest włączone w obiekcie GPO A. 

Obiekty GPO B i GPO C zawierają następujące skrypty uruchamiania komputera:

Obiekt GPO B: B.cmd i B.ps1
Obiekt GPO C: C.cmd i C.ps1

Przyjmowane jest założenie, że istnieją dwa komputery: KomputerIT i KomputerSprzedaż. 
Na komputerze KomputerIT są stosowane obiekty GPO A, B i C. Dlatego skrypty dotyczące obiektów GPO B i C będą na komputerze KomputerIT uruchamiane w następującej kolejności:

W obiekcie GPO B: B.ps1, B.cmd
W obiekcie GPO C: C.ps1, C.cmd
 
Na komputerze KomputerSprzedaż są stosowane obiekty GPO B i C, ale nie obiekt GPO A. Dlatego skrypty dotyczące obiektów GPO B i C będą na komputerze KomputerSprzedaż uruchamiane w następującej kolejności:

W obiekcieGPO B: B.cmd, B.ps1
W obiekcie GPO C: C.cmd, C.ps1

Uwaga: To ustawienie określa kolejność uruchamiania skryptów uruchamiania i zamykania komputera we wszystkich odpowiednich obiektach GPO. To ustawienie zasad można zastąpić dla określonych typów skryptów w określonym obiekcie GPO, konfigurując następujące ustawienia zasad dla obiektu GPO:
 
Konfiguracja komputera\Zasady\Ustawienia systemu Windows\Skrypty (uruchamianie/zamykanie)\Uruchamianie
Konfiguracja komputera\Zasady\Ustawienia systemu Windows\Skrypty (uruchamianie/zamykanie)\Zamykanie
</string>
      <string id="Run_User_PS_Scripts_First_Explain">To ustawienie zasad określa, czy skrypty środowiska Windows PowerShell będą uruchamiane przed innymi skryptami podczas logowania i wylogowywania użytkownika. Domyślnie skrypty środowiska PowerShell są uruchamiane po innych skryptach. 
 
Jeżeli to ustawienie zasad zostanie włączone, w obrębie każdego odpowiedniego obiektu zasad grupy (obiektu GPO) skrypty środowiska PowerShell będą uruchamiane przed innymi skryptami podczas logowania i wylogowywania użytkownika. 

Na przykład można przeanalizować następujący scenariusz: 

Istnieją trzy obiekty GPO (GPO A, GPO B i GPO C). To ustawienie zasad jest włączone w obiekcie GPO A. 

Obiekty GPO B i GPO C zawierają następujące skrypty logowania użytkownika:

Obiekt GPO B: B.cmd i B.ps1
Obiekt GPO C: C.cmd i C.ps1

Przyjmowane jest założenie, że istnieją dwaj użytkownicy: Tomasz Bator i Ewa Ciesielska. 
Dla użytkownika Tomasz są stosowane obiekty GPO A, B i C. Dlatego skrypty dotyczące obiektów GPO B i C będą dla użytkownika Tomasz uruchamiane w następującej kolejności:

W obiekcie GPO B: B.ps1, B.cmd
W obiekcie GPO C: C.ps1, C.cmd
 
Dla użytkownika Ewa są stosowane obiekty GPO B i C, ale nie obiekt GPO A. Dlatego skrypty dotyczące obiektów GPO B i C będą dla użytkownika Ewa uruchamiane w następującej kolejności:

W obiekcie GPO B: B.cmd, B.ps1
W obiekcie GPO C: C.cmd, C.ps1

Uwaga: To ustawienie określa kolejność uruchamiania skryptów logowania i wylogowywania użytkownika we wszystkich odpowiednich obiektach GPO. To ustawienie zasad można zastąpić dla określonych typów skryptów w określonym obiekcie GPO, konfigurując następujące ustawienia zasad dla obiektu GPO:
 
Konfiguracja użytkownika\Zasady\Ustawienia systemu Windows\Skrypty (logowanie/wylogowywanie)\Logowanier
Konfiguracja użytkownika\Zasady\Ustawienia systemu Windows\Skrypty (logowanie/wylogowywanie)\Wylogowywanie.

To ustawienie występuje w folderach Konfiguracja komputera i Konfiguracja użytkownika. Ustawienie określone w folderze Konfiguracja komputera ma pierwszeństwo przed ustawieniem określonym w folderze Konfiguracja użytkownika.
</string>
      <string id="Run_Shutdown_Script_Visible">Uruchom skrypty zamykania jawnie</string>
      <string id="Run_Shutdown_Script_Visible_Explain">Wyświetla instrukcje w skryptach zamykania podczas ich działania.

Skrypty zamykania są plikami wsadowymi instrukcji, które działają podczas ponownego uruchamiania systemu lub zamykania go przez użytkownika. Domyślnie system nie wyświetla instrukcji w skrypcie zamykania.

Jeśli to ustawienie zostanie włączone, system wyświetli każdą instrukcję w skrypcie zamykania podczas jego działania. Instrukcje pojawiają się w oknie wiersza polecenia.

Jeśli to ustawienie zostanie wyłączone lub nie zostanie skonfigurowane, instrukcje będą pomijane.</string>
      <string id="Run_Startup_Script_Sync">Uruchom skrypty uruchamiania asynchronicznie</string>
      <string id="Run_Startup_Script_Sync_Help">Pozwala systemowi na jednoczesne uruchamianie skryptów uruchamiania.

Skrypty uruchamiania są plikami wsadowymi, które działają, zanim użytkownik zostanie zaproszony do zalogowania się. Domyślnie system oczekuje na ukończenie każdego skryptu uruchamiania, zanim uruchomi następny skrypt.

Jeśli to ustawienie zostanie włączone, system nie będzie koordynował działania skryptów uruchamiania. W rezultacie będzie możliwe jednoczesne uruchamianie skryptów.

Jeśli to ustawienie zostanie wyłączone lub nie zostanie skonfigurowane, skrypt uruchamiania nie będzie mógł działać, dopóki poprzedni skrypt nie zostanie ukończony.

Uwaga: Począwszy od systemu operacyjnego Windows Vista, skrypty, które są skonfigurowane do uruchamiania jednoczesnego, nie są widoczne podczas uruchamiania niezależnie od stanu ustawienia zasad „Uruchom skrypty uruchamiania jawnie”.</string>
      <string id="Run_Startup_Script_Visible">Uruchom skrypty uruchamiania jawnie</string>
      <string id="Run_Startup_Script_Visible_Explain">Wyświetla instrukcje w skryptach uruchamiania podczas ich działania.

Skrypty uruchamiania są plikami wsadowymi instrukcji, które działają, zanim użytkownik zostanie zaproszony do zalogowania się. Domyślnie system nie wyświetla instrukcji w skrypcie uruchamiania.

Jeśli to ustawienie zostanie włączone, system wyświetli każdą instrukcję w skrypcie uruchamiania podczas jego działania. Instrukcje pojawiają się w oknie wiersza polecenia. To ustawienie jest przeznaczone dla zaawansowanych użytkowników.

Jeśli to ustawienie zostanie wyłączone lub nie zostanie skonfigurowane, instrukcje będą pomijane.

Uwaga: Począwszy od systemu operacyjnego Windows Vista, skrypty, które są skonfigurowane do uruchamiania asynchronicznie, nie są widoczne podczas uruchamiania niezależnie od stanu tego ustawienia.</string>
      <string id="Allow_Logon_Script_NetbiosDisabled">Zezwalaj na skrypty logowania przy wyłączonym systemie NetBIOS lub WINS</string>
      <string id="Allow_Logon_Script_NetbiosDisabled_Explain">To ustawienie zasad umożliwia uruchamianie skryptów logowania użytkownika, gdy nie skonfigurowano sufiksów DNS obejmujących wszystkie lasy, a usługi NetBIOS lub WINS są wyłączone. To ustawienie zasad obowiązuje dla wszystkich kont użytkowników interaktywnie logujących się do tego komputera.

Po włączeniu tego ustawienia zasad skrypty logowania będą działać po wyłączeniu usług NetBIOS lub WINS przy logowaniu w innym lesie i bez skonfigurowanych sufiksów DNS.

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, wykonywanie interaktywnych skryptów logowania użytkownika w innym lesie przy wyłączonych usługach NetBIOS lub WINS i nieskonfigurowanych sufiksach DNS nie będzie możliwe.</string>
      <string id="Scripts">Skrypty</string>
    </stringTable>
    <presentationTable>
      <presentation id="MaxGPOScriptWaitPolicy">
        <decimalTextBox refId="MaxGPOScriptWait" defaultValue="600">Sekundy:</decimalTextBox>
        <text>Zakres od 0 do 32 000, użyj 0 dla nieskończonego czasu oczekiwania</text>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
