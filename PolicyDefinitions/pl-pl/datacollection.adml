<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2015 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>tutaj wprowadź nazwę wyświetlaną</displayName>
  <description>tutaj wprowadź opis</description>
  <resources>
    <stringTable>
      <string id="AllowTelemetry">Zezwalaj na telemetrię</string>
      <string id="AllowTelemetry_0">0 — zabezpieczenia [tylko przedsiębiorstwo]</string>
      <string id="AllowTelemetry_1">1 — podstawowe</string>
      <string id="AllowTelemetry_2">2 — rozszerzone</string>
      <string id="AllowTelemetry_3">3 — pełne</string>
      <string id="AllowTelemetry_Explain">To ustawienie zasad określa ilość danych diagnostycznych systemu Windows wysyłanych do firmy Microsoft. Wartość 0 (Zabezpieczenia) powoduje wysyłanie do firmy Microsoft minimalnego zestawu danych w celu zachowania bezpieczeństwa systemu Windows. Składniki zabezpieczeń systemu Windows, takie jak Narzędzie do usuwania złośliwego oprogramowania i usługa Windows Defender, mogą wysyłać dane do firmy Microsoft na tym poziomie, jeśli są włączone. Ustawienie wartości 0 jest możliwe tylko w przypadku urządzeń Enterprise, EDU, IoT i Server. Ustawienie wartości 0 dla innych urządzeń jest równoważne z ustawieniem wartości 1. 

Wartość 1 (Podstawowe) powoduje wysyłanie takich samych danych jak wartość 0 oraz bardzo ograniczonej ilości danych diagnostycznych, takich jak podstawowe informacje o urządzeniu, dane związane z jakością oraz informacje dotyczące zgodności aplikacji. Należy zauważyć, że wartość 0 lub 1 spowoduje obniżenie poziomu działania pewnych funkcji na urządzeniu.

Wartość 2 (Rozszerzone) powoduje wysyłanie takich samych danych jak wartość 1 oraz dodatkowych danych, takich jak dotyczące użytkowania systemów Windows i Windows Server, programu System Center i aplikacji oraz ich działania, a także zaawansowanych danych dotyczących niezawodności.

Wartość 3 (Pełne) powoduje wysyłanie takich samych danych jak wartość 2 oraz zaawansowanych danych diagnostycznych służących do diagnozowania i rozwiązywania problemów z urządzeniem. Te dane mogą zawierać pliki i zawartość powodującą problem z urządzeniem.

Ustawienia danych diagnostycznych systemu Windows 10 dotyczą systemu operacyjnego Windows oraz aplikacji wchodzących w skład systemu Windows. Nie dotyczą aplikacji innych firm działających w systemie Windows 10.

Jeśli wyłączysz to ustawienie zasad albo go nie skonfigurujesz, użytkownicy będą mogli skonfigurować poziom telemetrii w ustawieniach.

</string>
      <string id="EnableExperimentation">Wyłącz funkcje lub ustawienia w wersji wstępnej</string>
      <string id="EnableExperimentation_1">Tylko ustawienia urządzenia</string>
      <string id="EnableExperimentation_2">Pełna obsługa wersji eksperymentalnych</string>
      <string id="EnableExperimentation_Explain">To ustawienie zasad określa, w jakim zakresie firma Microsoft może eksperymentować z produktem w celu poznania preferencji użytkownika lub zachowania urządzenia. Wartość 1 oznacza, że firma Microsoft może konfigurować tylko ustawienia urządzenia. Wartość 2 oznacza, że firma Microsoft może eksperymentować w pełnym zakresie.

Wyłączenie tego ustawienia zasad spowoduje wyłączenie wszystkich eksperymentów.

Jeśli nie skonfigurujesz tego ustawienia zasad, użytkownik będzie mógł skonfigurować opcję „Zezwalaj firmie Microsoft na wypróbowywanie funkcji w tej kompilacji” w ustawieniach.
</string>
      <string id="TelemetryProxy">Konfiguruj środowiska połączonych użytkowników i telemetrię</string>
      <string id="TelemetryProxy_Explain">To ustawienie zasad umożliwia przesyłanie dalej żądań dotyczących usługi Środowisko i telemetria połączonego użytkownika do serwera proxy.

Jeśli to ustawienie zasad zostanie włączone, będzie można określić nazwę FQDN lub adres IP urządzenia docelowego w sieci organizacji (i opcjonalnie numer portu). Połączenie będzie nawiązywane za pośrednictwem protokołu SSL (Secure Sockets Layer). Jeśli nazwany serwer proxy ulegnie awarii lub jeśli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, dane usługi Środowisko i telemetria połączonego użytkownika będą wysyłane do firmy Microsoft przy użyciu domyślnej konfiguracji serwera proxy.

Format tego ustawienia: &lt;serwer&gt;:&lt;port&gt; 
</string>
      
      <string id="CommercialId">Konfiguruj identyfikator komercyjny</string>
      <string id="CommercialId_Explain">To ustawienie zasad definiuje identyfikator używany do unikatowego kojarzenia danych telemetrycznych urządzenia jako należących do danej organizacji. Jeśli Twoja organizacja uczestniczy w programie wymagającym zidentyfikowania tego urządzenia jako należącego do organizacji, użyj tego ustawienia w celu podania tego identyfikatora. Wartość tego ustawienia zostanie podana przez firmę Microsoft w ramach procesu dołączania do programu. 

Jeśli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, firma Microsoft nie będzie mogła skojarzyć tej maszyny i jej danych telemetrycznych z organizacją za pomocą tego identyfikatora.
</string>

      <string id="DisableEnterpriseAuthProxy">Konfiguruj użycie uwierzytelnionego serwera proxy na potrzeby usługi Środowisko i telemetria połączonego użytkownika</string>
      <string id="DisableEnterpriseAuthProxy_1">Włącz użycie uwierzytelnionego serwera proxy</string>
      <string id="DisableEnterpriseAuthProxy_2">Wyłącz użycie uwierzytelnionego serwera proxy</string>
      <string id="DisableEnterpriseAuthProxy_Explain">To ustawienie zasad blokuje usłudze Środowisko i telemetria połączonego użytkownika możliwość automatycznego używania uwierzytelnionego serwera proxy do wysyłania danych do firmy Microsoft w systemie Windows 10. Jeśli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane, usługa Środowisko i telemetria połączonego użytkownika będzie automatycznie wysyłać dane do firmy Microsoft przy użyciu uwierzytelnionego serwera proxy. Włączenie tego ustawienia zasad spowoduje zablokowanie automatycznego używania uwierzytelnionego serwera proxy przez usługę Środowisko i telemetria połączonego użytkownika.</string>

    </stringTable>
    <presentationTable>
      <presentation id="AllowTelemetry">
        <dropdownList refId="AllowTelemetry" noSort="true" defaultItem="1"></dropdownList>
      </presentation>
      <presentation id="EnableExperimentation">
        <dropdownList refId="EnableExperimentation" noSort="true" defaultItem="1"></dropdownList>
      </presentation>
      <presentation id="TelemetryProxy">
        <textBox refId="TelemetryProxyName">
        <label>Nazwa serwera proxy:</label>
        </textBox>
      </presentation>
      <presentation id="CommercialID">
        <textBox refId="CommercialIdValue">
        <label>Identyfikator komercyjny:</label>
        </textBox>
      </presentation>
      <presentation id="DisableEnterpriseAuthProxy">
        <dropdownList refId="DisableEnterpriseAuthProxy" noSort="true" defaultItem="1"></dropdownList>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
