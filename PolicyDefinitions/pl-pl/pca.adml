<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>tutaj wprowadź wyświetlaną nazwę</displayName>
  <description>tutaj wprowadź opis</description>
  <resources>
    <stringTable>


      <!-- Overall category text -->
      <string id="PcaScenarioCategory">Diagnostyka zgodności aplikacji</string>
      

      <!-- Generic WDI text -->
      <string id="WdiScenarioExecutionPolicyLevelResolution">Wykrywanie, rozwiązywanie i rozpoznawanie problemów</string>
      <string id="WdiScenarioExecutionPolicyLevelTsOnly">Tylko wykrywanie i rozwiązywanie problemów</string>


      <!-- Individual scenario text -->
      <string id="DetectInstallFailuresText">Wykrywaj błędy instalowania aplikacji</string>
      <string id="DetectUpdateFailuresText">Wykrywaj aplikacje niezdolne do uruchamiania instalatorów podczas działania funkcji Kontrola konta użytkownika</string>
      <string id="DetectDeprecatedComponentFailuresText">Wykrywaj błędy aplikacji spowodowane przez przestarzałe biblioteki DLL systemu Windows</string>
      <string id="DetectDeprecatedCOMComponentFailuresText">Wykrywaj błędy aplikacji spowodowane przez przestarzałe obiekty modelu COM</string>
      <string id="DetectUndetectedInstallersText">Wykrywaj instalatory aplikacji, które muszą być uruchamiane przy użyciu konta administratora</string>
      <string id="DetectBlockedDriversText">Powiadamiaj o zablokowanych sterownikach</string>
      <!-- End individual scenario text -->


      <!-- Begin install failure scenario description -->
      <string id="DetectInstallFailuresExplain">To ustawienie zasad powoduje wykrywanie przez Asystenta zgodności programów błędów podczas instalowania aplikacji. 

Jeśli to ustawienie zasad zostanie włączone, asystent będzie wykrywał błędy działania instalatorów aplikacji za pomocą algorytmów heurystycznych. Gdy wystąpi zdarzenie potencjalnie zagrażające kontynuowaniu procesu instalacji, użytkownik otrzyma możliwość ponownego uruchomienia instalatora — tym razem w trybie zgodności z systemem Microsoft Windows XP.

Wyłączenie tego ustawienia zasad spowoduje, że asystent nie będzie wykrywał żadnych błędów w działaniu instalatorów programów.

Jeśli to ustawienie zasad nie zostanie skonfigurowane, asystent domyślnie będzie diagnozował wszystkie błędy.

Uwaga: włączenie ustawienia zasad „Wyłącz Asystenta zgodności programów” spowoduje wyłączenie opisywanej zasady. Aby Asystent zgodności programów działał, musi być uruchomiona usługa Zasady diagnostyki oraz usługa Asystent zgodności programów. Usługi te można skonfigurować za pośrednictwem przystawki Usługi dostępnej w programie Microsoft Management Console.</string>
      <!-- End install failure scenario description -->
      

      <!-- Begin update failure scenario description -->
      <string id="DetectUpdateFailuresExplain">To ustawienie zasad powoduje wykrywanie przez Asystenta zgodności programów błędów programów korzystających z funkcji Kontrola konta użytkownika. 

Jeśli to ustawienie zasad zostanie włączone, asystent będzie wykrywał programy, które nie mogą zainicjować procesów podrzędnych dla instalatorów (zazwyczaj odpowiedzialnych za aktualizacje programu). Z chwilą wykrycia takiego błędu asystent zastosuje tryb zgodności ELEVATECREATEPROCESS. W tym trybie po ponownym uruchomieniu programu spowoduje on zainicjowanie instalatora przy użyciu konta administratora.

Wyłączenie zasady spowoduje, że asystent nie będzie wykrywał aplikacji niezdolnych do uruchamiania instalatorów działających przy włączonej funkcji Kontrola konta użytkownika.

Jeśli to ustawienie zasad nie zostanie skonfigurowane, asystent będzie wykrywał programy, które nie mogą zainicjować procesów podrzędnych dla instalatorów (zazwyczaj odpowiedzialnych za aktualizacje programu).

Uwaga: włączenie ustawienia zasad „Wyłącz Asystenta zgodności programów” spowoduje wyłączenie opisywanej zasady. Aby Asystent zgodności programów działał, musi być uruchomiona usługa Zasady diagnostyki oraz usługa Asystent zgodności programów. Usługi te można skonfigurować za pośrednictwem przystawki Usługi dostępnej w programie Microsoft Management Console.</string>
      <!-- End update failure scenario description -->
      

      <!-- Begin deprecated component failure scenario description -->
      <string id="DetectDeprecatedComponentFailuresExplain">To ustawienie zasad określa, czy Asystent zgodności programów będzie diagnozował błędy ładowania bibliotek DLL w programach. 

Jeżeli to ustawienie zasad zostanie włączone, Asystent zgodności programów będzie wykrywał programy próbujące załadować starsze biblioteki DLL systemu Microsoft Windows, które zostały usunięte z bieżącej wersji systemu Windows. Gdy taki błąd zostanie wykryty, z chwilą zamknięcia programu Asystent zgodności programów powiadomi o nim użytkownika i zaoferuje opcję wyszukania rozwiązania w witrynie firmy Microsoft w sieci Web.

Jeżeli to ustawienie zasad zostanie wyłączone, Asystent zgodności programów nie będzie wykrywał programów próbujących załadować starsze biblioteki DLL systemu Windows.

Jeżeli to ustawienie zasad nie zostanie skonfigurowane, Asystent zgodności programów domyślnie będzie wykrywał programy próbujące załadować starsze biblioteki DLL systemu Windows.

Uwaga: Włączenie ustawienia zasad Wyłącz Asystenta zgodności programów spowoduje, że ta zasada nie będzie działać. Aby Asystent zgodności programów mógł działać, musi być uruchomiona usługa zasad diagnostyki oraz usługa asystenta zgodności programów. Te usługi można skonfigurować za pomocą przystawki Usługi dostępnej w programie Microsoft Management Console.</string>
      <!-- End deprecated component failure scenario description -->


      <!-- Begin deprecated COM component failure scenario description -->
      <string id="DetectDeprecatedCOMComponentFailuresExplain">To ustawienie zasad określa, czy Asystent zgodności programów będzie diagnozował błędy ładowania bibliotek DLL i tworzenia obiektów COM w programach. 

Jeżeli to ustawienie zasad zostanie włączone, Asystent zgodności programów będzie wykrywał programy próbujące utworzyć starsze obiekty modelu COM, które zostały usunięte z bieżącej wersji systemu Windows. Gdy taki błąd zostanie wykryty, z chwilą zamknięcia programu Asystent zgodności programów powiadomi o nim użytkownika i zaoferuje opcję wyszukania rozwiązania w witrynie firmy Microsoft w sieci Web.

Jeżeli to ustawienie zasad zostanie wyłączone, Asystent zgodności programów nie będzie wykrywał programów próbujących utworzyć starsze obiekty modelu COM.

Jeżeli to ustawienie zasad nie zostanie skonfigurowane, Asystent zgodności programów będzie wykrywał programy próbujące utworzyć starsze obiekty modelu COM.

Uwaga: Włączenie ustawienia zasad Wyłącz Asystenta zgodności programów spowoduje, że ta zasada nie będzie działać. Aby Asystent zgodności programów mógł działać, musi być uruchomiona usługa zasad diagnostyki oraz usługa asystenta zgodności programów. Te usługi można skonfigurować za pomocą przystawki Usługi dostępnej w programie Microsoft Management Console.</string>
      <!-- End deprecated COM component failure scenario description -->


      <!-- Begin undetected installer failure scenario description -->
      <string id="DetectUndetectedInstallersExplain">To ustawienie zasad określa, czy Asystent zgodności programów będzie wykrywał błędy instalatorów aplikacji, które działają przy użyciu kont innych niż konto administratora. 

Włączenie tego ustawienia zasad spowoduje wykrywanie przez asystenta instalatorów aplikacji, które nie są uruchamiane przy użyciu konta administratora przez funkcję Kontrola dostępu użytkownika. Gdy wystąpi zdarzenie potencjalnie zagrażające kontynuowaniu procesu instalacji, użytkownik otrzyma możliwość ponownego uruchomienia instalatora, tym razem przy użyciu konta administratora.

Wyłączenie tego ustawienia zasad spowoduje, że asystent nie będzie wykrywał instalatorów aplikacji, które nie są uruchamiane przy użyciu konta administratora przez usługę Kontrola dostępu użytkownika.

Jeśli to ustawienie zasad nie zostanie skonfigurowane, asystent będzie wykrywał instalatory aplikacji, które nie są uruchamiane przy użyciu konta administratora przez usługę Kontrola dostępu użytkownika.


Uwaga: włączenie ustawienia zasad „Wyłącz Asystenta zgodności programów” spowoduje wyłączenie opisywanej zasady. Aby Asystent zgodności programów działał, musi być uruchomiona usługa Zasady diagnostyki oraz usługa Asystent zgodności programów. Usługi te można skonfigurować za pośrednictwem przystawki Usługi dostępnej w programie Microsoft Management Console.</string>
      <!-- End undetected installer failure scenario description -->


      <!-- Begin blocked drivers scenario description -->
      <string id="DetectBlockedDriversExplain">To ustawienie zasad określa, czy Asystent zgodności programów będzie wykrywał sterowniki zablokowane w wyniku problemów ze zgodnością. 

Jeśli to ustawienie zasad zostanie włączone, asystent będzie powiadamiał użytkownika o problemach z zablokowanymi sterownikami. Komunikat będzie również zawierał łącze do witryny firmy Microsoft w sieci Web, gdzie można znaleźć rozwiązanie.

Jeśli to ustawienie zasad zostanie wyłączone, asystent nie będzie generował powiadomień o zablokowanych sterownikach. Uwaga: W przypadku wyłączenia tego ustawienia zasad użytkownik nie będzie otrzymywał żadnych propozycji rozwiązań problemów.

Jeśli ta zasada nie zostanie skonfigurowana, użytkownik będzie powiadamiany o problemach z zablokowanymi sterownikami. Komunikat będzie również zawierał łącze do witryny firmy Microsoft w sieci Web, gdzie można znaleźć rozwiązanie.

Uwaga: włączenie ustawienia zasad „Wyłącz Asystenta zgodności programów” spowoduje wyłączenie opisywanej zasady. Aby Asystent zgodności programów działał, musi być uruchomiona usługa Zasady diagnostyki oraz usługa Asystent zgodności programów. Usługi te można skonfigurować za pośrednictwem przystawki Usługi dostępnej w programie Microsoft Management Console.</string>
      <!-- End blocked drivers scenario description -->
    </stringTable>

    <presentationTable>
      <presentation id="DetectUpdateFailuresTable">
        <dropdownList refId="DetectUpdateFailuresLevel" noSort="true" defaultItem="1">Poziom wykonania scenariusza</dropdownList>
      </presentation>
      <presentation id="DetectDeprecatedComponentFailuresTable">
        <dropdownList refId="DetectDeprecatedComponentFailuresLevel" noSort="true" defaultItem="1">Poziom wykonania scenariusza</dropdownList>
      </presentation>
      <presentation id="DetectDeprecatedCOMComponentFailuresTable">
        <dropdownList refId="DetectDeprecatedCOMComponentFailuresLevel" noSort="true" defaultItem="1">Poziom wykonania scenariusza</dropdownList>
      </presentation>
      <presentation id="DetectUndetectedInstallersTable">
        <dropdownList refId="DetectUndetectedInstallersLevel" noSort="true" defaultItem="1">Poziom wykonania scenariusza</dropdownList>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
