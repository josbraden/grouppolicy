<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>tutaj wprowadź wyświetlaną nazwę</displayName>
  <description>tutaj wprowadź opis</description>
  <resources>
    <stringTable>
      <string id="AllowDefaultCredentials">Zezwalaj na delegowanie poświadczeń domyślnych</string>
      <string id="AllowDefaultCredentials_Explain">To ustawienie zasad jest stosowane do aplikacji używających składnika dostawcy obsługi zabezpieczeń Cred (na przykład serwera terminali).

To ustawienie zasad jest stosowane, gdy uwierzytelnianie serwera jest wykonywane za pomocą zaufanego certyfikatu X509 lub protokołu Kerberos.

Jeżeli to ustawienie zasad zostanie włączone, można określić, do których serwerów mogą być delegowane domyślne poświadczenia użytkownika (poświadczenia domyślne to poświadczenia, które są używane przy pierwszym logowaniu do systemu Windows).

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane (stan domyślny), delegowanie poświadczeń domyślnych na jakikolwiek komputer będzie niedozwolone.

Uwaga: Ustawienie Zezwalaj na delegowanie poświadczeń domyślnych można ustawić dla co najmniej jednej głównej nazwy usługi (SPN, Service Principal Name). Nazwa SPN reprezentuje serwer docelowy, do którego mogą być delegowane poświadczenia użytkownika. Przy podawaniu nazwy SPN dozwolone jest użycie jednego symbolu wieloznacznego.

Na przykład:
TERMSRV/host.zasobyludzkie.fabrikam.com — serwer terminali działający na komputerze host.zasobyludzkie.fabrikam.com;
TERMSRV/* — serwer terminali działający na wszystkich komputerach.
TERMSRV/*.zasobyludzkie.fabrikam.com — serwer terminali działający na wszystkich komputerach w domenie zasobyludzkie.fabrikam.com</string>
      <string id="AllowDefCredentialsWhenNTLMOnly">Zezwalaj na delegowanie poświadczeń domyślnych z zastosowaniem uwierzytelniania na serwerze tylko przy użyciu mechanizmu NTLM</string>
      <string id="AllowDefCredentialsWhenNTLMOnly_Explain">To ustawienie zasad jest stosowane do aplikacji używających składnika dostawcy obsługi zabezpieczeń Cred (na przykład serwera terminali).

Ta zasada jest stosowana, gdy uwierzytelnianie serwera jest wykonywane za pomocą mechanizmu NTLM.

Jeżeli to ustawienie zasad zostanie włączone, można określić, do których serwerów mogą być delegowane domyślne poświadczenia użytkownika (poświadczenia domyślne to poświadczenia, które są używane przy pierwszym logowaniu do systemu Windows).

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane (stan domyślny), delegowanie poświadczeń domyślnych do jakiegokolwiek komputera będzie niedozwolone.

Uwaga: Zasadę Zezwalaj na delegowanie poświadczeń domyślnych z zastosowaniem uwierzytelniania na serwerze tylko przy użyciu mechanizmu NTLM można ustawić dla co najmniej jednej głównej nazwy usługi (SPN). Nazwa SPN reprezentuje serwer docelowy, do którego mogą być delegowane poświadczenia użytkownika. Przy podawaniu nazwy SPN dozwolone jest użycie jednego symbolu wieloznacznego.

Na przykład:
TERMSRV/host.zasobyludzkie.fabrikam.com — serwer terminali działający na komputerze host.zasobyludzkie.fabrikam.com;
TERMSRV/* — serwer terminali działający na wszystkich komputerach. 
TERMSRV/*.zasobyludzkie.fabrikam.com — serwer terminali działający na wszystkich komputerach w domenie zasobyludzkie.fabrikam.com</string>
      <string id="AllowFreshCredentials">Zezwalaj na delegowanie świeżych poświadczeń</string>
      <string id="AllowFreshCredentials_Explain">To ustawienie zasad jest stosowane do aplikacji używających składnika dostawcy obsługi zabezpieczeń Cred (na przykład serwera terminali).

To ustawienie zasad jest stosowane, gdy uwierzytelnianie serwera jest wykonywane za pomocą zaufanego certyfikatu X509 lub protokołu Kerberos.

Jeżeli to ustawienie zasad zostanie włączone, można określić, do których serwerów mogą być delegowane świeże poświadczenia użytkownika (świeże poświadczenia to poświadczenia, które należy podać podczas uruchamiania aplikacji.

Jeżeli to ustawienie zasad nie zostanie skonfigurowane (stan domyślny), po właściwie przeprowadzonym uwierzytelnieniu wzajemnym będzie dozwolone delegowanie świeżych poświadczeń do serwera terminali działającego na dowolnym komputerze (TERMSRV/*).

Jeżeli to ustawienie zasad zostanie wyłączone, delegowanie świeżych poświadczeń na jakikolwiek komputer będzie niedozwolone.

Uwaga: Ustawienie Zezwalaj na delegowanie świeżych poświadczeń można ustawić dla co najmniej jednej głównej nazwy usługi (SPN). Nazwa SPN reprezentuje serwer docelowy, do którego mogą być delegowane poświadczenia użytkownika. Przy podawaniu nazwy SPN dozwolone jest użycie jednego symbolu wieloznacznego.

Na przykład:
TERMSRV/host.zasobyludzkie.fabrikam.com
— serwer terminali działający na komputerze host.zasobyludzkie.fabrikam.com;
TERMSRV/* — serwer terminali działający na wszystkich komputerach. 
TERMSRV/*.zasobyludzkie.fabrikam.com — serwer terminali działający na wszystkich komputerach w domenie zasobyludzkie.fabrikam.com</string>
      <string id="AllowFreshCredentialsWhenNTLMOnly">Zezwalaj na delegowanie świeżych poświadczeń z zastosowaniem uwierzytelniania na serwerze tylko przy użyciu mechanizmu NTLM</string>
      <string id="AllowFreshCredentialsWhenNTLMOnly_Explain">To ustawienie zasad jest stosowane do aplikacji używających składnika dostawcy obsługi zabezpieczeń Cred (na przykład serwera terminali).

Ta zasada jest stosowana, gdy uwierzytelnianie serwera jest wykonywane za pomocą mechanizmu NTLM.

Jeżeli to ustawienie zasad zostanie włączone, można określić, do których serwerów mogą być delegowane świeże poświadczenia użytkownika (świeże poświadczenia to poświadczenia, które należy podać podczas uruchamiania aplikacji).

Jeżeli to ustawienie zasad nie zostanie skonfigurowane (stan domyślny), po właściwie przeprowadzonym uwierzytelnieniu wzajemnym będzie dozwolone delegowanie świeżych poświadczeń do serwera terminali działającego na dowolnym komputerze (TERMSRV/*).

Jeżeli to ustawienie zasad zostanie wyłączone, delegowanie świeżych poświadczeń do jakiegokolwiek komputera będzie niedozwolone.

Uwaga: Zasadę Zezwalaj na delegowanie świeżych poświadczeń z zastosowaniem uwierzytelniania na serwerze tylko przy użyciu mechanizmu NTLM można ustawić dla co najmniej jednej głównej nazwy usługi (SPN). Nazwa SPN reprezentuje serwer docelowy, do którego mogą być delegowane poświadczenia użytkownika. Przy podawaniu nazwy SPN dozwolone jest użycie jednego symbolu wieloznacznego.

Na przykład:
 TERMSRV/host.zasobyludzkie.fabrikam.com — serwer terminali działający na komputerze host.zasobyludzkie.fabrikam.com;
TERMSRV/* — serwer terminali działający na wszystkich komputerach. 
TERMSRV/*.zasobyludzkie.fabrikam.com — serwer terminali działający na wszystkich komputerach w domenie zasobyludzkie.fabrikam.com</string>
      <string id="AllowSavedCredentials">Zezwalaj na delegowanie zapisanych poświadczeń</string>
      <string id="AllowSavedCredentials_Explain">To ustawienie zasad jest stosowane do aplikacji używających składnika dostawcy obsługi zabezpieczeń Cred (na przykład serwera terminali).

To ustawienie zasad jest stosowane, gdy uwierzytelnianie serwera jest wykonywane za pomocą zaufanego certyfikatu X509 lub protokołu Kerberos.

Jeżeli to ustawienie zasad zostanie włączone, można określić, do których serwerów mogą być delegowane zapisane poświadczenia użytkownika (zapisane poświadczenia to poświadczenia wybrane do zapisania/zapamiętania przy użyciu menedżera poświadczeń systemu Windows).

Jeżeli to ustawienie zasad nie zostanie skonfigurowane (stan domyślny), po właściwie przeprowadzonym uwierzytelnieniu wzajemnym będzie dozwolone delegowanie zapisanych poświadczeń do serwera terminali działającego na dowolnym komputerze (TERMSRV/*).

Jeżeli to ustawienie zasad zostanie wyłączone, delegowanie zapisanych poświadczeń do jakiegokolwiek komputera będzie niedozwolone.

Uwaga: Zasadę Zezwalaj na delegowanie zapisanych poświadczeń można ustawić dla co najmniej jednej głównej nazwy usługi (SPN). Nazwa SPN reprezentuje serwer docelowy, do którego mogą być delegowane poświadczenia użytkownika. Przy podawaniu nazwy SPN dozwolone jest użycie jednego symbolu wieloznacznego.

Na przykład: 
TERMSRV/host.zasobyludzkie.fabrikam.com — serwer terminali działający na komputerze host.zasobyludzkie.fabrikam.com;
TERMSRV/* — serwer terminali działający na wszystkich komputerach. 
TERMSRV/*.zasobyludzkie.fabrikam.com — serwer terminali działający na wszystkich komputerach w domenie zasobyludzkie.fabrikam.com</string>
      <string id="AllowSavedCredentialsWhenNTLMOnly">Zezwalaj na delegowanie zapisanych poświadczeń z zastosowaniem uwierzytelniania na serwerze tylko przy użyciu mechanizmu NTLM</string>
      <string id="AllowSavedCredentialsWhenNTLMOnly_Explain">To ustawienie zasad jest stosowane do aplikacji używających składnika dostawcy obsługi zabezpieczeń Cred (na przykład serwera terminali).

Ta zasada jest stosowana, gdy uwierzytelnianie serwera jest wykonywane za pomocą mechanizmu NTLM.

Jeżeli to ustawienie zasad zostanie włączone, można określić, do których serwerów mogą być delegowane zapisane poświadczenia użytkownika (zapisane poświadczenia to poświadczenia wybrane do zapisania/zapamiętania przy użyciu menedżera poświadczeń systemu Windows). 

Jeżeli to ustawienie zasad nie zostanie skonfigurowane (stan domyślny), po właściwie przeprowadzonym uwierzytelnieniu wzajemnym będzie dozwolone delegowanie zapisanych poświadczeń do serwera terminali działającego na dowolnym komputerze (TERMSRV/*), o ile komputer kliencki nie należy do domeny. Jeżeli klient jest przyłączony do domeny, delegowanie zapisanych poświadczeń do jakiegokolwiek komputera jest niedozwolone domyślnie.

Jeżeli to ustawienie zasad zostanie wyłączone, delegowanie zapisanych poświadczeń do jakiegokolwiek komputera będzie niedozwolone.

Uwaga: Zasadę Zezwalaj na delegowanie zapisanych poświadczeń z zastosowaniem uwierzytelniania na serwerze tylko przy użyciu mechanizmu NTLM można ustawić dla co najmniej jednej głównej nazwy usługi (SPN). Nazwa SPN reprezentuje serwer docelowy, do którego mogą być delegowane poświadczenia użytkownika. Przy podawaniu nazwy SPN dozwolone jest użycie jednego symbolu wieloznacznego.

Na przykład: 
TERMSRV/host.zasobyludzkie.fabrikam.com — serwer terminali działający na komputerze host.zasobyludzkie.fabrikam.com;
TERMSRV/* — serwer terminali działający na wszystkich komputerach. 
TERMSRV/*.zasobyludzkie.fabrikam.com — serwer terminali działający na wszystkich komputerach w domenie zasobyludzkie.fabrikam.com</string>
      <string id="CredentialsDelegation">Delegowanie poświadczeń</string>
      <string id="DenyDefaultCredentials">Odmawiaj delegowania poświadczeń domyślnych</string>
      <string id="DenyDefaultCredentials_Explain">To ustawienie zasad jest stosowane do aplikacji używających składnika dostawcy obsługi zabezpieczeń Cred (na przykład serwera terminali).

Jeżeli to ustawienie zasad zostanie włączone, można określić, do których serwerów NIE mogą być delegowane domyślne poświadczenia użytkownika (poświadczenia domyślne to poświadczenia, które są używane przy pierwszym logowaniu do systemu Windows).

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane (stan domyślny), nie zostanie określony żaden serwer.

Uwaga: Zasadę Odmawiaj delegowania poświadczeń domyślnych można ustawić dla co najmniej jednej głównej nazwy usługi (SPN). Nazwa SPN reprezentuje serwer docelowy, do którego NIE mogą być delegowane poświadczenia użytkownika. Przy podawaniu nazwy SPN dozwolone jest użycie jednego symbolu wieloznacznego.

Na przykład: 
TERMSRV/host.zasobyludzkie.fabrikam.com — serwer terminali działający na komputerze host.zasobyludzkie.fabrikam.com;
TERMSRV/* — serwer terminali działający na wszystkich komputerach. 
TERMSRV/*.zasobyludzkie.fabrikam.com — serwer terminali działający na wszystkich komputerach w domenie zasobyludzkie.fabrikam.com.

Tego ustawienia można użyć w połączeniu z ustawieniem Zezwalaj na delegowanie poświadczeń domyślnych w celu zdefiniowania wyjątków dla określonych serwerów, które byłyby dozwolone w przypadku używania symboli wieloznacznych na liście serwerów Zezwalaj na delegowanie poświadczeń domyślnych.</string>
      <string id="DenyFreshCredentials">Odmawiaj delegowania świeżych poświadczeń</string>
      <string id="DenyFreshCredentials_Explain">To ustawienie zasad jest stosowane do aplikacji używających składnika dostawcy obsługi zabezpieczeń Cred (na przykład serwera terminali).

Jeżeli to ustawienie zasad zostanie włączone, można określić, do których serwerów NIE mogą być delegowane świeże poświadczenia użytkownika (świeże poświadczenia to poświadczenia, które należy podać podczas uruchamiania aplikacji.

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane (stan domyślny), nie zostanie określony żaden serwer.

Uwaga: Zasadę Odmawiaj delegowania świeżych poświadczeń można ustawić dla co najmniej jednej głównej nazwy usługi (SPN). Nazwa SPN reprezentuje serwer docelowy, do którego NIE mogą być delegowane poświadczenia użytkownika. Przy podawaniu nazwy SPN dozwolone jest użycie jednego symbolu wieloznacznego.

Na przykład: 
TERMSRV/host.zasobyludzkie.fabrikam.com — serwer terminali działający na komputerze host.zasobyludzkie.fabrikam.com;
TERMSRV/* — serwer terminali działający na wszystkich komputerach. 
TERMSRV/*.zasobyludzkie.fabrikam.com — serwer terminali działający na wszystkich komputerach w domenie zasobyludzkie.fabrikam.com.

Tego ustawienia można użyć w połączeniu z ustawieniem Zezwalaj na delegowanie świeżych poświadczeń w celu zdefiniowania wyjątków dla określonych serwerów, które byłyby dozwolone w przypadku używania symboli wieloznacznych na liście serwerów Zezwalaj na delegowanie świeżych poświadczeń.</string>
      <string id="DenySavedCredentials">Odmawiaj delegowania zapisanych poświadczeń</string>
      <string id="DenySavedCredentials_Explain">To ustawienie zasad jest stosowane do aplikacji używających składnika dostawcy obsługi zabezpieczeń Cred (na przykład serwera terminali).

Jeżeli to ustawienie zasad zostanie włączone, można określić, do których serwerów NIE mogą być delegowane zapisane poświadczenia użytkownika (zapisane poświadczenia to poświadczenia wybrane do zapisania/zapamiętania przy użyciu menedżera poświadczeń systemu Windows).

Jeżeli to ustawienie zasad zostanie wyłączone lub nie zostanie skonfigurowane (stan domyślny), nie zostanie określony żaden serwer.

Uwaga: Zasadę Odmawiaj delegowania zapisanych poświadczeń można ustawić dla co najmniej jednej głównej nazwy usługi (SPN). Nazwa SPN reprezentuje serwer docelowy, do którego NIE mogą być delegowane poświadczenia użytkownika. Przy podawaniu nazwy SPN dozwolone jest użycie jednego symbolu wieloznacznego.

Na przykład: 
TERMSRV/host.zasobyludzkie.fabrikam.com — serwer terminali działający na komputerze host.zasobyludzkie.fabrikam.com;
TERMSRV/* — serwer terminali działający na wszystkich komputerach. 
TERMSRV/*.zasobyludzkie.fabrikam.com — serwer terminali działający na wszystkich komputerach w domenie zasobyludzkie.fabrikam.com.

Tego ustawienia można użyć w połączeniu z ustawieniem Zezwalaj na delegowanie zapisanych poświadczeń w celu zdefiniowania wyjątków dla określonych serwerów, które byłyby dozwolone w przypadku używania symboli wieloznacznych na liście serwerów Zezwalaj na delegowanie zapisanych poświadczeń.</string>
      <string id="SUPPORTED_VISTA">Windows Vista</string>
    </stringTable>
    <presentationTable>
      <presentation id="AllowDefaultCredentials">
        <listBox refId="AllowDefaultCredentials_Name">Dodaj serwery do listy:</listBox>
        <checkBox refId="ConcatenateDefaults_ADC" defaultChecked="true">Dołącz domyślne ustawienia systemu do powyższych parametrów</checkBox>
      </presentation>
      <presentation id="AllowDefCredentialsWhenNTLMOnly">
        <listBox refId="AllowDefCredentialsWhenNTLMOnly_Name">Dodaj serwery do listy:</listBox>
        <checkBox refId="ConcatenateDefaults_ADCN" defaultChecked="true">Dołącz domyślne ustawienia systemu do powyższych parametrów</checkBox>
      </presentation>
      <presentation id="AllowFreshCredentials">
        <listBox refId="AllowFreshCredentials_Name">Dodaj serwery do listy:</listBox>
        <checkBox refId="ConcatenateDefaults_AFC" defaultChecked="true">Dołącz domyślne ustawienia systemu do powyższych parametrów</checkBox>
      </presentation>
      <presentation id="AllowFreshCredentialsWhenNTLMOnly">
        <listBox refId="AllowFreshCredentialsWhenNTLMOnly_Name">Dodaj serwery do listy:</listBox>
        <checkBox refId="ConcatenateDefaults_AFCN" defaultChecked="true">Dołącz domyślne ustawienia systemu do powyższych parametrów</checkBox>
      </presentation>
      <presentation id="AllowSavedCredentials">
        <listBox refId="AllowSavedCredentials_Name">Dodaj serwery do listy:</listBox>
        <checkBox refId="ConcatenateDefaults_ASC" defaultChecked="true">Dołącz domyślne ustawienia systemu do powyższych parametrów</checkBox>
      </presentation>
      <presentation id="AllowSavedCredentialsWhenNTLMOnly">
        <listBox refId="AllowSavedCredentialsWhenNTLMOnly_Name">Dodaj serwery do listy:</listBox>
        <checkBox refId="ConcatenateDefaults_ASCN" defaultChecked="true">Dołącz domyślne ustawienia systemu do powyższych parametrów</checkBox>
      </presentation>
      <presentation id="DenyDefaultCredentials">
        <listBox refId="DenyDefaultCredentials_Name">Dodaj serwery do listy:</listBox>
        <checkBox refId="ConcatenateDefaults_DDC" defaultChecked="true">Dołącz domyślne ustawienia systemu do powyższych parametrów</checkBox>
      </presentation>
      <presentation id="DenyFreshCredentials">
        <listBox refId="DenyFreshCredentials_Name">Dodaj serwery do listy:</listBox>
        <checkBox refId="ConcatenateDefaults_DFC" defaultChecked="true">Dołącz domyślne ustawienia systemu do powyższych parametrów</checkBox>
      </presentation>
      <presentation id="DenySavedCredentials">
        <listBox refId="DenySavedCredentials_Name">Dodaj serwery do listy:</listBox>
        <checkBox refId="ConcatenateDefaults_DSC" defaultChecked="true">Dołącz domyślne ustawienia systemu do powyższych parametrów</checkBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
