<?xml version="1.0" encoding="utf-8"?>
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://www.microsoft.com/GroupPolicy/PolicyDefinitions">
  <displayName>Połącz</displayName>
  <description>Zaznacz element, aby wyświetlić jego opis</description>
  <resources>
    <stringTable>
      <!-- general -->
      <string id="Connect">Połącz</string>
      
      <!-- prevent file sync-->
      <string id="AllowProjectionToPC">Nie zezwalaj na projekcję na tym komputerze</string>
      <string id="AllowProjectionToPC_help">To ustawienie zasad umożliwia wyłączenie projekcji na komputerze.

 

Jeśli to ustawienie zostanie włączone, nie będzie można wykryć komputera i wyświetlać na nim obrazu, o ile użytkownik nie uruchomi ręcznie aplikacji Połączenie.

 

Jeśli to ustawienie zostanie wyłączone lub nie zostanie skonfigurowane, będzie można wykryć komputer i wyświetlać na nim obraz tylko na powyższym ekranie blokady. Użytkownik może też na stałe włączyć lub wyłączyć to ustawienie z wyjątkiem ręcznego uruchomienia połączenia.</string>

      <string id="RequirePinForPairing">Wymagaj numeru PIN do parowania</string>
      <string id="RequirePinForPairing_help">To ustawienie zasad umożliwia włączenie wymagania numeru PIN do parowania.  

Jeśli zostanie włączone, podczas parowania nowych urządzeń będzie zawsze wymagany numer PIN. Jeśli zostanie wyłączone lub nie zostanie skonfigurowane, numer PIN nie będzie wymagany do parowania.</string>
   </stringTable>
  </resources>
</policyDefinitionResources>
