<?xml version="1.0"?>
<policyDefinitionResources revision="1.0" schemaVersion="1.0">
	<displayName>Przesyłanie dalej zdarzeń</displayName>

	<description>Definicje zasad dla przesyłania dalej zdarzeń</description>

	<resources>

		<stringTable>

			<string id="EventForwarding">Przesyłanie dalej zdarzeń</string>

                        <string id="ForwarderResourceUsage">ForwarderResourceUsage</string>
                        <string id="ForwarderResourceUsage_Help">Steruje użyciem zasobów usługi przesyłania dalej. Każde ustawienie jest stosowane do wszystkich subskrypcji usługi przesyłania dalej.</string>
 
			<string id="SubscriptionManager">Skonfiguruj adres serwera, interwał odświeżania i urząd certyfikacji wystawcy dla docelowego menedżera subskrypcji</string>

			<string id="Subscription_Help">To ustawienie zasad umożliwia skonfigurowanie adresu serwera, interwału odświeżania oraz urzędu certyfikacji wystawcy docelowego menedżera subskrypcji. Menedżer subskrypcji to komputer, do którego są przesyłane dalej zdarzenia.

Składnia: opcja=wartość[,opcja=wartość]*
Opcje:
Server (obowiązkowa) — adres komputera, do którego mają być przesyłane dalej zdarzenia. Jeśli serwer używa domyślnego protokołu transportu (HTTP) i portu (5985), należy użyć składni „Server=&lt;FQDN&gt;”, gdzie &lt;FQDN&gt; to w pełni kwalifikowana nazwa domeny serwera. W przeciwnym wypadku należy podać pełny adres URL. Na przykład w przypadku korzystania z protokołu HTTPS: Server=https://&lt;FQDN&gt;:5986/wsman/SubscriptionManager/WEC
</string>

			<string id="SubscriptionManager_Listbox">Menedżerowie subskrypcji</string>
		</stringTable>

	
		<presentationTable>

			<presentation id="SubscriptionManager">
                                
				<listBox refId="SubscriptionManager_Listbox">Menedżerowie subskrypcji</listBox>

			</presentation>

                        <presentation id="ForwarderResourceUsage">

                                <decimalTextBox refId="MaxForwardingRate" spinStep="100">Maksymalna dozwolona szybkość przesyłania dalej (zdarzenia/s) usługi przesyłania dalej: </decimalTextBox>

                        </presentation>

		</presentationTable>

	</resources>

</policyDefinitionResources>
