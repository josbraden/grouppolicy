<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
    <displayName>BranchCache</displayName>
    <description>O BranchCache permite que os clientes recuperem com segurança o conteúdo na filial em vez de ter que recuperá-lo do servidor que está hospedando o conteúdo. Dependendo do modo de implantação, o conteúdo pode ser recuperado de outros clientes na filial ou de um servidor de cache hospedado na filial. Um cliente só pode recuperar conteúdo da filial se ele for autorizado pelo servidor a fazê-lo. O uso do BranchCache reduz os custos no link da rede de longa distância (WAN) que conecta suas filiais ao centro de dados ou às matrizes e aumenta as velocidades de download para conteúdo que já tenha sido baixado na filial.
    </description>

    <resources>
        <stringTable>
            <string id="WBC_Cat">BranchCache</string>

            <string id="WBC_Enable">Ativar o BranchCache</string>

            <string id="WBC_Enable_Help">Essa configuração de diretiva especifica se o BranchCache está habilitado no computador cliente. O BranchCache reduz o uso dos links da rede de longa distância (WAN) que conecta as filiais ao centro de dados ou às matrizes e aumenta as velocidades de acesso para conteúdo que já tenha sido baixado na filial. O BranchCache faz isso permitindo que os computadores em uma filial armazenem em cache os arquivos e o tráfego HTTP dos servidores Intranet nos quais o BranchCache esteja habilitado e, em seguida, compartilhe com segurança os arquivos com outros computadores na filial. Os computadores na filial podem recuperar o conteúdo de um servidor de cache hospedado na filial (ao usar o modo Cache Hospedado) ou de outros computadores cliente na filial (ao usar o modo Cache Distribuído). Para acessar o conteúdo armazenado em cache, o computador deve ter permissões para acessar o conteúdo no servidor de origem.

Habilita essa configuração de diretiva nos computadores cliente nas filiais nas quais a largura de banda WAN esteja lenta, a latência esteja alta e haja um número de computadores cliente que precisam acessar os mesmos dados dos servidores no escritório central.

Se você habilitar essa configuração de diretiva, o BranchCache será ativado. Para que essa configuração de diretiva entre em vigor, você também deverá instalar o recurso BranchCache no computador cliente.

Se você habilitar ou não definir essa configuração de diretiva, o BranchCache será desativado.
            </string>

            <string id="WBC_Cache_Percent">Definir a porcentagem do espaço em disco usada pelo cache do computador cliente</string>

            <string id="WBC_Cache_Percent_Help">Essa configuração de diretiva altera a porcentagem padrão do espaço em disco total a ser dedicado para armazenamento em cache do conteúdo recuperado com o BranchCache. Esse conteúdo está disponível para outros computadores cliente solicitantes se eles forem autorizados pelo servidor a acessarem o conteúdo.

Se você habilitar essa configuração de diretiva, poderá configurar a porcentagem de espaço total em disco a ser alocada para o cache.

Se você desabilitar ou não definir essa configuração de diretiva, o cache será definido em 5% do espaço total em disco no computador cliente.
            </string>

            <string id="WBC_HostedCache_Location">Definir o modo Cache Hospedado do BranchCache</string>

            <string id="WBC_HostedCache_Location_Help">Essa configuração de diretiva especifica se o computador cliente deve usar o modo Cache Hospedado e, se puder, qual é o endereço do servidor BranchCache. O modo Cache Hospedado permite que um computador cliente recupere o conteúdo de um servidor BranchCache que atua como cache central (hospedado) de uma filial. Ao usar o modo Cache Hospedado, o conteúdo baixado pelos computadores cliente habilitados para BranchCache é levado para o servidor BranchCache, que, por sua vez, serve como cache centralizado para outros computadores cliente habilitados para BranchCache na filial. Os computadores cliente usam o protocolo SSL (Secure Sockets Layer) para se comunicarem com o servidor BranchCache.

Para usar essa configuração de diretiva, você deverá especificar o endereço do servidor BranchCache, e esse servidor deve corresponder ao Nome de Domínio Totalmente Qualificado (FQDN) do servidor especificado no certificado do servidor BranchCache. Além disso, o certificado raiz do certificado do servidor BranchCache deve estar no repositório raiz confiável do computador cliente.

Se você habilitar essa configuração de diretiva e especificar um local válido do cache armazenado em host, o modo Cache Hospedado será ativado. Para que essa configuração de diretiva entre em vigor, você também precisará habilitar a configuração de diretiva "Ativar BranchCache".

Se você desabilitar ou não definir essa configuração de diretiva, o modo Cache Hospedado será desativado.
            </string>

            <string id="WBC_Distributed_Enable">Definir o modo Cache Distribuído do BranchCache</string>

            <string id="WBC_Distributed_Enable_Help">Essa configuração de diretiva especifica se o computador cliente deve usar o modo Cache Distribuído. Esse modo BranchCache permite que um computador cliente recupere o conteúdo que foi baixado e armazenado em cache por outros computadores cliente na filial. Para acessar o conteúdo armazenado em cache dos outros computadores cliente na filial, o computador deverá ter permissões para acessar o conteúdo no servidor de origem.

Essa configuração de diretiva especifica se o modo Cache Distribuído é usado. Habilite essa configuração de diretiva ao usar o BranchCache nas filiais para o qual não há nenhum servidor atuando como um cache hospedado.

Se você habilitar essa configuração de diretiva, o modo Cache Distribuído será usado. Para que essa configuração de diretiva entre em vigor, você também precisará habilitar a configuração de diretiva "Ativar BranchCache".

Se você desabilitar ou não definir essa configuração de diretiva, o modo Cache Distribuído será desativado.
            </string>

            <string id="WBC_SMB_Enable">Configurar o BranchCache para arquivos de rede</string>

            <string id="WBC_SMB_Enable_Help">Essa configuração de diretiva altera o valor de latência de rede completo padrão acima do qual os arquivos de rede são armazenados em cache pelos computadores cliente na filial. O BranchCache para arquivos de rede permite que os computadores em uma filial armazenem em cache os dados dos servidores Intranet nos quais o BranchCache esteja habilitado e, em seguida, compartilhe com segurança os arquivos com outros computadores na filial. Um dos tipos de conteúdo que pode ser armazenado em cache em uma filial são arquivos de rede usando o protocolo SMB (Server Message Block).

Por padrão, os arquivos de rede são armazenados em cache na filial quando a latência completa de rede da rede de longa distância (WAN) é superior a 80 milissegundos. Para armazenar em cache sempre os arquivos de rede baixados na filial, defina o valor de latência de rede como 0. Para desabilitar o armazenamento em cache da filial para arquivos de rede, defina o valor de latência para um valor muito alto.

Se você habilitar essa configuração de diretiva, poderá configurar a latência completa de rede acima da qual os arquivos de rede devem ser armazenados em cache pelos computadores cliente na filial.

Se você desativar ou não definir essa configuração de diretiva, o computador cliente armazenará os arquivos de rede se a latência completa de rede do link de rede de longa distância estiver acima de 80 milissegundos.
            </string>

        </stringTable>

        <presentationTable>
            <presentation id="WBC_Cache_Size_Exact_Presentation">
                <decimalTextBox refId="WBC_Cache_Size_Exact_dctxtbox" defaultValue="10485760" spinStep="10485760">
                </decimalTextBox>
            </presentation>

            <presentation id="WBC_Cache_Size_Percent_Presentation">
                <decimalTextBox refId="WBC_Cache_Size_Percent_dctxtbox" defaultValue="5" spinStep="5">
                </decimalTextBox>
            </presentation>

            <presentation id="WBC_Cache_Location_TextBox">
                <textBox refId="WBC_Cache_TextBox">
                    <label>Digite o local do Cache do Computador Cliente</label>
                </textBox>
            </presentation>

            <presentation id="WBC_Hosted_Cache_Location_TextBox">
                <textBox refId="WBC_Cache_TextBox">
                    <label>Digite o local do cache hospedado</label>
                </textBox>
            </presentation>

            <presentation id="WBC_SMB_DecimalTextBox">
              <text>Digite o valor total de latência de rede em milissegundos acima do qual os arquivos de rede devem ser armazenados em cache na filial</text>  
              <decimalTextBox refId="WBC_SMBLatency_DecimalTextBox" defaultValue="80" spinStep="20">
                </decimalTextBox>
            </presentation>

        </presentationTable>
    </resources>
</policyDefinitionResources>
