<?xml version="1.0"?>
<policyDefinitionResources revision="1.0" schemaVersion="1.0">
	<displayName>Encaminhamento de Eventos</displayName>

	<description>Definições de Diretiva para Encaminhamento de Eventos</description>

	<resources>

		<stringTable>

			<string id="EventForwarding">Encaminhamento de Eventos</string>

                        <string id="ForwarderResourceUsage">ForwarderResourceUsage</string>
                        <string id="ForwarderResourceUsage_Help">Controla o uso de recursos para o encaminhador. Cada configuração se aplica a todas as assinaturas para o encaminhador.</string>
 
			<string id="SubscriptionManager">Configure o endereço do servidor, o intervalo de atualização e a autoridade de emissão de certificado de um Gerenciador de Assinatura de destino</string>

			<string id="Subscription_Help">Esta configuração de diretiva permite que você configure o endereço do servidor, o intervalo de atualização e a CA (autoridade de certificação) de emissão de um Gerenciador de Assinatura de destino. O Gerenciador de Assinatura é o computador para o qual os eventos são encaminhados.

Sintaxe: Option=Value[,Option=Value]*
Opções:
Servidor (obrigatório) - endereço do computador para o qual os eventos deverão ser encaminhados. Quando o servidor usar o protocolo de transporte padrão (HTTP) e a porta (5985), use "Server=&lt;FQDN&gt;" onde &lt;FQDN&gt; é o nome de domínio totalmente qualificado do servidor. Caso contrário, especifique a URL completa. Por exemplo, ao usar o protocolo HTTPS, "Server=https://&lt;FQDN&gt;:5986/wsman/SubscriptionManager/WEC"
</string>

			<string id="SubscriptionManager_Listbox">SubscriptionManagers</string>
		</stringTable>

	
		<presentationTable>

			<presentation id="SubscriptionManager">
                                
				<listBox refId="SubscriptionManager_Listbox">SubscriptionManagers</listBox>

			</presentation>

                        <presentation id="ForwarderResourceUsage">

                                <decimalTextBox refId="MaxForwardingRate" spinStep="100">A taxa de encaminhamento máxima (eventos/segundo) permitida para o encaminhador: </decimalTextBox>

                        </presentation>

		</presentationTable>

	</resources>

</policyDefinitionResources>
