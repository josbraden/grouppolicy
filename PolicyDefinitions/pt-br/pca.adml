<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>digite o nome para exibição aqui</displayName>
  <description>digite a descrição aqui</description>
  <resources>
    <stringTable>


      <!-- Overall category text -->
      <string id="PcaScenarioCategory">Diagnóstico de Compatibilidade de Aplicativo</string>
      

      <!-- Generic WDI text -->
      <string id="WdiScenarioExecutionPolicyLevelResolution">Detecção, Solução de Problemas e Resolução</string>
      <string id="WdiScenarioExecutionPolicyLevelTsOnly">Somente Detecção e Solução de Problemas</string>


      <!-- Individual scenario text -->
      <string id="DetectInstallFailuresText">Detectar falhas de instalação do aplicativo</string>
      <string id="DetectUpdateFailuresText">Detectar aplicativos incapazes de abrir instaladores no UAC</string>
      <string id="DetectDeprecatedComponentFailuresText">Detectar falhas de aplicativo causadas por DLLs do Windows substituídos</string>
      <string id="DetectDeprecatedCOMComponentFailuresText">Detectar falhas de aplicativos causados pelo objetos COM substituídos</string>
      <string id="DetectUndetectedInstallersText">Detectar instaladores de aplicativo que precisam ser executados como administrador</string>
      <string id="DetectBlockedDriversText">Notificar drivers bloqueados</string>
      <!-- End individual scenario text -->


      <!-- Begin install failure scenario description -->
      <string id="DetectInstallFailuresExplain">Esta diretiva configura o PCA (Auxiliar de Compatibilidade de Programa) para diagnosticar falhas com as instalações de aplicativos. 

Se você habilitar esta configuração de diretiva, o PCA será configurado para detectar falhas na execução dos instaladores de aplicativo através de heurística. Quando forem detectadas possíveis falhas, o PCA fornecerá ao usuário a opção de reiniciar o instalador com o modo de compatibilidade do Microsoft Windows XP.

Se você desabilitar essa configuração de diretiva, o PCA não será configurado para detectar falhas na execução dos instaladores de programa.

Se você não configurar essa diretiva, o PCA ativará esse cenário de diagnóstico por padrão.

Observação: desabilitar a configuração de diretiva "Desativar Auxiliar de Compatibilidade de Programa" fará com que essa configuração de diretiva não tenha efeito. O DPS (Serviço de Diretiva de Diagnóstico) e o Serviço do Auxiliar de Compatibilidade de Programa devem estar em execução para que o PCA seja executado. Esses serviços podem ser configurados com o snap-in Serviços para o Console de Gerenciamento Microsoft.</string>
      <!-- End install failure scenario description -->
      

      <!-- Begin update failure scenario description -->
      <string id="DetectUpdateFailuresExplain">Essa diretiva configura o PCA (Auxiliar de Compatibilidade de Programa) para diagnosticar falhas com os programas no UAC (Controle de Conta de Usuário). 

Se você habilitar esta configuração de diretiva, o PCA vai detectar os programas que não conseguem iniciar processos filhos que são instaladores (em geral, atualizadores). Quando essa falha for detectada, o PCA aplicará o modo de compatibilidade ELEVATECREATEPROCESS, que permite que o programa abra com êxito o instalador com privilégios de administrador na próxima vez que o programa for executado.

Se você desabilitar essa configuração de diretiva, o PCA não detectará os aplicativos que não conseguem abrir os instaladores executados no UAC.

Se você não configurar essa diretiva, o PCA detecta os programas que não conseguiram abrir processos filhos que são instaladores (em geral, atualizadores).

Observação: desabilitar a configuração de diretiva "Desativar Auxiliar de Compatibilidade de Programa" fará com que essa configuração de diretiva não tenha efeito. O DPS (Serviço de Diretiva de Diagnóstico) e o Serviço do Auxiliar de Compatibilidade de Programa devem estar em execução para que o PCA seja executado. Esses serviços podem ser configurados com o snap-in Serviços para o Console de Gerenciamento Microsoft.</string>
      <!-- End update failure scenario description -->
      

      <!-- Begin deprecated component failure scenario description -->
      <string id="DetectDeprecatedComponentFailuresExplain">Essa configuração de diretiva determina se o PCA (Auxiliar de Compatibilidade de Programa) diagnosticará falhas no carregamento de DLL nos programas. 

Se você habilitar essa configuração de diretiva, o PCA detectará os programas que tentarem carregar DLLs herdadas do Microsoft Windows que tenham sido removidos desta versão do Windows. Quando for detectada essa falha, depois que o programa for encerrado, o PCA notificará o usuário sobre esse problema e fornecerá uma opção para verificar o site da Microsoft em busca de soluções.

Se você desabilitar essa configuração de diretiva, o PCA não detectará os programas que tentarem carregar DLLs herdadas do Windows.

Se você não definir essa configuração de diretiva, o PCA detectará os programas que tentarem carregar DLLs herdadas do Windows.

Observação: desabilitar a configuração de diretiva "Desativar Auxiliar de Compatibilidade de Programa" fará com que esta configuração de diretiva não tenha efeito. O DPS (Serviço de Diretiva de Diagnóstico) e o Serviço do Auxiliar de Compatibilidade de Programa devem estar em execução para que o PCA seja executado. Esses serviços podem ser configurados com o snap-in Serviços para o Console de Gerenciamento Microsoft.</string>
      <!-- End deprecated component failure scenario description -->


      <!-- Begin deprecated COM component failure scenario description -->
      <string id="DetectDeprecatedCOMComponentFailuresExplain">Essa configuração de diretiva determina se o PCA (Auxiliar de Compatibilidade de Programa) diagnosticará falhas no carregamento de DLL ou na criação de objetos COM nos programas. 

Se você habilitar essa configuração de diretiva, o PCA detectará programas que tentarem carregar objetos COM herdados que tenham sido removidos nesta versão do Windows. Quando for detectada essa falha, depois que o programa for encerrado, o PCA notificará o usuário sobre esse problema e fornecerá uma opção para verificar o site da Microsoft em busca de soluções.

Se você desabilitar essa configuração de diretiva, o PCA não detectará os programas que tentarem criar os objetos COM herdados.

Se você não definir essa configuração de diretiva, o PCA detectará os programas que tentarem criar objetos COM herdados.

Observação: desabilitar a configuração de diretiva "Desativar Auxiliar de Compatibilidade de Programa" fará com que essa configuração de diretiva não tenha efeito. O DPS (Serviço de Diretiva de Diagnóstico) e o Serviço do Auxiliar de Compatibilidade de Programa devem estar em execução para que o PCA seja executado. Esses serviços podem ser configurados com o snap-in de Serviços para o Console de Gerenciamento Microsoft.</string>
      <!-- End deprecated COM component failure scenario description -->


      <!-- Begin undetected installer failure scenario description -->
      <string id="DetectUndetectedInstallersExplain">Essa configuração de diretiva determina se o PCA (Auxiliar de Compatibilidade de Programa) diagnosticará falhas nos instaladores de aplicativo que não são detectados para executar como administrador. 

Se você habilitar essa configuração de diretiva, o PCA será configurado para detectar os instaladores de aplicativos que não têm privilégios para serem executados como administrador concedidos pelo UAC (Controle de Acesso de Usuário). Quando forem detectadas possíveis falhas, o PCA fornecerá ao usuário a opção de reiniciar o instalador como administrador.

Se você desabilitar essa configuração de diretiva, o PCA não detectará os instaladores de aplicativos que não tiverem privilégios de serem executados como administrador concedidos pelo UAC.

Se você não definir essa configuração de diretiva, o PCA será configurado para detectar os instaladores de aplicativos que não tiverem privilégios de serem executados como administrador concedidos pelo UAC.


Observação: desabilitar a configuração de diretiva "Desativar Auxiliar de Compatibilidade de Programa" fará com que esta configuração de diretiva não tenha efeito. O DPS (Serviço de Diretiva de Diagnóstico) e o Serviço do Auxiliar de Compatibilidade de Programa devem estar em execução para que o PCA seja executado. Esses serviços podem ser configurados com o snap-in Serviços para o Console de Gerenciamento Microsoft.</string>
      <!-- End undetected installer failure scenario description -->


      <!-- Begin blocked drivers scenario description -->
      <string id="DetectBlockedDriversExplain">Essa configuração de diretiva determina se o PCA (Auxiliar de Compatibilidade de Aplicativo) diagnosticará os drivers bloqueados devido a problemas de compatibilidade. 

Se você habilitar essa configuração de diretiva, o PCA notificará o usuário sobre problemas de driver bloqueado com a opção de verificar o site da Microsoft em busca de soluções.

Se você desabilitar essa configuração de diretiva, o PCA não notificará o usuário sobre problemas de driver bloqueado. Observação: com essa configuração de diretiva desabilitada, o usuário não receberá soluções para os drivers bloqueados.

Se você não definir esta configuração de diretiva, o PCA notificará o usuário sobre problemas de driver bloqueado com a opção de verificar o site da Microsoft em busca de soluções.

Observação: desabilitar a configuração de diretiva "Desativar Auxiliar de Compatibilidade de Programa" fará com que esta configuração de diretiva não tenha efeito. O DPS (Serviço de Diretiva de Diagnóstico) e o Serviço do Auxiliar de Compatibilidade de Programa devem estar em execução para que o PCA seja executado. Esses serviços podem ser configurados com o snap-in Serviços para o Console de Gerenciamento Microsoft.</string>
      <!-- End blocked drivers scenario description -->
    </stringTable>

    <presentationTable>
      <presentation id="DetectUpdateFailuresTable">
        <dropdownList refId="DetectUpdateFailuresLevel" noSort="true" defaultItem="1">Nível de Execução do Cenário</dropdownList>
      </presentation>
      <presentation id="DetectDeprecatedComponentFailuresTable">
        <dropdownList refId="DetectDeprecatedComponentFailuresLevel" noSort="true" defaultItem="1">Nível de Execução do Cenário</dropdownList>
      </presentation>
      <presentation id="DetectDeprecatedCOMComponentFailuresTable">
        <dropdownList refId="DetectDeprecatedCOMComponentFailuresLevel" noSort="true" defaultItem="1">Nível de Execução do Cenário</dropdownList>
      </presentation>
      <presentation id="DetectUndetectedInstallersTable">
        <dropdownList refId="DetectUndetectedInstallersLevel" noSort="true" defaultItem="1">Nível de Execução do Cenário</dropdownList>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
