<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>digite o nome para exibição aqui</displayName>
  <description>digite a descrição aqui</description>
  <resources>
    <stringTable>
      <string id="DNS_Client">Cliente DNS</string>
      <string id="DNS_Domain">Sufixo DNS específico de conexão</string>
      <string id="DNS_Domain_Help">Especifica um sufixo DNS específico da conexão. Esta configuração prevalece sobre os sufixos DNS específicos da conexão definidos nos computadores aos quais ela é aplicada, aqueles configurados localmente e aqueles configurados por meio do protocolo DHCP.

Aviso: o sufixo DNS específico da conexão definido nesta configuração será aplicado a todas as conexões de rede utilizadas por computadores multihomed aos quais esta configuração for aplicada.

Para usar esta configuração, clique em Habilitar e digite, no campo disponível, o valor de cadeia de caracteres que representa o sufixo DNS.

Se não estiver definida, esta configuração não será aplicada a nenhum computador e os computadores usarão seus parâmetros de configuração local ou DHCP.</string>
      <string id="DNS_NameServer">Servidores DNS</string>
      <string id="DNS_NameServer_Help">Define os servidores DNS para os quais um computador envia consultas ao tentar resolver nomes.

Aviso: a lista dos servidores DNS definidos nesta configuração prevalece sobre os servidores DNS configurados localmente e sobre aqueles configurados por meio do protocolo DHCP. A lista de servidores DNS se aplicará a todas as conexões de rede de computadores multihomed aos quais esta configuração for aplicada.

Para usar esta configuração, clique em Habilitar e digite uma lista de endereços IP delimitados por espaço (em formato decimal com ponto) no campo disponível. Se você habilitar esta configuração, deverá digitar pelo menos um endereço IP.

Se não estiver definida, esta configuração não será aplicada a nenhum computador e os computadores usarão seus parâmetros locais ou configurados por meio do protocolo DHCP.</string>
      <string id="DNS_PrimaryDnsSuffix">Sufixo DNS primário</string>
      <string id="DNS_PrimaryDnsSuffix_Help">Especifica o sufixo primário do sistema de nomes de domínios (DNS) para todos os computadores afetados. O sufixo DNS primário é usado no registro do nome DNS e na resolução de nome DNS.

Esta configuração permite que você especifique um sufixo DNS primário para um grupo de computadores e impede que usuários, inclusive administradores, o alterem.

Se você desabilitar esta configuração ou não a definir, cada computador usará seu sufixo DNS primário local, que geralmente é o nome DNS do domínio do Active Directory no qual ele ingressa. Entretanto, administradores podem usar Sistema no Painel de Controle para alterar o sufixo DNS primário de um computador.

Para usar esta configuração, na caixa de texto apropriada, digite o sufixo DNS primário completo que você deseja atribuir. Por exemplo, microsoft.com.

Esta configuração não desabilita a caixa de diálogo Sufixo DNS e Nome de Computador NetBIOS que os administradores usam para alterar o sufixo DNS primário de um computador. Entretanto, se os administradores digitarem um sufixo, ele será ignorado enquanto esta configuração estiver habilitada.

Importante: para que as alterações feitas nesta configuração entrem em vigor, você deve reiniciar o Windows em todos os computadores afetados pela configuração.

Observação: para alterar o sufixo DNS primário de um computador sem definir uma diretiva, clique em Sistema no Painel de Controle, clique na guia Identificação de Rede, em Propriedades, em Mais e digite um sufixo na caixa Sufixo DNS primário deste computador.

Para obter mais informações sobre DNS, consulte DNS (Domain Name System) na Ajuda.</string>
      <string id="DNS_RegisterAdapterName">Registrar registros DNS com sufixo DNS específico da conexão</string>
      <string id="DNS_RegisterAdapterName_Help">Determina se um computador que estiver realizando um registro dinâmico poderá registrar os registros de recursos A e PTR com uma concatenação do seu Nome de Computador e de um sufixo DNS específico da conexão, além de registrá-los com uma concatenação do seu Nome de Computador e do sufixo DNS primário.

Aviso: se habilitada, esta configuração de grupo será aplicada a todas as conexões de rede de computadores multihomed aos quais ela for aplicada.

Por padrão, um cliente DNS que realiza um registro de DNS dinâmico registra os registros de recursos A e PTR com uma concatenação do seu Nome de Computador e do sufixo DNS primário. Por exemplo, a concatenação do Nome de Computador "meucomputador" e do sufixo DNS primário "microsoft.com" resultaria em "meucomputador.microsoft.com".

Se esta configuração estiver habilitada, o computador registrará os registros de recursos A e PTR com o sufixo DNS específico da conexão, além de registrá-los com o sufixo DNS primário. Por exemplo, a concatenação do nome de computador "meucomputador" e do sufixo DNS específico da conexão "VPNconnection" seria usada ao registrar os registros de recursos A e PTR, resultando em "meucomputador.VPNconnection". Observe que se o registro de DNS dinâmico estiver desativado em um computador ao qual essa configuração for aplicada, independentemente das definições dessa configuração, o computador não tentará fazer o registro de DNS dinâmico dos registros A e PTR que contenham uma concatenação do seu nome de computador e do sufixo DNS específico da conexão. Se o registro de DNS dinâmico estiver desabilitado em uma conexão de rede específica de um computador ao qual esta configuração for aplicada, independentemente das definições dessa configuração, o computador não tentará fazer o registro de DNS dinâmico dos registros A e PTR que contenham uma concatenação do seu Nome de Computador e do sufixo DNS específico da conexão.

Se esta configuração estiver desabilitada, um cliente DNS não registrará os registros de recursos A e PTR com seu sufixo DNS específico da conexão.

Se não estiver definida, esta configuração não será aplicada a nenhum computador e os computadores usarão a sua configuração local.</string>
      <string id="DNS_RegisterReverseLookup">Registrar registros PTR</string>
      <string id="DNS_RegisterReverseLookup_DoNotRegister">Não registrar</string>
      <string id="DNS_RegisterReverseLookup_Help">Determina se o registro de registros de recursos PTR está habilitado para os computadores aos quais essa diretiva se aplica.

Por padrão, os clientes DNS configurados para executar o registro de DNS dinâmico tentarão registrar os registros de recursos PTR somente se tiverem executado com êxito o registro de recursos A correspondente. Os registros de recursos A mapeiam um nome de host DNS para o endereço IP do host e os registros de recursos PTR mapeiam o endereço IP do host para o nome do host DNS.

Para habilitar esta configuração, clique em Habilitar e selecione um dos seguintes valores:

Não registrar - os computadores não tentarão registrar os registros de recursos PTR.

Registrar - os computadores tentarão registrar os registros de recursos PTR, independentemente do êxito do registro dos registros A.

Registrar somente se o registro de registros A tiver êxito - os computadores tentarão efetuar os registros de recursos PTR somente se tiverem registrado com êxito os registros de recursos A correspondentes.

Se não estiver definida, esta configuração não será aplicada a nenhum computador e os computadores usarão a sua configuração local.</string>
      <string id="DNS_RegisterReverseLookup_Register">Registrar</string>
      <string id="DNS_RegisterReverseLookup_RegisterOnlyIfASucceeds">Registrar somente se o registro A tiver êxito </string>
      <string id="DNS_RegistrationEnabled">Atualização Dinâmica</string>
      <string id="DNS_RegistrationEnabled_Help">Determina se a atualização dinâmica está habilitada.

Os computadores configurados para atualização dinâmica registram e atualizam automaticamente seus registros de recursos DNS em um servidor DNS.

Se você habilitar esta configuração, os computadores aos quais ela for aplicada poderão usar o registro de DNS dinâmico em todas as suas conexões de rede, dependendo da configuração de cada conexão de rede individual. Para um registro de DNS dinâmico ser ativado em uma conexão de rede específica, é necessário e suficiente que as configurações específicas da conexão e do computador permitam o registro de DNS dinâmico. Essa configuração controla a propriedade específica do computador que controla o registro de DNS dinâmico. Se habilitar esta configuração, você permitirá que a atualização dinâmica seja definida individualmente para cada conexão de rede.

Se desabilitá-la, os computadores aos quais ela for aplicada não poderão usar o registro de DNS dinâmico em nenhuma de suas conexões de rede, independentemente da configuração das conexões de rede individuais.

Se não estiver definida, esta configuração não será aplicada a nenhum computador e os computadores usarão a sua configuração local.</string>
      <string id="DNS_RegistrationOverwritesInConflict">Substituir Endereços em Conflito</string>
      <string id="DNS_RegistrationOverwritesInConflict_Help">Determina se um cliente DNS que tentar registrar seu registro de recursos A deverá substituir um ou mais desses registros existentes que contenham endereços IP conflitantes.

Esta configuração destina-se a computadores que registram registros de recursos A em zonas DNS que não oferecem suporte à Atualização Dinâmica Segura. A Atualização Dinâmica Segura preserva a propriedade dos registros de recursos e não permite que um cliente DNS substitua registros registrados por outros computadores.

Durante a atualização dinâmica de uma zona que não usa a Atualização Dinâmica Segura, um cliente DNS poderá descobrir que um registro de recursos A existente associa o nome do host DNS do cliente a um endereço IP de um outro computador. De acordo com a configuração padrão, o cliente DNS tenta substituir o registro de recursos A existente por um registro que associe o nome DNS ao endereço IP do cliente.

Se você habilitar esta configuração, os clientes DNS tentarão substituir registros de recursos A conflitantes durante a atualização dinâmica.

Se esta configuração for desabilitada, o cliente DNS ainda executará a atualização dinâmica de registros de recursos A, mas se ele tentar atualizar registros de recursos A com conflitos, essa tentativa falhará e um erro será registrado no log do Visualizador de Eventos.

Se não estiver definida, esta configuração não será aplicada a nenhum computador e os computadores usarão a sua configuração local.</string>
      <string id="DNS_RegistrationRefreshInterval">Intervalo de Atualização de Registro</string>
      <string id="DNS_RegistrationRefreshInterval_Help">Especifica o Intervalo de Atualização de Registros de recursos A e PTR para computadores aos quais essa configuração está aplicada. Esta configuração pode ser aplicada somente a computadores que usam a atualização dinâmica.

Os computadores que executam o Windows 2000 Professional e o Windows XP Professional e estejam configurados para realizar o registro de DNS dinâmico de recursos A e PTR fazem periodicamente um novo registro em servidores DNS, mesmo que os dados dos registros não tenham sido alterados. Esse novo registro é necessário para indicar aos servidores DNS que estejam configurados para remover (eliminar) automaticamente registros fora de uso que esses registros são atuais e devem ser preservados no banco de dados.

Aviso: se os registros de recursos DNS estiverem registrados em zonas com o recurso de eliminação ativado, o valor dessa configuração não deverá ser maior do que o intervalo de atualização configurado para essas zonas. Definir um Intervalo de Atualização de Registro maior do que o intervalo de atualização das zonas DNS poderá resultar na exclusão indesejada de registros de recursos A e PTR.

Para especificar o Intervalo de Atualização de Registro, clique em Habilitar e digite um valor maior que 1800. Lembre-se de que esse valor especifica o Intervalo de Atualização de Registro em segundos; por exemplo, 1800 segundos correspondem a 30 minutos.

Se não estiver definida, esta configuração não será aplicada a nenhum computador e os computadores usarão a sua configuração local.</string>
      <string id="DNS_RegistrationTtl">TTL definido nos registros A e PTR</string>
      <string id="DNS_RegistrationTtl_Help">Especifica o valor do campo Tempo de Vida (TTL) em recursos A e PTR registrados pelos computadores aos quais essa configuração for aplicada.

Para especificar o TTL, clique em Habilitar e digite um valor em segundos (por exemplo, o valor 900 corresponde a 15 minutos).

Se esta configuração não for definida, ela não será aplicada a nenhum computador.</string>
      <string id="DNS_SearchList">Lista de pesquisa de sufixos DNS</string>
      <string id="DNS_SearchList_Help">Determina os sufixos DNS a serem anexados a um nome de rótulo único não qualificado antes do envio de uma consulta DNS desse nome.

Nomes de rótulo único não qualificados não contêm pontos, como "exemplo". Esses nomes são diferentes de nomes de domínio totalmente qualificados, como "exemplo.microsoft.com".

Com esta configuração habilitada, quando um usuário enviar uma consulta de um nome de rótulo único, como "exemplo", um cliente DNS local anexará um sufixo, como "microsoft.com", resultando na consulta "exemplo.microsoft.com", antes de enviar a consulta para um servidor DNS.

Se habilitar esta configuração, você poderá especificar os sufixos DNS a serem anexados antes do envio de uma consulta de um nome de rótulo único não qualificado. Os valores dos sufixos DNS nessa configuração podem ser definidos com cadeias de caracteres separadas por vírgula, como "microsoft.com,serverua.microsoft.com,office.microsoft.com". Um sufixo DNS é anexado a cada envio da consulta. Se a consulta não tiver êxito, um novo sufixo DNS será adicionado no lugar do sufixo que falhou e uma nova consulta será enviada. Os valores são usados na ordem em que aparecem na cadeia de caracteres, começando pelo valor mais à esquerda e prosseguindo para a direita.

Se você habilitar esta configuração, especifique pelo menos um sufixo.

Se desativá-la, o sufixo DNS primário e os sufixos DNS específicos da conexão de rede serão anexados às consultas não qualificadas.

Se não estiver definida, esta configuração não será aplicada a nenhum computador e os computadores usarão a sua configuração local.</string>
      <string id="DNS_UpdateSecurityLevel">Atualizar Nível de Segurança</string>
      <string id="DNS_UpdateSecurityLevel_Help">Especifica se os computadores aos quais esta configuração estiver aplicada usarão a atualização dinâmica segura ou padrão ao registrarem registros DNS.

Para habilitar esta configuração, clique em Habilitar e selecione um dos seguintes valores:

Não seguro seguido de seguro - se esta opção for escolhida, os computadores enviarão atualizações dinâmicas seguras somente quando atualizações dinâmicas não seguras forem recusadas.

Apenas Não Seguro - se esta opção for escolhida, os computadores enviarão somente atualizações dinâmicas não seguras.

Apenas Seguro - se esta opção for escolhida, os computadores enviarão somente atualizações dinâmicas seguras.

Se não estiver definida, esta configuração não será aplicada a nenhum computador e os computadores usarão a sua configuração local.</string>
      <string id="DNS_UpdateSecurityLevel_OnlySecure">Apenas Seguro</string>
      <string id="DNS_UpdateSecurityLevel_OnlyUnsecure">Apenas Não Seguro</string>
      <string id="DNS_UpdateSecurityLevel_UnsecureFollowedBySecure">Não seguro seguido de seguro</string>
      <string id="DNS_UpdateTopLevelDomainZones">Atualizar Zonas de Domínio de Nível Superior</string>
      <string id="DNS_UpdateTopLevelDomainZones_Help">Especifica se os computadores aos quais esta diretiva for aplicada poderão enviar atualizações dinâmicas para as zonas definidas com um nome de rótulo único, também conhecidas como zonas de domínio de nível superior; por exemplo, "com".

Por padrão, um cliente DNS configurado para executar atualizações DNS dinâmicas envia essas atualizações para a zona DNS com autoridade sobre seus registros de recursos DNS, a menos que essa zona seja uma zona raiz e de domínio de nível superior.

Se esta diretiva estiver habilitada, os computadores aos quais ela for aplicada enviarão atualizações dinâmicas para qualquer zona com autoridade sobre os registros de recursos que o computador precisa atualizar, exceto a zona raiz.

Se esta diretiva estiver desabilitada, os computadores aos quais ela for aplicada não enviarão atualizações dinâmicas para as zonas raiz e/ou de domínio de nível superior com autoridade sobre os registros de recursos que o computador precisa atualizar.

Se não estiver definida, esta configuração não será aplicada a nenhum computador e os computadores usarão a sua configuração local.</string>
      <string id="DNS_UseDomainNameDevolution">Devolução de Sufixo DNS Primário</string>
      <string id="DNS_UseDomainNameDevolution_Help">Determina se o cliente DNS devolve o sufixo DNS primário em um processo de resolução de nome.

Quando um usuário envia uma consulta de um nome de rótulo único, como "exemplo", um cliente DNS local anexa um sufixo, como "microsoft.com", o que resulta na consulta "exemplo.microsoft.com", antes que a consulta seja enviada para um servidor DNS.

Se uma Lista de Pesquisa de Sufixo DNS não for especificada, o cliente DNS anexará o sufixo DNS primário a um nome de rótulo único e, se a consulta falhar, o sufixo DNS específico da conexão será anexado para uma nova consulta. Se nenhuma dessas consultas for resolvida, o cliente devolverá o sufixo DNS primário do computador (eliminará o rótulo mais à esquerda do Sufixo DNS Primário), anexará esse Sufixo DNS Primário devolvido ao nome de rótulo único e enviará essa nova consulta para um servidor DNS.

Por exemplo, se o sufixo DNS primário ooo.aaa.microsoft.com for anexado ao nome de rótulo único não terminado em ponto "exemplo" e a consulta DNS de exemplo.ooo.aaa.microsoft.com falhar, o cliente DNS devolverá o sufixo DNS primário (eliminará o rótulo mais à esquerda) e enviará uma consulta para exemplo.aaa.microsoft.com. Se ela falhar, o sufixo DNS primário será devolvido, se estiver sob um nível de devolução especificado, e a consulta exemplo.microsoft.com será enviada. Se essa consulta falhar, a devolução continuará, se estiver sob um nível de devolução especificado, e a consulta exemplo.microsoft.com será enviada. O sufixo DNS primário não será mais devolvido além do nível de devolução. 

Se esta configuração for habilitada com o nível de devolução adequado, os clientes DNS nos computadores aos quais ela for aplicada tentarão resolver nomes que sejam concatenações do nome de rótulo único a ser resolvido e do Sufixo DNS Primário devolvido.

Se esta configuração for desabilitada, os clientes DNS nos computadores aos quais ela for aplicada não tentarão resolver nomes que sejam concatenações do nome de rótulo único a ser resolvido e do sufixo DNS Primário devolvido.

Se não for definida, essa configuração não será aplicada a nenhum computador e os computadores usarão a sua configuração local.</string>
      <string id="DNS_DomainNameDevolutionLevel">Nível de Devolução do Sufixo DNS Primário</string>
      <string id="DNS_DomainNameDevolutionLevel_Help">Esta configuração de política determina o nível de devolução do sufixo de Domain Name System (DNS) que serão usados pelos clientes DNS, se estes executarem a devolução de sufixo DNS primário em um processo de resolução de nome. Quando a devolução de sufixos DNS é ativada, o rótulo mais à esquerda de um sufixo DNS primário é descartado em todas as tentativas de consulta sucessivas, quando a consulta falha para um sufixo DNS primário foi anexado. O nível de devolução indica o número mínimo de rótulos que devem ser adicionados à string de consulta assim que o sufixo DNS primário for devolvido.

Quando um usuário envia uma consulta para um nome de rótulo único, como "exemplo", um cliente DNS local anexa um sufixo, como "microsoft.com", à consulta antes de enviá-la ao servidor DNS. Neste caso, isso resulta na consulta "exemplo.microsoft.com."
 
Se uma lista de pesquisa de sufixo DNS não for especificada, o cliente DNS anexará o sufixo DNS primário a um nome de rótulo único. Se a consulta falhar, o sufixo DNS específico de conexão será anexado a uma nova consulta. Se nenhuma dessas consultas forem resolvidas, o cliente devolverá o sufixo DNS primário do computador, anexando-o ao nome de rótulo único e enviando a nova consulta a um servidor DNS.

Por exemplo, se o servidor DNS primário aaa.bbb.ccc.ddd.microsoft.com for anexado ao nome de rótulo único "exemplo" (sem ponto no final) e a devolução do sufixo DNS estiver ativada e configurada para o nível 3, as seguintes consultas serão executadas:  

Exemplo.aaa.bbb.ccc.ddd.microsoft.com (Se essa consulta falhar, o sufixo DNS primário devolverá para bbb.ccc.ddd.microsoft.com na próxima.)

Exemplo.bbb.ccc.ddd.microsoft.com (Se essa consulta falhar, o sufixo DNS primário devolverá para ccc.ddd.microsoft.com na próxima.)

Exemplo.ccc.ddd.microsoft.com (Se essa consulta falhar, o sufixo DNS primário devolverá para ddd.microsoft.com na próxima.)

Exemplo.ddd.microsoft.com (Se essa consulta falhar, nenhuma outra consulta poderá ser feita em seguida porque o nível de devolução foi configurado para 3 e o sufixo DNS primário contém 3 rótulos.)
 
Se esta configuração de diretiva for ativada, os clientes DNS nos computadores onde ela for aplicada tentarão resolver nomes que são concatenações do nome de rótulo único e o sufixo DNS primário devolvido. Os clientes DNS devolverão o sufixo DNS primário em cada tentativa de consulta até que o nome seja resolvido com êxito, o nível de devolução especificado seja atingido ou o sufixo DNS primário tenha dois rótulos.
 
Se a configuração de diretiva for desativada ou não for configurada, os clientes DNS dos computadores em que esta estiver aplicada não tentarão resolver nomes que são concatenações do nome de rótulo único e o sufixo DNS primário devolvido. Se um FRD (domínio raiz da floresta) estiver presente, nenhuma lista de pesquisa for configurada e a consulta for para um nome de rótulo único, o cliente DNS será devolvido ao FRD até que o nome seja resolvido com êxito.</string>
      <string id="DNS_TurnOffMulticast">Desativar Inicialização por Multicast</string>
      <string id="DNS_TurnOffMulticast_Help">O LLMNR (Local Link Multicast Name Resolution) é um protocolo secundário de resolução de nome. As consultas são enviadas pelo Link Local, uma sub-rede única, de uma máquina cliente usando Multicast à qual outro cliente no mesmo link, que também possui o LLMNR habilitado, pode responder. O LLMNR apresenta resolução de nome em cenários em que a resolução de nome DNS convencional não é possível.

Se você habilitar esta configuração de diretiva, a resolução de nome Multicast ou LLMNR será desativada para a máquina em todos os adaptadores de rede disponíveis mas não configurados.

Se você desabilitar‏ esta configuração de diretiva, a resolução de nome Multicast ou LLMNR será ativada para a máquina em todos os adaptadores de rede disponíveis mas não configurados.

Se você não definir esta configuração, a resolução de nome Multicast ou LLMNR será ativada por padrão para a máquina em todos os adaptadores de rede disponíveis mas não configurados.</string>
      <string id="DNS_AppendToMultiLabelName">Permitir Anexação de Sufixos DNS a Consultas de Nome com Vários Termos Não Qualificado</string>
      <string id="DNS_AppendToMultiLabelName_Help">Especifica se os computadores aos quais essa configuração se aplica podem anexar sufixos a um nome com vários termos não qualificado, antes de enviar consultas de DNS subsequentes, se a consulta do nome original falhar.

Um nome que contém pontos, mas não termina com ponto, chama-se nome com vários termos não qualificado, por exemplo, "server.corp". Um nome totalmente qualificado terminaria com um ponto, por exemplo, "server.corp.contoso.com.".

Se você habilitar esta configuração, os sufixos poderão ser anexados a um nome com vários termos não qualificado, se a consulta do nome original falhar. Por exemplo, o nome com vários termos não qualificado "server.corp" será consultado primeiro pelo Cliente DNS. Se a consulta for bem-sucedida, a resposta voltará para o cliente. Se a consulta falhar, o nome com vários termos não qualificado será anexado com Sufixos DNS configurados para as consultas do computador. Esses sufixos podem ser derivados de uma combinação do sufixo do domínio primário do Cliente DNS local, um sufixo de domínio específico da conexão e/ou Lista de Pesquisa de Sufixos DNS.

Por exemplo, se o Cliente DNS local recebe uma consulta de "server.corp", e um sufixo de domínio primário é configurado como "contoso.com", com essa configuração, o Cliente DNS enviará uma consulta de "server.corp.contoso.com.", se a consulta do nome original de "server.corp" falhar. 

Se você desabilitar esta configuração, nenhum sufixo será anexado a consultas de nome com vários termos não qualificado, se a consulta do nome original falhar. 

Se você não definir esta configuração, os computadores usarão sua configuração de Cliente DNS local para determinar o comportamento da consulta para nomes com vários termos não qualificados.</string>
    </stringTable>
    <presentationTable>
      <presentation id="DNS_Domain">
        <textBox refId="DNS_DomainLabel">
          <label>Sufixo DNS:</label>
        </textBox>
      </presentation>
      <presentation id="DNS_NameServer">
        <textBox refId="DNS_NameServerLabel">
          <label>Endereços IP:</label>
        </textBox>
      </presentation>
      <presentation id="DNS_PrimaryDnsSuffix">
        <textBox refId="DNS_PrimaryDnsSuffixBox">
          <label>Digite um sufixo DNS primário:</label>
        </textBox>
      </presentation>
      <presentation id="DNS_RegisterReverseLookup">
        <dropdownList refId="DNS_RegisterReverseLookup_Box" defaultItem="0">Registrar registros PTR:</dropdownList>
      </presentation>
      <presentation id="DNS_RegistrationRefreshInterval">
        <decimalTextBox refId="DNS_RegistrationRefreshIntervalLabel" defaultValue="1800" spinStep="100">Segundos:</decimalTextBox>
      </presentation>
      <presentation id="DNS_DomainNameDevolutionLevel">
        <decimalTextBox refId="DNS_DomainNameDevolutionLevelLabel" defaultValue="2" spinStep="1">Definir o nível de devolução do sufixo DNS primário</decimalTextBox>
      </presentation>
      <presentation id="DNS_RegistrationTtl">
        <decimalTextBox refId="DNS_RegistrationTtlLabel" defaultValue="600" spinStep="100">Segundos:</decimalTextBox>
      </presentation>
      <presentation id="DNS_SearchList">
        <textBox refId="DNS_SearchListLabel">
          <label>Sufixos DNS:</label>
        </textBox>
      </presentation>
      <presentation id="DNS_UpdateSecurityLevel">
        <dropdownList refId="DNS_UpdateSecurityLevel_Box" defaultItem="2">Atualizar Nível de Segurança:</dropdownList>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
