<?xml version="1.0" encoding="utf-8"?>
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://www.microsoft.com/GroupPolicy/PolicyDefinitions">
  <displayName>连接</displayName>
  <description>选择一个要查看其描述的项目</description>
  <resources>
    <stringTable>
      <!-- general -->
      <string id="Connect">连接</string>
      
      <!-- prevent file sync-->
      <string id="AllowProjectionToPC">不允许投影到这台电脑</string>
      <string id="AllowProjectionToPC_help">使用此策略设置可以禁止投影到某台电脑。

 

如果启用此设置，将无法发现你的电脑，也不能投影到其上，除非用户手动启动“连接”应用。

 

如果关闭或未配置此设置，则可发现你的电脑，但投影到其上时只能投影到锁屏界面上方。用户也可以选择始终启用它或关闭它，手动启动除外。</string>

      <string id="RequirePinForPairing">需要 PIN 进行配对</string>
      <string id="RequirePinForPairing_help">你可以使用此策略设置来要求配对时提供 PIN。

如果启用此设置，则新设备的配对仪式始终需要 PIN。如果关闭或未配置此设置，则配对不需要 PIN。</string>
   </stringTable>
  </resources>
</policyDefinitionResources>
