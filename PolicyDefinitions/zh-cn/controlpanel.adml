<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>请在此处输入显示名称</displayName>
  <description>请在此处输入描述</description>
  <resources>
    <stringTable>
      <string id="DisallowCpls">隐藏指定的“控制面板”项</string>
      <string id="DisallowCpls_Help">隐藏指定的控制面板项。

此设置从“控制面板”窗口和“开始”菜单中删除控制面板项(如“鼠标”、“系统”或“个性化”)。

若要隐藏控制面板项，请单击“显示”。在“显示内容”对话框的“值”列中，输入控制面板项的规范名称(例如“Microsoft.Mouse”、“Microsoft.System”或“Microsoft.Personalization”)。

注意: 对于 Windows Vista、Windows Server 2008 以及早期版本的 Windows，应输入模块名称(例如“timedate.cpl”或“inetcpl.cpl”)。如果某个控制面板项没有 CPL 文件或 CPL 文件包含多个小程序，则应输入其模块名称和字符串资源标识号(例如，输入“@systemcpl.dll,-1”指定“系统”，输入“@themecpl.dll,-1”指定“个性化”)。

在 MSDN 的 http://go.microsoft.com/fwlink/?LinkId=122973 中可以找到控制面板项的规范名称和模块名称的完整列表。

此设置影响“开始”菜单和“控制面板”窗口，以及访问控制面板项的其他方式(如“帮助和支持”中的快捷方式或使用 control.exe 的命令行)。

如果同时启用“隐藏指定的‘控制面板’项”设置和“只显示指定的‘控制面板’项”设置，则忽略“只显示指定的‘控制面板’项”设置。

注意: 不能使用此设置在“桌面”上下文菜单中隐藏“显示控制面板”项。若要隐藏“显示控制面板”项以防止用户修改计算机的显示设置，请改用“禁用‘显示控制面板’”设置。</string>
      <string id="ForceClassicControlPanel">在打开“控制面板”时始终打开所有控制面板项</string>
      <string id="ForceClassicControlPanel_Help">将“所有控制面板项”设置为默认“控制面板”视图。

如果禁用此策略，则“控制面板主页”为默认视图。</string>
      <string id="NoControlPanel">禁止访问“控制面板”</string>
      <string id="NoControlPanel_Help">禁用所有“控制面板”程序。

此设置防止“控制面板”的程序文件 Control.exe 启动。这样，用户无法启动“控制面板”或运行任何“控制面板”项。

此设置还从“开始”菜单删除“控制面板”。此设置还从 Windows 资源管理器删除“控制面板”文件夹。

如果用户尝试从上下文菜单上的“属性”项选择“控制面板”项，则会显示一条消息，说明某设置阻止此操作。</string>
      <string id="RestrictCpls">只显示指定的“控制面板”项</string>
      <string id="RestrictCpls_Help">隐藏在此设置中所指定之外的所有控制面板项和文件夹。

此设置从“控制面板”窗口和“开始”菜单中删除所有控制面板项(如“鼠标”、“系统”或“个性化”)。控制面板中仅显示在此设置中指定的项。

若要显示控制面板项，请单击“显示”，然后在“显示内容”对话框的“值”列中，输入控制面板项的规范名称(例如“Microsoft.Mouse”、“Microsoft.System”或“Microsoft.Personalization”)。

注意: 对于 Windows Vista、Windows Server 2008 以及早期版本的 Windows，应输入模块名称(例如“timedate.cpl”或“inetcpl.cpl”)。如果某个控制面板项没有 CPL 文件或 CPL 文件包含多个小程序，则应输入其模块名称和字符串资源标识号(例如，输入“@systemcpl.dll,-1”指定“系统”，输入“@themecpl.dll,-1”指定“个性化”)。

在 MSDN 的 http://go.microsoft.com/fwlink/?LinkId=122973 中可以找到控制面板项的规范名称和模块名称的完整列表。

此设置影响“开始”菜单和控制面板，以及访问控制面板项的其他方式(如“帮助和支持”中的快捷方式或使用 control.exe 的命令行)。

如果同时启用“隐藏指定的‘控制面板’项”设置和“只显示指定的‘控制面板’项”设置，则忽略“只显示指定的‘控制面板’项”设置。

注意: 不能使用此设置在“桌面”上下文菜单中隐藏“显示控制面板”项。若要隐藏“显示控制面板”项以防止用户修改计算机的显示设置，请改用“禁用‘显示控制面板’”设置。</string>
    </stringTable>
    <presentationTable>
      <presentation id="DisallowCpls">
        <listBox refId="DisallowCplsList">不允许的“控制面板”项的列表</listBox>
      </presentation>
      <presentation id="RestrictCpls">
        <listBox refId="RestrictCplsList">允许的“控制面板”项的列表</listBox>
      </presentation>
    </presentationTable>
  </resources>
</policyDefinitionResources>
