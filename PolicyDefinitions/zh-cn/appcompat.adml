<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>请在此处输入显示名称</displayName>
  <description>请在此处输入描述</description>
  <resources>
    <stringTable>
      <string id="AppCompat">应用程序兼容性</string>
      <string id="AppCompat_Prevent16Bit_Help">指定是否防止所有用户运行 MS-DOS 子系统 (ntvdm.exe)。此设置影响操作系统中 16 位应用程序的启动。默认情况下，允许所有用户运行 MS-DOS 子系统。

您可以使用此设置关闭 MS-DOS 子系统，这将减少资源使用并防止用户运行 16 位应用程序。若要运行任何 16 位应用程序或任何具有 16 位组件的应用程序，则必须允许运行 ntvdm.exe。启动第一个 16 位应用程序时，会启动 MS-DOS 子系统。该进程正在运行时，任何后续 16 位应用程序会更快地启动，但是系统的总体资源使用将增加。

如果将该状态设置为“已启用”，则会防止运行 ntvdm.exe，而这又会防止运行任何 16 位应用程序。另外，任何具有 16 位安装程序或其他 16 位组件的 32 位应用程序也无法运行。

如果将该状态设置为“已禁用”，则应用默认设置并允许所有用户运行 MS-DOS 子系统。

如果将该状态设置为“未配置”，则应用默认设置并允许所有用户运行 ntvdm.exe。但是，如果管理员将注册表 DWORD 值 HKLM\System\CurrentControlSet\Control\WOW\DisallowedPolicyDefault 设置为 1，则默认值更改为防止运行所有 16 位应用程序。

注意: “计算机配置”和“用户配置”中均包括此设置。如果同时配置了此设置，则“计算机配置”中的设置会替代“用户配置”中的设置。</string>
      <string id="AppCompat_Prevent16BitMach_Help">指定是否防止在该计算机上运行 MS-DOS 子系统 (ntvdm.exe)。此设置影响操作系统中 16 位应用程序的启动。默认情况下，允许此计算机上的所有用户运行 MS-DOS 子系统。

您可以使用此设置关闭 MS-DOS 子系统，这将减少资源使用并防止用户运行 16 位应用程序。若要运行任何 16 位应用程序或任何具有 16 位组件的应用程序，则必须允许运行 ntvdm.exe。启动第一个 16 位应用程序时，会启动 MS-DOS 子系统。MS-DOS 子系统正在运行时，任何后续 16 位应用程序会更快地启动，但是系统上的总体资源使用将增加。

如果将该状态设置为“已启用”，则会防止运行 ntvdm.exe，而这又会防止运行任何 16 位应用程序。另外，任何具有 16 位安装程序或其他 16 位组件的 32 位应用程序也无法运行。

如果将状态设置为“已禁用”，则应用默认设置并允许该计算机上的所有用户运行 MS-DOS 子系统。

如果将状态设置为“未配置”，则应用默认设置并允许所有用户运行 ntvdm.exe。但是，如果管理员将注册表 DWORD 值 HKLM\System\CurrentControlSet\Control\WOW\DisallowedPolicyDefault 设置为 1，则默认值更改为防止运行所有 16 位应用程序。

注意: “计算机配置”和“用户配置”中均包括此设置。如果同时配置了此设置，则“计算机配置”中的设置会替代“用户配置”中的设置。</string>
      <string id="AppCompat_RemoveProgramCompatPropPage_Help">此策略控制“程序兼容性”属性页命令行解释器扩展的可见性。此命令行解释器扩展在任何程序快捷方式或可执行文件的属性上下文菜单上可见。

兼容性属性页显示一组选项，可以选择这些选项并应用于应用程序，以解决影响旧版应用程序的最常见问题。启用此策略设置可从上下文菜单中删除属性页，但是不会影响以前应用于使用此接口的应用程序的兼容性设置。</string>
        <string id="AppCompat_TurnOffApplicationImpactTelemetry_Help">此策略控制系统中应用程序遥测引擎的状态。

应用程序遥测是一种跟踪机制，用于跟踪应用程序对特定 Windows 系统组件的匿名使用情况。

通过选择“启用”来关闭应用程序遥测将停止收集使用情况的数据。

如果关闭客户体验改善计划，也将关闭应用程序遥测，无论如何设置此策略都是如此。

禁用遥测将对所有新启动的应用程序生效。为了确保已对所有应用程序停止了遥测收集，请重新启动计算机。</string>
      <string id="AppCompat_TurnOffEngine_Help"> 此策略控制系统中应用程序兼容性引擎的状态。

该引擎是加载程序的一部分，每次在系统上启动应用程序时，它都检查兼容性数据库。如果找到匹配该应用程序的结果，它将提供运行时解决方案或兼容性修复程序，如果应用程序出现已知问题，则显示“应用程序帮助”消息。

关闭应用程序兼容性引擎将增强系统性能。但是，这将降低许多流行的旧版应用程序的兼容性，而且并不能阻止安装已知的不兼容应用程序。(例如: 如果安装旧的防病毒应用程序，则可能导致蓝屏。)

Windows 资源保护功能和 Windows 的用户帐户控制功能使用应用程序兼容性引擎提供对应用程序问题的缓解。如果关闭该引擎，这些缓解将不会应用到应用程序，而其安装程序和这些应用程序可能会安装失败或无法正常运行。

此选项对服务器管理员很有用，他们需要更快的性能并了解他们所用应用程序的兼容性。对于每秒钟可能启动数百次应用程序且加载程序的性能至关重要的 Web 服务器，该选项尤其有用。

注意: 由于性能原因，许多系统进程都缓存此设置的值。如果更改了此设置，请重新启动以确保系统准确反映这些更改。</string>
      <string id="AppCompat_TurnOffProgramCompatibilityAssistant_Help">此策略控制系统中“程序兼容性助理”的状态。
      
PCA 在运行时监视用户启动的程序以便发现已知的兼容性问题。无论何时只要检测到应用程序的潜在问题，PCA 都将使用指针提示用户推荐的解决方案。有关 PCA 包括的各种问题检测方案及其配置策略的详细信息，请参阅“系统-&gt;疑难解答和诊断-&gt;应用程序兼容性诊断”下的策略。 

默认情况下 PCA 为启用状态。

如果启用该策略设置，将关闭 PCA。此选项对服务器管理员很有用，他们需要更快的性能并了解他们所使用应用程序的兼容性。注意: 如果 PCA 关闭，运行应用程序时不会向用户提供已知兼容性问题的解决方案。

如果禁用或未配置该策略设置，PCA 将启用。 

注意: 若要执行 PCA，必须运行诊断策略服务 (DPS) 和程序兼容性助手服务。可以使用 Microsoft 管理控制台的服务管理单元配置这些服务。</string>
      <string id="AppCompat_TurnOffUserActionRecord_Help">该策略控制系统中问题步骤记录程序功能的状态。

问题步骤记录程序能够捕获用户在遇到问题之前所采取的步骤。问题步骤记录程序生成的输出数据将是另一种类型的数据集合，能够通过反馈方案(如 Windows 错误报告)来启用，用于帮助开发人员了解问题。捕获的数据将包含对下列内容的描述: 用户操作(涉及诸如用户通过键盘和鼠标输入的信息)、有关交互的 UI 对象的元数据以及屏幕截图。需要时，问题步骤记录程序提供打开和关闭数据集合的特定选项。

默认情况下，在问题报告方案期间，客户可以打开问题步骤记录程序。

如果启用该策略设置，则在该计算机上问题步骤记录程序功能将被禁用。

如果禁用或未配置该策略设置，则客户将可以使用问题步骤记录程序。</string>

      <string id="AppCompat_TurnOffSwitchBack_Help">该策略控制系统中 Switchback 兼容性引擎的状态。

Switchback 是一种通过为旧应用程序提供较旧的行为，为新应用程序提供新行为，来提供到较旧的应用程序的常规兼容性迁移的机制。

默认情况下，Switchback 处于打开状态。

如果启用该策略设置，Switchback 将被关闭。关闭 Switchback 可能会降低较旧的应用程序的兼容性。对于要求性能并且关注其所使用的应用程序的兼容性的服务器管理员来说，该选项非常有用。

如果禁用或未配置该策略设置，则将打开 Switchback。

请在更改设置之后，重新启动系统，以确保系统准确地反映这些更改。</string>
      <string id="AppCompat_TurnOffProgramInventory_Help">此策略控制系统中程序清单收集器的状态。
      
PDU 列出系统上的程序和文件的清单并将这些文件的有关信息发送给 Microsoft。这些信息用于帮助将文件与程序进行关联并诊断应用程序兼容性问题。

默认情况下 PDU 为启用状态。

如果启用此策略设置，则会关闭清单收集并且不会将数据发送给 Microsoft。启用此设置还会禁用通过 PCA 收集安装数据。

如果禁用或未配置此策略设置，则会启用清单收集。</string>

      <string id="AppCompatPrevent16Bit">防止访问 16 位应用程序</string>
      <string id="AppCompatPrevent16BitMach">防止访问 16 位应用程序</string>
      <string id="AppCompatRemoveProgramCompatPropPage">删除程序兼容性属性页</string>
      <string id="AppCompatTurnOffApplicationImpactTelemetry">关闭应用程序遥测</string>
      <string id="AppCompatTurnOffEngine">关闭应用程序兼容性引擎</string>
      <string id="AppCompatTurnOffProgramCompatibilityAssistant">关闭程序兼容性助理</string>
      <string id="AppCompatTurnOffSwitchBack">关闭 SwitchBack 兼容性引擎</string>
      <string id="AppCompatTurnOffUserActionRecord">关闭问题步骤记录器</string>
      <string id="AppCompatTurnOffProgramInventory">关闭程序清单</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
